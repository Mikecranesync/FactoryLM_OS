---
title: "Commit Notes - 2025-03-04"
date: 2025-03-04
tags: [project/Archon, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-03-04

## Archon — Push at 01:37 UTC

**Commits:** 1
**Trigger:** `1c05540` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1c05540` | Update streamlit_ui.py | Cole Medin |

> **What changed:** Fixed the Docker command for Cursor IDE integration by adding the `-i` flag for interactive mode and setting the `GRAPH_SERVICE_URL` environment variable to connect to the host service. Also updated the Docker image name from `archon:latest` to `archon-mcp:latest`.

**Key file:** `streamlit_ui.py`

## cmms — Push at 20:55 UTC

**Commits:** 2
**Trigger:** `b527350` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b527350` | Merge branch 'main' of https://github.com/Grashjs/cmms | Ibrahima G. Coulibaly |
| `c1909a3` | docs: update | Ibrahima G. Coulibaly |

> **What changed:**
> - `b527350`: Merge branch 'main' of https://github.com/Grashjs/cmms. Files: `mobile/i18n/i18n.ts`, `mobile/i18n/translations/pt_BR.ts`.
> - `c1909a3`: Renamed the env var `MINIO_PUBLIC_ENDPOINT` → `PUBLIC_MINIO_ENDPOINT` everywhere (env, README, docker-compose, application.yml) so the public MinIO URL is consistent with the other `PUBLIC_*` variables.  
Updated WebMvcConfig to expose `/api/public/**` endpoints and clarified in README that the MUI key is for the Pro license.  

Key files: `.env`, `README.MD`, `docker-compose.yml`, `application.yml`, `WebMvcConfig.java`
