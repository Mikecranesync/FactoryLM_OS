---
title: "Commit Notes - 2025-02-26"
date: 2025-02-26
tags: [project/Archon, type/commit-note]
status: active
---

# Commit Notes - 2025-02-26

## Archon — Push at 11:07 UTC

**Commits:** 4
**Trigger:** `bfbdf97` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `bfbdf97` | Updated V3-MCP-SUPPORT code to use local LLMs. | Aniket1995 |
| `40a8c6c` | Updated V3-MCP-SUPPORT code to use local LLMs. | Aniket1995 |
| `1fe160b` | Merge branch 'main' into using-local-llm | Aniket |
| `990662b` | set the embedding model in an environment variable | Aniket1995 |

> **What changed:**
> - `bfbdf97`: Switched the V3-MCP-SUPPORT iteration from cloud to local LLMs by adding a new `mcp-config.json` that points the MCP client to a local Archon Python server.  
Key file: `iterations/v3-mcp-support/mcp-config.json`
> - `40a8c6c`: Switched the V3-MCP-SUPPORT iteration from hard-coded OpenAI endpoints to configurable local LLMs (Ollama).  
Added `EMBEDDING_MODEL` env var and logic that auto-detects Ollama via `localhost` in `BASE_URL`, then routes both chat and embedding calls to the local service.  
Key files: `.env.example`, `archon_graph.py`, `crawl_pydantic_ai_docs.py`, `pydantic_ai_coder.py`, `mcp-config.json`.
> - `1fe160b`: Merge branch 'main' into using-local-llm. Files: `.env.example`, `.github/ISSUE_TEMPLATE/bug_report.md`, `.github/ISSUE_TEMPLATE/config.yml`, `.github/ISSUE_TEMPLATE/feature_request.md`, `README.md` (+29 more).
> - `990662b`: Made the embedding model configurable via the new `EMBEDDING_MODEL` env var instead of hard-coding `nomic-embed-text:latest` vs `text-embedding-3-small`.  
Key files: `.env.example`, `crawl_pydantic_ai_docs.py`, `pydantic_ai_coder.py`
