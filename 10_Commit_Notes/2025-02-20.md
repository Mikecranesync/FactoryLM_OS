---
title: "Commit Notes - 2025-02-20"
date: 2025-02-20
tags: [project/Archon, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-02-20

## Archon — Push at 15:13 UTC

**Commits:** 1
**Trigger:** `2a1f92a` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `2a1f92a` | Update setup_mcp.py | Edilson Lima |

> **What changed:** setup_mcp.py now auto-detects the OS and picks the right venv paths: Windows uses Scripts/*.exe while macOS/Linux uses bin/* without extension. This single change makes the script work cross-platform without manual edits.

## cmms — Push at 17:09 UTC

**Commits:** 7
**Trigger:** `676f976` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `676f976` | docs: update | Ibrahima G. Coulibaly |
| `7c28839` | docs: update | Ibrahima G. Coulibaly |
| `673bea5` | fix: jwt | Ibrahima G. Coulibaly |
| `7409647` | docs: update | Ibrahima G. Coulibaly |
| `1603230` | fix: user invite error | Ibrahima G. Coulibaly |
| `d5501f6` | docs: gcp | Ibrahima G. Coulibaly |
| `576c884` | chore: make env variables optional | Ibrahima G. Coulibaly |

> **What changed:**
> - `676f976`: README formatting fix: cleaned up table alignment and whitespace in the environment-variables section.  
Key file: README.MD
> - `7c28839`: Updated README.MD to fix formatting and wording of the environment-variables table; no functional code changed.  
Key file: README.MD
> - `673bea5`: Fixed two JWT-related bugs:  
1. Registration now verifies the JWT secret before auto-login (was hard-coded to localhost).  
2. Google Analytics only fires when a tracking ID is present.  

Key files: `App.tsx`, `RegisterJWT.tsx`
> - `7409647`: Added a new “Run” section to the README that shows the single docker-compose command needed to start the service.  
README.MD
> - `1603230`: Fixed a user-invite bug that was silently failing and added a clearer README warning not to delete the .env file.  
The invite success/error snackbars now display the correct translated messages.  

Key files: README.MD, frontend/src/content/own/PeopleAndTeams/People.tsx
> - `d5501f6`: Added Google Cloud Storage support: new env var `GCP_JSON` (service-account key) plus `GCP_BUCKET` and `GCP_PROJECT_ID`, a `GCPService` to handle uploads, and a setup guide.  
Key files: GCP-setup.md, README.MD, api/src/main/java/com/grash/service/GCPService.java
> - `576c884`: Made all environment variables optional by clearing their default placeholder values and updated docs to reflect this. Key files: `.env`, `README.MD`, `api/README.md`, `docker-compose.yml`.
