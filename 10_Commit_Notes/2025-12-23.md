---
title: "Commit Notes - 2025-12-23"
date: 2025-12-23
tags: [project/Agent-Factory, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-23

## Agent-Factory — Push at 23:03 UTC

**Commits:** 14
**Trigger:** `66ab044` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `66ab044` | feat: Integrate Machine Library into orchestrator_bot | Mikecranesync |
| `0fa8641` | fix(kb-evaluator): Convert Decimal similarity scores to float | Mikecranesync |
| `a54b280` | feat: Add Personal Machine Library for Telegram bot | Mikecranesync |
| `f34018a` | fix(core): Initialize llm_router before loading SME agents | Mikecranesync |
| `d8a621f` | fix(core): Add missing gap_detector.py module | Mikecranesync |
| `70c739d` | feat(rivet-pro): Phase 3 SME Agents - PRODUCTION | Mikecranesync |
| `37e5071` | fix: Remove page_number column from retriever query (schema mismatch) | Mikecranesync |
| `dedf621` | feat: Refactor handle_message with LangSmith tracing (Batch 1) | Mikecranesync |
| `8e1c243` | docs: Document photo handler bug fix (Part 4 complete) | Mikecranesync |
| `30c45a0` | docs: Add pre-deployment validation checklist (Part 3 complete) | Mikecranesync |
| `8177135` | feat: Add schema validation script (Part 3) | Mikecranesync |
| `57887bc` | feat: Add RequestTrace logging for photo handler (Part 2 complete) | Mikecranesync |
| `ea6f758` | feat: Add RequestTrace logging for text messages (Part 2) | Mikecranesync |
| `960e8f7` | fix: retriever.py uses manufacturer column (not vendor) | Mikecranesync |

> **What changed:**
> - `66ab044`: The orchestrator_bot now integrates the Machine Library: users can enter a troubleshooting mode tied to a specific machine, send queries enriched with that machine’s metadata, and have answers logged to its history.  
Key changes: register /library and /done commands, add active_machine context checks in handle_message, and persist every query to user_machine_history.  
File touched: agent_factory/integrations/telegram/orchestrator_bot.py
> - `0fa8641`: Fixed a TypeError in the knowledge base evaluator by converting PostgreSQL Decimal similarity scores to floats before mathematical operations. This resolves crashes in confidence and variance calculations when multiplying Decimal types with floats.

**Key file:** `agent_factory/routers/kb_evaluator.py`
> - `a54b280`: **Summary:**  
Added a personal machine library to the Telegram bot, letting technicians save equipment with photos/notes and troubleshoot them with context-enriched queries. New `/library` command plus inline buttons manage the fleet; once a machine is “active,” every follow-up message is automatically prefixed with its metadata before hitting the Rivet troubleshooter.  

**Key files:**  
`library.py`, `library_db.py`, `handlers.py`, `bot.py`, `002_add_user_machines.sql`
> - `f34018a`: Fixed a startup crash in the orchestrator by moving LLM-router creation before SME-agent loading; added a non-blocking `_trigger_research_async` helper for background KB-gap research.  
Key file: `agent_factory/core/orchestrator.py`
> - `d8a621f`: Added the missing `gap_detector.py` module that orchestrator.py was trying to import; it implements “Route C” by scanning user queries for equipment identifiers, rating KB coverage, and auto-generating high/medium/low-priority ingestion triggers with targeted search terms and sources.  
Key file: `agent_factory/core/gap_detector.py`
> - `70c739d`: **Summary:**  
Replaced all mock SME agents with production-ready specialists (Generic, Siemens, Rockwell, Safety) that now receive the actual KB atoms as context, eliminating the [MOCK] placeholder responses. The orchestrator passes retrieved documents to each agent, enabling real LLM-generated answers with citations and vendor-specific expertise.

**Key files:**  
`orchestrator.py`, `kb_evaluator.py`, `routing.py`, plus four new agent modules under `rivet_pro/agents/`
> - `37e5071`: Fixed a schema mismatch that was blocking KB searches: removed the non-existent `page_number` column from the SQL query and set `page_number=None` in the result object. This restores searchability for the 1,964 knowledge atoms.

Key file: `agent_factory/rivet_pro/rag/retriever.py`
> - `dedf621`: Refactored the Telegram bot’s `handle_message()` into four traceable functions (`format_user_response`, `send_to_user`, `send_admin_trace`, `send_admin_error`) and added LangSmith decorators for distributed tracing.  
Fixed cross-platform log-path handling so the logger works on both Windows dev boxes and the Linux VPS.  
Key files: `orchestrator_bot.py`, `trace_logger.py`.
> - `8e1c243`: Fixed the photo-handler bug: `retriever.py` was querying a non-existent `vendor` column, causing KB searches to fail silently. Updated query to use `manufacturer` and added full request tracing (logs + Telegram alerts) plus a pre-deploy schema validator to prevent future mismatches.

Key files touched: `retriever.py`, new `trace_logger.py`, `orchestrator_bot.py`, new `validate_schema.py`.
> - `30c45a0`: Added a 4-step pre-deployment checklist to SYSTEM_MANIFEST.md that must be run before every VPS push: schema validation, import check, deploy commands, and post-deploy verification. This prevents broken deployments by catching schema mismatches and import errors early.
> - `8177135`: Added a pre-deploy schema validator that checks the `knowledge_atoms` table columns against the SELECT list in `retriever.py`; exits 1 if any referenced column is missing from the DB.  
Key file: `scripts/validate_schema.py`
> - `57887bc`: Added detailed RequestTrace logging to the Telegram photo handler, capturing timing for OCR and routing steps plus key events like PHOTO_RECEIVED, OCR_COMPLETE, ROUTE_DECISION and RESPONSE_SENT.  
Key file: agent_factory/integrations/telegram/orchestrator_bot.py
> - `ea6f758`: Added production-grade request tracing for text messages: every Telegram text now gets a unique trace ID, JSONL file logging (traces.jsonl / errors.jsonl), timing capture, and a formatted summary sent to the admin chat.  
Key files: agent_factory/core/trace_logger.py (new), agent_factory/integrations/telegram/orchestrator_bot.py
> - `960e8f7`: Fixed a column mismatch in the document retriever: the SQL query now selects the correct `manufacturer` column instead of the non-existent `vendor` column, and the result mapping is adjusted accordingly. This ensures vendor information is accurately retrieved from the database.

Key file: `agent_factory/rivet_pro/rag/retriever.py`

## cmms — Push at 23:11 UTC

**Commits:** 2
**Trigger:** `4fc1009` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `4fc1009` | refactor: use LicensingState | Ibrahima G. Coulibaly |
| `80a8fd0` | feat: license entitlements | Ibrahima G. Coulibaly |

> **What changed:**
> - `4fc1009`: Replaced the simple `/license/validity` boolean endpoint with a richer `/license/state` that returns a `LicensingState` object exposing entitlements. Controllers now call `licenseService.hasEntitlement(...)` to gate NFC/barcode scans and workflow creation, centralizing license enforcement.

**Key files:** `LicenseController.java`, `AssetController.java`, `WorkflowController.java`, `LicensingState.java`, `LicenseService.java`
> - `80a8fd0`: Added full license-entitlement support: new DTOs model licenses, entitlements, and validation requests/responses, plus backend service logic and a frontend toggle for the CHECKLIST entitlement.  
Key files: api/src/main/java/com/grash/service/LicenseService.java, api/src/main/java/com/grash/dto/license/*, frontend/src/content/own/Settings/Checklists/index.tsx
