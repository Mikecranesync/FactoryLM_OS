---
title: "Commit Notes - 2024-08-03"
date: 2024-08-03
tags: [project/frame_realtime_gemini_voicevision, type/commit-note]
status: active
---

# Commit Notes - 2024-08-03

## frame_realtime_gemini_voicevision — Push at 13:03 UTC

**Commits:** 6
**Trigger:** `3a0c646` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `3a0c646` | fixed a couple of un-awaited sleeps | CitizenOneX |
| `92a52c0` | Update README.md | CitizenOneX |
| `5720c0b` | updated README with screenshots, frameshots | CitizenOneX |
| `6fd14d4` | added DisplayHelper based on Python SDK for text wrapping to support multi-line text display. Updated README | CitizenOneX |
| `7c26788` | added single-line speech to text display showing partial and final results separately | CitizenOneX |
| `52d3ce2` | added flutter_blue_plus and associated configuration file changes | CitizenOneX |

> **What changed:**
> - `3a0c646`: Fixed two race conditions by properly awaiting 100ms and 500ms delays in display text rendering and device break-signal timing.  
Key files: lib/display_helper.dart, lib/main.dart
> - `92a52c0`: Added a note about the 40 MB on-device Vosk model’s limited vocabulary and long-utterance issues, plus swapped the broken inline video for a GitHub-hosted link.  
Key file: README.md
> - `5720c0b`: README now shows live visuals—added a demo video, two phone screenshots, and a “frameshot” of the wearable display—so users can see the speech-to-text flow at a glance. iOS display name shortened to “Speech-To-Text (Host)” for clarity.  
Key files: README.md, docs/*.{png,mp4}, ios/Runner/Info.plist
> - `6fd14d4`: Added a new `DisplayHelper` class that wraps long text into multiple lines sized for Frame’s 640-pixel display, using per-character width mapping and configurable alignment. Updated README to clarify the mic source is currently the host phone and added ProGuard rules for JNA to keep Vosk working after obfuscation. Key files: `lib/display_helper.dart`, `README.md`, `android/app/proguard-rules.pro`.
> - `7c26788`: Added real-time speech-to-text display to the Flutter app: a single line now shows partial (live) and final (completed) transcripts separately.  
Key files: lib/main.dart (UI), lib/bluetooth.dart (new BLE layer).
> - `52d3ce2`: Added flutter_blue_plus to enable Bluetooth Low Energy scanning & connections.  
Key files: pubspec.yaml, AndroidManifest.xml, Info.plist (plus proguard rules).
