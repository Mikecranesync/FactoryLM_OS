---
title: "Commit Notes - 2025-09-27"
date: 2025-09-27
tags: [project/Backlog.md, project/Thefuture, type/commit-note]
status: active
---

# Commit Notes - 2025-09-27

## Backlog.md — Push at 19:12 UTC

**Commits:** 18
**Trigger:** `1929dbe` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1929dbe` | TASK-275 - Extract task reordering logic to core module (#378) | Alex Gavrilescu |
| `8b3431f` | TASK-262 - TUI: Show all configured status columns in Kanban (#377) | Alex Gavrilescu |
| `df04b7a` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `1a2ca1c` | TASK-281 - Fix backlog init skip option (#376) | Alex Gavrilescu |
| `469ee3d` | Create task-281 - Fix-backlog-init-skip-option.md | MrLesk |
| `96bb405` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `b5d2e84` | TASK-280 - Fix TUI task list selection and detail pane synchronization bug (#375) | Alex Gavrilescu |
| `45662a5` | TASK-280 - Create task for TUI selection/detail pane sync bug | MrLesk |
| `468ef0b` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `110b194` | TASK-279 - Add cleanup functionality to web UI for managing completed tasks (#374) | Alex Gavrilescu |
| `c4d7235` | Run task cleanup | MrLesk |
| `4964b87` | refactor: simplify TaskList cleanup refresh logic | MrLesk |
| `b84781d` | fix: properly refresh filtered task list after cleanup | MrLesk |
| `4090fb8` | fix: refresh TaskList view after cleanup execution | MrLesk |
| `90de2fc` | fix: update cleanup modal messages to clarify completed folder accessibility | MrLesk |
| `3cf8acb` | TASK-274 - Fix Kanban boards missing live updates (#373) | Alex Gavrilescu |
| `2f3ca85` | Update task-274 - Fix-Kanban-boards-missing-live-updates.md | MrLesk |
| `74d5107` | TASK-274 - Fix Kanban boards missing live updates | MrLesk |

> **What changed:**
> - `1929dbe`: Centralized task-reordering logic into a new `core/reorder.ts` module, eliminating duplicate ordinal calculations in web and server layers. Web UI now delegates ordering to the API, and the server endpoint uses `Core.reorderTask` with conflict-safe helpers.

Key files: `src/core/reorder.ts`, `src/core/backlog.ts`, `src/server/index.ts`, `src/web/components/Board.tsx`, `src/web/lib/api.ts`
> - `8b3431f`: TUI Kanban now always renders every status defined in backlog/config.yml as a column, even when empty, matching the web UI.  Navigation and selection remain stable with zero-task columns; unknown task statuses still append after the configured ones.  
Key files: src/board.ts, src/ui/board.ts, backlog/tasks/task-262 - TUI-Show-all-configured-status-columns-in-Kanban.md
> - `df04b7a`: Updated README.md to reflect the latest project board status and bumped version to v1.14.2. Key changes include marking TASK-281 (backlog init skip option fix) as Done and refreshing the generation timestamp. No functional code changes—purely documentation housekeeping.

Files touched: README.md, package.json
> - `1a2ca1c`: Fixed the interactive `backlog init` so choosing “skip” no longer triggers a retry; the wizard now accepts the skip and continues with zero agent-instruction files.  
Key files: `src/utils/agent-selection.ts`, `src/cli.ts`, plus new tests in `src/test/agent-selection.test.ts` and `src/test/cli.test.ts`.
> - `469ee3d`: Created task-281 to fix a bug where `backlog init` rejects the “Do not add instructions” choice and forces the user to pick agent files, blocking headless setups.  
Key file: backlog/tasks/task-281 - Fix-backlog-init-skip-option.md
> - `96bb405`: README.md refreshed to v1.14.1, showing the current Kanban snapshot (TASK-280 now in-progress, TASK-272 moved to To-Do) and the new generation timestamp.  
Key files: README.md, package.json
> - `b5d2e84`: Fixed TUI task list/detail-pane sync: arrow navigation now keeps panes aligned, filters auto-select first task, and focus transitions from search/filters immediately refresh details.  
Key files: `src/ui/components/generic-list.ts`, `src/ui/task-viewer-with-search.ts`, `src/ui/task-viewer.ts`.
> - `45662a5`: Created high-priority bug task TASK-280 to fix TUI off-by-one navigation: detail pane lags one task behind the list cursor and filtered views start on the wrong item.  
Key file: backlog/tasks/task-280 - Fix-TUI-task-list-selection-and-detail-pane-synchronization-bug.md
> - `468ef0b`: Updated README board snapshot to v1.13.5 and bumped package.json version. Adds new TUI drag-mode and core reordering tasks, marks TASK-279 (web cleanup) and TASK-274 (live updates) as Done.  
Key files: README.md, package.json
> - `110b194`: Added web UI cleanup functionality for completed tasks: new modal lets users preview and bulk-move old Done tasks (1 day–1 year) to the completed folder. Buttons appear in the Done column (Board) and TaskList header; success toast confirms the count moved.

Key files: src/server/index.ts, src/web/components/CleanupModal.tsx, src/web/components/Board.tsx, src/web/components/TaskList.tsx
> - `c4d7235`: Moved a batch of completed backlog items into the `backlog/completed` folder; no code changed.  
Key files: the nine `.md` task cards covering TUI scroll fix, Web-UI task highlighting, heading truncation fix, agent-selection refactor, acceptance-criteria bug, npm publishing, and the three content-store/search refactors.
> - `4964b87`: TaskList now auto-refreshes when its tasks prop changes by adding it to the useEffect dependency array, eliminating the need for manual re-filtering after cleanup. Removed 30+ lines of redundant filtering logic from handleCleanupSuccess, letting React effects handle updates reactively.

Key file: src/web/components/TaskList.tsx
> - `b84781d`: Fixed stale filter counts after cleanup by forcing a re-fetch of filtered results; now both server-side search and client-side fallback update the task list and count immediately.  
Key file: `src/web/components/TaskList.tsx`
> - `4090fb8`: TaskList now refreshes its data after a cleanup operation by accepting an onRefreshData prop, ensuring the view stays in sync—especially the “All statuses” view that was previously stale.  
Key files: src/web/App.tsx, src/web/components/TaskList.tsx
> - `90de2fc`: Fixed cleanup modal copy to accurately reflect that completed tasks are archived and removed from the board, not “still accessible.”  
Key files: src/web/components/CleanupModal.tsx, src/server/index.ts, src/web/components/Board.tsx
> - `3cf8acb`: Live-update plumbing restored: Web Kanban now reflects task moves, status config changes and external edits without refresh, while the TUI board auto-refreshes when another CLI edits tasks.  
Unified task UI: replaced separate TaskForm with a single TaskDetailsModal that handles both create and edit (with archive button re-added), keeping all prior UX niceties.  

Key files: src/web/App.tsx, src/web/components/TaskDetailsModal.tsx, src/core/content-store.ts, src/utils/config-watcher.ts, src/ui/board.ts
> - `2f3ca85`: Marked the final three checklist items (#4–#6) in the Kanban live-update task as complete, covering the modal’s create-mode tweaks for optional task prop, editable title, and archive handling.  
Key file: backlog/tasks/task-274 - Fix-Kanban-boards-missing-live-updates.md
> - `74d5107`: Fixed Kanban boards not receiving live updates by implementing a WebSocket-based real-time sync between server and both TUI and web clients; core changes in src/server/index.ts, src/ui/board.ts, and src/web/components/Board.tsx. Also archived three completed tasks and updated task metadata.

## Thefuture — Push at 01:41 UTC

**Commits:** 6
**Trigger:** `2c4411f` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `2c4411f` | Fix response format to use actual system date/time | Daniel Miessler |
| `40b0fb3` | Update README to reflect v0.2.1 release | Daniel Miessler |
| `3f1afdb` | Update hooks system and fix README dates | Daniel Miessler |
| `a9594be` | Updated hooks. | Daniel Miessler |
| `339dde3` | Updated hooks and LDC command. | Daniel Miessler |
| `4f88183` | Updated load dynamic content command. | Daniel Miessler |

> **What changed:**
> - `2c4411f`: Fixed the response format to use actual system date/time instead of hardcoded timestamps. This ensures the voice notification system receives proper timing information from live system calls rather than placeholder values.

**Key file:** `.claude/context/CLAUDE.md`
> - `40b0fb3`: Updated README to announce v0.2.1 release, highlighting the new Hooks System with portable installation support. Key file: README.md
> - `3f1afdb`: Sanitized the hooks system for public release by replacing hard-coded paths with `${PAI_DIR}` variables and removing any sensitive data. Fixed duplicate September 25th dates in the README changelog.

**Key files:** README.md
> - `a9594be`: Updated Claude hooks to improve session initialization and tab title generation. The session-start hook now validates the stop-hook configuration and sets initial "Kai Ready" tab titles, while the stop-hook generates more meaningful 4-word summaries from completed task descriptions rather than just user prompts.

Key files: `.claude/hooks/session-start-hook.ts`, `.claude/hooks/stop-hook.ts`
> - `339dde3`: Added a new Claude agent configuration for a pentester role with strict security testing protocols. The agent enforces defensive-only security practices, mandatory voice notifications after each response, and requires loading context files before any operations. Key file: `.claude/agents/pentester.md`
> - `4f88183`: Updated the load-dynamic-requirements command to use explicit `read` commands instead of checkmarks for loading context files, removed obsolete website and consulting sections, and hard-coded `PAI_DIR` to `/Users/daniel/.claude` in settings.json. README now warns that `PAI_DIR` must be set manually. Key files: `.claude/commands/load-dynamic-requirements.md`, `.claude/settings.json`, `README.md`.
