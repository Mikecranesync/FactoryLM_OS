---
title: "Commit Notes - 2025-10-18"
date: 2025-10-18
tags: [project/Backlog.md, project/Thefuture, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-10-18

## Backlog.md — Push at 20:59 UTC

**Commits:** 2
**Trigger:** `9f18a55` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `9f18a55` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `f1d0bc3` | TASK-302 - Support flexible ID formats for tasks and docs (#408) | Alex Gavrilescu |

> **What changed:**
> - `9f18a55`: Updated README.md to reflect the current project board status (v1.17.1) and bumped the package.json version accordingly. Key files: README.md, package.json.
> - `f1d0bc3`: Unified task & document ID parsing: CLI, MCP, and server now accept any mix of “TASK-123/task-123/0123” or “DOC-123/doc-123/0123” and normalize internally.  
Key helpers added in `src/utils/task-path.ts` and `src/utils/document-id.ts`; all lookup paths refactored to use them.  
Files touched: `src/cli.ts`, `src/core/backlog.ts`, `src/mcp/tools/documents/handlers.ts`, `src/server/index.ts`, plus new tests.

## Thefuture — Push at 23:45 UTC

**Commits:** 8
**Trigger:** `31bfcee` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `31bfcee` | Consolidate redundant documentation files | Daniel Miessler |
| `db311b3` | Add comprehensive getting-started documentation with automated setup | Daniel Miessler |
| `c310e43` | feat: Add automatic documentation update system via pre-commit hook | Daniel Miessler |
| `3fe63cd` | Update README with v0.5.0 skills-based architecture details | Daniel Miessler |
| `c81e846` | Move core identity to skill description, eliminate hook-based context loading | Daniel Miessler |
| `b8f0f09` | Rename PAI.md to SKILL.md and remove redundant README | Daniel Miessler |
| `38a6525` | Add skills-based PAI context system for 60-70% token reduction | Daniel Miessler |
| `cbb0eff` | added to deny several dangerous disk and volume management commands for macOS via `diskutil(8)` | Emory L. |

> **What changed:**
> - `31bfcee`: **Documentation cleanup** – removed duplicate onboarding files (START-HERE.md, quick-start.md) and streamlined README to a single pointer to `./setup.sh` and the consolidated how-to-start guide.  
Key files: README.md, START-HERE.md (deleted), documentation/quick-start.md (deleted), documentation/README.md
> - `db311b3`: Added comprehensive onboarding docs and an automated setup script to eliminate PAI’s steep first-run barrier. New users can now run `./setup.sh` for a guided, colorized install or follow the friendly step-by-step guide; veterans get a quick-reference card for daily commands.

Key files: `setup.sh`, `documentation/how-to-start.md`, `documentation/QUICK-REFERENCE.md`, `START-HERE.md`
> - `c310e43`: Added an automatic documentation updater that runs during pre-commit: changed files are mapped to relevant docs (e.g. skills/ → skills-system.md) and those docs get a fresh timestamp and a “Recent Updates” entry in README.md. The hook is non-blocking—if the update fails it only warns—so commits still succeed.  
Key files: hooks/update-documentation.ts, hooks/pre-commit-with-docs.template, documentation/auto-documentation.md
> - `3fe63cd`: Updated README to document v0.5.0's skills-based architecture that cuts token usage 92.5% by moving core identity into the skill's YAML frontmatter and loading full context only on demand. Key file: README.md
> - `c81e846`: **Summary:**  
Moved PAI’s core identity from a per-prompt hook (`MINIMAL.md`) into the skill’s YAML front-matter so it’s always in the system prompt; deleted `MINIMAL.md` and stripped the hook down to a zero-token “update tab titles only” job. Net result: 400-token overhead removed from every user interaction while full context remains available on-demand via the PAI skill.

**Key files:**  
`hooks/update-tab-titles.ts`, `skills/PAI/SKILL.md`, `skills/PAI/MINIMAL.md` (removed)
> - `b8f0f09`: Renamed the PAI skill’s entry file from PAI.md to SKILL.md and deleted the duplicate README.md so the skill now follows Claude Code’s standard of using a single SKILL.md as both context and documentation.  
Key files touched: skills/PAI/PAI.md (removed), skills/PAI/README.md (removed), skills/PAI/SKILL.md (new).
> - `38a6525`: Replaced the monolithic PAI context system with a tiered, skills-based architecture that loads only the minimal identity context (~250 tokens) by default and pulls in modular context files on demand, cutting daily token usage by ~140k (60-70%). The new `skills/PAI/` module auto-routes requests to the right context tier based on keywords, while `hooks/update-tab-titles.ts` (formerly `load-global-context.ts`) now just handles tab naming.
> - `cbb0eff`: Blocked ~20 macOS diskutil sub-commands (partition, erase, reformat, zeroDisk, APFS & CoreStorage ops, etc.) plus a few stray rm/dd patterns in settings.json to stop PAI from accidentally nuking disks or volumes.

## cmms — Push at 10:27 UTC

**Commits:** 2
**Trigger:** `8322db4` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `8322db4` | feat: Account soft deletion | amb00u |
| `a658c70` | feat: Account soft deletion | amb00u |

> **What changed:**
> - `8322db4`: Added a soft-delete endpoint (`/users/softdelete/{id}`) that lets a user deactivate their own account or an admin deactivate any user in the same company.  
It disables the account, marks the subscription as inactive, and appends the user ID to the email to keep it unique.  
Key file: `UserController.java`
> - `a658c70`: Added a soft-delete endpoint (`/softdelete/{id}`) that lets a user disable their own account or lets a company admin disable any user in the company; the user’s email is suffixed with the id (and company name for admins) to keep it unique while preserving data integrity.  
Key file: `UserController.java`
