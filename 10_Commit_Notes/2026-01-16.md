---
title: "Commit Notes - 2026-01-16"
date: 2026-01-16
tags: [project/RideView, project/Rivet-PRO, type/commit-note]
status: active
---

# Commit Notes - 2026-01-16

## RideView — Push at 20:53 UTC

**Commits:** 7
**Trigger:** `6048746` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `6048746` | feat: Add Detection Tuning screen with real-time parameter adjustment (v3.1.0) | Mikecranesync |
| `06af0a9` | feat: Add brightness/contrast controls to Kivy app (v3.0.0) | Mikecranesync |
| `fe46e69` | fix: Rename config to app_config to avoid Kivy conflict | Mikecranesync |
| `110ecec` | feat: Add Kivy cross-platform UI with video recording (v2.5.0) | Mikecranesync |
| `6e93485` | fix: Resolve uint16 overflow in circle/washer detectors | Mikecranesync |
| `dd99aca` | feat: Add fastener shape detection (v2.0.0) | Mikecranesync |
| `77c5e27` | chore: Prepare v1.0.0 release | Mikecranesync |

> **What changed:**
> - `6048746`: Added a real-time Detection Tuning screen (v3.1.0) with 8 sliders for live adjustment of preprocessing, threshold, and morphology parameters. All changes apply instantly to the running detection pipeline, accessible via a new Tuning button on the main screen.

Key files: `tuning_screen.py`, `detector.py`, `preprocessor.py`, `stripe_validator.py`, `main_screen.py`
> - `06af0a9`: **Summary:** Added real-time brightness/contrast sliders (50–200%) to the Kivy settings screen; adjustments apply to the live camera preview via OpenCV’s convertScaleAbs. Settings screen is now reachable from the main screen, and v2.5 PRD documentation is included.

**Key files:** src/rideview/mobile/screens/settings_screen.py, src/rideview/mobile/camera_provider.py, src/rideview/mobile/screens/main_screen.py, RideView-PRD-v2.5-recording-crossplatform.md
> - `fe46e69`: Renamed the `config` parameter to `app_config` throughout the mobile app to avoid collision with Kivy’s built-in `config` attribute. Also set `KIVY_NO_ARGS=1` so Kivy won’t swallow command-line arguments.

Key file: `src/rideview/mobile/app.py`
> - `110ecec`: Added a Kivy-based cross-platform UI (v2.5.0) that runs on desktop and mobile with `--mobile`.  
Key additions: video recording with MP4 + JSON metadata, Groq LLM Tier-2 validation, threaded detection worker, and recording management UI.  
Main files: `src/rideview/mobile/` (14 new), `__main__.py`, `config/default.yaml`.
> - `6e93485`: Fixed uint16 overflow bugs in circle/washer detection by converting numpy uint16 coordinates to Python int before arithmetic operations. Added validation to prevent crashes from empty ROI regions and invalid bounding boxes.

**Key files:** `src/rideview/core/detector.py`, `src/rideview/detection/fastener_detector.py`
> - `dd99aca`: **Summary:** Added fastener shape detection to eliminate false positives by automatically detecting bolts, nuts, washers, and screws before stripe analysis. The system now creates ROI boundaries from detected fasteners and skips analysis if no fastener is found in strict mode.

**Key files:** `src/rideview/detection/fastener_detector.py`, `src/rideview/core/detector.py`, `config/default.yaml`
> - `77c5e27`: **RideView v1.0.0 release prep**: Added comprehensive user manual, finalized CI/CD workflows, and locked down configuration files for production. Key files updated include `docs/USER_MANUAL.md`, GitHub Actions workflows, and core detection modules (`camera.py`, `detector.py`, `color_segmenter.py`).

## Rivet-PRO — Push at 12:51 UTC

**Commits:** 23
**Trigger:** `b56b1da` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b56b1da` | Phase 5: Analytics Dashboard (#10) | Mikecranesync |
| `f4fc515` | Technical Debt Cleanup Sprint (#9) | Mikecranesync |
| `7614ac5` | docs: Update context files - Phase 4 SME Chat testing complete | Mikecranesync |
| `fd896b4` | fix: Use effective_message for SME chat callback handler | Mikecranesync |
| `da1416c` | feat: Integrate SME Chat commands into production bot | Mikecranesync |
| `6e12341` | docs: Update PRD and progress log - Phase 4 SME Chat complete | Mikecranesync |
| `b119989` | feat(SME-CHAT-010): Integration tests for full SME chat flow | Mikecranesync |
| `8910f51` | feat(SME-CHAT-009): Unit tests for SME chat service and personalities | Mikecranesync |
| `2aefbde` | feat(SME-CHAT-008): Confidence-based routing in orchestrator | Mikecranesync |
| `fdd77de` | feat(SME-CHAT-007): Telegram message routing for chat mode | Mikecranesync |
| `d8324cb` | feat(SME-CHAT-006): Telegram /chat command handler | Mikecranesync |
| `f365592` | feat(SME-CHAT-005): SME chat service core | Mikecranesync |
| `b383c71` | feat(SME-CHAT-004): RAG service for SME context | Mikecranesync |
| `213b99c` | feat(SME-CHAT-003): SME personalities configuration | Mikecranesync |
| `f8ceb8e` | feat(SME-CHAT-002): Pydantic models for SME chat | Mikecranesync |
| `08beec7` | feat(SME-CHAT-001): Database migration for SME chat sessions | Mikecranesync |
| `fa93ccd` | feat: Improve OCR for challenging images (dirty/rotated nameplates) | Mikecranesync |
| `3b6850c` | feat: Track pending validations for human-in-the-loop without explicit reply | Mikecranesync |
| `48011c6` | docs: Update quick context and add resume prompt for Search Transparency feature | Mikecranesync |
| `7e93be9` | fix: Add Windows console UTF-8 encoding support | Mikecranesync |
| `cdea1af` | feat: Integrate Phase 3 pipeline components with bot | Mikecranesync |
| `2a2b8b8` | feat: Lower human-in-loop threshold to 50% confidence | Mikecranesync |
| `c8065f2` | feat: Add human-in-the-loop manual validation | Mikecranesync |

> **What changed:**
> - `b56b1da`: Added admin-only analytics commands: `/adminstats` for instant system metrics and `/report` for weekly summaries.  Integrated response-time tracking and KB health stats via new `AnalyticsService`.  
Key files: `bot.py`, `analytics_service.py`.
> - `f4fc515`: **Tech debt cleanup sprint completed** – extracted hard-coded Ralph chat ID into config, made node callbacks persist across restarts in PostgreSQL, added cached DB lookup for troubleshooting trees (cuts Claude API calls), and replaced silent `except:pass` blocks with proper logging.  
Key files touched: `rivet_pro/config/settings.py`, `rivet_pro/troubleshooting/callback.py`, `rivet_pro/troubleshooting/commands.py`, `rivet_pro/adapters/telegram/bot.py` (plus new migration `027_node_callback_mappings.sql`).
> - `7614ac5`: Phase 4 SME Chat is now fully tested and ready to merge. The feature adds vendor-specific chat personalities with RAG-enhanced responses and confidence-based routing, accessible via `/chat [vendor]` and `/endchat` commands. Key files updated: `docs/QUICK_CONTEXT.md` and `docs/RESUME_PROMPT.md`.
> - `fd896b4`: Fixed a NoneType error in the SME chat callback handler by using `update.effective_message` instead of `update.message`, which is None for callback queries. This allows proper handling when users select vendors from inline keyboards.

**Key file:** `rivet_pro/adapters/telegram/bot.py`
> - `da1416c`: **Summary:**  
The Telegram bot now supports interactive SME (Subject Matter Expert) chat sessions. Users can start a chat with `/chat [vendor]` or pick from a menu, converse with the SME, and end with `/endchat`. Messages are routed to the SME handler when a session is active, and command hints appear in Telegram’s menu.

**Key files:**  
`rivet_pro/adapters/telegram/bot.py`
> - `6e12341`: **Summary:** Completed Phase 4 SME Chat feature with 10 user stories implemented. Added conversational chat with vendor-specific SME agents (Hans/Siemens, Mike/Rockwell, Erik/ABB) featuring distinct personalities and RAG-enhanced responses. All 50 tests (38 unit + 12 integration) are passing.

**Key files:** `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
> - `b119989`: Added comprehensive integration tests for the SME chat flow, covering session lifecycle, multi-turn conversations, RAG integration, and vendor-specific personalities (Siemens, Rockwell, ABB). Fixed asyncpg JSONB handling bugs by serializing equipment_context as JSON strings for inserts and parsing JSONB results consistently. Key files: `tests/integration/test_sme_chat_flow.py`, `rivet/services/sme_chat_service.py`.
> - `8910f51`: Added comprehensive unit tests for SME chat functionality: 19 tests covering all 7 vendor personalities (Siemens, Rockwell, ABB, etc.) and 19 tests for chat service operations including session management, confidence scoring, and safety warnings. Fixed enum handling in sme_chat_service.py to work with Pydantic's use_enum_values=True. Key files: test_sme_personalities.py, test_sme_chat_service.py, sme_chat_service.py
> - `2aefbde`: Added confidence-based routing to SME chat: high-confidence queries (≥0.85) get fast, cheap KB answers; medium (0.70-0.85) trigger full SME synthesis; low (<0.70) ask clarifying questions instead of guessing.  
Key file: rivet/services/sme_chat_service.py
> - `fdd77de`: **Summary:** Added Telegram message routing for SME chat mode—when `sme_chat_active` is set, incoming messages bypass the normal troubleshoot flow and are handled by the new `handle_sme_chat_message` function. Responses show SME badge, confidence emoji, safety warnings, sources, and typing indicator; after 3 consecutive errors the bot suggests `/endchat`.

**Key file:** `rivet/integrations/telegram.py`
> - `d8324cb`: **Summary:**  
Added `/chat` and `/endchat` commands to Telegram bot, letting users start/stop SME chat sessions with vendor-specific experts (Siemens, Rockwell, etc.). `/chat` auto-detects vendor from recent equipment context or shows an inline picker; sessions store state in `user_data`. Also fixed an import typo in `kb_search.py`.

**Key files:**  
`rivet/integrations/telegram.py`, `rivet/workflows/kb_search.py`
> - `f365592`: **Summary:**  
Introduced `SMEChatService` to manage SME chat sessions with personality-driven responses, RAG context, and safety warning extraction. Core methods handle session lifecycle (start, chat, close) and persist messages with cost, confidence, and safety metadata. Key files: `rivet/services/sme_chat_service.py`, `rivet/services/__init__.py`.
> - `b383c71`: **Summary:**  
Added a RAG service (`SMERagService`) that retrieves manufacturer-filtered knowledge atoms for SME chat. It enhances queries using conversation history, performs vector search via `KnowledgeService`, and formats results as markdown for LLM prompts. Includes confidence scoring and usage tracking.

**Key files:**  
`rivet/services/sme_rag_service.py`, `rivet/services/__init__.py`
> - `213b99c`: **Summary:** Added SME personality system with 7 vendor-specific personas (Siemens, Rockwell, ABB, Schneider, Mitsubishi, FANUC, Generic) each with distinct voice characteristics, expertise areas, and response formatting. Includes helper functions for personality lookup and LLM prompt generation to make chat responses feel like real vendor specialists.

**Key files:** `rivet/prompts/sme/personalities.py`
> - `f8ceb8e`: Added Pydantic models for the new SME Chat feature, defining enums for vendor personalities (Siemens, Rockwell, etc.), session states, message roles, and confidence-based routing levels. Created core models for chat sessions (with equipment context) and messages (with RAG metadata), plus helper functions for SME names and confidence badges.

Key files: `rivet/models/sme_chat.py`, `rivet/models/__init__.py`
> - `08beec7`: Added database schema for SME chat sessions: two new tables track multi-turn conversations with vendor-specific experts (Siemens, Rockwell, ABB, etc.) and store messages with RAG metadata like confidence scores and knowledge atoms used. Includes helper functions to fetch active sessions, retrieve conversation history, and auto-timeout inactive sessions after 30 minutes.  
Key file: `rivet_pro/migrations/026_sme_chat_sessions.sql`
> - `fa93ccd`: OCR now detects image problems (rotated, dirty, blurry) and caps confidence accordingly—major issues at 65 %, minor at 80 %—forcing multi-provider verification. Users get tailored photo tips when issues are found. Key files: ocr_service.py, models/ocr.py, telegram/bot.py, response_formatter.py
> - `3b6850c`: Added human-in-the-loop validation tracking so users can confirm/correct bot guesses even without Telegram’s reply feature. When confidence ≥50% the bot stores the pending validation and accepts a simple “yes/no” message within 5 min.

Key file: rivet_pro/adapters/telegram/bot.py
> - `48011c6`: Updated project docs to reflect the new Search Transparency feature: when manual search fails, the bot now shows a transparency report and asks the user to validate any ≥50% confidence candidate URL. Added a resume prompt file and refreshed the quick context with the latest commit, commands, and key files like manual_service.py and search_report.py.
> - `7e93be9`: Fixed Windows console encoding to properly display Unicode emojis by creating `rivet_pro/core/utils/encoding.py` with a `fix_windows_encoding()` function that wraps stdout/stderr with UTF-8 TextIOWrapper. Updated the test script to use emoji characters (✅❌⚠️📊💡) instead of ASCII placeholders, making output more visually informative. The fix is safe to import multiple times and only applies on Windows systems.
> - `cdea1af`: **Phase 3 pipeline integration complete** – bot now routes all text queries through a unified pipeline that tracks workflow states (CREATED → IN_PROGRESS → COMPLETED) and automatically fails over across Groq → OpenAI → Claude.  
**Key files:** `bot.py` (new `/pipeline` stats command), `pipeline_integration.py` (orchestration facade), `agent_executor.py` (vendor routing), `025_llm_cache.sql` (response caching).
> - `2a2b8b8`: Lowered the confidence threshold from 70% to 50% for flagging potential equipment manual matches, causing more candidate URLs to be shown to users for validation. This increases user feedback opportunities while they're already voting on matches.

Key file: `rivet_pro/core/utils/response_formatter.py`
> - `c8065f2`: Added human-in-the-loop validation: when a manual search fails but has a >70% confidence candidate, the bot now asks “Is this correct?” and stores user Yes/No replies.  
Validated URLs are cached for future searches; rejections are logged for model improvement.  
Key files: bot.py, search_report.py, manual_service.py, response_formatter.py, 025_manual_feedback.sql
