---
title: "Commit Notes - 2025-09-20"
date: 2025-09-20
tags: [project/Backlog.md, project/Thefuture, type/commit-note]
status: active
---

# Commit Notes - 2025-09-20

## Backlog.md — Push at 15:19 UTC

**Commits:** 2
**Trigger:** `d0b2e7b` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `d0b2e7b` | TASK-273.03 - Build core Fuse search service (#365) | Alex Gavrilescu |
| `e4d00db` | TASK-273.02 - Introduce shared content store (#364) | Alex Gavrilescu |

> **What changed:**
> - `d0b2e7b`: Added a unified Fuse.js-powered SearchService that indexes tasks, docs, and decisions from the ContentStore and supports live re-indexing on updates.  It exposes a single search/filter interface for the CLI/TUI, eliminating duplicate filtering logic.  
Key files: src/core/search-service.ts, src/core/backlog.ts, src/types/index.ts
> - `e4d00db`: Added a singleton ContentStore that eagerly loads tasks/docs/decisions into memory and keeps them fresh via file-system watchers; legacy code can opt in through Core.getContentStore() without breaking.  
Key files: src/core/content-store.ts (new), src/core/backlog.ts, src/test/content-store.test.ts.

## Thefuture — Push at 22:55 UTC

**Commits:** 7
**Trigger:** `8b05c23` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `8b05c23` | Removed .DS_Store | Daniel Miessler |
| `6bc3136` | Updated updates section. | Daniel Miessler |
| `11d7b82` | Updated README. | Daniel Miessler |
| `1c4b27e` | Merge branch 'main' of github.com:danielmiessler/PAI | Daniel Miessler |
| `117a481` | Added .env instructions | Daniel Miessler |
| `0910abe` | Updated prereqs | Daniel Miessler |
| `0dd0f87` | Merge pull request #9 from mvoehringer/feature/fix-path | Daniel Miessler 🛡️ |

> **What changed:**
> - `8b05c23`: Removed the macOS system file `.DS_Store` from the repo to keep version control clean and platform-agnostic.  
Key file: `.DS_Store`
> - `6bc3136`: Added a new updates section to README.md announcing the `/voice-server` directory for voice capabilities (requires Eleven Labs API key), closed open PRs including a `PAI_PATH` fix, and ongoing work on missing hooks. Key file: README.md
> - `11d7b82`: README now clarifies that users must manually export `PAI_HOME` in their shell config and shows the exact line to add; the setup flow is split into numbered steps.  
Key file: README.md
> - `1c4b27e`: Merge branch 'main' of github.com:danielmiessler/PAI. Files: `.claude/commands/capture-learning.md`, `.claude/commands/capture-learning.ts`, `.claude/commands/load-dynamic-requirements.md`, `.claude/context/CLAUDE.md`, `.claude/context/tools/CLAUDE.md` (+4 more).
> - `117a481`: Added `.env-example` template and updated README to guide users on creating their own `.env` file outside the repo for API keys. Replaced the broken `claude` symlink with a working `claude_directory` symlink pointing to `.claude/`.

Key files: `.env-example`, `README.md`, `claude_directory`
> - `0910abe`: Updated Claude's prerequisite system by adding new hooks for context compression, session management, and voice server integration. The changes introduce notification capabilities for context compression events and establish a voice server infrastructure with macOS service integration.

Key files: `.claude/hooks/context-compression-hook.ts`, `.claude/voice-server/server.ts`, `.claude/voice-server/macos-service/`
> - `0dd0f87`: Merge pull request #9 from mvoehringer/feature/fix-path. Files: `.claude/commands/capture-learning.md`, `.claude/commands/capture-learning.ts`, `.claude/commands/load-dynamic-requirements.md`, `.claude/context/CLAUDE.md`, `.claude/context/tools/CLAUDE.md` (+4 more).
