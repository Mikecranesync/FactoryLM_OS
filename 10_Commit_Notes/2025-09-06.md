---
title: "Commit Notes - 2025-09-06"
date: 2025-09-06
tags: [project/Backlog.md, project/My-Ralph, type/commit-note]
status: active
---

# Commit Notes - 2025-09-06

## Backlog.md — Push at 23:42 UTC

**Commits:** 23
**Trigger:** `fa53e76` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `fa53e76` | Create task-259 - Add-task-list-filters-for-Status-and-Priority.md | Alex Gavrilescu |
| `55acea6` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `a3e63b9` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `0cef060` | TASK-258 - Minimize task list footer: show only basic controls (navigate, task list/detail, edit, quit) | Alex Gavrilescu |
| `5d30fd1` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `3a80343` | TASK-258 - Filesystem: ignore leading zeros in task IDs (#333) | Alex Gavrilescu |
| `3924454` | Update task-247 - Web-UI-Read-only-Description-with-Edit-Toggle-(Hide-Editor-Mode-Buttons).md | Alex Gavrilescu |
| `a771213` | Update task-247 - Web-UI-Read-only-Description-with-Edit-Toggle-(Hide-Editor-Mode-Buttons).md | Alex Gavrilescu |
| `c13a14d` | TASK-247 - Web UI - Read-only Description with Edit Toggle (Hide Editor Mode Buttons) (#332) | Alex Gavrilescu |
| `e3056b8` | TASK-248 - TUI: circular up/down in list and Kanban (#329) | Alex Gavrilescu |
| `c1d2b60` | TASK-253 - Web Board: robust 4+ column horizontal scroll (#327) | Alex Gavrilescu |
| `ff4e88b` | TASK-250 - Docs: recursive listing/view + global IDs (#326) | Alex Gavrilescu |
| `04b3b06` | TASK-251 - Harden branch normalization to avoid origin/origin (#325) | Alex Gavrilescu |
| `cfbb819` | TASK-254 - Init: default No for Claude agent install (#324) | Alex Gavrilescu |
| `07c35c8` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `770f716` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `d35b828` | Fix running backlog without any commands | Alex Gavrilescu |
| `64cb524` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `d49b59f` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `b5d1fc0` | Fix release pipeline | Alex Gavrilescu |
| `b9fa426` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `60f9da3` | Cleanup tasks | Alex Gavrilescu |
| `b0e63d7` | TASK-252 - Packaging: stable .bin/backlog via wrapper (#323) | Alex Gavrilescu |

> **What changed:**
> - `fa53e76`: Added a new task (#259) to implement status and priority filters in the TUI task list. The filters will let users quickly narrow tasks by status (To Do, In Progress, Done) and priority (high, medium, low) while keeping the interface minimal. Key file: backlog/tasks/task-259 - Add-task-list-filters-for-Status-and-Priority.md
> - `55acea6`: Bumped version to 1.10.1 and refreshed the auto-generated project-status table in the README with the latest timestamp.  
Key files: README.md, package.json
> - `a3e63b9`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `README.md`, `package.json`.
> - `0cef060`: Simplified the task viewer footer to show only essential controls (navigate, list/detail toggle, edit, quit) by removing filter/move badges and extra shortcuts. This declutters the UI for a cleaner experience.  
Key file: src/ui/task-viewer.ts
> - `5d30fd1`: Updated README.md to reflect the new v1.10.0 release and refreshed the auto-generated Kanban board snapshot, showing current task counts and status.  
Key files: README.md, package.json
> - `3a80343`: Task ID resolution now ignores leading zeros and dotted-segment padding: `task-0001` is found when querying `1`, and `3.01` matches `3.1`.  
No filenames or ID generation changed—only the resolver logic in `src/utils/task-path.ts` was updated, with tests added in `src/test/task-path.test.ts`.
> - `3924454`: Updated backlog task-247 to “Done” and appended a full PR description: the task popup now uses a modern Preview/Edit modal with first-class fields (Description, AC, Plan, Notes), a sidebar for metadata, keyboard shortcuts, and a new `/api/tasks/:id/complete` endpoint.  
Key file: `backlog/tasks/task-247 - Web-UI-Read-only-Description-with-Edit-Toggle-(Hide-Editor-Mode-Buttons).md`
> - `a771213`: Completed the final acceptance criterion for task-251: the board now correctly falls back to local-only task loading when remoteOperations is disabled, preventing any remote-branch normalization attempts. Key file: backlog/tasks/task-251 - Board-harden-remote-branch-normalization-to-avoid-origin-origin-refs.md
> - `c13a14d`: Redesigned the task modal into a wider, two-pane layout: preview mode by default with a single Edit toggle, inline AC checkboxes, and a sticky sidebar for status/assignee/etc. Edit mode now hosts dedicated editors for Description, AC, Plan, Notes with one Save/Cancel for all; keyboard shortcuts (E, Esc, Cmd+S) and completion flow added. Key files: TaskDetailsModal.tsx, Modal.tsx, plus refreshed styles and API helpers.
> - `e3056b8`: Added circular (wrap-around) navigation to the TUI task list and Kanban columns: pressing up from the first item jumps to the last, and down from the last loops back to the first. Empty or single-item lists/columns now handle navigation without errors or crashes.  
Key files: src/ui/components/generic-list.ts, src/ui/board.ts
> - `c1d2b60`: Fixed the web Kanban board to cleanly handle 4+ columns by switching from a brittle min-w-fit flex to a CSS-grid layout (`grid-auto-flow: column` + `minmax(20rem, 1fr)`), enabling smooth horizontal scroll without header/card mis-alignment; 1–3 column responsive layouts stay intact.

Key files: `src/web/components/Board.tsx`, `src/web/styles/style.css`
> - `ff4e88b`: Backlog now fully supports nested docs: `backlog doc list` and `view <id>` work recursively under backlog/docs, and new docs always get the next global ID even offline.  
Key files: src/file-system/operations.ts (recursive list), src/cli.ts (view by ID), src/test/docs-recursive.test.ts, README.md
> - `04b3b06`: Hardened remote branch normalization to prevent malformed refs like origin/origin from breaking the backlog board; normalizeRemoteBranch now strips invalid prefixes and listRecentRemoteBranches filters out HEAD/ambiguous entries. Added comprehensive unit tests and confirmed remoteOperations=false keeps everything local.  
Key files: src/core/task-loader.ts, src/git/operations.ts, src/test/task-loader-branch-normalization.test.ts
> - `cfbb819`: Changed the `backlog init` flow so the Claude Code agent is now opt-in instead of opt-out: the prompt defaults to “No” and Enter skips installation. Non-interactive mode keeps the agent off unless `--install-claude-agent true` is explicitly passed. Key files touched: `src/cli.ts`, `scripts/cli.cjs`, docs (`README.md`, `AGENTS.md`, `CLAUDE.md`), and a new test `src/test/cli-init-claude-default.test.ts`.
> - `07c35c8`: Bumped version to 1.9.5 and refreshed the auto-generated project-status table in the README with the latest timestamp and counts.  
Key files: README.md, package.json
> - `770f716`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `README.md`, `package.json`.
> - `d35b828`: Fixed a crash when running `backlog` with no subcommands under Bun by filtering out the injected binary path that Bun’s global shim passes as an argument.  
Key files: `scripts/cli.cjs`, `src/cli.ts`.
> - `64cb524`: Bumped version to 1.9.4 and refreshed the auto-generated project-status table in the README with the latest timestamp and counts.  
Key files: README.md, package.json
> - `d49b59f`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `README.md`, `package.json`.
> - `b5d1fc0`: Fixed a release pipeline bug where the Linux/macOS binary wasn’t marked executable, causing the final archive to contain a non-runnable file.  
Key file: `.github/workflows/release.yml`
> - `b9fa426`: Updated README.md and package.json to reflect the current project board status and bumped the version to v1.9.3. The board now shows several completed tasks, including packaging fixes and TUI enhancements, while maintaining the same in-progress and pending tasks.
> - `60f9da3`: Completed and archived the task for fixing case-insensitive priority filtering in the backlog.  
Key file: backlog/completed/task-245 - Fix-case-insensitive-priority-filtering.md
> - `b0e63d7`: Fixed local installs so `npm i -D backlog.md` now reliably creates `node_modules/.bin/backlog` that runs the correct platform binary via a Node wrapper.  
Platform-specific tarballs no longer declare a `bin`, while the root package’s `bin` points to `scripts/cli.cjs`; CI added cross-OS install-sanity tests.  

Key files: `package.json`, `.github/workflows/release.yml`, `src/test/packaging-bin.test.ts`

## My-Ralph — Push at 05:49 UTC

**Commits:** 3
**Trigger:** `0635b65` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `0635b65` | Fix grep -c fallback logic causing syntax error in double bracket test | frankbria |
| `2ed13c3` | Fix arithmetic expansion for integer comparison in ralph_loop.sh | frankbria |
| `d6b3802` | Fix syntax error in ralph_loop.sh line 280 | frankbria |

> **What changed:**
> - `0635b65`: Fixed a bash syntax error in `ralph_loop.sh` by removing the `|| echo 0` fallback that was injecting extra “0” lines into variables when `grep -c` found no matches. Now explicitly sets variables to 0 when empty, preventing double-bracket test failures.
> - `2ed13c3`: Fixed integer comparison in ralph_loop.sh by wrapping grep counts in `$((...))` to force numeric context, preventing bash syntax errors when comparing against floating-point values.  
Key file: ralph_loop.sh
> - `d6b3802`: Fixed bash syntax error by removing quotes around numeric fallback values in `grep` commands within `should_exit_gracefully()`. This ensures arithmetic comparisons work correctly when counting items in `@fix_plan.md`.

**Key file:** `ralph_loop.sh`
