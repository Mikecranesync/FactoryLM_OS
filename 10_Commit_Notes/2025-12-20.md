---
title: "Commit Notes - 2025-12-20"
date: 2025-12-20
tags: [project/Agent-Factory, project/Thefuture, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-20

## Agent-Factory — Push at 16:01 UTC

**Commits:** 14
**Trigger:** `3ec76f4` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `3ec76f4` | feat: Add vision model support for Print→Guide pipeline (Phase 1) | Mikecranesync |
| `6af9d84` | feat: Implement ClaudeExecutor for SCAFFOLD platform (#81) | Mikecranesync |
| `e0657e5` | feat: Implement SafetyMonitor for SCAFFOLD cost/time tracking | Mikecranesync |
| `d283ba0` | feat: Implement ScaffoldLogger for SCAFFOLD platform | Mikecranesync |
| `bb7e096` | test: Add telegram bot sandbox test suite | Mikecranesync |
| `8cab271` | docs: Mark task-scaffold-git-worktree-manager as Done (already complete via PR #75) | Mikecranesync |
| `a1f91d9` | docs: Mark task-scaffold-orchestrator as Done (already complete via PR #75) | Mikecranesync |
| `78308fc` | fix: LangChain 1.2.0 compatibility layer (Option C) | Mikecranesync |
| `38b199d` | feat: Implement ContextAssembler for SCAFFOLD platform (task-scaffold-context-assembler) | Mikecranesync |
| `4cd9c71` | docs: Autonomous session report Dec 19 - Admin panel verified complete | Mikecranesync |
| `118e1b9` | feat: Telegram-SCAFFOLD Integration - Headless Chatbot Interface (#75) | Mikecranesync |
| `54bd1e2` | feat(backlog): Add 20 TIER subtasks + CLAUDE_START_HERE_UPDATED.md (#76) | Mikecranesync |
| `66d2ac7` | docs: Add cost-optimized routing documentation (#74) | Mikecranesync |
| `446a1d6` | config: Disable CodeRabbit PR blocking (#77) | Mikecranesync |

> **What changed:**
> - `3ec76f4`: Added vision model support to the Print→Guide pipeline by configuring Claude 3.5 Sonnet and GPT-4V as vision-capable models, creating a new VISION routing tier, and updating the LLM router to handle image inputs. Key files: agent_factory/llm/config.py, agent_factory/llm/types.py, validate_vision.py.
> - `6af9d84`: **Added ClaudeExecutor for headless Claude Code CLI execution.** New ExecutionResult model captures task outcomes, success detection, file changes, and cost tracking. ClaudeExecutor integrates with ContextAssembler to run `claude-code --non-interactive`, parsing output for success indicators like exit code 0, "completed successfully", git commits, and test passes. Key files: `agent_factory/scaffold/claude_executor.py`, `agent_factory/scaffold/models.py`, `tests/scaffold/test_claude_executor.py`.
> - `e0657e5`: Added a SafetyMonitor class to enforce hard limits on API cost ($5), runtime (4h), and consecutive failures (3) during SCAFFOLD orchestrator sessions.  
It acts as a circuit-breaker—tripping when any limit is hit and auto-resetting on success—returning a clear (allowed, reason) tuple so callers can abort gracefully.

Key files: agent_factory/scaffold/safety_monitor.py, tests/scaffold/test_safety_monitor.py
> - `d283ba0`: Added a structured JSONL logger for SCAFFOLD orchestrator sessions that records session lifecycle and per-task cost/time metrics.  
Key files: agent_factory/scaffold/logger.py (new), tests/scaffold/test_logger.py (new).
> - `bb7e096`: Added a sandbox-only test suite (`test_telegram_sandbox.py`) that validates every Telegram bot import and all 20 admin command handlers without touching the real API.  
It confirms async signatures and Windows-safe ASCII output, giving green lights for core, admin panel, and handler layers so the bot can now be safely tried in a private Telegram channel.
> - `8cab271`: Marked the Git Worktree Manager task as Done—already implemented in PR #75.  
Key files: `agent_factory/scaffold/worktree_manager.py` and `agent_factory/scaffold/models.py`.
> - `a1f91d9`: **Summary:** Marked the headless orchestrator skeleton task as complete—PR #75 already delivered the full implementation. The orchestrator now fetches tasks from Backlog.md, routes them via labels, manages sessions/budgets, and writes results back.

**Key files:** backlog/tasks/task-scaffold-orchestrator - BUILD-Headless-Orchestrator-Skeleton.md
> - `78308fc`: Added a LangChain 1.2.0 compatibility shim (`agent_factory/compat/langchain_shim.py`) that wraps the new `create_agent()` API in the old `AgentExecutor` interface, unblocking all imports without downgrading.  
Key files touched: `agent_factory.py`, `agent_presets.py`, `chat_session.py`, plus the new shim module; all now import from the shim and start cleanly.
> - `38b199d`: Built a ContextAssembler for the SCAFFOLD platform that bundles CLAUDE.md prompts, a depth-limited file tree, recent git commits, and the task spec into a single string for Claude Code CLI. Key files: agent_factory/scaffold/context_assembler.py and its test suite.
> - `4cd9c71`: **Summary:** Discovered the Telegram admin panel is already complete (8/8 phases, 20 commands), but a critical LangChain 1.2.0 API change (`AgentExecutor` removed) blocks all agent operations. **Key files:** `AUTONOMOUS_SESSION_DEC19.md`, `agent_factory/core/agent_factory.py`. Recommend immediate downgrade to LangChain 0.2.16 to unblock testing and Task-30.
> - `118e1b9`: **Summary:**  
Added a natural-language Telegram interface to the autonomous SCAFFOLD task runner. Users can now type `/scaffold Build a login API` and the bot will spin up the orchestrator in a background thread, returning real-time status and a PR link when done. A new `NLTaskParser` converts free-form text into structured tasks, while `ScaffoldHandlers` manages async execution, cost/time guards, and session tracking.

**Key files:**  
`agent_factory/integrations/telegram/nl_task_parser.py` · `agent_factory/integrations/telegram/scaffold_handlers.py`
> - `54bd1e2`: **Summary:** Added 20 TIER subtasks across three autonomous-company layers (TIER 0 Factory, TIER 1 Research, Skunk-Works R&D) plus a unified entry-point doc. The commit creates the full backlog for a 64-hour build-out of a CEO-commanded multi-agent system and wires the new Telegram handlers into the bot.

**Key files:**  
- `CLAUDE_START_HERE_UPDATED.md` (new 311-line unified guide)  
- `backlog/tasks/task-38.*` … `task-40.*` (20 new task specs)  
- `agent_factory/integrations/telegram/tier0_handlers.py` (new CEO interface)  
- `telegram_bot.py` (+17 lines to register TIER 0 handlers)
> - `66d2ac7`: **Cost-optimized routing infrastructure is now live** — the AgentFactory now defaults to intelligent model selection (enable_routing=True) via a new LangChain adapter that routes queries to the cheapest capable model (gpt-4o-mini for simple tasks, claude-3.5 for coding, etc.), cutting LLM costs ~73%.  

Key files: `agent_factory/llm/langchain_adapter.py` (new RoutedChatModel), `agent_factory/core/agent_factory.py` (routing enabled), plus stub caches and streaming modules.
> - `446a1d6`: Disabled CodeRabbit’s PR-blocking behavior (`request_changes_workflow: false`) so PRs #74-#76 can merge while still receiving automated reviews.  
Key file: `.coderabbit.yaml`

## Thefuture — Push at 12:14 UTC

**Commits:** 6
**Trigger:** `f8a04c3` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `f8a04c3` | Fix missing references - issue #200 | Daniel Miessler |
| `90c99b9` | Fix: Update all directory references to use Title Case paths | Daniel Miessler |
| `cfaa319` | Fix: Remove duplicate Observability directory and update references | Daniel Miessler |
| `ef7e82d` | Fix: Setup script now respects user's directory choice | Daniel Miessler |
| `b969fa9` | Fix: Statusline MCP count now checks both .mcpServers and .mcp.json | Daniel Miessler |
| `f137e5e` | Capitalize top-level directories and fix settings.json | Daniel Miessler |

> **What changed:**
> - `f8a04c3`: Fixed broken references in the skill system documentation: updated 4 dead links from `SKILL-STRUCTURE-AND-ROUTING.md` to the correct `SkillSystem.md`, and copied the missing `SkillWorkflowNotification` script from Kai to PAI so workflow tracking works. Key files: `CONSTITUTION.md`, `SKILL.md`, `SkillSystem.md`, and the new `Tools/SkillWorkflowNotification` script.
> - `90c99b9`: Fixed broken history system by updating all directory references from lowercase to Title Case. Changed hooks to use HISTORY_DIR constant instead of hardcoded paths, updated 50+ documentation files, and fixed observability server paths. Key files: capture-all-events.ts, capture-tool-output.ts, capture-session-summary.ts, and multiple SKILL.md files.
> - `cfaa319`: Removed the duplicate `.claude/Observability/` directory (80+ files) and updated all documentation references to point to the canonical `.claude/Skills/Observability/` version. Fixed path casing from lowercase 'skills' to 'Skills' in README.md and HookSystem.md. Key files modified: README.md, .claude/Skills/CORE/HookSystem.md, and the entire deleted `.claude/Observability/` tree.
> - `ef7e82d`: Fixed the setup script to properly handle user input with extra spaces by trimming whitespace in the `ask_input()` function. This ensures menu selections like "2 " are correctly matched to their corresponding case statements instead of falling through to default.

**Key file:** `.claude/setup.sh`
> - `b969fa9`: Statusline MCP count now checks both `.mcp.json` (current Claude Code default) and `.mcpServers` in `settings.json` (legacy), combining counts and names from both sources. Fixed null/empty value handling to prevent errors when files are missing or empty.

**Key file:** `.claude/statusline-command.sh`
> - `f137e5e`: Renamed all top-level directories from lowercase to Title Case (Agents, Commands, Hooks, Skills, Tools, Docs) and updated 558+ file references across setup scripts, TypeScript files, documentation, and workflows. Also added the missing PAI_SIMPLE_COLORS environment variable to settings.json to fix terminal display issues.

Key files: `.claude/Hooks/lib/pai-paths.ts`, `.claude/Commands/paiupdate.md`, `.claude/settings.json`

## cmms — Push at 16:04 UTC

**Commits:** 2
**Trigger:** `7d16ea9` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `7d16ea9` | feat: uetq events | Ibrahima G. Coulibaly |
| `64ab66a` | feat: uetq events | Ibrahima G. Coulibaly |

> **What changed:**
> - `7d16ea9`: Added a TypeScript ignore comment to suppress a type error when pushing UET (Universal Event Tracking) events to the `window.uetq` queue. This allows GA4-style events to be forwarded to Microsoft Ads without breaking the build.

Key file: frontend/src/utils/overall.ts
> - `64ab66a`: Added Microsoft UET (Bing Ads) event tracking alongside the existing GA4 events in the shared analytics helper.  
Key file: frontend/src/utils/overall.ts
