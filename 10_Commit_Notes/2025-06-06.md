---
title: "Commit Notes - 2025-06-06"
date: 2025-06-06
tags: [project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-06-06

## Backlog.md — Push at 18:51 UTC

**Commits:** 2
**Trigger:** `6a5fa08` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `6a5fa08` | TASK-1 - CLI: Setup Core Project (Bun, TypeScript, Git, Linters) | Alex Gavrilescu |
| `c5c37af` | TASK-0 - Bootstrap Backlog.md Project Definition with Initial Tasks | Alex Gavrilescu |

> **What changed:**
> - `6a5fa08`: Initialized the Bun + TypeScript CLI project with Biome linting/formatting, a basic test suite, and project documentation. Set up standard ignore patterns and added CLAUDE.md for AI development guidance.

Key files: package.json, tsconfig.json, biome.json, index.ts, index.test.ts
> - `c5c37af`: Bootstrapped the Backlog.md project by scaffolding the full `.backlog` folder hierarchy (archive, decisions, docs, drafts, milestones, tasks) and populating it with a `config.yml`, milestone files (M-0 … M-5), and 13 granular task files that define the CLI → GUI roadmap. Added supporting files (`AGENTS.md`, readme stubs) so the repo now has a complete, version-controlled task-tracking skeleton ready for development.

## cmms — Push at 15:42 UTC

**Commits:** 3
**Trigger:** `0fde513` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `0fde513` | fix:parent location selection | Ibrahima G. Coulibaly |
| `954fe56` | fix: minio container name | Ibrahima G. Coulibaly |
| `ff7ae07` | fix: minio container name | Ibrahima G. Coulibaly |

> **What changed:**
> - `0fde513`: Fixed parent location dropdown by switching from hard-coded root fetch (`locationsHierarchy`) to the already-loaded mini list (`locationsMini`) and using the generic `fetchLocations` instead of the removed `fetchRootLocations`.  
Key file: `frontend/src/content/own/components/form/CustomSelect2.tsx`
> - `954fe56`: Renamed the MinIO container from generic “minio” to “atlas_minio” to avoid naming collisions when multiple projects run on the same host.  
Key file: docker-compose.yml
> - `ff7ae07`: Fixed missing container_name for the MinIO service in docker-compose.yml so the container is now explicitly named "minio", ensuring predictable references in scripts and networks.
