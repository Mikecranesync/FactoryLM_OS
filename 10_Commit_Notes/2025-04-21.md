---
title: "Commit Notes - 2025-04-21"
date: 2025-04-21
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-21

## cmms — Push at 22:41 UTC

**Commits:** 6
**Trigger:** `8153940` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `8153940` | feat: enhance internationalization support in calendar component | Ibrahima G. Coulibaly |
| `68b7c56` | fix: refine attribute extraction for Microsoft OAuth2 integration | Ibrahima G. Coulibaly |
| `83f7f8b` | chore: oauth2 microsoft log attributes | Ibrahima G. Coulibaly |
| `bd91215` | fix: oauth2 microsoft | Ibrahima G. Coulibaly |
| `6026bf0` | fix: oauth2 redirect uri | Ibrahima G. Coulibaly |
| `d7d25b9` | feat: add configuration for optional license fingerprint requirement | Ibrahima G. Coulibaly |

> **What changed:**
> - `8153940`: Calendar now respects the user’s chosen language: month/year headers and first-day-of-week automatically switch with the active locale. Replaced hard-coded enUS/fr imports with the central `supportedLanguages` map so any configured language just works.  
Key files: `Actions.tsx`, `Calendar/index.tsx`, `i18n.ts`.
> - `68b7c56`: Fixed Microsoft OAuth2 user mapping to use standard claims (`email`, `sub`, `given_name`, `family_name`) instead of Azure-specific ones (`mail`, `id`, `givenName`, `surname`), ensuring reliable name/email extraction. Also cleaned up debug prints in both OAuth handler and LicenseService.  
Key files: `OAuth2AuthenticationSuccessHandler.java`, `LicenseService.java`
> - `83f7f8b`: Added debug logging to dump Microsoft OAuth2 attributes during user creation; helps troubleshoot missing profile data.  
Key file: OAuth2AuthenticationSuccessHandler.java
> - `bd91215`: Fixed Microsoft OAuth2 login by checking multiple email fields (`mail`, `preferred_username`, `userPrincipalName`) and adding null-safe handling for missing `surname`. Added debug logging to trace attribute mapping issues.

Key file: `OAuth2AuthenticationSuccessHandler.java`
> - `6026bf0`: Fixed OAuth2 redirect URIs by replacing Spring’s dynamic `{baseUrl}` placeholder with the explicit `api.host` property, ensuring callbacks always hit the correct backend URL.  
Key file: `OAuth2ClientRegistrationConfig.java`
> - `d7d25b9`: Added a toggle (`LICENSE_FINGERPRINT_REQUIRED`, default true) so license validation can run without sending the machine fingerprint—handy for locked-down or air-gapped deployments.  
Key files: `LicenseService.java`, `application.yml`, `docker-compose.yml`, `.env.example`.
