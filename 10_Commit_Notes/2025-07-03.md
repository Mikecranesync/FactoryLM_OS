---
title: "Commit Notes - 2025-07-03"
date: 2025-07-03
tags: [project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-07-03

## Backlog.md — Push at 21:30 UTC

**Commits:** 14
**Trigger:** `95c1b69` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `95c1b69` | task-108 - Create task task-108 | Alex Gavrilescu |
| `f95545e` | task-107 - Create task task-107 | Alex Gavrilescu |
| `76a29e4` | task-106 - Create task task-106 | Alex Gavrilescu |
| `5677966` | Further improve agent guidelines | Alex Gavrilescu |
| `14ca75e` | task-105 - Create task task-105 | Alex Gavrilescu |
| `5845902` | TASK-104 - Add --notes flag to task edit command for implementation n… (#148) | Alex Gavrilescu |
| `a2f8c76` | Improve agent guidelines to prevent Claude from implementing what is not requested | Alex Gavrilescu |
| `ec39b05` | fix test timing out on mac CI | Alex Gavrilescu |
| `41efb7b` | Initialize main branch in unit tests | Alex Gavrilescu |
| `88d4d9a` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `0105061` | Fix Biome 2.0 issues | Alex Gavrilescu |
| `1f4277d` | Optimize agent guidelines (#147) | Alex Gavrilescu |
| `2c2ab27` | Bump packages and migrate to Biome 2.0 | Alex Gavrilescu |
| `3c8ca17` | Add TASK-104 - Add --notes flag to task edit command for implementation notes | Alex Gavrilescu |

> **What changed:**
> - `95c1b69`: Created backlog task-108 to fix a bug where editing one section of a task (e.g., description) was wiping out other sections like acceptance criteria. The fix will ensure each section updates independently without affecting the rest of the task file.
> - `f95545e`: Created a new backlog task to add a CLI command `backlog agents --update-instructions` that refreshes AI-agent instruction files (.cursorrules, CLAUDE.md, etc.) with the latest versions shipped in the Backlog CLI. Key file: `.backlog/tasks/task-107 - Add-agents-update-instructions-command.md`
> - `76a29e4`: Created backlog task-106 to add `--desc` as a convenience alias for the existing `-d`/`--description` flag. Key file: `.backlog/tasks/task-106 - Add-desc-alias-for-description-flag.md`.
> - `5677966`: Clarified that any user request to “create a task” should be handled through the Backlog.md CLI, not manual file edits.  
Key file: src/guidelines/agent-guidelines.md
> - `14ca75e`: Created task-105 to rename the hidden `.backlog` folder to visible `backlog`, fixing file-referencing issues with tools like Claude and improving usability outside the CLI. Key file: `.backlog/tasks/task-105 - Remove-dot-from-.backlog-folder-name.md`.
> - `5845902`: Added `--notes` flag to `backlog task edit` so you can append implementation notes when marking a task Done. Notes are saved under a new “## Implementation Notes” section and merge cleanly if the section already exists.  
Key files: `src/cli.ts`, `src/markdown/serializer.ts`, plus new test suites.
> - `a2f8c76`: Updated agent guidelines to prevent scope creep by clarifying that implementation must strictly follow acceptance criteria—no extras unless AC is updated or a new task created. Key file: `src/guidelines/agent-guidelines.md`.
> - `ec39b05`: Fixed a flaky test on macOS CI by disabling auto-commit during task creation and switching from file-system globbing to the internal listTasks() API to verify the task was created.  
Key file: src/test/core.test.ts
> - `41efb7b`: All unit tests now explicitly initialize Git repos with `git init -b main`, ensuring the default branch is always named “main” instead of relying on Git’s system default. This eliminates test flakiness when the local Git config defaults to “master” or another branch name.

Key files touched: every test file that spins up a temporary Git repo—acceptance-criteria, cli-dependency, cli-parent-shorthand, cli, core, dependency, implementation-plan, parent-id-normalization, remote-id-conflict, and start-id tests.
> - `88d4d9a`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `.cursorrules`, `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `src/guidelines/agent-guidelines.md`.
> - `0105061`: Updated the project to comply with Biome 2.0 linting/formatting rules: narrowed the included file patterns in `biome.json`, removed an unused import in the post-uninstall script, and fixed a handful of unused-variable and import-order warnings across the CLI, file-system, git, UI and test modules.  
Key files touched: `biome.json`, `scripts/postuninstall.cjs`, `src/cli.ts`, `src/file-system/operations.ts`, `src/git/operations.ts`.
> - `1f4277d`: Simplified the AI agent guidelines from 357 lines to 40 by removing verbose sections and focusing on essential commands and structure. This makes the guidelines more concise and likely to be followed by AI agents when using the backlog.md CLI tool.

Key files: `.cursorrules`, `src/guidelines/agent-guidelines.md`
> - `2c2ab27`: Upgraded the project to Biome 2.0.6, switching from the deprecated `organizeImports` top-level key to the new `assist.actions.source.organizeImports` setting and tightening the style lint rules.  
Key files touched: biome.json, package.json, bun.lock.
> - `3c8ca17`: Added backlog task TASK-104 to implement a `--notes` flag for the `backlog task edit` command, letting users attach multi-line implementation notes when marking a task Done.  
Key file: `.backlog/tasks/task-104 - Add-notes-flag-to-task-edit-command-for-implementation-notes.md`

## cmms — Push at 19:33 UTC

**Commits:** 1
**Trigger:** `dbefecd` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `dbefecd` | fix: handle null assigned users in labor section of work order report | Ibrahima G. Coulibaly |

> **What changed:** Fixed a null-pointer crash in the work-order PDF when a labor line has no assigned user; now prints blank instead of blowing up.  
Key file: `api/src/main/resources/templates/work-order-report.html`
