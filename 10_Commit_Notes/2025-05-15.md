---
title: "Commit Notes - 2025-05-15"
date: 2025-05-15
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-05-15

## cmms — Push at 20:07 UTC

**Commits:** 5
**Trigger:** `96640aa` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `96640aa` | fix: get users mini | Ibrahima G. Coulibaly |
| `1d715c9` | fix: getPendingRequestsCount condition | Ibrahima G. Coulibaly |
| `2d0dc44` | feat: add support for fetching all users including requesters via getUsersMini | Ibrahima G. Coulibaly |
| `28b2a24` | refactor: calendar event handling to support generic event types and unified permission checks | Ibrahima G. Coulibaly |
| `7c444ba` | feat: add localized notifications for completed work orders | Ibrahima G. Coulibaly |

> **What changed:**
> - `96640aa`: Fixed a null-safety bug in the `/users/mini` endpoint by replacing the unsafe ternary check (`withRequesters ? …`) with `Boolean.TRUE.equals(withRequesters)` to prevent NPE when the param is omitted.  
Key file: `UserController.java`
> - `1d715c9`: Fixed a permission check bug that was preventing pending request counts from loading for most users. Replaced the overly restrictive `hasViewOtherPermission` check with a simple role check that excludes only REQUESTER users.  
**Key file:** `frontend/src/layouts/ExtendedSidebarLayout/Sidebar/SidebarMenu/index.tsx`
> - `2d0dc44`: Added an optional `withRequesters` flag to `/users/mini` so the API can now return every enabled user (not just workers).  
Frontend & mobile Redux layers keep this full list in `allUsersMini`, and CompanySettingsContext fetches it when authenticated; sidebar only loads pending requests if the user can view others’ requests.

Key files:  
- api/src/main/java/com/grash/controller/UserController.java  
- frontend/src/slices/user.ts & contexts/CompanySettingsContext.tsx  
- mobile/slices/user.ts & contexts/CompanySettingsContext.tsx
> - `28b2a24`: Calendar events now support both WorkOrder and PreventiveMaintenance via a generic `CalendarEvent<T>` and a unified `canViewWorkOrderBase` permission helper moved to `WorkOrderBase`.  
Key files: `WorkOrderController`, `CalendarEvent`, `WorkOrderBase`.
> - `7c444ba`: Added localized push/email notifications when a work order is marked complete; admins with the right permissions now get alerts in their language. Broadened exception handling so one failing notification won’t crash the batch.  
Key files: WorkOrderController.java, NotificationService.java, all messages_*.properties.
