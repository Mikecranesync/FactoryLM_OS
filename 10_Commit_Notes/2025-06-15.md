---
title: "Commit Notes - 2025-06-15"
date: 2025-06-15
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-06-15

## Backlog.md — Push at 21:22 UTC

**Commits:** 16
**Trigger:** `83d4dac` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `83d4dac` | TASK-74 - Fix TUI crash on Windows (#103) | Alex Gavrilescu |
| `dc976b2` | TASK-73 - Fix Windows binary package name (#102) | Alex Gavrilescu |
| `994f40f` | TASK-70 - CI: eliminate extra binary download (#98) | Alex Gavrilescu |
| `4abcbd5` | TASK-72 - Fix board view on Windows without terminfo (#100) | Alex Gavrilescu |
| `d2fc3de` | TASK-71 - Fix single task view regression (#99) | Alex Gavrilescu |
| `20787ca` | TASK-69 - CLI: start tasks IDs at 1 (#97) | Alex Gavrilescu |
| `9610a69` | TASK-68 - Verify Windows binary uses .exe (#96) | Alex Gavrilescu |
| `eec8d85` | Move all dependencies as dev dependencies since we are publishing a self contained package | Alex Gavrilescu |
| `7433b15` | TASK-67 - Add -p shorthand for --parent option in task create command (#95) | Alex Gavrilescu |
| `4d33eba` | Update icons in readme.md | Alex Gavrilescu |
| `997d243` | Update license icon | Alex Gavrilescu |
| `bc8fc7f` | Add TASK-67 - Add -p shorthand for --parent option in task create command | Alex Gavrilescu |
| `238877c` | remove unnecessary readme | Alex Gavrilescu |
| `f8c161a` | Fix github release workflow | Alex Gavrilescu |
| `b2c3ba4` | Archive migration to ink tasks | Alex Gavrilescu |
| `3284b79` | TASK-61 - Embed blessed in standalone binary (#94) | Alex Gavrilescu |

> **What changed:**
> - `83d4dac`: Fixed Windows TUI crash by disabling blessed’s tput lookup (`{ tput: false }`) in every screen init; wrapped creation in a shared helper.  
Key files: src/ui/tui.ts, src/ui/board.ts, src/ui/task-viewer.ts, src/ui/loading.ts, src/ui/components/generic-list.ts
> - `dc976b2`: Fixed Windows CLI by mapping `win32` → `windows` so the correct `backlog.md-windows-x64` binary is found and spawned.  
Added `resolveBinary.cjs` helper, updated `cli.cjs`, tests, and release workflow to bundle the fix.

**Key files:** `scripts/resolveBinary.cjs`, `scripts/cli.cjs`, `.github/workflows/release.yml`, `src/test/resolveBinary.test.ts`
> - `994f40f`: Switched to platform-specific npm packages so `npm i -g backlog.md` installs only the matching binary, eliminating the old GitHub-download step.  
Key files: `.github/workflows/release.yml`, `package.json`, removed `scripts/cli-download.cjs` & `scripts/getBinaryName.cjs`.
> - `4abcbd5`: Fixed Windows crash on `backlog board view` by disabling blessed’s Tput layer (`tput: false`) in every screen creation, eliminating the ENOENT hunt for missing terminfo files.  
Key files touched: src/ui/board.ts, src/ui/tui.ts, src/ui/components/generic-list.ts, src/ui/loading.ts, src/ui/task-viewer.ts, plus test updates.
> - `d2fc3de`: Fixed a regression where `backlog task <id>` was showing raw markdown instead of the enhanced split-pane viewer. Now both `backlog task <id>` and `backlog task view <id>` use the same interactive UI and pre-select the requested task, while non-TTY output stays plain. Key files: src/cli.ts, src/test/cli.test.ts.
> - `20787ca`: CLI now numbers the first task as `task-1` instead of `task-0` by initializing the ID counter at 0 instead of -1. Added a test that verifies `backlog task create` produces a file starting with `task-1 -`.

Key files: src/cli.ts, src/test/start-id.test.ts
> - `9610a69`: Windows packaging fixed: the CLI wrapper now correctly appends `.exe` to the binary name on Windows so `npx backlog` and `bunx backlog` work.  
Key files: `scripts/getBinaryName.cjs` (new shared helper), `scripts/cli.cjs`, `scripts/cli-download.cjs`, plus updated release workflow and tests.
> - `eec8d85`: Moved runtime deps to devDeps since the CLI is bundled into a self-contained package for publishing—nothing is installed by consumers.  
Key file: package.json
> - `7433b15`: Added `-p` shorthand for `--parent` in `backlog task create`, so `backlog task create "Task title" -p task-5` now works alongside the existing `--parent` flag. Updated CLI option definition and added tests to ensure both forms behave identically. Key files: `src/cli.ts`, `src/test/cli-parent-shorthand.test.ts`.
> - `4d33eba`: Updated the npm and Bun badge icons in the README to use inline HTML with consistent styling and a blue color for the npm badge. No functional changes—purely cosmetic polish.  
Key file: readme.md
> - `997d243`: Fixed the license badge URL in the README to point to the correct GitHub user/org (MrLesk instead of your-org).  
Key file: readme.md
> - `bc8fc7f`: Added a backlog task (TASK-67) to implement `-p` as a shorthand for the `--parent` flag in the `task create` CLI command. The change is purely a usability enhancement—no code yet, just the spec.
> - `238877c`: Deleted the npm-publishing guide since the repo no longer needs to publish to npm; the workflow and secrets have been removed.  
Key file: docs/npm-publishing.md
> - `f8c161a`: Fixed the GitHub release workflow to properly build and publish the CLI binary. The workflow now uploads raw binaries instead of archives, uses a new download script for npm packages, and correctly bundles only essential files for npm publishing.

**Key files modified:**  
`.github/workflows/release.yml`, `.npmignore`, `scripts/cli-download.cjs`, `scripts/cli.cjs`
> - `b2c3ba4`: Archived the 9-part migration plan that replaced the Blessed-based terminal UI with Ink/React components.  
Key files: all `.backlog/archive/tasks/task-60*` subtasks covering dependency swap, component rewrites, and cleanup.
> - `3284b79`: **Summary:**  
The build pipeline now bundles the `blessed` UI library directly into the standalone binary, eliminating the need for users to install it locally. CI and release workflows were updated to remove `--external blessed`, ensuring the dependency is embedded. This allows the `backlog` CLI to run globally in any directory, including non-Node.js projects.

**Key files modified:**  
`.github/workflows/ci.yml`, `.github/workflows/release.yml`, `package.json`, `scripts/build-standalone.js`
