---
title: "Commit Notes - 2025-09-28"
date: 2025-09-28
tags: [project/Backlog.md, project/Thefuture, type/commit-note]
status: active
---

# Commit Notes - 2025-09-28

## Backlog.md — Push at 12:13 UTC

**Commits:** 2
**Trigger:** `6aa82f6` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `6aa82f6` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `8b3540b` | Feat: Remove background from splash screen logo (#379) | Alex Gavrilescu |

> **What changed:**
> - `6aa82f6`: Updated README.md and package.json to reflect the current project board snapshot for v1.14.3, moving TASK-275 (core module refactor) and TASK-262 (TUI Kanban columns) from To-Do and In-Progress into Done. No functional code changes—just documentation refresh.
> - `8b3540b`: Replaced the solid-block splash logo with a spaced-out version that removes the background fill, improving readability on terminals with dark or transparent backgrounds.  
Key file: src/ui/splash.ts

## Thefuture — Push at 22:32 UTC

**Commits:** 6
**Trigger:** `263d97f` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `263d97f` | Bump version to v0.2.3 | Daniel Miessler |
| `43b02a1` | Remove dots from hidden files for better visibility | Daniel Miessler |
| `08de5e1` | Move .env-example to PAI_DIRECTORY | Daniel Miessler |
| `8ff95c6` | Fix .env file location confusion | Daniel Miessler |
| `152d047` | Fix hardcoded path in session-start-hook.ts | Daniel Miessler |
| `2256a04` | Release v0.2.2 - Directory Visibility Update | Daniel Miessler |

> **What changed:**
> - `263d97f`: Released v0.2.3: renamed the hidden `.claude` folder to visible `PAI_DIRECTORY`, flattened all dot-files (`.zshrc`, `.env-example`), and replaced every hard-coded path with the `${PAI_DIR}` variable so the tool can be installed anywhere and still work with any AI vendor.  
Key file: README.md
> - `43b02a1`: Renamed dot-prefixed files (.zshrc → zshrc-aliases, .env-example → env-example) to make them visible in file managers and fix compatibility issues with Obsidian and other tools. Removed broken zshrc_file symlink and updated all documentation references. Key files: env-example, zshrc-aliases, README.md
> - `08de5e1`: **Configuration reorganization**: Moved `.env-example` from project root into `PAI_DIRECTORY` to consolidate all configuration files in one location. Updated documentation to reference the new path and removed a broken symbolic link.

**Key files**: `PAI_DIRECTORY/.env-example`, `README.md`, `PAI_DIRECTORY/documentation/README.md`
> - `8ff95c6`: **Summary:** Fixed environment file location confusion by standardizing on `${PAI_DIR}/.env` instead of `~/.env`. This keeps configuration files with the PAI installation rather than polluting the home directory, making the system more portable and maintainable.

**Key files modified:** `.env-example`, `README.md`, `quick-start.md`, `voice-system.md`, `web-research.md`, `CLAUDE.md`, `voice-server/README.md`
> - `152d047`: Fixed hardcoded user path in session-start-hook.ts by replacing it with dynamic PAI_DIR environment variable. Now uses configurable PAI_DIR/hooks/stop-hook.ts path with fallback to $HOME/PAI/PAI_DIRECTORY.

Key file: PAI_DIRECTORY/hooks/session-start-hook.ts
> - `2256a04`: Renamed the hidden `.claude` folder to `PAI_DIRECTORY` to fix dot-prefix compatibility issues with tools like Obsidian and emphasize platform-agnostic support. Updated all documentation and scripts to use the `${PAI_DIR}` variable instead of hard-coded paths.

Key files: `PAI_DIRECTORY/` (formerly `.claude/`), `README.md`, and all documentation files under `PAI_DIRECTORY/documentation/`.
