---
title: "Commit Notes - 2025-12-08"
date: 2025-12-08
tags: [project/Agent-Factory, project/Backlog.md, project/Thefuture, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-08

## Agent-Factory — Push at 15:58 UTC

**Commits:** 1
**Trigger:** `3451b00` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `3451b00` | feat: Telegram bot context retention fix + lessons learned database | Mikecranesync |

> **What changed:** Fixed Telegram bot's 0% context retention by replacing broken ConversationBufferMemory with direct prompt injection of chat history into agent inputs. Added comprehensive lessons learned database documenting 5 key insights from the debugging process. Key files: bot.py, agent_presets.py, session.py, and new docs/lessons_learned/ directory.

## Backlog.md — Push at 22:33 UTC

**Commits:** 2
**Trigger:** `1e3083b` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1e3083b` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `a1b9430` | TASK-340 - Fix nix build by pinning bun2nix to V1 (#455) | Tim Kleinschmidt |

> **What changed:**
> - `1e3083b`: Updated README to reflect the current project board and bumped package.json to v1.27.1. Key files: README.md, package.json.
> - `a1b9430`: Pinned bun2nix to its V1 revision (85d692d) so the generated bun.nix stays a plain attribute set that flake.nix’s mkBunDerivation expects, restoring successful nix builds.  
Key files: bun.nix, package.json, scripts/update-nix.sh

## Thefuture — Push at 18:41 UTC

**Commits:** 1
**Trigger:** `b70e808` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b70e808` | feat: Add intelligent PAI update system with /paiupdate command | Daniel Miessler |

> **What changed:** Added an intelligent PAI update system with `/paiupdate` (alias `/pa`) that safely fetches upstream changes to a staging area, analyzes differences against your customizations, and performs smart merges while preserving your settings and custom files. The system automatically backs up before changes and handles settings.json specially to add new keys without overwriting your values.

Key files: `.claude/commands/paiupdate.md`, `.claude/commands/pa.md`, `.gitignore`

## cmms — Push at 02:52 UTC

**Commits:** 4
**Trigger:** `cb50a49` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `cb50a49` | fix: company default roles | Ibrahima G. Coulibaly |
| `a892e69` | fix: skip pm past execution | Ibrahima G. Coulibaly |
| `25b5f03` | fix: meter reading value type | Ibrahima G. Coulibaly |
| `a00b505` | fix: meter reading value type | Ibrahima G. Coulibaly |

> **What changed:**
> - `cb50a49`: Fixed a bug where new companies weren’t getting the correct default roles.  
Switched the role list from a lazily-initialized Set to an empty List and now explicitly assign default roles via RoleService when a company is created.  
Key files: CompanySettings.java, CompanyService.java
> - `a892e69`: Fixed a bug where overdue PM schedules would “catch up” by firing every missed execution; now they simply skip to the next scheduled time.  
Key file: `ScheduleService.java`
> - `25b5f03`: Fixed the meter reading value column to store decimals by changing its type from integer to double.  
Key file: `api/src/main/resources/db/changelog/2025_12_08_1765155752_reading_value.xml`
> - `a00b505`: Fixed meter reading value type from `long`/`String` to `double` in both DTO and entity to support decimal readings. Added Liquibase migration to update the database column type accordingly. Key files: `ReadingPatchDTO.java`, `Reading.java`, `master.xml`.
