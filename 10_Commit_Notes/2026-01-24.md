---
title: "Commit Notes - 2026-01-24"
date: 2026-01-24
tags: [project/RideView, type/commit-note]
status: active
---

# Commit Notes - 2026-01-24

## RideView — Push at 16:10 UTC

**Commits:** 17
**Trigger:** `b037fa1` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b037fa1` | feat: v5.7.0 - Add calibration screen with live HSV adjustment | Mikecranesync |
| `d148998` | fix: v5.6.4 - Tune shape validation based on real torque stripe analysis | Mikecranesync |
| `d11dedd` | fix: v5.6.3 - Add shape validation to prevent early detection triggers | Mikecranesync |
| `7c2ffe9` | debug: v5.6.2 - Add magenta debug indicator for line_points | Mikecranesync |
| `ac789d6` | fix: v5.6.1 - Fix glowing stripe overlay rendering | Mikecranesync |
| `163d056` | feat: v5.6.0 - Add PDF report generation | Mikecranesync |
| `aa4f8cc` | feat: v5.2.0 - Enhanced visual feedback and glowing stripe overlay | Mikecranesync |
| `d809af8` | feat: v5.1.0 - Multi-color stripe detection (orange/white/yellow) | hharp |
| `148ef4c` | feat: Add training data labels for 87 frames | hharp |
| `1689d93` | feat: Add self-validating agentic engine with Claude Code hooks | Mikecranesync |
| `5e72917` | feat: v5.0.0 - Replace PCA skeleton with connected components | hharp |
| `b8b304f` | feat: v5.0 SkeletonWalker algorithm - 82.8% accuracy | hharp |
| `154d15e` | feat: v4.2 stability fixes - Kalman filter, smart zoom, hysteresis | hharp |
| `67c19ac` | feat: UX overhaul v4.1.0 - frame border, circle overlay, auto-focus/zoom | hharp |
| `3ca3281` | feat: Snap-to-stripe tracking + red/green pass indicator (v4.0.9) | hharp |
| `926592e` | fix: Lower min area threshold for smaller stripes (v4.0.8) | hharp |
| `d85285a` | feat: Add video recording (REC button) for v4.0.7 | hharp |

> **What changed:**
> - `b037fa1`: **Summary:** Added a live calibration screen (v5.7.0) that lets users adjust HSV color thresholds and shape filters via sliders, eliminating the need for code changes when lighting or stripe colors vary. Settings auto-save to JSON and persist across restarts.

**Key files:** `main_simple.py` (new CalibrationSettings class and UI), `buildozer.spec` (version bump)
> - `d148998`: Relaxed shape validation thresholds to better match real torque stripe characteristics (short/wide paint marks), improving detection rate from ~60% to 87%. Key files: main_simple.py, buildozer.spec
> - `d11dedd`: **Stripe detection hardened** to reduce false positives: minimum blob size raised to 1500 px, added aspect-ratio (≥2:1) and solidity (≥50 %) filters, and tightened white-pixel thresholds. Only elongated, solid shapes now trigger.  
Files: `main_simple.py`, `buildozer.spec` (v5.6.3)
> - `7c2ffe9`: Added a magenta debug square (50×50) in the top-left corner that appears when line_points are detected, helping distinguish between data detection and rendering issues. Version bumped to v5.6.2.

**Files modified:** buildozer.spec, main_simple.py
> - `ac789d6`: Fixed the glowing stripe overlay rendering in RideView v5.6.1 by properly closing the contour shape and improving point sampling (max 100 points) for performance. The overlay now correctly traces the detected stripe contour with visible 12/6/3px glow layers in green/red for PASS/FAIL states.

Key files: main_simple.py, buildozer.spec
> - `163d056`: Added professional PDF report generation to the RideView inspection workflow. Now when users save results, the app auto-creates a polished PDF (with color-coded PASS/FAIL, header, summary, and measurements table) alongside the existing TXT and JSON exports. Key files: main_simple.py (added _export_pdf_report), src/rideview/reports/pdf_generator.py (new module), buildozer.spec (added fpdf2 dep).
> - `aa4f8cc`: **v5.2.0 adds multi-modal feedback** to the torque-stripe checker: a glowing contour overlay now shows exactly what the algorithm detects, while state changes trigger a 72pt PASS/FAIL banner, full-screen color flash, and haptic vibration (1 pulse=PASS, 3=FAIL).  

**Key files:** `main_simple.py` (UI/feedback logic), `buildozer.spec` (added plyer + VIBRATE permission).
> - `d809af8`: feat: v5.1.0 - Multi-color stripe detection (orange/white/yellow). Files: `buildozer.spec`, `main_simple.py`.
> - `148ef4c`: feat: Add training data labels for 87 frames. Files: `extract_frames.py`, `training_frames/labels.csv`.
> - `1689d93`: Added a self-validating agentic engine to Claude Code that automatically validates code changes via hooks on Write/Edit/Stop events. New `/shippa` command ships production-ready code with automated checks for tests, linting, and formatting.

**Key files:** `.claude/commands/shippa.md`, `.claude/hooks/validators/*.py`, `CLAUDE.md`
> - `5e72917`: feat: v5.0.0 - Replace PCA skeleton with connected components. Files: `buildozer.spec`, `main_simple.py`.
> - `b8b304f`: feat: v5.0 SkeletonWalker algorithm - 82.8% accuracy. Files: `test_v43_analyzer.py`.
> - `154d15e`: feat: v4.2 stability fixes - Kalman filter, smart zoom, hysteresis. Files: `buildozer.spec`, `main_simple.py`, `test_data/v409_test/recording_20260123_195847.avi`, `test_data/v410_test/recording_20260123_202838.avi`.
> - `67c19ac`: feat: UX overhaul v4.1.0 - frame border, circle overlay, auto-focus/zoom. Files: `buildozer.spec`, `main_simple.py`.
> - `3ca3281`: feat: Snap-to-stripe tracking + red/green pass indicator (v4.0.9). Files: `buildozer.spec`, `main_simple.py`, `test_data/v408_test/recording_20260123_194305.avi`.
> - `926592e`: fix: Lower min area threshold for smaller stripes (v4.0.8). Files: `buildozer.spec`, `main_simple.py`, `test_data/v407_test/recording_20260123_192703.avi`.
> - `d85285a`: feat: Add video recording (REC button) for v4.0.7. Files: `buildozer.spec`, `main_simple.py`.
