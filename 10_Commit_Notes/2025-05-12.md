---
title: "Commit Notes - 2025-05-12"
date: 2025-05-12
tags: [project/cmms, project/frame_realtime_gemini_voicevision, type/commit-note]
status: active
---

# Commit Notes - 2025-05-12

## cmms — Push at 18:08 UTC

**Commits:** 4
**Trigger:** `f97a8c7` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `f97a8c7` | fix: remove custom equals and hashCode implementations from AssetImportDTO and LocationImportDTO | Ibrahima G. Coulibaly |
| `a9d6702` | refactor: location lookup to return multiple matches and update usages to select first | Ibrahima G. Coulibaly |
| `ccae214` | refactor: location import ordering to robustly identify top-level locations and prevent duplicates | Ibrahima G. Coulibaly |
| `c12b6d5` | refactor: asset import and lookup to support multiple assets with same name | Ibrahima G. Coulibaly |

> **What changed:**
> - `f97a8c7`: Removed custom equals/hashCode from AssetImportDTO and LocationImportDTO so identity is now based on object reference instead of just name + parentName. This prevents import duplicates that could arise when two rows share the same names but represent distinct items.  
Files touched: AssetImportDTO.java, LocationImportDTO.java
> - `a9d6702`: Location lookup now returns all matches instead of one; callers pick the first.  
Added equals/hashCode to LocationImportDTO so duplicate rows are detected during import.  
Key files: LocationService, LocationRepository, AssetService, WorkOrderService, MeterService, LocationImportDTO.
> - `ccae214`: Location import ordering now correctly spots true top-level locations (no parent or parent missing from the file) and uses a visited-set to stop the same location being added twice, fixing duplicates and orphaned entries.  
Key file: `api/src/main/java/com/grash/service/LocationService.java`
> - `c12b6d5`: Asset lookup now returns all matches (List) instead of a single Optional, letting the system handle name collisions gracefully. Import ordering was rewritten to dedupe and correctly nest parent/child assets, eliminating duplicates and mis-links.  
Key files: AssetRepository, AssetService, AssetImportDTO, WorkOrderService

## frame_realtime_gemini_voicevision — Push at 13:14 UTC

**Commits:** 1
**Trigger:** `3c6860d` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `3c6860d` | updated model name from gemini-2.0-flash-exp to gemini-2.0-flash-live-001; added new voices Leda, Orus, Zephyr | CitizenOneX |

> **What changed:** Updated the Gemini model from `gemini-2.0-flash-exp` to the newer `gemini-2.0-flash-live-001` and added three new voice options—Leda, Orus, and Zephyr—to the voice enum.  
Key file: `lib/gemini_realtime.dart`
