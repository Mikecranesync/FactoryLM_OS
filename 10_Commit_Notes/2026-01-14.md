---
title: "Commit Notes - 2026-01-14"
date: 2026-01-14
tags: [project/My-Ralph, project/Rivet-PRO, type/commit-note]
status: active
---

# Commit Notes - 2026-01-14

## My-Ralph — Push at 02:44 UTC

**Commits:** 1
**Trigger:** `509a969` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `509a969` | fix(workflow) Patch opencode bug in GitHub Actions timeout | Test User |

> **What changed:** Fixed a GitHub Actions timeout bug in the OpenCode review workflow by adding a 10-minute timeout and excluding the workflow file from self-triggering. Also updated Claude settings to allow additional bash commands and tools needed for the review process.

Key files: `.github/workflows/opencode-review.yml`, `.claude/settings.local.json`

## Rivet-PRO — Push at 23:50 UTC

**Commits:** 19
**Trigger:** `fffabbe` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `fffabbe` | feat(task-1.3): Integrate startup validation into bot | Mikecranesync |
| `741d62e` | feat(task-1.1): Create stabilize-rivet.ps1 script | Mikecranesync |
| `40c006c` | feat: Add Ralph Wiggum Resilience PRD tasks to Backlog.md | Mikecranesync |
| `ccb4049` | feat: Initialize Backlog.md for task management | Mikecranesync |
| `f525f33` | docs: Complete ARCHITECTURE.md rewrite with full database documentation | Mikecranesync |
| `cf9caaf` | feat: Add Neon MCP integration and DevOps automation workflows | Mikecranesync |
| `af31411` | feat: Add comprehensive Telegram bot commands | Mikecranesync |
| `6c677e8` | feat: Implement session memory system for context persistence | Mikecranesync |
| `030b12b` | feat(AUTO-KB-002): Implement Product Family Discoverer | Mikecranesync |
| `7802a7a` | feat(AUTO-KB-001): Create enrichment queue schema and service | Mikecranesync |
| `e4ea142` | feat: Add AUTO-KB story support to Ralph and insert 13 autonomous KB enrichment stories | Mikecranesync |
| `ecd9af7` | chore: Clean up repository - archive old docs locally | Mikecranesync |
| `caae9f3` | feat(STABLE-001 to STABLE-013): Implement stable/experimental split with feature flags | Mikecranesync |
| `f46dfa3` | Merge ralph/bug-fixes-round1: 5 critical bug fixes | Mikecranesync |
| `28e562d` | docs: Update Ralph PRD and progress log - all 5 stories complete | Mikecranesync |
| `e61e628` | fix(CRITICAL-LOGGING-001): Record ALL equipment lookups in interactions | Mikecranesync |
| `09bd082` | fix(CRITICAL-MANUAL-001): Improve manual search logging and filtering | Mikecranesync |
| `f6c7db7` | fix(FEEDBACK-USEREG-001): Create user in database on /start | Mikecranesync |
| `2a49f63` | fix(FEEDBACK-LINK-001): Fix PDF links not opening on mobile | Mikecranesync |

> **What changed:**
> - `fffabbe`: **Summary:** Bot now validates DATABASE_URL on startup, rejecting invalid endpoints like "ep-lingering-salad" before any database connections are attempted. This completes the Stability Foundation epic (task-1).

**Key files:** run_bot.py, task status files in backlog/tasks/
> - `741d62e`: Created a PowerShell stabilization script that locks in the correct Neon database endpoint (ep-purple-hall) and prevents accidental use of the wrong one (ep-lingering-salad). Added startup validation to verify database connectivity and core tables exist, plus a pre-commit hook to block .env file commits.

Key files: scripts/stabilize-rivet.ps1, rivet_pro/core/startup_validation.py, .github/WORKING_CONFIG.md
> - `40c006c`: Added 7 epics with 20 subtasks to Backlog.md covering Ralph Wiggum Resilience improvements: stability foundation, database failover (Neon→Turso→Supabase), data sync, state machine, LLM failover, Telegram message queue, and integration testing. Key files: backlog/tasks/task-1 through task-7.1 with detailed implementation steps for each resilience component.
> - `ccb4049`: Created a new task management system for RIVET PRO project with Backlog.md v1.28.0, establishing three workflow statuses (To Do, In Progress, Done) and enabling remote operations with MCP connector integration. The configuration sets up automated browser opening on port 6420 and includes branch activity monitoring for the project's task tracking infrastructure.
> - `f525f33`: **Summary:**  
Completely rewrote the ARCHITECTURE.md file to document the real production stack: Neon Postgres (ep-purple-hall-ahimeyn0) as primary, Supabase as 1.9 GB backup, 120-table schema with row counts, 17 service layers, LLM provider chain, data-flow diagrams, and startup/troubleshooting commands. Adds clarity on which Neon endpoint is live and warns against using the obsolete one.

**Key file:**  
ARCHITECTURE.md
> - `cf9caaf`: Added Neon database integration with automated PR workflows: each pull request now gets its own isolated database branch for safe testing, with automatic cleanup on PR close. Also introduced AI code review via CodeRabbit and a cross-platform bot launcher.

Key files: `.github/workflows/neon-branch.yml`, `.github/workflows/neon-cleanup.yml`, `.github/workflows/neon-migration-preview.yml`, `.coderabbit.yaml`, `run_bot.py`
> - `af31411`: Added comprehensive Telegram bot commands including `/wo create` for work-order creation, `/help` with full command list, `/menu` with inline buttons, `/library` for browsing equipment, and session controls `/reset` and `/done`.  
Key file: `rivet_pro/adapters/telegram/bot.py`
> - `6c677e8`: **Session memory system implemented** to survive context window clears and restarts. Two-tier approach: MCP memory graph for searchable project entities, plus file-based persistence via CLAUDE.md (current state/preferences), QUICK_CONTEXT.md (30-second restore), and SESSION_LOG.md (append-only history).

**Key files**: CLAUDE.md, docs/QUICK_CONTEXT.md, docs/SESSION_LOG.md
> - `030b12b`: Added a ProductFamilyDiscoverer service that maps a single model (e.g. "S7-1200") to its broader product family ("S7 Series") and auto-generates 10-20 related model numbers. It stores the family in the product_families table, falls back to hard-coded patterns for major vendors (Siemens, Allen-Bradley, ABB, Schneider), and can optionally verify members via Tavily search. Key file: rivet_pro/core/services/product_family_discoverer.py
> - `7802a7a`: Added a priority-based enrichment queue system for autonomous knowledge-base expansion, including 4 new tables (enrichment_queue, product_families, manual_files, enrichment_stats) and a service class that can queue jobs, fetch the next pending task, and update status. Key files: `rivet_pro/core/services/enrichment_queue_service.py`, `rivet_pro/migrations/018_autonomous_kb_enrichment.sql`.
> - `e4ea142`: Ralph now supports a new AUTO-KB epic: 13 autonomous stories that build a 24/7 enrichment pipeline—queue, family-discovery, background worker, and health monitoring—so the bot auto-expands its knowledge base when users search for unknown equipment.

Key files: auto_kb_stories.sql (new), scripts/ralph/ralph_local.py
> - `ecd9af7`: Repository cleanup moved 137 historical files (docs, scripts, SQL) to a local archive/ folder to protect credentials while adopting trunk-based development. Updated .gitignore to exclude backups, temp files, and the new archive directory. Added Ralph autonomous agent docs and service files for future development workflows.

Key files: .gitignore, CLAUDE.md, rivet-bot.service, scripts/ralph/ralph_local.py
> - `caae9f3`: Implemented a complete trunk-based development workflow with feature flags, enabling safe experimentation without long-lived branches. The system includes a flag manager (`rivet_pro/core/feature_flags.py`), CLI tools for toggling flags, and automated monthly reviews. Manual matcher service was migrated as a demo, showing flag-controlled behavior (classic vs LLM-enhanced matching).

**Key files:** `rivet_pro/core/feature_flags.py`, `rivet_pro/config/feature_flags.json`, `rivet_pro/core/services/manual_matcher_service.py`
> - `f46dfa3`: Merged ralph/bug-fixes-round1: 5 critical fixes—PDF links now work on mobile, /start creates DB users, manual search logs & filters improved, Pro tier bypass verified, and ALL lookups are now recorded in the interactions table.  
Key files: .env.example (expanded config), .dockerignore (build exclusions), plus scattered workflow & service fixes.
> - `28e562d`: Updated Ralph's PRD to reflect completion of Bug Fixes Round 1, marking all 5 critical user-reported issues as resolved: PDF link formatting, user registration on /start, manual search functionality, Pro tier bypass verification, and interaction logging. The project branch and description were updated to track this bug-fix milestone.

Key files: scripts/ralph/prd.json, scripts/ralph/progress.txt
> - `e61e628`: **Summary:** Fixed critical logging gap by ensuring every equipment photo lookup is recorded in the interactions table, not just those that return manuals. Now logs immediately after OCR, updates with equipment_id when matched, and sets outcome to 'manual_delivered' when manuals are found. Also corrected telegram_id type from string to integer for proper database joins.

**Key files:** `rivet_pro/adapters/telegram/bot.py`
> - `09bd082`: Fixed misleading log messages to show whether Tavily or n8n is being used for manual searches, added Tavily result count logging, and expanded URL pre-filtering to include manufacturer support pages (document, literature, support, download paths) so more relevant manual URLs pass initial screening.

Key file: rivet_pro/core/services/manual_service.py
> - `f6c7db7`: The `/start` command now creates users in the database with a free tier subscription. Uses `ON CONFLICT` to safely handle returning users without errors.

**Key file:** `rivet_pro/adapters/telegram/bot.py`
> - `2a49f63`: Fixed PDF links failing on mobile by escaping underscores in URLs (Telegram Markdown treats them as italics) and adding a fallback plain URL in backticks for manual copying. Updated helper text to reflect both tap-to-open and copy-paste options.

**Key file:** `rivet_pro/core/utils/response_formatter.py`
