---
title: "Commit Notes - 2025-12-01"
date: 2025-12-01
tags: [project/Thefuture, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-01

## Thefuture — Push at 21:19 UTC

**Commits:** 4
**Trigger:** `f15f921` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `f15f921` | feat: Update skill system with canonical TitleCase structure | Daniel Miessler |
| `1e7b717` | docs: Update documentation with Observability skill and add validation | Daniel Miessler |
| `ab97c36` | feat: Add Observability skill - real-time agent monitoring dashboard | Daniel Miessler |
| `7b3031e` | fix: Handle string/array content in stop-hook transcript parsing | smol compute |

> **What changed:**
> - `f15f921`: **Summary**: Replaced the old skill creation system with a new canonical TitleCase structure, consolidating `create-skill` and `system-createskill` into a single `Createskill` skill with four workflows (Create, Validate, Update, Canonicalize). Updated documentation to enforce TitleCase naming and `${PAI_DIR}/` path portability.

**Key files modified**:  
- `.claude/skills/CORE/SKILL.md`  
- `.claude/skills/CORE/SkillSystem.md`  
- `.claude/skills/Createskill/` (new consolidated skill)
> - `1e7b717`: Updated docs for the new Observability skill and hardened the pre-commit hook. README now lists Observability in the Examples section, and the hook now blocks commits on broken markdown links, auto-updates docs, and scans for API keys or email leaks.

Key files: README.md, .claude/hooks/pre-commit-with-docs.template, .claude/hooks/validate-docs.ts
> - `ab97c36`: Added a real-time observability dashboard for monitoring PAI multi-agent activity. The dashboard streams events via WebSocket and displays agent sessions, tool calls, and hook executions with live charts and swim lane visualizations. Key files: `.claude/skills/observability/manage.sh`, `apps/server/src/index.ts`, `apps/client/src/App.vue`.
> - `7b3031e`: Fixed Claude stop-hook transcript parsing to handle both string and array content formats, ensuring reliable detection of completion markers (🎯/🗣️). Updated voice notification payload to match initialize-pai-session.ts format with voice_id and priority fields, enabling consistent voice server delivery while preserving tab-title behavior.

Key file: `.claude/hooks/stop-hook.ts`

## cmms — Push at 23:22 UTC

**Commits:** 2
**Trigger:** `fa9d34b` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `fa9d34b` | fix: typo | Ibrahima G. Coulibaly |
| `f200d17` | fix: getNextOccurence | Ibrahima G. Coulibaly |

> **What changed:**
> - `fa9d34b`: Fixed a typo in the method name from `getNextOccurence` to `getNextOccurrence` across the codebase. This change ensures consistent spelling and proper method invocation for scheduling work orders and notifications.

**Key files modified:**  
- `api/src/main/java/com/grash/service/ScheduleService.java`  
- `api/src/main/java/com/grash/utils/Helper.java`
> - `f200d17`: Fixed the `getNextOccurence` utility so it correctly advances the supplied date past “now” instead of short-circuiting when the original date is already in the future.  
Key file: `api/src/main/java/com/grash/utils/Helper.java`
