---
title: "Commit Notes - 2025-08-26"
date: 2025-08-26
tags: [project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-08-26

## Backlog.md — Push at 21:13 UTC

**Commits:** 33
**Trigger:** `a666641` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `a666641` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `f217ba2` | Create task-244 - TUI-add-live-updates-via-watch-in-task-list-and-kanban.md | Alex Gavrilescu |
| `f0e7784` | TASK-233 - Implement live task watcher (#303) | Alex Gavrilescu |
| `79f48f3` | TASK-237 - Normalize remote branch refs (#304) | Alex Gavrilescu |
| `0dd1210` | TASK-241 - consolidate assignee normalization helper (#307) | Alex Gavrilescu |
| `9d24896` | TASK-242 - Centralize task filtering logic (#305) | Alex Gavrilescu |
| `25348c8` | Resolve merge conflicts for TASK-241: keep normalizeAssignee helper usage and update task file | Alex Gavrilescu |
| `88ebb84` | Add TASK-243: Enable TUI task reordering (#306) | Alex Gavrilescu |
| `69842f8` | Merge origin/main into tasks/take-over-task-242 and resolve conflicts (task-242 + cli sequences import) | Alex Gavrilescu |
| `383e81b` | TASK-237 - Finalize DoD: add notes, set Done | Alex Gavrilescu |
| `0b14bfd` | TASK-237 - Normalize remote branch refs | Alex Gavrilescu |
| `11237af` | TASK-233 - Finalize DoD: check ACs, add notes, set Done | Alex Gavrilescu |
| `f7001aa` | TASK-233 - Implement live task watcher | Alex Gavrilescu |
| `390f834` | TASK-241 - Finalize DoD: check ACs, add notes, set Done | Alex Gavrilescu |
| `2f13546` | TASK-242 - Finalize DoD: check ACs, add notes, set Done | Alex Gavrilescu |
| `33c8688` | TASK-217.01 - Sequences server: endpoints for list and move (#310) | Alex Gavrilescu |
| `4a8c6bd` | Update task definition | Alex Gavrilescu |
| `f1167e2` | TASK-217.01 - Sequences server: endpoints for list and move | Alex Gavrilescu |
| `bf736a6` | TASK-215 - Implement TUI view for sequences (#309) | Alex Gavrilescu |
| `5d839b2` | Update task-215 - Implement-TUI-view-for-sequences.md | Alex Gavrilescu |
| `e183049` | TASK-215.05 - TUI sequences: tests and stability (#308) | Alex Gavrilescu |
| `70274b0` | TASK-215.04 - TUI sequences: create new sequences via drop positions | Alex Gavrilescu |
| `3d2c788` | TASK-241 - add assignee normalization utility | Alex Gavrilescu |
| `f3a6fa6` | TASK-243 - Enable TUI task reordering | Alex Gavrilescu |
| `ba3b783` | TASK-242 - Centralize task filtering logic | Alex Gavrilescu |
| `6a6aa77` | TASK-215.03 - TUI sequences: move tasks with dependency updates (#301) | Alex Gavrilescu |
| `6eebca9` | TASK-215.02 - TUI sequences: navigation and detail view (#299) | Alex Gavrilescu |
| `08fcb7b` | TASK-215.01 - TUI sequences: read-only view (#296) | Alex Gavrilescu |
| `53f2eb4` | TASK-214 - Add CLI command to list sequences (#295) | Alex Gavrilescu |
| `5db78da` | TASK-213 - Compute sequences from task dependencies (#294) | Alex Gavrilescu |
| `8e5ea04` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `2e2334d` | Cleanup tasks | Alex Gavrilescu |
| `cba3236` | Consolidate duplicate AC sections and overwrite Implementation Notes (#300) | Alex Gavrilescu |

> **What changed:**
> - `a666641`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `backlog/tasks/task-233 - MVP-Live-task-watcher-in-TUI-(Bun.watch).md`, `src/server/index.ts`, `src/ui/unified-view.ts`, `src/utils/task-watcher.ts`, `src/web/App.tsx`.
> - `f217ba2`: Created backlog task-244 to implement live updates in the TUI task list and kanban views using the existing file-watcher. The feature will keep filters/sorts intact, preserve selection, and allow toggling via hotkey or CLI flag.
> - `f0e7784`: Live task watcher is now active: TUI and server share a Bun.watch utility that incrementally updates the task list/board on add/change/remove without resetting filters, while the server broadcasts “tasks-updated” to all WebSocket clients and the React app refreshes on receipt.

Key files: src/utils/task-watcher.ts, src/ui/unified-view.ts, src/server/index.ts, src/web/App.tsx
> - `79f48f3`: Fixed Git ref parsing so remote task loading no longer chokes on “origin/origin” or other malformed refs.  
Now accepts `main`, `origin/main`, `refs/remotes/origin/main` and normalizes all to `origin/<branch>` while skipping invalid refs like `HEAD` or `origin/HEAD`.  

Key files: `src/core/task-loader.ts`, `src/test/task-loader-branch-normalization.test.ts`
> - `0dd1210`: Introduced a single `normalizeAssignee` helper that converts a string assignee to an array, eliminating the duplicate inline logic in `Core.createTask`, `Core.createDraft`, `Core.updateTask`, and the Markdown serializer. All task/draft create-or-update paths now treat assignee consistently, and tests validate both string and array inputs.

**Key files:** `src/utils/assignee.ts`, `src/core/backlog.ts`, `src/markdown/serializer.ts`, `src/test/core.test.ts`
> - `9d24896`: Centralized task filtering by status and assignee into `FileSystem.listTasks(filter)` to eliminate duplicate logic. CLI task list and server endpoints now reuse the shared filter, and unit tests cover the new behavior.

Key files: `src/file-system/operations.ts`, `src/cli.ts`, `src/server/index.ts`, `src/test/filesystem.test.ts`
> - `25348c8`: Resolved merge conflicts for TASK-241 by keeping the `normalizeAssignee` helper and updating task files. Key changes include marking sequence-related tasks (213–218) as Done, updating their assignee to `@codex`, and refining acceptance criteria to reflect implemented features like unsequenced tasks and layered topological sorting.

**Key files modified:**  
- `backlog/tasks/task-213` through `task-218`  
- `src/core/sequences.ts`  
- `src/types/index.ts`
> - `88ebb84`: Added backlog task TASK-243 to enable TUI task reordering via Shift+Arrow keys, reusing the existing core move function. Key file: backlog/tasks/task-243 - Enable-TUI-task-reordering-with-Shift-and-arrow-keys.md
> - `69842f8`: Merged the latest mainline into the task-242 branch, resolving conflicts across the new sequences feature and CLI/TUI plumbing.  
Key outcome: task-filtering logic is now centralized (eliminating duplication) while keeping the freshly-built sequences, CLI list, and TUI views intact.

Files touched: src/cli.ts, src/core/sequences.ts, src/ui/sequences.ts, src/utils/task-sorting.ts, backlog/tasks/task-242…md, plus assorted test and type files.
> - `383e81b`: Task 237 closed: normalized remote branch refs to prevent “origin/origin” errors during remote task loading. Added tests and confirmed warnings gone in backlog browser.  
Key file: backlog/tasks/task-237 - Fix-invalid-git-ref-'origin-origin'-during-remote-task-loading.md
> - `0b14bfd`: Fixed the “origin/origin” Git ref error by adding a normalizer that strips prefixes and rejects invalid refs (HEAD, origin, origin/HEAD) before querying Git.  
Key files: src/core/task-loader.ts (new normalizeRemoteBranch helper), src/test/task-loader-branch-normalization.test.ts (tests), backlog task note (moved to In Progress).
> - `11237af`: Marked TASK-233 as Done after verifying all nine acceptance criteria.  
Added implementation notes summarizing the shared Bun.watch utility, incremental TUI updates, and WebSocket-driven React refresh.  

Key file: backlog/tasks/task-233 - MVP-Live-task-watcher-in-TUI-(Bun.watch).md
> - `f7001aa`: Added live task watching to both the web UI and TUI.  
The server now broadcasts “tasks-updated” to all WebSocket clients when tasks are added/changed/removed, and the unified TUI refreshes its local task list in real-time via Bun.watch.  

Key files: src/utils/task-watcher.ts, src/server/index.ts, src/ui/unified-view.ts, src/web/App.tsx
> - `390f834`: Task-241 closed: added normalizeAssignee helper and wired it into Core.createTask/createDraft/updateTask plus YAML serialization, eliminating duplicated string→array logic.  
Key files: backlog/tasks/task-241.md (status Done, ACs ticked, notes added), src/web/styles/style.css (regenerated Tailwind v4 build).
> - `2f13546`: Task-242 closed: centralized task filtering by status/assignee into FileSystem.listTasks(filter), eliminating duplicate logic in CLI and web server; added unit tests.  
Key files: backlog/tasks/task-242…md (status → Done, ACs ticked, notes added), src/web/styles/style.css (regenerated Tailwind build).
> - `33c8688`: Added server endpoints for listing and moving tasks within sequences: `GET /sequences` returns active tasks split into unsequenced and sequences (Done excluded), while `POST /sequences/move` enforces join semantics and prevents moving non-isolated tasks to Unsequenced. Core logic now exposes `listActiveSequences` and `moveTaskInSequences`, ensuring consistent behavior between TUI and API.  
Key files: `src/server/index.ts`, `src/core/sequences.ts`, `src/core/backlog.ts`.
> - `4a8c6bd`: Added the two new sequences endpoints (`GET /sequences`, `POST /sequences/move`) to the server; they simply delegate to the existing core helpers so the API and TUI now share the same move logic. Marked the corresponding task “Sequences server: endpoints for list and move” as complete.

Key files: `src/server/index.ts`, `backlog/tasks/task-217.01 - Sequences-server-endpoints-for-list-and-move.md`
> - `f1167e2`: Added server endpoints to list and reorder sequences: GET /api/sequences returns active tasks split into unsequenced and ordered sequences, while POST /api/sequences/move lets users move a task into a specific sequence or back to unsequenced with join-semantics (updates only affected task dependencies). Core logic moved to `src/core/sequences.ts` and wired through `src/server/index.ts`; UI layer updated to call new endpoints.
> - `bf736a6`: Added a full TUI view for task sequences: read-only list, keyboard nav, open task detail, drag-drop to move tasks between sequences (auto-updates deps), and create new sequences via drop zones.  
Key files: src/ui/sequences.ts, src/core/sequences.ts, src/cli.ts plus new tests.
> - `5d839b2`: task-215 closed: full TUI sequences view is now live. Users can arrow through sequences, move tasks with join/insert semantics, and see an “Unsequenced” bucket; all acceptance criteria pass.  
Key file: backlog/tasks/task-215 - Implement-TUI-view-for-sequences.md
> - `e183049`: Added comprehensive tests and a new `canMoveToUnsequenced` helper to prevent moving tasks with dependencies or dependents into the Unsequenced bucket; TUI now blocks invalid moves and shows a clear message. Key files: `src/core/sequences.ts`, `src/ui/sequences.ts`, and the new test suite `src/test/sequences-unsequenced-eligibility.test.ts`.
> - `70274b0`: Added drag-and-drop “insert-between” zones to the TUI: dropping a task between two numbered sequences now creates a new sequence at that spot, updates dependencies (moved task depends on prior sequence; next sequence depends on moved task), and renumbers later sequences. Key files: src/core/sequences.ts (new helper), src/ui/sequences.ts (drop UI), src/test/sequences-insert-between.test.ts (coverage).
> - `3d2c788`: Centralized assignee normalization into a reusable `normalizeAssignee()` utility, eliminating the same inline logic in `Core.createTask`, `createDraft`, `updateTask`, and the Markdown serializer. Added tests verifying string-to-array conversion for both task and draft flows.  
Key files: `src/utils/assignee.ts`, `src/core/backlog.ts`, `src/markdown/serializer.ts`, `src/test/core.test.ts`
> - `f3a6fa6`: Added a new task definition to implement TUI task reordering via Shift+Arrow keys, leveraging the existing core move logic.  
Key file: backlog/tasks/task-243 - Enable-TUI-task-reordering-with-Shift-and-arrow-keys.md
> - `ba3b783`: Centralized task filtering logic into `filesystem.listTasks()` to eliminate duplication between CLI and server. CLI and server now pass status/assignee filters directly to the shared method instead of re-implementing the filter logic. Key files: `src/file-system/operations.ts`, `src/cli.ts`, `src/server/index.ts`.
> - `6a6aa77`: Added full move-mode to the TUI sequences view: arrow keys pick a target sequence (or “Unsequenced”), Enter applies the move, Esc cancels, and the active task shows a clear “-> ” highlight. Moves update dependencies—joining a sequence auto-depends on every task in the prior sequence—and only isolated tasks may return to the Unsequenced bucket.  
Key files touched: src/ui/sequences.ts, src/core/sequences.ts, src/cli.ts, plus tests src/test/sequences-move.test.ts and related backlog docs.
> - `6eebca9`: Interactive `sequences` command now filters out Done tasks and lets you arrow-key through the remaining ones; Enter pops a detail card, Esc closes it (or quits if no popup). Key files: src/cli.ts, src/ui/sequences.ts
> - `08fcb7b`: Added a read-only TUI for `backlog sequence list` that renders each sequence as a bordered block with its tasks listed inside; interactive mode now opens this scrollable view (plain text output unchanged). Key files: src/ui/sequences.ts, src/cli.ts, plus updated tests and docs.
> - `53f2eb4`: Added `backlog sequence list` CLI command to inspect task execution sequences. Interactive by default; `--plain` flag prints machine-readable text. Reused existing `computeSequences` logic and added tests for plain output. Key files: `src/cli.ts`, `src/test/cli-sequences-plain.test.ts`.
> - `5db78da`: Added `computeSequences()` to split tasks into parallelizable execution layers; tasks with satisfied deps move to the next layer, cycles surface in a final layer. New `Sequence` type exported for CLI/TUI/web reuse. Key files: `src/core/sequences.ts`, `src/types/index.ts`, `src/test/sequences.test.ts`.
> - `8e5ea04`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `backlog/tasks/task-235 - Consolidate-duplicate-AC-sections-and-overwrite-Implementation-Notes.md`, `backlog/tasks/task-235 - Fix-duplicate-Acceptance-Criteria-sections-in-tasks.md`, `src/core/acceptance-criteria.ts`, `src/markdown/serializer.ts`, `src/test/acceptance-criteria.test.ts` (+2 more).
> - `2e2334d`: Closed three housekeeping issues: added “plain” field to task create/edit/print flows, fixed case-insensitive status grouping/normalization, and resolved the missing libstdc++.so.6 Nix build failure.  
Key files: backlog/completed/task-230, task-231, task-232.
> - `cba3236`: Fixed two content-consistency bugs: duplicate Acceptance Criteria sections are now consolidated into one marked section, and `--notes` edits overwrite (not append to) Implementation Notes.  
Key files: `src/markdown/serializer.ts`, `src/core/acceptance-criteria.ts`, plus updated tests.

## cmms — Push at 11:45 UTC

**Commits:** 1
**Trigger:** `f3135a4` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `f3135a4` | feat: Adjust feature availability in pricing (main) | Ibrahima G. Coulibaly |

> **What changed:** Basic plan now excludes Checklists, Time/Cost Tracking, and File Upload; API no longer exposes PURCHASE_ORDER.  
Key files: `pricingData.ts`, `ApiApplication.java`
