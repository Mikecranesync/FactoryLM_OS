---
title: "Commit Notes - 2025-03-07"
date: 2025-03-07
tags: [project/Archon, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-03-07

## Archon — Push at 15:50 UTC

**Commits:** 1
**Trigger:** `9ea1918` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `9ea1918` | Removing langgraph-api from requirements.txt so it is autoresolved, helps with Python 3.13 | Cole Medin |

> **What changed:** Dropped the pinned `langgraph-api` package from both requirements files so it can be auto-resolved, fixing install issues on Python 3.13.  
Key files: `requirements.txt`, `iterations/v4-streamlit-ui-overhaul/requirements.txt`

## cmms — Push at 21:49 UTC

**Commits:** 3
**Trigger:** `c442195` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c442195` | fix: optional env vars | Ibrahima G. Coulibaly |
| `b871c26` | fix: null env vars | Ibrahima G. Coulibaly |
| `f7779c4` | fix: missing import templates | Ibrahima G. Coulibaly |

> **What changed:**
> - `c442195`: Made storage config vars optional by defaulting to empty strings instead of nulls, removing the need for SpEL `:#{null}` expressions.  
Key files: `GCPService.java`, `MinioService.java`, `application.yml` (and dropped `application-k8s.yml`).
> - `b871c26`: Fixed malformed Spring `@Value` annotations that were missing a closing brace, allowing the app to start when optional env vars are absent. Made MinIO config keys nullable and added `minioPublicEndpoint` to the null-check guard so the service initializes cleanly in dev.  
Key files: `SubscriptionController.java`, `MinioService.java`, `UserService.java`
> - `f7779c4`: Fixed missing import templates by restoring CSV template files for assets, locations, meters, parts and work orders in English, French and Turkish. Updated ImportController to serve these templates from the classpath instead of external storage. Added CodeBuddy documentation files.

Key files: ImportController.java, import-templates/*/*.csv
