---
title: "Commit Notes - 2025-03-13"
date: 2025-03-13
tags: [project/Archon, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-03-13

## Archon — Push at 13:44 UTC

**Commits:** 1
**Trigger:** `1016ba4` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1016ba4` | Environment variable overhaul | Cole Medin |

> **What changed:** **Environment variable overhaul**  
Switched from inferring LLM/embedding providers via URL heuristics to an explicit `LLM_PROVIDER` / `EMBEDDING_PROVIDER` dropdown; all keys now live in `workbench/env_vars.json` and the UI walks you through re-entering them.  
Key files: `streamlit_pages/environment.py`, `archon/archon_graph.py`, `utils/utils.py`

## cmms — Push at 15:57 UTC

**Commits:** 7
**Trigger:** `0e60a88` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `0e60a88` | chore: do not send super admin in mails | Ibrahima G. Coulibaly |
| `2785ea7` | docs: super admin guide | Ibrahima G. Coulibaly |
| `d375de2` | feat: default super admin user | Ibrahima G. Coulibaly |
| `a90f24f` | docs: default super admin user | Ibrahima G. Coulibaly |
| `6965c9e` | feat: default superadmin user | Ibrahima G. Coulibaly |
| `f865bab` | fix: checklist patch | Ibrahima G. Coulibaly |
| `8a21046` | fix: file content type | Ibrahima G. Coulibaly |

> **What changed:**
> - `0e60a88`: Hard-coded the super-admin email address so the registration-mail sender skips it; prevents internal test accounts from cluttering inboxes.  
Key file: `UserService.java`
> - `2785ea7`: Updated the title of the password-update doc from generic “User” to “Super admin” to clarify the guide’s scope.  
Key file: dev-docs/SuperAdmin password update guide.md
> - `d375de2`: Ensures a default super-admin user is created at startup only when none exists for the role’s company; prevents duplicate users on restarts.  
Key file: `api/src/main/java/com/grash/ApiApplication.java`
> - `a90f24f`: Added a dev-doc page that gives the default super-admin credentials (superadmin@test.com / pls_change_me) and the exact REST calls to sign in and change the password.  Updated README to link to the new dev-docs folder.  
Key files: README.MD, dev-docs/SuperAdmin.md
> - `6965c9e`: The app now auto-creates a default super-admin user at startup if none exists.  
Key file: `api/src/main/java/com/grash/ApiApplication.java`
> - `f865bab`: Fixed checklist PATCH so category, description and name are actually updated instead of ignored. Only ChecklistService.java touched; the missing setter calls were added before tasks are rebuilt.
> - `8a21046`: Fixed the missing content-type detection in file uploads. The utility now correctly returns `text/csv` for CSV files and `application/pdf` for PDFs instead of always returning null.  
Key file: `api/src/main/java/com/grash/utils/MultipartFileImpl.java`
