---
title: "Commit Notes - 2025-04-19"
date: 2025-04-19
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-19

## cmms — Push at 23:59 UTC

**Commits:** 5
**Trigger:** `5d23686` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5d23686` | chore: check license key before sending | Ibrahima G. Coulibaly |
| `52205fc` | feat: license | Ibrahima G. Coulibaly |
| `ac6ed0e` | feat: implement SSO support and update dependencies | Ibrahima G. Coulibaly |
| `ec1da00` | feat: Initialize SSO with environment-based configuration | Ibrahima G. Coulibaly |
| `3ed02a5` | feat: sso init | Ibrahima G. Coulibaly |

> **What changed:**
> - `5d23686`: Added a null/empty check for the license key before hitting the validation API, preventing unnecessary network calls and early-failing validation. Renamed the env var from `MUI_X_LICENSE` to `LICENSE_KEY` and updated `.gitignore` to exclude `docker-compose-dev.yml`.  
Key files: `api/src/main/java/com/grash/service/LicenseService.java`, `.env.example`
> - `52205fc`: Added a license-checking layer: SSO is now gated by a valid Atlas CMMS license key (`LICENSE_KEY`) that is validated online; the key is read from env and checked against a remote endpoint using a hardware fingerprint.  
Key files: `LicenseService.java` (new), `WebSecurityConfig.java`, `application.yml`, `docker-compose.yml`, `README.MD`.
> - `ac6ed0e`: Added optional SSO login via Google or Microsoft: consolidated config into `application.yml`, added `ENABLE_SSO` flag, and updated the login page to show/hide SSO buttons.  
Key files: `OAuth2ClientRegistrationConfig.java`, `LoginJWT.tsx`, `application.yml`, `.env.example`.
> - `ec1da00`: SSO is now opt-in via a Spring profile: move config to `application-sso.yml`, delete the obsolete `SSOController`/`SSOLoginRequest`, and only wire OAuth2 login when the `sso` profile is active.  
Key files: `WebSecurityConfig.java`, `application-sso.yml`, `SSOController.java` (removed).
> - `3ed02a5`: Added full OAuth2 SSO support: backend now exposes /oauth2/** endpoints and success/failure handlers, while the frontend gained dedicated login/callback pages and i18n labels.  
Key files: WebSecurityConfig.java, SSOController.java, OAuth2 handlers, and frontend router/oauth.tsx.
