---
title: "Commit Notes - 2026-01-26"
date: 2026-01-26
tags: [project/jarvis-workspace, type/commit-note, project/RideView, project/factorylm]
status: active
---

# Commit Notes - 2026-01-26

## jarvis-workspace — Push at 12:20 UTC

**Commits:** 1
**Trigger:** `4ebeed7` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `4ebeed7` | Initial Jarvis workspace - AGENTS, SOUL, USER, IDENTITY, projects | Mikecranesync |

> **What changed:** **Summary:**  
Created the initial Jarvis workspace with a full agentic framework: core identity files (SOUL, USER, IDENTITY), operational rules (AGENTS, TOOLS, HEARTBEAT), memory system (MEMORY + daily logs), and five starter projects (IndustrialSkillsHub-native, factorylm-* suite, pi-gateway, rideview). The setup defines how the AI should bootstrap, remember, and behave across solo and group contexts.

**Key files:**  
AGENTS.md, SOUL.md, USER.md, IDENTITY.md, MEMORY.md, HEARTBEAT.md, TOOLS.md, memory/2026-01-26.md, projects/*/

## RideView — Push at 13:06 UTC

**Commits:** 2
**Trigger:** `2196eb7` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `2196eb7` | v5.9.0: WiFi Borescope Support | Mikecranesync |
| `05057ef` | v5.8.0: OpenCV overlay fix - draw glowing line directly on frame | Mikecranesync |

> **What changed:**
> - `2196eb7`: Added WiFi borescope support: new toggle button lets users switch from the built-in camera to a WiFi stream (DEPSTECH/Teslong) or USB camera. A settings popup auto-fills common borescope URLs and the frame-grabber now pulls from whichever source is active.  
Key files: wifi_borescope.py (new), main_simple.py, buildozer.spec
> - `05057ef`: Fixed the mobile overlay by drawing the glow & gap markers directly on the frame with OpenCV (same as web) instead of fighting Kivy canvas coordinates.  
New helper functions push the annotated frame into an Image widget and clear it when nothing is detected.  
Key files: main_simple.py, buildozer.spec, OVERLAY_FIX.md

## factorylm — Push at 03:09 UTC

**Commits:** 10
**Trigger:** `9f9d673` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `9f9d673` | Merge branch 'ralph/prd-003-plc-client' - PRD-003 Complete | Mikecranesync |
| `e88b32e` | feat(PLC-008): Add integration tests and README | Mikecranesync |
| `6e71cc8` | feat(PLC-007): Add unit tests for all components | Mikecranesync |
| `6ec12f8` | feat(PLC-006): Implement MockPLC for testing | Mikecranesync |
| `aa18172` | feat(PLC-005): Implement Micro820PLC | Mikecranesync |
| `4a05d56` | feat(PLC-004): Implement ModbusTCPClient wrapper | Mikecranesync |
| `b443d3e` | feat(PLC-003): Implement MachineState dataclass | Mikecranesync |
| `1ab0d1a` | feat(PLC-002): Implement BasePLCClient abstract class | Mikecranesync |
| `d76a77f` | feat(PLC-001): Create plc-client directory structure | Mikecranesync |
| `463cbf9` | feat: Add Ralph loop infrastructure for PRD-003 PLC Client | Mikecranesync |

> **What changed:**
> - `9f9d673`: Merge branch 'ralph/prd-003-plc-client' - PRD-003 Complete. Files: `plc-client/.env.example`, `plc-client/.gitignore`, `plc-client/README.md`, `plc-client/pytest.ini`, `plc-client/requirements.txt` (+19 more).
> - `e88b32e`: Added comprehensive integration tests (11 tests) and documentation for the FactoryLM PLC client library. The new test suite validates end-to-end workflows including motor control, fault handling, and state monitoring using the MockPLC implementation. Key files: `plc-client/README.md` (complete API docs and usage examples) and `plc-client/tests/integration/test_end_to_end.py` (integration test suite).
> - `6e71cc8`: Added comprehensive unit test coverage for the PLC client components, creating 68 tests across MockPLC, Micro820PLC, ModbusTCPClient, and MachineState models. All tests use mocking to simulate hardware behavior without requiring actual PLC connections. Key files: test_micro820.py, test_mock_plc.py, test_modbus_client.py, test_models.py.
> - `6ec12f8`: Added MockPLC - an in-memory PLC simulator for testing without hardware. It realistically models temperature rising with motor runtime and current varying with speed, plus provides convenience methods to start/stop motor and trigger faults. Key files: plc-client/src/factorylm_plc/plc/mock_plc.py and __init__.py
> - `aa18172`: Added Micro820PLC client that reads motor data (speed, current, temp, pressure) from Modbus registers 100-103 and status bits from coils 0-2. Includes convenience methods to start/stop motor and set speed, plus automatic temperature scaling. Key files: plc-client/src/factorylm_plc/plc/micro820.py and __init__.py
> - `4a05d56`: Added a robust ModbusTCPClient wrapper around pymodbus with automatic retries (3×), 5 s timeout, and full logging of reads/writes. Exposes read_registers, read_coils, write_register, write_coil and acts as a context manager for clean connection handling.

Key files: plc-client/src/factorylm_plc/modbus/client.py, plc-client/src/factorylm_plc/modbus/__init__.py
> - `b443d3e`: Created MachineState dataclass to standardize PLC data across different machine types. It captures motor status, environmental readings, and fault states with JSON serialization and health-check helpers.

Key file: plc-client/src/factorylm_plc/models.py
> - `1ab0d1a`: Created the abstract BasePLCClient class that defines the standard interface all PLC providers must implement. It includes connection management, state reading, and register/coil writing methods with full type hints and documentation. Also updated the factory function to make host parameter optional for mock PLCs and added graceful import handling for MachineState.

Key files: plc-client/src/factorylm_plc/plc/base.py, plc-client/src/factorylm_plc/__init__.py
> - `d76a77f`: Created the initial PLC client package structure under `plc-client/` with factorylm_plc module, supporting Micro820 and mock PLCs via Modbus TCP. Set up packaging (setup.py), testing (pytest), and environment configuration (.env) for industrial I/O operations.
> - `463cbf9`: **Summary:**  
Ralph loop infrastructure added for PRD-003 PLC Client project. Defines 8 user stories for a Micro820 Modbus TCP client, including abstract base class, register mapping, retry logic, mock PLC for testing, and factory function. Sets up agent instructions and append-only progress tracking.

**Key files:**  
`scripts/ralph/prd.json`, `scripts/ralph/prompt.md`, `scripts/ralph/progress.txt`
