---
title: "Commit Notes - 2026-01-22"
date: 2026-01-22
tags: [project/IndustrialSkillsHub, type/commit-note, project/IndustrialSkillsHub-native, project/My-Ralph]
status: active
---

# Commit Notes - 2026-01-22

## IndustrialSkillsHub — Push at 01:05 UTC

**Commits:** 1
**Trigger:** `1771ff9` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1771ff9` | fix: UI improvements and conditional static export for Vercel | Mikecranesync |

> **What changed:** Fixed responsive layout for category buttons to show on tablets (md instead of lg) and added bilingual labels. Made static export conditional on MOBILE_BUILD env var to enable Vercel deployment with Clerk middleware. Key files: app/(marketing)/footer.tsx, next.config.mjs, vercel.json

## IndustrialSkillsHub-native — Push at 07:25 UTC

**Commits:** 2
**Trigger:** `148e3cf` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `148e3cf` | feat: Working native React Native app with browser-based auth | Mikecranesync |
| `acba3b2` | Initial commit | Mikecranesync |

> **What changed:**
> - `148e3cf`: **Summary:** Migrated from Capacitor WebView to pure Expo React Native with browser-based OAuth to bypass Google's disallowed_useragent error. Added sign-in screen using expo-web-browser for Google authentication, secure token storage with expo-secure-store, and EAS Build configuration for Play Store deployment.

**Key files:** App.tsx, src/screens/SignInScreen.tsx, metro.config.js, eas.json, package.json
> - `acba3b2`: Initial commit. Files: `.gitignore`, `App.tsx`, `app.json`, `assets/adaptive-icon.png`, `assets/favicon.png` (+6 more).

## My-Ralph — Push at 05:04 UTC

**Commits:** 5
**Trigger:** `019b8c7` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `019b8c7` | Merge main into fix/test-prd-import-version-handler | Test User |
| `f6791e4` | chore(git): stop tracking .claude/settings.local.json | Test User |
| `d63f300` | fix(session): clear exit signals on session reset (issue #91) | Test User |
| `761db2f` | Merge main and apply code review fixes | Test User |
| `da1aed3` | fix(analyzer,monitor): EXIT_SIGNAL detection and monitor paths (v0.10.1) | Test User |

> **What changed:**
> - `019b8c7`: Merged main branch into PR #107, resolving conflicts by keeping both updated .ralph/ subfolder references and the new --version handler in mock scripts. Key changes include moving all Ralph-generated files to .ralph/ subfolder, adding cross-platform timeout utilities, and updating version to v0.10.1 with 321 tests. Modified core files: .gitignore, lib/timeout_utils.sh, tests/helpers/mocks.bash, and all shell scripts now use .ralph/ paths.
> - `f6791e4`: Stopped tracking `.claude/settings.local.json` (Claude-specific permissions config) that was incorrectly tracked despite being in `.gitignore`. No functional impact—just cleans up the repo and respects the ignore rule.

Key file: `.claude/settings.local.json`
> - `d63f300`: Fixed session reset to clear stale completion indicators that caused premature exits (issue #91). Enhanced `reset_session()` to reset `.exit_signals` to empty structure and remove `.response_analysis` file, ensuring fresh state across sessions. Added 2 tests validating the fix.

**Key files**: `ralph_loop.sh`, `tests/unit/test_session_continuity.bats`
> - `761db2f`: Merged main branch and applied code review fixes to JSON parsing logic in Ralph v0.10.1. Added defensive handling for empty jq results and prioritized session_id from result objects over init messages, plus a regression test for arrays with session_id only in result elements. Key files: lib/response_analyzer.sh, tests/unit/test_json_parsing.bats, ralph_monitor.sh (path fixes).
> - `da1aed3`: Fixed three v0.10.0 regressions: EXIT_SIGNAL is now correctly parsed from Claude’s JSON .result field, ralph_monitor.sh points to the new .ralph/ subfolder paths, and indented checkboxes in fix_plan.md are recognized. Added a safety circuit-breaker that forces exit after five consecutive “completion” flags to prevent runaway loops.

Key files: lib/response_analyzer.sh, ralph_loop.sh, ralph_monitor.sh
