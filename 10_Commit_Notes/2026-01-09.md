---
title: "Commit Notes - 2026-01-09"
date: 2026-01-09
tags: [project/My-Ralph, project/Rivet-PRO, type/commit-note]
status: active
---

# Commit Notes - 2026-01-09

## My-Ralph — Push at 23:34 UTC

**Commits:** 29
**Trigger:** `3d91d92` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `3d91d92` | Merge pull request #61 from frankbria/feature/prd-import-tests | Frank Bria |
| `b06d979` | test(import): add 22 comprehensive tests for ralph_import.sh | Test User |
| `27df197` | Merge pull request #60 from frankbria/feature/project-setup-tests | Frank Bria |
| `a6587aa` | fix(test): improve test reliability and remove tautologies | Test User |
| `c68e484` | test(setup): add 36 comprehensive tests for setup.sh | Test User |
| `4a83b87` | Merge pull request #59 from frankbria/feature/installation-tests | Frank Bria |
| `3503b9b` | fix: address code review feedback | frankbria |
| `31ba3eb` | docs(readme): add X follow badge for @FrankBria18044 | frankbria |
| `6688c27` | test(install): add 14 comprehensive installation tests | frankbria |
| `4da4d52` | Merge pull request #58 from frankbria/fix/prompt-file-flag | Frank Bria |
| `30cdf78` | fix(loop): replace non-existent --prompt-file with -p flag | frankbria |
| `0fbb92a` | Add Awesome Code Code mention to README.md | frankbria |
| `6c08757` | docs: update README.md and CLAUDE.md for v0.9.1 | frankbria |
| `970e683` | Merge pull request #53 from frankbria/fix/ci-coverage-bats-path | Frank Bria |
| `7db4a63` | fix(ci): disable coverage threshold due to kcov subprocess limitation | frankbria |
| `0c0b0b6` | fix(ci): use full path to bats for kcov coverage | frankbria |
| `f286710` | Merge pull request #52 from frankbria/feature/issue-10-cli-parsing-tests | Frank Bria |
| `3e76f80` | fix(ci): properly build kcov from source | frankbria |
| `5823003` | fix(ci): build kcov from source instead of apt-get | frankbria |
| `54963d4` | ci: add kcov coverage measurement for bash scripts | frankbria |
| `7407f0f` | test(cli): add --allowed-tools test and code review report | frankbria |
| `fffcc26` | test(cli): add comprehensive CLI argument parsing tests | frankbria |
| `f476022` | Merge pull request #47 from frankbria/feature/phase-1.1-modern-cli-commands | Frank Bria |
| `c7c6c93` | fix(security): use jq for JSON construction in analyze_response() | frankbria |
| `50f6ef7` | fix(security): address code review security findings | frankbria |
| `e7e54d8` | docs(review): add Phase 1.1 code review report | frankbria |
| `da5640e` | feat(cli): add modern CLI commands with JSON output support (Phase 1.1) | frankbria |
| `aab6d90` | Add Claude Code GitHub Actions workflow | Frank Bria |
| `0d30de4` | Add Claude Code Review workflow | Frank Bria |

> **What changed:**
> - `3d91d92`: Merge pull request #61 from frankbria/feature/prd-import-tests. Files: `CLAUDE.md`, `tests/helpers/fixtures.bash`, `tests/integration/test_prd_import.bats`.
> - `b06d979`: Added comprehensive test coverage for the PRD import functionality in Ralph, bringing total tests from 201 to 223. The new test suite validates file format conversions, output file creation, project naming, error handling, and edge cases for the `ralph_import.sh` script.

**Key files modified:** `tests/integration/test_prd_import.bats`, `tests/helpers/fixtures.bash`, `CLAUDE.md`
> - `27df197`: Merge pull request #60 from frankbria/feature/project-setup-tests. Files: `CLAUDE.md`, `tests/integration/test_project_setup.bats`.
> - `a6587aa`: Fixed flaky test setup by switching from global git config to env vars and bypassing shell aliases with `command git`. Removed unused mocks and corrected misleading test names/assertions. Key file: tests/integration/test_project_setup.bats
> - `c68e484`: Added 36 comprehensive integration tests for the `setup.sh` script, validating project initialization including directory creation, template copying, git setup, and README generation. Test coverage increased from 165 to 201 tests total.

**Key files modified:** `tests/integration/test_project_setup.bats`, `CLAUDE.md`
> - `4a83b87`: Merge pull request #59 from frankbria/feature/installation-tests. Files: `CLAUDE.md`, `README.md`, `package.json`, `tests/integration/test_installation.bats`.
> - `3503b9b`: Updated project metadata to v0.9.3 and corrected test counts to 165 (111 unit + 54 integration) across README.  
Fixed the installation test suite by adding the missing mock setup.sh and ensuring all three external dependencies (jq, git, node/npx) are properly mocked, eliminating test failures.
> - `31ba3eb`: Added an X (Twitter) follow badge for @FrankBria18044 to the README header.  
README.md
> - `6688c27`: Added comprehensive test suite for the install.sh script with 14 new tests covering installation, uninstallation, dependency detection, and PATH handling. Updated npm test script to run tests recursively and bumped version to v0.9.3 with test count increased to 165.

Key files: tests/integration/test_installation.bats, package.json, CLAUDE.md
> - `4da4d52`: Merge pull request #58 from frankbria/fix/prompt-file-flag. Files: `CLAUDE.md`, `README.md`, `ralph_loop.sh`, `tests/unit/test_cli_modern.bats`.
> - `30cdf78`: Fixed a critical bug in `build_claude_command()` that was trying to use the non-existent `--prompt-file` flag; now reads the prompt file and passes its content with the `-p` flag. Added 6 new TDD tests and error handling for missing prompt files. Key files: `ralph_loop.sh`, `tests/unit/test_cli_modern.bats`.
> - `0fbb92a`: Added a badge to the README showing the project is listed in the Awesome Claude Code collection. Updated Claude settings to allow web fetches from about.codecov.io and github.com domains.

Key files: README.md, .claude/settings.local.json
> - `6c08757`: Updated documentation for v0.9.1 release: README now shows 145 tests (up from 98), adds circuit breaker commands, and notes CI/CD integration; CLAUDE.md gains comprehensive testing docs including all 7 test files, GitHub Actions workflows, and coverage limitations due to kcov subprocess tracing issues.
> - `970e683`: Merge pull request #53 from frankbria/fix/ci-coverage-bats-path. Files: `.github/workflows/test.yml`.
> - `7db4a63`: Disabled the 70% coverage threshold in CI because kcov can’t trace subprocesses spawned by bats tests (a known bats-core limitation). Coverage reporting remains for reference, but the 100% test-pass rate is now the sole quality gate. Key file: `.github/workflows/test.yml`.
> - `0c0b0b6`: Fixed kcov coverage by using the full path to the bats test runner; the subprocess kcov spawns couldn’t find the npm-installed binary.  
Key file: `.github/workflows/test.yml`
> - `f286710`: Merge pull request #52 from frankbria/feature/issue-10-cli-parsing-tests. Files: `.github/workflows/test.yml`, `docs/code-review/2026-01-08-cli-parsing-tests-review.md`, `tests/unit/test_cli_parsing.bats`.
> - `3e76f80`: Fixed the CI coverage step by building kcov from source instead of trying to fetch non-existent pre-built binaries; adds all required build dependencies and a version check.  Also improves debug output when coverage artifacts are missing.

Key file: .github/workflows/test.yml
> - `5823003`: CI now builds kcov from source after Ubuntu dropped the package; adds fallback download, graceful coverage failures, and better error logging.  
Key file: `.github/workflows/test.yml`
> - `54963d4`: Added a CI job that measures code coverage for bash scripts using kcov; it runs BATS tests under kcov, enforces a configurable 70% threshold, and uploads HTML/JSON artifacts. Keeps the fast test job separate while giving visibility into how much of ralph_loop.sh and lib/ is exercised.

Key file: .github/workflows/test.yml
> - `7407f0f`: Added dedicated test for `--allowed-tools` flag validation to address code review gap, plus formal review report showing 0 critical/major issues and 1 resolved minor issue. Key files: `tests/unit/test_cli_parsing.bats` (test added), `docs/code-review/2026-01-08-cli-parsing-tests-review.md` (report).
> - `fffcc26`: Added 26 BATS tests to exhaustively validate `ralph_loop.sh` CLI flags—help, short/long forms, value setting, circuit-breaker actions, invalid input handling, flag order, and multi-flag combos—using `--help` as a safe early-exit to avoid running the main loop.  
Key file: `tests/unit/test_cli_parsing.bats`
> - `f476022`: Merge pull request #47 from frankbria/feature/phase-1.1-modern-cli-commands. Files: `CLAUDE.md`, `README.md`, `docs/code-review/2026-01-08-phase-1.1-modern-cli-review.md`, `lib/response_analyzer.sh`, `ralph_loop.sh` (+2 more).
> - `c7c6c93`: Fixed JSON injection vulnerability in response analyzer by replacing heredoc templates with `jq --arg`/`--argjson` for safe string escaping.  
Key file: `lib/response_analyzer.sh`
> - `50f6ef7`: Fixed three security vulnerabilities: JSON injection in `response_analyzer.sh` by replacing heredoc with `jq --arg/--argjson`, shell injection in `ralph_loop.sh` by switching to command arrays, and added input validation for `--allowed-tools` flag using a whitelist of valid tool patterns. All tests pass.
> - `e7e54d8`: Added the Phase 1.1 code-review report for the new JSON-output / modern-CLI feature.  
Key findings: 0 critical issues, 3 major (command-injection risk in ralph_loop.sh, lax input validation, fragile error handling), 5 minor; 43 tests pass and backward-compat is kept, so the branch can ship after fixing the flagged security and reliability gaps.
> - `da5640e`: Added modern Claude CLI flags (`--output-format json`, `--allowed-tools`, `--continue`) and JSON parsing to let Ralph reliably extract structured status/exit signals from Claude instead of fragile text scraping. Key files: `lib/response_analyzer.sh` (new JSON helpers), `ralph_loop.sh` (flag wiring), plus 43 new bats tests.
> - `aab6d90`: Added a GitHub Actions workflow that triggers Claude Code when @claude is mentioned in issues, PRs, or reviews. The bot can read repo contents and CI results to assist with code review, issue triage, and documentation updates.

Key file: `.github/workflows/claude.yml`
> - `0d30de4`: Added a GitHub Actions workflow that automatically triggers Claude AI code reviews on pull requests. It only reviews substantial changes (5+ files or 20+ lines) and skips documentation/config-only changes. Key file: `.github/workflows/claude-code-review.yml`

## Rivet-PRO — Push at 15:32 UTC

**Commits:** 5
**Trigger:** `ddbed08` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `ddbed08` | feat: Add agent prompts for 3-agent sprint | Mikecranesync |
| `282d272` | feat: Add ABB test fixture and 3-agent sprint infrastructure | Mikecranesync |
| `2e6f381` | feat(deployment): Complete database setup for Manual Hunter (Tab 1) | Mikecranesync |
| `d1a5d27` | docs: Add comprehensive implementation guide with step-by-step instructions | Mikecranesync |
| `d109a59` | feat(n8n): Add RIVET Manual Hunter workflow with multi-tier search | Mikecranesync |

> **What changed:**
> - `ddbed08`: Added three new agent prompt files to structure a sprint: Agent 1 codifies all logic into n8n workflows first, Agent 2 handles MCP server integration, and Agent 3 provides a debug harness. Key files: agents/AGENT1_WORKFLOWS.md, agents/AGENT2_MCP.md, agents/AGENT3_HARNESS.md.
> - `282d272`: Added ABB ACS580 test fixture and 3-agent sprint infrastructure to let Claude test n8n workflows automatically. Key files: fixtures/abb_test_case.py (ABB fixture), SPRINT_LIVE_TESTING.md (sprint plan), scripts/test_abb_pipeline.* (test runners), tests/e2e/test_abb_pipeline.py (end-to-end test).
> - `2e6f381`: **Summary:** Completed Manual Hunter database deployment (Tab 1 of 4) to Neon PostgreSQL, creating tables for equipment manual caching (`manuals`) and priority queue management (`manual_requests`) with full indexing and constraints. Loaded 10 test manuals and 5 pending requests to verify the schema works for the AI-powered manual search system.

**Key files:** `create_manual_hunter_tables_v2.py`, `DEPLOYMENT_STATUS_TAB1.md`, `.env.example`
> - `d1a5d27`: Added a 480-line implementation guide (`IMPLEMENTATION_STEPS.md`) that walks through database setup, API credential creation, and n8n workflow import for the new RIVET Manual Hunter automation.  
Key files: `IMPLEMENTATION_STEPS.md`
> - `d109a59`: **Summary**: Implemented a 3-tier intelligent manual search system for RIVET Pro that escalates through free (Tavily/Groq) → cheap (Serper/DeepSeek) → expensive (Perplexity) APIs to find equipment manuals, with PostgreSQL caching and human queue fallback. The workflow includes 22 nodes, cost optimization (80% free tier usage), and direct Telegram messaging for all outcomes.

**Key files**: `rivet_manual_hunter.json` (main workflow), `manual_hunter_tables.sql` (database schema), `MANUAL_HUNTER_SETUP.md` (setup guide), `PHOTO_BOT_V2_INTEGRATION_GUIDE.md` (integration docs)
