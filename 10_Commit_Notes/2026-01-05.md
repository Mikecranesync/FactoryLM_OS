---
title: "Commit Notes - 2026-01-05"
date: 2026-01-05
tags: [project/Rivet-PRO, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2026-01-05

## Rivet-PRO — Push at 23:54 UTC

**Commits:** 21
**Trigger:** `0ae9757` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `0ae9757` | Wire CLARIFY handling into troubleshoot orchestrator | Mikecranesync |
| `04b290a` | Enhance research.py with gap persistence and CLARIFY logic | Mikecranesync |
| `cf4a3dc` | Implement Knowledge Base foundation and production vector search | Mikecranesync |
| `9b24633` | Add comprehensive deployment resume for 2026-01-05 session | Mikecranesync |
| `a95caee` | Comment out auto-run migrations in API startup | Mikecranesync |
| `7b22645` | Add comprehensive resume context for session continuity | Mikecranesync |
| `7683941` | Add Telegram bot commands for CMMS management | Mikecranesync |
| `f78b935` | Add production CMMS web API with JWT authentication | Mikecranesync |
| `e3f37bf` | Add execute_query_async wrapper for EquipmentService compatibility | Mikecranesync |
| `6877ac9` | Integrate CMMS equipment creation into bot | Mikecranesync |
| `45d56cf` | Fix Gemini 404 error: Update to Gemini 2.5 models | Mikecranesync |
| `a7bbf8f` | Fix Telegram Markdown parsing error in OCR responses | Mikecranesync |
| `9b507bd` | Update vision provider chain with Groq Llama 4 models | Mikecranesync |
| `80cfdd6` | Fix OCR vision providers: Groq model + Gemini API format | Mikecranesync |
| `1740dd1` | Add OCR flow test script for automated testing | Mikecranesync |
| `74aef1e` | Fix API key attribute name: gemini_api_key → google_api_key | Mikecranesync |
| `96f412c` | Remove remaining get_settings() call in OCR service | Mikecranesync |
| `bdb4798` | Add bot instance lock to prevent multiple sessions | Mikecranesync |
| `599d04b` | Fix import error: Change get_settings to settings singleton | Mikecranesync |
| `6e2239b` | Fix: Use absolute imports in start_bot.py | Mikecranesync |
| `10c1b5a` | Day 1 Complete: Wire OCR & SME services to Telegram bot | Mikecranesync |

> **What changed:**
> - `0ae9757`: Added CLARIFY route to the troubleshooting orchestrator: when confidence <0.4 it now asks the user a single clarifying question instead of logging a KB gap. The TroubleshootResult gained a clarification_prompt field and the orchestrator returns early with route='clarify' in this scenario. Key file: rivet/workflows/troubleshoot.py
> - `04b290a`: **Research workflow now intelligently handles low-confidence queries.** When confidence <0.4 it returns a single clarifying question (manufacturer, fault code, symptoms, or details); 0.4-0.7 logs a knowledge gap to the DB and queues async research. Key additions: fault-code regex extraction, OCR context use, and research-queue helpers.  
**Files:** `rivet/workflows/research.py`
> - `cf4a3dc`: **Summary:**  
Built the knowledge base foundation with `knowledge_atoms`/`knowledge_gaps` tables, pgvector, and auto-priority triggers, plus a production-grade vector search service. The new `kb_search` route answers Siemens fault-code queries directly when confidence ≥ 0.85, logs gaps for 0.4-0.7, or asks for clarification below 0.4.

**Key files:**  
`rivet/models/knowledge.py`, `rivet/services/knowledge_service.py`, `rivet/services/embedding_service.py`, `rivet/workflows/kb_search.py`, `rivet_pro/migrations/009_knowledge_atoms.sql`
> - `9b24633`: **Summary:**  
Rivet Pro CMMS was successfully deployed to production on 2026-01-05. The FastAPI backend (20+ JWT-secured endpoints), Telegram bot (/equip, /wo, /stats), and Caddy reverse-proxy with auto-SSL are all live on 72.60.175.144; migration 007 added web-auth columns to the Neon Postgres DB.

**Key files touched:**  
DEPLOYMENT_RESUME_2026-01-05.md
> - `a95caee`: Disabled automatic database migrations on API startup since they’ve already been applied manually, preventing duplicate or conflicting migration runs.  
Key file: `rivet_pro/adapters/web/main.py`
> - `7b22645`: Added a comprehensive resume context file (RESUME_CONTEXT.md) that captures the full project state of Rivet Pro CMMS, including completed phases 1-8 (FastAPI web API, Telegram bot commands, database migrations, and deployment readiness). This serves as a single source of truth for session continuity and project handoff.
> - `7683941`: **Summary:** Added three new Telegram bot commands (`/equip`, `/wo`, `/stats`) for CMMS management, enabling users to list/search equipment, view work orders with status/priority emojis, and see personal statistics. The bot now integrates WorkOrderService alongside EquipmentService for full mobile CMMS access.

**Key files:** `rivet_pro/adapters/telegram/bot.py`
> - `f78b935`: **Summary:** Added a complete FastAPI web API to Rivet Pro with JWT authentication, enabling web-based CMMS operations. The API includes endpoints for authentication, equipment management, work orders, statistics, and OCR-based nameplate uploads. Key files include the FastAPI application (`rivet_pro/adapters/web/main.py`), authentication router (`auth.py`), and database migration for web auth support.

**Key files modified:**  
- `rivet_pro/adapters/web/main.py`  
- `rivet_pro/adapters/web/routers/auth.py`  
- `rivet_pro/migrations/007_web_auth.sql`
> - `e3f37bf`: Added `execute_query_async` to the database client so EquipmentService can run SQL with a consistent interface—choose “all”, “one”, or “none” fetch behavior and always get plain dicts back.  
Key file: `rivet_pro/infra/database.py`
> - `6877ac9`: The Telegram bot now automatically creates or matches equipment records in the CMMS after successful OCR processing. When users send equipment photos, the bot extracts data via OCR, then either creates a new equipment record (EQ-2025-XXXX) or matches to existing equipment, displaying the result with status ("Created" or "Matched").

Key files: `rivet_pro/adapters/telegram/bot.py`
> - `45d56cf`: Updated Gemini model references from deprecated 1.5 versions to 2.5 versions to resolve 404 errors. This fixes the vision provider chain configuration that was failing due to Google's deprecation of Gemini 1.5 models.

Key files: `rivet_pro/adapters/llm/router.py`
> - `a7bbf8f`: Fixed Telegram bot's OCR response formatting by switching from Markdown to HTML to prevent parsing errors with special characters in model numbers like "2080-LC20-20QBB". Updated bold text formatting from ** to <b> tags.

**Key file:** `rivet_pro/adapters/telegram/bot.py`
> - `9b507bd`: Updated the vision provider chain to prioritize cost-efficiency, placing Gemini Flash ($0.075/M) first and adding Groq's new Llama 4 Scout ($0.11/M) and Maverick ($0.50/M) models. Fixed a Gemini API bug by adding the `text=` parameter name and updated Groq cost calculation since it's no longer free.

**Key files:** `rivet_pro/adapters/llm/router.py`
> - `80cfdd6`: Fixed OCR vision providers by updating Groq to use the current 90b vision model (11b was decommissioned) and corrected Gemini's API format for the new Google GenAI SDK using proper types.Content and types.Part structures. This resolves the ValidationError with 18 validation errors that was breaking vision processing.

**Key files modified:** `rivet_pro/adapters/llm/router.py`
> - `1740dd1`: Added a test script (`test_ocr_flow.py`) that automates the OCR pipeline without needing to send photos through Telegram. It downloads a sample image (or uses mock data), runs the full `analyze_image()` flow, and prints the extracted manufacturer, model, serial, and confidence—making it easy to debug OCR issues step-by-step.
> - `74aef1e`: Fixed API key reference bug in LLM router by changing `gemini_api_key` to the correct `google_api_key` attribute name used in settings. This resolves the AttributeError when initializing the Gemini service.

**Key file:** `rivet_pro/adapters/llm/router.py`
> - `96f412c`: Fixed a NameError in OCR service by removing the missed `get_settings()` call on line 210, replacing it with the already-imported `settings` singleton. This resolves the crash when the photo handler attempts to use OCR functionality.

**Key file:** `rivet_pro/core/services/ocr_service.py`
> - `bdb4798`: Added file-based locking to prevent multiple bot instances from running simultaneously, which was causing Telegram API conflicts. The bot now uses `/tmp/rivet_bot.lock` with fcntl to ensure only one instance can start, with automatic cleanup on shutdown.

**Key file:** `rivet_pro/start_bot.py`
> - `599d04b`: Fixed import errors in OCR and LLM router by replacing non-existent `get_settings()` function with the correct `settings` singleton. This resolves photo upload functionality that was broken due to the import failures. Key files: `ocr_service.py`, `router.py`.
> - `6e2239b`: Fixed broken relative imports in the bot entry-point by switching to absolute package paths (`rivet_pro.*`).  
Key file: `rivet_pro/start_bot.py`
> - `10c1b5a`: **Summary:** Day 1 delivered a working Telegram bot that routes photos to OCR for nameplate extraction and text to SME experts for troubleshooting advice, complete with streaming UX and error handling. Key files: `rivet_pro/adapters/telegram/bot.py`, `start_bot.py`, `__main__.py`. Bot is ready for VPS deployment.

## cmms — Push at 17:07 UTC

**Commits:** 3
**Trigger:** `68abacd` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `68abacd` | fix: implement invite functionality and handle disabled email invitations | Ibrahima G. Coulibaly |
| `05a4533` | fix: refine enabled/paid user count logic in PaddleService | Ibrahima G. Coulibaly |
| `b2303d2` | fix: migrate Paddle instance to useRef for better state management | Ibrahima G. Coulibaly |

> **What changed:**
> - `68abacd`: Enabled the invite-by-email flow when registrations happen through an invite link: the frontend now dispatches `inviteUsers` in invitation mode and the backend only sends the invitation email if the feature is enabled.  
Key files: `AuthController.java`, `UserService.java`, `RegisterJWT.tsx`
> - `05a4533`: Fixed the subscription upgrade/downgrade logic by correctly comparing the number of paid, enabled users (`enabledPaidUsersCount`) against those already in the subscription (`subscriptionUsersCount`).  
Key file: `api/src/main/java/com/grash/service/PaddleService.java`
> - `b2303d2`: Fixed a React state bug by replacing the mutable `paddle` variable with a `useRef` hook, ensuring the Paddle checkout instance persists across re-renders. Updated all references to use `paddle.current` for reliable access.  
Key file: `frontend/src/content/own/Subscription/Plans/index.tsx`
