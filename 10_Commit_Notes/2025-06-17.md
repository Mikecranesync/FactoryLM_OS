---
title: "Commit Notes - 2025-06-17"
date: 2025-06-17
tags: [project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-06-17

## Backlog.md — Push at 20:38 UTC

**Commits:** 9
**Trigger:** `5fd790e` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5fd790e` | Update readme and Agents guidelines | Alex Gavrilescu |
| `7d5b414` | Add special character as space to prevent windows glitch | Alex Gavrilescu |
| `9442c50` | Update neo-neo-bblessed to the latest version | Alex Gavrilescu |
| `9bedff9` | Add double space as padding in the title of the full screen pages | Alex Gavrilescu |
| `7d96cd3` | Improve package uninstall flow | Alex Gavrilescu |
| `ce17cec` | Update neo-neo-bblessed package version | Alex Gavrilescu |
| `119face` | TASK-81 - Fix task list navigation skipping issue (#109) | Alex Gavrilescu |
| `72a0a2b` | TASK-80 - Preserve case in task filenames for better agent discoverability (#108) | Alex Gavrilescu |
| `7be004f` | TASK-79 - Fix task list ordering - sort by decimal ID not string (#107) | Alex Gavrilescu |

> **What changed:**
> - `5fd790e`: Updated the Cursor AI rules and agent docs to formalize the backlog.md workflow: tasks now live in `.backlog/tasks/`, must include YAML front-matter with id/title/status/assignee, and work is driven entirely through CLI commands (`backlog task list|edit|create`).  
Key files touched: `.cursorrules`, `AGENTS.md`, `src/guidelines/*`, plus a new test to enforce the task-file format.
> - `7d5b414`: Fixed a Windows rendering bug by replacing regular spaces with non-breaking spaces (`\u00A0`) in list labels. This prevents the terminal from collapsing or misaligning the padding around status headers and list titles. Modified files: `src/ui/board.ts`, `src/ui/components/generic-list.ts`.
> - `9442c50`: Upgraded the terminal UI library from neo-neo-bblessed 1.0.2 → 1.0.3, switching node-pty from a hard dependency to an optional peer. This trims the install size and removes the native build requirement unless pty support is actually needed.

Key files: package.json, bun.lock
> - `9bedff9`: Added double spaces around titles in fullscreen TUI components to create visual padding.  
Key files: `src/ui/board.ts`, `src/ui/components/generic-list.ts`.
> - `7d96cd3`: Adds a post-uninstall script that automatically removes the optional platform-specific binaries (backlog.md-*-*) when the main package is uninstalled, preventing orphaned native modules.  
Key files: scripts/postuninstall.cjs (new), .github/workflows/release.yml
> - `ce17cec`: Switched the terminal UI library from a GitHub fork to the published npm package `neo-neo-bblessed@1.0.2`, locking in a stable version and adding the new `node-pty` dependency it now requires.  
Key files: `package.json`, `bun.lock`.
> - `119face`: Fixed task list navigation skipping by eliminating conflicting key handlers in `src/ui/components/generic-list.ts`. Disabled blessed's built-in navigation (`keys: false`) and switched to manual navigation matching the working board view implementation, ensuring consistent one-item-at-a-time movement.
> - `72a0a2b`: Removed the forced lowercase conversion from `sanitizeFilename` so task titles now appear in filenames with their original casing (e.g., “UPDATED-Title-With-CAPS.md”), making it easier for AI agents to match tasks by title.  
Backward-compatible: existing lowercase files still load and all 246 tests pass.

Key files: `src/file-system/operations.ts`, `src/test/filesystem.test.ts`, plus new mixed-case task examples in `.backlog/`.
> - `7be004f`: Fixed task list ordering to sort by numeric ID instead of string comparison, resolving the issue where task-10 appeared before task-2. Added a new `task-sorting.ts` utility module with numeric comparison functions and updated both filesystem operations and board view to use consistent numeric sorting.

Key files: `src/utils/task-sorting.ts`, `src/file-system/operations.ts`, `src/board.ts`

## cmms — Push at 23:30 UTC

**Commits:** 1
**Trigger:** `687bdaa` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `687bdaa` | refactor: add `locationId` to `AssetMiniDTO` and update asset selection logic to support location filtering | Ibrahima G. Coulibaly |

> **What changed:** Added locationId to AssetMiniDTO so the frontend can filter assets by location without extra calls. Refactored SelectAssetModal to use the new field and build the asset tree client-side, removing the old server-side hierarchy fetch.  
Key files: AssetMiniDTO.java, AssetMapper.java, SelectAssetModal.tsx, asset.ts, asset.ts (Redux).
