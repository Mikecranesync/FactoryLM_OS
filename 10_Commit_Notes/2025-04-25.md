---
title: "Commit Notes - 2025-04-25"
date: 2025-04-25
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-25

## cmms — Push at 21:32 UTC

**Commits:** 13
**Trigger:** `c7df49d` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c7df49d` | fix: date format | Ibrahima G. Coulibaly |
| `2eb795b` | refactor: pagination to use sorting for retrieving company and child entities | Ibrahima G. Coulibaly |
| `1e85ccf` | fix: use activeStart-end in calendar view | Ibrahima G. Coulibaly |
| `9d0183d` | fix: missing wo category generated by pm | Ibrahima G. Coulibaly |
| `c365ba4` | chore: enhance task management with improved transactional handling and frontend adjustments | Ibrahima G. Coulibaly |
| `5b9118c` | feat: remove workflow condition | Ibrahima G. Coulibaly |
| `6383ad5` | chore: date time picker input format | Ibrahima G. Coulibaly |
| `60c5164` | chore: show pm tasks in drawer | Ibrahima G. Coulibaly |
| `ca84559` | chore: remove .codebuddy | Ibrahima G. Coulibaly |
| `acb826a` | chore: SMTP host and port env vars | Ibrahima G. Coulibaly |
| `9b95736` | fix: missing ALLOWED_ORGANIZATION_ADMINS | Ibrahima G. Coulibaly |
| `d28c3d1` | Merge pull request #34 from Grashjs/location-sorting | Ibrahima G. Coulibaly |
| `3fa7332` | feat: add server-side sorting and enhance pagination for frontend components | Ibrahima G. Coulibaly |

> **What changed:**
> - `c7df49d`: Fixed 12-hour clock display in the date-time picker by switching from lowercase “hh” to 24-hour “HH”, ensuring times always render correctly regardless of AM/PM setting.  
Key file: frontend/src/content/own/components/form/index.tsx
> - `2eb795b`: Switched Asset & Location child endpoints from Pageable to Sort-only queries, dropping pagination and returning plain Lists.  
Key files: AssetController, LocationController, AssetService/Repository, LocationService/Repository.
> - `1e85ccf`: Fixed calendar date-range bug by switching from `currentStart/End` to `activeStart/End`, ensuring the fetched work-order events always match the visible calendar period.  
Key file: `frontend/src/content/own/WorkOrders/Calendar/index.tsx`
> - `9d0183d`: Fixed a bug where the category field was not being copied when converting a WorkOrderBase to a WorkOrder, causing work orders created via preventive maintenance to appear without a category.  
Key file: WorkOrderService.java
> - `c365ba4`: - Backend: TaskController now wraps all endpoints in a single transaction, and TaskBaseService immediately links new TaskOptions to the saved task, removing the need for an extra refresh.  
- Frontend: SingleTask.tsx cleaned up (formatting, optional chaining) to avoid runtime errors when options or users are missing.  

Files: TaskController.java, TaskBaseService.java, SingleTask.tsx
> - `5b9118c`: Added a delete button to each workflow condition row so users can remove individual conditions instead of being stuck with them.  
Key file: frontend/src/content/own/Settings/Workflows/index.tsx
> - `6383ad5`: The date-time picker now respects the company’s preferred date format (MM/DD/YY vs DD/MM/YY) instead of always using one fixed layout.  
Key file: frontend/src/content/own/components/form/index.tsx
> - `60c5164`: Added a read-only task list to the PM-details drawer so users can see the tasks linked to a preventive-maintenance record without leaving the screen.  
Key files: `PMDetails.tsx`, `PreventiveMaintenance/index.tsx`
> - `ca84559`: Removed the `.codebuddy` directory and its auto-generated project summary, eliminating an obsolete documentation artifact.  
Key files deleted: `.codebuddy/.gitignore`, `.codebuddy/summary.md`.
> - `acb826a`: Added `SMTP_HOST` and `SMTP_PORT` environment variables so the app can connect to any SMTP server instead of being hard-coded to Gmail. Updated `.env.example`, README, Spring config, and Docker Compose to expose and document these new optional settings.
> - `9b95736`: Added the missing `ALLOWED_ORGANIZATION_ADMINS` environment variable to the backend service in docker-compose.yml, enabling the app to restrict admin access to specified org members.  
Key file: docker-compose.yml
> - `d28c3d1`: Merge pull request #34 from Grashjs/location-sorting. Files: `api/src/main/java/com/grash/advancedsearch/SearchCriteria.java`, `api/src/main/java/com/grash/controller/AssetController.java`, `api/src/main/java/com/grash/controller/LocationController.java`, `api/src/main/java/com/grash/repository/AssetRepository.java`, `api/src/main/java/com/grash/repository/LocationRepository.java` (+32 more).
> - `3fa7332`: Added server-side sorting to all major data grids (Parts, People, Teams, Requests, Customers, Vendors, Meters) by wiring the grid’s onSortModelChange to update SearchCriteria with the chosen field and SortDirection; pagination is now also fully server-driven.  
Key files: frontend/src/content/own/Inventory/Parts.tsx (and 7 sibling list views).
