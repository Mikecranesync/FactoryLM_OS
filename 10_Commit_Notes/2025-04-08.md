---
title: "Commit Notes - 2025-04-08"
date: 2025-04-08
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-08

## cmms — Push at 21:13 UTC

**Commits:** 12
**Trigger:** `7f8859c` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `7f8859c` | refactor: update `FileController` to use DTOs for better data handling | Ibrahima G. Coulibaly |
| `2e4c4e1` | chore: update Dockerfile to increase memory limit and handle legacy peer dependencies | Ibrahima G. Coulibaly |
| `1166ae5` | chore: frontend use npm ci | Ibrahima G. Coulibaly |
| `f1a68bb` | ci: split docker push jobs | Ibrahima G. Coulibaly |
| `fc287cf` | ci: docker arm version | Ibrahima G. Coulibaly |
| `85638ed` | fix: __RUNTIME_CONFIG__ | Ibrahima G. Coulibaly |
| `2d726ca` | docs: readme | Ibrahima G. Coulibaly |
| `70300bb` | docs: readme | Ibrahima G. Coulibaly |
| `43d490e` | docs: readme | Ibrahima G. Coulibaly |
| `6a4849f` | docs: readme | Ibrahima G. Coulibaly |
| `49b2255` | ci: push docker images on push | Ibrahima G. Coulibaly |
| `392511a` | feat: docker images | Ibrahima G. Coulibaly |

> **What changed:**
> - `7f8859c`: Refactored FileController to expose only curated data: search/getById now return FileShowDTO, and updates accept FilePatchDTO instead of the full entity.  
Key files: FileController.java, FilePatchDTO.java
> - `2e4c4e1`: Build now allocates 4 GB RAM and tolerates peer-dep mismatches, preventing OOM crashes and install failures on the legacy stack.  
Key file: frontend/Dockerfile
> - `1166ae5`: Switched the frontend Docker build from `npm install` to `npm ci`, locking the dependency tree to the exact versions in package-lock.json for faster, reproducible builds.  
Key file: frontend/Dockerfile
> - `f1a68bb`: Split the monolithic Docker push job into two parallel jobs—one for backend and one for frontend—so each image builds independently and failures are isolated.  
Key file: `.github/workflows/push-docker.yml`
> - `fc287cf`: **Summary:** Added ARM64 support to the Docker build pipeline so both backend and frontend images are now built for both AMD64 and ARM64 architectures. This enables native performance on Apple Silicon and other ARM-based systems.

**Key files:** `.github/workflows/push-docker.yml`
> - `85638ed`: Fixed runtime config access by adding optional-chaining (`?.`) to every `window.__RUNTIME_CONFIG__` reference, preventing crashes when the global is undefined.  
Key file: `frontend/src/config.ts`
> - `2d726ca`: Updated README to clarify that the Android app needs the custom *backend* server URL, not just any server URL.  
Key file: README.MD
> - `70300bb`: Updated README setup instructions to clarify that users need to download both `.env.example` and `docker-compose.yml` files before running the container. Minor wording tweaks make the steps clearer and more sequential.

Key file: README.MD
> - `43d490e`: Added a link to the Android mobile app on Google Play so users know they can connect the mobile client to their self-hosted server.  
Key file: README.MD
> - `6a4849f`: README now tells users to download the standalone docker-compose.yml instead of cloning the repo; clone instructions removed and the compose file itself dropped the version line.  
Key files: README.MD, docker-compose.yml
> - `49b2255`: Added a GitHub Action that automatically builds and publishes fresh `atlas-cmms-backend` and `atlas-cmms-frontend` Docker images to Docker Hub whenever code is pushed to the main branch.  
Key file: `.github/workflows/push-docker.yml`
> - `392511a`: Added full Docker support: new docker-compose.yml and Dockerfiles for the API and frontend, plus nginx config for the React build.  
Env vars were flattened (dropped REACT_APP_ prefix) and a MUI-X license key was added; the frontend build now pulls its config from the container at runtime instead of compile-time.  
Key files: docker-compose.yml, frontend/Dockerfile, frontend/nginx-custom.conf, .env.example, frontend/src/config.ts
