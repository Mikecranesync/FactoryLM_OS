---
title: "Commit Notes - 2025-01-16"
date: 2025-01-16
tags: [project/frame_realtime_gemini_voicevision, type/commit-note]
status: active
---

# Commit Notes - 2025-01-16

## frame_realtime_gemini_voicevision — Push at 07:49 UTC

**Commits:** 5
**Trigger:** `1e82479` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1e82479` | added user configuration for system instruction | CitizenOneX |
| `d228709` | added dropdown selector for gemini voice | CitizenOneX |
| `2bd3e5f` | updated connection code to allow app to provide desired voice and system instruction | CitizenOneX |
| `297f598` | removed superfluous clear() from frame display during streaming. Switched back to minified lua for main frame_app | CitizenOneX |
| `2934c34` | updated to simple_frame_app 4.0.1 for compatibility with Frame firmware v25.008.1013. Refactored Gemini realtime code out of main app | CitizenOneX |

> **What changed:**
> - `1e82479`: Added a persistent text field for users to customize the AI’s system instruction (stored in SharedPreferences) and replaced the hard-coded “pirate” prompt. Also added voice-name persistence and minor UI layout tweaks.  
Key file: lib/main.dart
> - `d228709`: Added a dropdown to select the Gemini voice (Puck, Charon, Vega, etc.) instead of hard-coding Puck; the chosen voice is now stored in state and passed to the connect call. UI shifted from center to top-start alignment to fit the new control.

Key file: lib/main.dart
> - `2bd3e5f`: Added runtime voice and system-instruction selection to the Gemini connection: the app can now pick any of the five voices (Puck, Charon, Kore, Fenrir, Aoede) and supply a custom system prompt instead of being hard-coded.  
Key files: lib/gemini_realtime.dart (new enum + dynamic config), lib/main.dart (example pirate prompt).
> - `297f598`: Removed an unnecessary `clear_display()` call that was wiping the screen right after a photo request, preventing users from seeing feedback. Reverted to the minified Lua build for the main app to save space.  
Key files: `assets/frame_app.lua`, `assets/frame_app.min.lua`
> - `2934c34`: Upgraded the Flutter app to simple_frame_app 4.0.1 for compatibility with Frame firmware v25.008.1013, renaming CAMERA_SETTINGS_MSG → CAPTURE_SETTINGS_MSG and adding auto-exposure every 100 ms.  Pulled the Gemini realtime WebSocket logic out of main.dart into its own lib/gemini_realtime.dart for cleaner separation.

Key files: assets/frame_app.lua, lib/gemini_realtime.dart, lib/main.dart, pubspec.yaml
