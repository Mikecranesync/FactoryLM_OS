---
title: "Commit Notes - 2026-01-18"
date: 2026-01-18
tags: [project/RideView, project/Rivet-PRO, type/commit-note]
status: active
---

# Commit Notes - 2026-01-18

## RideView — Push at 22:34 UTC

**Commits:** 6
**Trigger:** `44f11ac` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `44f11ac` | fix: use --onedir instead of --onefile for faster Windows build | Mikecranesync |
| `783a2ea` | fix: remove android.features causing toolchain error | Mikecranesync |
| `49abe51` | fix: add libltdl-dev and other build dependencies for Android APK | Mikecranesync |
| `db267fa` | Fix CI workflow for Windows and Android builds | Mikecranesync |
| `90c14fc` | Add Google Play Store assets and publishing files | Mikecranesync |
| `a7eae47` | Release v3.2.0 - Performance optimizations & build packaging | Mikecranesync |

> **What changed:**
> - `44f11ac`: Switched the Windows build from `--onefile` to `--onedir` to cut PyInstaller build time; the release now zips the `dist/RideView` folder instead of the single EXE.  
Key file: `.github/workflows/build-release.yml`
> - `783a2ea`: Removed the `android.features` line from buildozer.spec to fix a toolchain build error; camera features are now handled solely through AndroidManifest permissions.  
Key file: buildozer.spec
> - `49abe51`: Fixed missing build dependencies for Android APK compilation by adding libltdl-dev and other essential libraries (zlib, ncurses, gdbm, nss, ssl, readline, sqlite) to the Ubuntu package list in the GitHub Actions workflow. This resolves build failures when compiling Python extensions and native Android components. Key file: `.github/workflows/build-release.yml`.
> - `db267fa`: Fixed the CI workflow to properly build Windows and Android releases. Key changes: simplified PyInstaller command (removed spec file dependency), fixed Windows path separator issue, added Java setup for Android builds, and made releases no longer fail if Android build fails. Modified file: `.github/workflows/build-release.yml`.
> - `90c14fc`: Added complete Google Play Store publishing assets: app icon, feature graphic, screenshot template, store metadata, and privacy policy. Created keystore generation script and updated buildozer.spec for release signing configuration. Key files: playstore/ assets, PRIVACY.md, buildozer.spec, create_keystore.bat.
> - `a7eae47`: Released v3.2.0 with major performance gains: frame drops cut from 69% to <30% via a new FrameSynchronizer, adaptive 15-30 FPS, and 5-frame buffering. Added CI/CD pipelines that auto-build Windows .exe and Android APK on every tag, plus calibration and test-runner screens. Key files: .github/workflows/build-release.yml, src/rideview/mobile/frame_sync.py, rideview.spec, buildozer.spec.

## Rivet-PRO — Push at 23:59 UTC

**Commits:** 9
**Trigger:** `ec1f4a3` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `ec1f4a3` | feat: Migrate KB Intelligence Services from Agent Factory | Mikecranesync |
| `3f553e9` | fix: improve error handling and logging across production code | Mike Crane |
| `d3e4d28` | chore: Clean up technical debt - add untracked source files | Mikecranesync |
| `dc014e5` | feat(EXPERT-008): Expert Response Templates for technician-respectful responses | Mikecranesync |
| `152c4c6` | feat(EXPERT-007): Voice-First Optimization for speech-to-text input | Mikecranesync |
| `5f1b110` | feat(EXPERT-006): Context Retriever for equipment history injection | Mikecranesync |
| `ebcf32d` | feat(EXPERT-005): Multi-Signal Router with 3-signal voting | Mikecranesync |
| `f462632` | feat(intelligence): Shop Floor Expert Phase 1 + Streaming | Mikecranesync |
| `7a81ae6` | WhatsApp Adapter Integration (#14) | Mikecranesync |

> **What changed:**
> - `ec1f4a3`: Migrated the knowledge-base intelligence stack from Agent Factory into the main Rivet codebase, adding a 24/7 Redis-driven ingestion pipeline. Four new workers (kb_ingestion, url_scheduler, daily_kb_builder, weekly_kb_maintenance) plus GitHub Actions keep the KB fresh by automatically scraping OEM docs, deduplicating content, and researching coverage gaps. Key files: kb_ingestion_worker.py, daily_kb_builder.py, kb-maintenance.yml, 031_kb_jobs_queue.sql.
> - `3f553e9`: Replaced silent exception handling with debug logging across the codebase to improve troubleshooting. Key changes include adding logged exceptions for photo processing failures, database query errors, PDF decryption issues, and encoding wrapper failures.

**Files modified:** `bot.py`, `manual_download_manager.py`, `encoding.py`, `fallback.py`
> - `d3e4d28`: Added a new Manual QA System with PDF processing, Telegram Q&A, CLI tools, and web API endpoints. Also added network documentation and updated .gitignore to exclude Ralph session files and temp audio. Key files: manual_qa_handler.py, manual_qa.py, manual_qa_cli.py, manual_qa_service.py, pdf_chunker_service.py, migrations 029-030.
> - `dc014e5`: Added intelligent response templates that detect technician expertise level and tailor prompts accordingly. The system recognizes 40+ technical terms and 15+ equipment references to classify users as beginner/intermediate/expert, then serves expert prompts that skip basics and jump to diagnosis while filtering condescending phrases.

Key files: `rivet_pro/core/intelligence/response_templates.py`, `rivet_pro/core/intelligence/__init__.py`, `scripts/test_response_templates.py`
> - `152c4c6`: Added a VoiceNormalizer module that cleans up speech-to-text input by stripping filler words, expanding spoken abbreviations (vee eff dee → VFD), fixing common STT errors (bare ring → bearing), and preserving technical terms. Key files: rivet_pro/core/intelligence/voice_normalizer.py (new), rivet_pro/core/intelligence/__init__.py, scripts/test_voice_normalizer.py.
> - `5f1b110`: Added a context retriever that injects the top 3 relevant past work orders/equipment issues into the LLM prompt when users troubleshoot or search equipment. It scores matches by equipment keywords, symptoms and fault codes, then formats the results as bullet points to keep the AI aware of historical fixes.  
Key files: rivet_pro/core/intelligence/context_retriever.py, rivet_pro/core/intelligence/__init__.py
> - `ebcf32d`: **Summary:** Implemented a 3-signal voting router that combines keyword matching (45%), semantic similarity (35%), and LLM fallback (20%) for intent classification. The system achieves 100% accuracy while only requiring LLM calls for 4% of queries by using high-confidence keyword shortcuts and agreement boosts.

**Key files:** `rivet_pro/core/intelligence/multi_signal_router.py`, `rivet_pro/core/intelligence/__init__.py`, `scripts/test_multi_signal.py`
> - `f462632`: **Shop Floor Expert Phase 1** introduces real-time streaming responses and fixes critical intent classification issues. The system now correctly routes symptom-based queries (smell/sound issues) to troubleshooting, logging requests to work orders, and fault history to equipment searches. Key files: `intent_classifier.py` for classification fixes, `router.py` for streaming LLM support, and `bot.py` for Telegram integration.
> - `7a81ae6`: **Summary:**  
Integrated WhatsApp Cloud API adapter into the Rivet Pro bot platform, adding secure webhook endpoints (`/whatsapp`) and client functions for sending text/images and marking messages as read. The adapter is isolated from Telegram and requires five optional environment variables (phone number ID, business account ID, access token, verify token, app secret) sourced from Meta’s Developer Portal. All WhatsApp stories (WHATSAPP-001 through WHATSAPP-007) are now complete and the adapter is production-ready.

**Key files modified:**  
`rivet_pro/adapters/whatsapp/client.py` – WhatsApp Cloud API client  
`rivet_pro/adapters/web/routers/whatsapp.py` – webhook verification & message handling  
`rivet_pro/config/settings.py` – new WhatsApp config fields  
`.env.example` – documented WhatsApp environment variables
