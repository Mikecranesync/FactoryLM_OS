---
title: "Commit Notes - 2025-10-24"
date: 2025-10-24
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-10-24

## cmms — Push at 23:59 UTC

**Commits:** 2
**Trigger:** `8f1e9da` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `8f1e9da` | feat: disable NFC in scan screen for ios && add * for required field in register | MalickDiaw |
| `34ca361` | feat: config & run ios - account deletion | MalickDiaw |

> **What changed:**
> - `8f1e9da`: Disabled NFC scanning on iOS (barcode-only) by hiding NFC form fields and removed the NFC plugin from the build. Added a “* = required” notice and appended asterisks to mandatory register fields across all languages.  
Key files: mobile/components/form/index.tsx, mobile/screens/auth/RegisterScreen.tsx, mobile/app.config.ts, plus all i18n translation files.
> - `34ca361`: Added account-deletion to both web (JWTAuthContext) and mobile (AuthContext) via a soft-delete API call; wired it into the Profile UI with i18n labels.  Also bumped Expo SDK 47→48, disabled tablet support, and generated full iOS project ready for TestFlight build 3.

Key files: JWTAuthContext.tsx, AuthContext.tsx, Profile.tsx, app.config.ts, ios/AtlasCMMS.xcodeproj
