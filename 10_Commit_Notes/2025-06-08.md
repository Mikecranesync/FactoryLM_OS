---
title: "Commit Notes - 2025-06-08"
date: 2025-06-08
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-06-08

## Backlog.md — Push at 20:15 UTC

**Commits:** 20
**Trigger:** `1fef5ce` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1fef5ce` | Add section regarding implementation notes in the definition of done | Alex Gavrilescu |
| `1441fab` | TASK-4.6 - CLI: Add empty assignee array field for new tasks (#19) | Alex Gavrilescu |
| `03890ef` | TASK-4.5 - CLI: Init reporter name and global/local config (#18) | Alex Gavrilescu |
| `b72e565` | TASK-4.11 - Add definition of done (#17) | Alex Gavrilescu |
| `13f1ec3` | TASK-4.4 - CLI: Task Archiving and State Transitions (#16) | Alex Gavrilescu |
| `5f66659` | Add subtask 4.11: add definition of done (#15) | Alex Gavrilescu |
| `5e3b2f1` | TASK-4.3 - CLI: Task Editing (#14) | Alex Gavrilescu |
| `38e8535` | Add tests for task 4.2 - Task list & view commands | Alex Gavrilescu |
| `f0cd917` | Mandate tests as standard acceptance criteria in task 4.8 | Alex Gavrilescu |
| `065fc34` | task-4.10 add CLI subtask for marking tasks done (#13) | Alex Gavrilescu |
| `daef119` | Fix frontmatter metadata | Alex Gavrilescu |
| `5844275` |  Add subtask 4.5: initialize reporter name (#12) | Alex Gavrilescu |
| `01a0949` | Add subtask 4.8: parent id normalization (#11) | Alex Gavrilescu |
| `6e0c1f0` | Add subtask 4.8: enforce description header (#8) | Alex Gavrilescu |
| `cebd42a` | Add subtask 6.1: add subtask for local CLI installation (#9) | Alex Gavrilescu |
| `dcbf77d` | Add subtask 4.7: parse unquoted dates (#10) | Alex Gavrilescu |
| `31f5ab2` | Add subtask 4.6: empty assignee array (#7) | Alex Gavrilescu |
| `6051fa5` | Add task-13 for agent instruction prompt (#5) | Alex Gavrilescu |
| `1f90ae4` | Add MIT license to the project | Alex Gavrilescu |
| `2d605d7` | TASK-4.2 - CLI: Task Listing and Viewing (#4) | Alex Gavrilescu |

> **What changed:**
> - `1fef5ce`: Updated the definition-of-done in `.cursorrules`, `AGENTS.md`, and `CLAUDE.md` to require an “Implementation Notes” section in every completed task, documenting key technical decisions and changes. The sample task `.backlog/tasks/task-4.6` now includes these notes, showing how assignee arrays are normalized and tested.
> - `1441fab`: CLI now creates tasks with `assignee: []` instead of a string; parser/serializer updated to handle arrays. Legacy single-string assignees are auto-normalized to arrays on load. Key files: `src/cli.ts`, `src/core/backlog.ts`, `src/markdown/*`, `readme.md`.
> - `03890ef`: `backlog init` now prompts for a default reporter name and lets users store it globally (~/.backlog/user) or locally (.user, auto-added to .gitignore).  
Key files: src/cli.ts (new prompts & file writing), src/constants/index.ts (added USER constant), readme.md (usage docs).
> - `b72e565`: Added a consistent “Definition of Done” section to the three main agent guideline files (.cursorrules, AGENTS.md, CLAUDE.md) and closed the backlog task. The new section clarifies that a task is complete only when acceptance criteria are met, tests pass, docs are updated, and the task is marked Done via the Backlog CLI.
> - `13f1ec3`: Added CLI commands to archive tasks/drafts and promote/demote between drafts and tasks, moving files accordingly and auto-committing the change.  
Key files: src/cli.ts, src/core/backlog.ts, src/file-system/operations.ts
> - `5f66659`: Added new sub-task 4.11 to document a “Definition of Done” for agent guidelines. It requires updating AGENTS.md, CLAUDE.md, and .cursorrules with criteria for verifying acceptance, tests, docs, and task closure. Key file: `.backlog/tasks/task-4.11 - docs-add-definition-of-done-to-agent-guidelines.md`.
> - `5e3b2f1`: Added `task edit <id>` CLI command to update any task field (title, description, status, assignee, labels) with options like `--title`, `--add-label`, `--remove-label`, etc.; changes are saved and auto-committed.  
Key files: src/cli.ts (new command), readme.md (docs), src/test/cli.test.ts (tests), .backlog/tasks/task-4.3 - cli-task-edit.md (marked Done).
> - `38e8535`: Added comprehensive test coverage for the `task list` and `task view` CLI commands. Tests verify empty-state handling, status-based grouping, config-driven status ordering, and detailed task display including ID prefix flexibility.

Key file: `src/test/cli.test.ts`
> - `f0cd917`: Updated task 4.8 to require tests as part of acceptance criteria, not just documentation updates. Key file: `.backlog/tasks/task-4.8 - cli-enforce-description-header.md`
> - `065fc34`: Added a new CLI subtask (task-4.10) that requires agents to mark tasks “Done” via the backlog CLI after testing.  
Key file: `.backlog/tasks/task-4.10 - cli--remind-agents-to-mark-tasks-done-via-cli.md`
> - `daef119`: Updated backlog task metadata: marked tasks 1, 2, and 4.1 as Done and assigned them to @MrLesk; added the new umbrella task-4 and linked all sub-tasks to it via parent_task_id; corrected task-4.4’s dependency to task-4.1.

Key files: all modified files under .backlog/tasks/task-*.md
> - `5844275`: Added a new subtask (4.5) to make `backlog init` prompt for a default reporter name and let the user decide whether to store it globally or locally; local storage will create a `.user` file and update `.gitignore`.  
Key file: `.backlog/tasks/task-4.5 - cli-init-prompts-for-reporter-name-and-global-local-config.md`
> - `01a0949`: Added backlog item 4.9 to fix CLI task-id normalization: ensure `--parent 4` is stored as `task-4` and all commands accept both `task-<n>` and plain `<n>` inputs.  
Key file: `.backlog/tasks/task-4.9 - cli-normalize-task-id-inputs.md`
> - `6e0c1f0`: Added subtask 4.8 to the backlog: the CLI must now auto-insert a “## Description” header when creating tasks, and all docs must require acceptance criteria.  
Key file: `.backlog/tasks/task-4.8 - cli-enforce-description-header.md`
> - `cebd42a`: Added a new subtask (6.1) to enable local CLI installation via bunx/npx by publishing the package to npm, removing the “private” flag, and updating docs.  
Key file: `.backlog/tasks/task-6.1 - cli-local-installation-support-for-bunx-npx.md`
> - `dcbf77d`: Added subtask 4.7 to let the CLI parse unquoted created_date values in task frontmatter and accept multiple date formats; the global date format is now configurable in .backlog/config.yml.  
Key files: .backlog/config.yml, .backlog/tasks/task-4.7 - cli-parse-unquoted-created_date.md
> - `31f5ab2`: Added a new backlog task (4.6) to ensure the CLI creates tasks with an empty `assignee: []` array instead of a string, aligning the data model with future multi-assignee support.  
Key file: `.backlog/tasks/task-4.6 - cli-add-empty-assignee-array-field-for-new-tasks.md`
> - `6051fa5`: Added a new backlog task (#13) to enhance `backlog init` with an optional prompt for injecting AI-agent instructions (Codex, Claude, Google Jules) into the repo via `AGENTS.md`, `.CLAUDE.md`, or `.cursorrules`.  
Key file: `.backlog/tasks/task-13 - cli--add-agent-instruction-prompt.md`
> - `1f90ae4`: Added the MIT license to the project, granting broad reuse rights while disclaiming warranty.  
Key file: LICENSE
> - `2d605d7`: Added CLI commands to list tasks grouped by status (`backlog task list`) and view individual tasks with Markdown rendering (`backlog task view <id>`).  
Key files: `src/cli.ts` (new commands), `.backlog/tasks/task-4.2 - cli-task-list-view.md` (marked done), `readme.md` (updated usage).
