---
title: "Commit Notes - 2025-12-24"
date: 2025-12-24
tags: [project/Agent-Factory, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-24

## Agent-Factory — Push at 23:55 UTC

**Commits:** 31
**Trigger:** `4ca60fb` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `4ca60fb` | feat(persistence): Add rock-solid multi-tier conversation state persistence | Mikecranesync |
| `199ee6c` | fix(telegram): Add missing add_from_ocr function | Mikecranesync |
| `c428b01` | fix(telegram): Wire up 'Save to Library' button + fix OCR hallucination | Mikecranesync |
| `d39da0f` | docs: Update NEXT_ACTIONS.md with OCR Pipeline deployment | Mikecranesync |
| `8080dce` | fix(ocr): Replace old GPT-4o Vision call with OCR Pipeline | Mikecranesync |
| `fece31a` | fix: Initialize kb_manager in main() not post_init() | Mikecranesync |
| `6d3c8fc` | feat: Enable KB ingestion via Telegram commands | Mikecranesync |
| `b11aaf4` | docs: Update NEXT_ACTIONS.md - OCR enhancement complete | Mikecranesync |
| `fc610ee` | feat(ocr): Complete OCR enhancement implementation - Phase 1-5 | Mikecranesync |
| `386ca12` | feat: Lower KB coverage thresholds for small KB (1,964 atoms) | Mikecranesync |
| `c9dad29` | fix(telegram): Add missing 'Save to Library' button in photo handler | Mikecranesync |
| `cc13050` | Merge Fix #1: Route C Latency (36s → <5s) | Mikecranesync |
| `3166e7f` | test: Add Route C performance tests | Mikecranesync |
| `5711e5f` | fix: Replace ILIKE keyword filtering with tsquery matching - fixes ts_rank returning 0.0 | Mikecranesync |
| `46d63a1` | perf: Make KB evaluation async to avoid blocking event loop | Mikecranesync |
| `f294e08` | perf: Add 5-minute LLM response cache to reduce API costs | Mikecranesync |
| `ddea80b` | perf: Parallelize Route C gap detection + LLM response | Mikecranesync |
| `f74b777` | perf: Add timing instrumentation to Route C pipeline | Mikecranesync |
| `de099f9` | fix: Rename config to rag_config to avoid LangSmith @traceable conflict | Mikecranesync |
| `6fe7291` | fix: Use to_tsquery instead of plainto_tsquery for OR operator support | Mikecranesync |
| `437033b` | Merge Fix #2: KB Population (0 → 21 atoms) | Mikecranesync |
| `9eed3bf` | fix: Add KB population validation script | Mikecranesync |
| `dd09e06` | fix: Add startup KB health check with warning log | Mikecranesync |
| `2b29b44` | fix: Make KB atom count dynamic and flexible schema upload | Mikecranesync |
| `a16f431` | fix: Filter stop words from ts_query to fix 0.0 similarity scores | Mikecranesync |
| `e21a1a6` | debug: Add detailed logging for similarity scores in search_docs() | Mikecranesync |
| `8fe4804` | feat: Add model_number and part_number filtering to search_docs() | Mikecranesync |
| `4dd6b7a` | fix: Add missing logger import to kb_evaluator.py | Mikecranesync |
| `4d14964` | debug: Add logging to KB evaluator to diagnose RAG layer issue | Mikecranesync |
| `68806b8` | docs: Mark page_number schema issue as FIXED | Mikecranesync |
| `2c8cbbe` | fix: Use source_pages column for citation accuracy in retriever | Mikecranesync |

> **What changed:**
> - `4ca60fb`: Added bulletproof 4-tier database fallback: Neon → Supabase → Railway → local SQLite. Every user input now auto-saves conversation state; if all cloud providers fail, the bot falls back to a local SQLite file (`data/local.db`) so nothing is lost and chats can resume after restarts. Key files: `database_manager.py` (new LocalDatabaseProvider + failover logic), `conversation_state.py` (state manager with 24 h TTL), `002_conversation_states.sql` (schema).
> - `199ee6c`: Fixed critical NameError by implementing the missing `add_from_ocr()` function that pre-fills OCR-extracted data when users tap "Save to Library". The function now handles OCR results, shows a preview with confidence warnings, and integrates into the ConversationHandler entry points.  

**Key file:** `agent_factory/integrations/telegram/library.py`
> - `c428b01`: Fixed Telegram bot's broken "Save to Library" button by adding the missing CB_SAVE_OCR callback handler that routes to add_from_ocr(). Also stopped GPT-4o from hallucinating fault codes by restricting extraction to only LED/LCD displays, not printed nameplates. Key files: library.py, gpt4o_provider.py
> - `d39da0f`: Fixed a critical AttributeError by replacing the old GPT-4o Vision direct call with the new OCR Pipeline; bot is now live on the VPS with 1964 KB atoms loaded and ready for user testing. Updated NEXT_ACTIONS.md to reflect the successful deployment and current status.

**Key file**: NEXT_ACTIONS.md
> - `8080dce`: Fixed a critical AttributeError by replacing the hard-coded GPT-4o Vision call with the OCRPipeline, which now returns a proper OCRResult object instead of a JSON string. This resolves the crash when the “Save to Library” button tried to access `.confidence` on a string.

Key file: `agent_factory/integrations/telegram/orchestrator_bot.py`
> - `fece31a`: Moved KBManager initialization from post_init() to main() to ensure the keyboard manager exists before handlers are registered, preventing AttributeError when commands try to access kb_manager.handle_kb().  
Key file: agent_factory/integrations/telegram/orchestrator_bot.py
> - `6d3c8fc`: Telegram bot now supports live KB ingestion: `/kb_ingest` pushes URLs to the VPS Redis queue via SSH and `/kb_queue` reports the current backlog. Enabled real subprocess calls and registered the new commands.

Files touched: `kb_manager.py`, `orchestrator_bot.py`
> - `b11aaf4`: **Summary:** Completed all 5 phases of the OCR enhancement project (commit 80f4be2), including dual providers, KB filtering, auto-fill library with Save button, quality validation, and LangSmith tracing. The bot is deployed and running on VPS, ready for user testing to verify the "Save to Library" button appears after OCR processing.

**Key files modified:** NEXT_ACTIONS.md
> - `fc610ee`: **Summary:**  
Implemented a complete OCR enhancement pipeline with dual-provider fallback (GPT-4o primary, Gemini backup), KB model number filtering, auto-fill library integration, quality validation with confidence scoring, and LangSmith tracing. The new OCR module is fully deployed and tested on the VPS.

**Key files:**  
`agent_factory/integrations/telegram/ocr/` (5 new modules), `tests/test_ocr_integration.py`, `tests/benchmark_ocr_performance.py`
> - `386ca12`: Relaxed the routing thresholds so small KBs (1,964 atoms) can reach Route A/B instead of always falling back to Route C.  
Key file: agent_factory/schemas/routing.py
> - `c9dad29`: Fixed missing "Save to Library" button in Telegram photo handler that was supposed to be in Phase 3. Now shows context-aware save prompt after OCR detection when confidence ≥50% and equipment data found. Key file: `agent_factory/integrations/telegram/orchestrator_bot.py`
> - `cc13050`: Route C latency slashed from 36s to <5s by running KB gap detection and LLM response generation in parallel and adding a 5-min LLM cache.  
Key files: orchestrator.py, kb_evaluator.py, performance.py, test_route_c_performance.py
> - `3166e7f`: Added performance tests for Route C to ensure latency stays under 5s after recent optimizations (parallel gap detection + LLM, async KB, caching). Tests verify cache speedups and parallel execution timing.  
Key file: tests/test_route_c_performance.py
> - `5711e5f`: Switched from ILIKE to PostgreSQL tsquery for keyword filtering, fixing the bug where ts_rank always returned 0.0. Now the full-text rank actually reflects relevance.

Key file: agent_factory/rivet_pro/rag/retriever.py
> - `46d63a1`: Made KB evaluation non-blocking by adding an async wrapper that runs the existing sync evaluate() in a thread pool; orchestrator now awaits evaluate_async(), cutting 2–4 s of latency and letting the event loop handle other requests concurrently.  
Key files: agent_factory/core/orchestrator.py, agent_factory/routers/kb_evaluator.py
> - `f294e08`: Added a 5-minute in-memory cache for LLM responses keyed by route_type + vendor + query hash, cutting ~30-40% of repeat API calls and saving ~$0.002 per hit.  
Key file: agent_factory/core/orchestrator.py
> - `ddea80b`: Route C now runs knowledge-gap detection and LLM response in parallel with `asyncio.gather()`, cutting total latency from ~36s to <5s. Gap logging and ingestion triggers are fire-and-forget so they never block the user-facing reply.  
Key file: `agent_factory/core/orchestrator.py`
> - `f74b777`: Added lightweight timing decorators to the orchestrator so every Route C (no-KB) call and its sub-steps log millisecond-level latency.  
New `performance.py` module supplies `@timed_operation` and context-manager helpers; key functions in `orchestrator.py` now auto-log execution time.
> - `de099f9`: Renamed the `config` parameter to `rag_config` in the RAG search pipeline to prevent a naming collision with LangSmith’s `@traceable` decorator.  
Key files: `agent_factory/rivet_pro/rag/retriever.py`, `agent_factory/routers/kb_evaluator.py`
> - `6fe7291`: Switched PostgreSQL text search from `plainto_tsquery` to `to_tsquery` so queries containing OR operators (|) are parsed correctly instead of being escaped.  
Added optional LangSmith tracing around the search function to capture vendor/model filters and result stats.  
Key file: `agent_factory/rivet_pro/rag/retriever.py`
> - `437033b`: The bot now queries the actual atom count from the database instead of hard-coding 1,057, fixing the “0 → 21 atoms” issue. A new validation script (`validate_kb_population.py`) was added to confirm the KB is populated, and startup logs warn if the table is empty and prompt running `upload_atoms_to_neon.py`.

Key files: `orchestrator_bot.py`, `validate_kb_population.py`, `upload_atoms_to_neon.py`
> - `9eed3bf`: Added a validation script (`scripts/validate_kb_population.py`) that checks whether the `knowledge_atoms` table is populated and exits with success/failure so CI or local dev can gate on it.  
Key file: `scripts/validate_kb_population.py`
> - `dd09e06`: Added a startup health check that warns if the knowledge base is empty; logs clear instructions to populate it.  
Key file: `agent_factory/integrations/telegram/orchestrator_bot.py`
> - `2b29b44`: The bot now shows the real-time count of knowledge-base atoms (21) instead of the hard-coded 1,057; a new helper queries the DB on every `/start` and `/status`.  
The upload script was also made more tolerant—mapping `vendor`/`manufacturer`, `type`/`atom_type`, etc.—and now loads from a single JSON file (`atoms-with-embeddings.json`) rather than a directory.  

Key files: `orchestrator_bot.py`, `upload_atoms_to_neon.py`
> - `a16f431`: Fixed 0.0 similarity scores by stripping English stop words from the PostgreSQL ts_query before ranking.  
Key file: agent_factory/rivet_pro/rag/retriever.py
> - `e21a1a6`: Added debug logging to expose the PostgreSQL similarity scores returned by the search query. Now prints the query parameters and, for every result set, the min/max/average similarity so we can quickly judge result quality without manual inspection.

Key file: agent_factory/rivet_pro/rag/retriever.py
> - `8fe4804`: **Summary:**  
Enhanced `search_docs()` to accept optional `model_number` and `part_number` parameters extracted from OCR, enabling tighter filtering on product families and exact part matches. Model numbers are normalized (strip hyphens/spaces, uppercase) and matched against the `product_family` column, while part numbers are searched within document titles and content.

**Key files:**  
`agent_factory/rivet_pro/rag/retriever.py`
> - `4dd6b7a`: Fixed a runtime crash by adding the missing `logging` import and initializing a module-level logger in `kb_evaluator.py`.  
Key file: `agent_factory/routers/kb_evaluator.py`.
> - `4d14964`: Added debug logging to the KB evaluator so we can see whether the mock or real RAG path is taken and how many docs are retrieved; also plumbed an optional model_number parameter through to the RAG search for finer-grained filtering.  
Key file: agent_factory/routers/kb_evaluator.py
> - `68806b8`: Fixed two critical production issues: Route C was stuck at 54% confidence due to missing GROQ_API_KEY (now restored), and kb_evaluator.py crashed on Decimal/float type mismatch (converted to float). Also documented limited VFD KB coverage as expected behavior. Key files: ISSUES_LOG.md
> - `2c8cbbe`: Fixed citation page numbers in the RAG retriever by querying the existing `source_pages` array and extracting the first page; previously citations always showed None.  
Key file: `agent_factory/rivet_pro/rag/retriever.py`

## cmms — Push at 22:23 UTC

**Commits:** 13
**Trigger:** `5f9571e` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5f9571e` | fix: license names | Ibrahima G. Coulibaly |
| `934acad` | feat: payment success page | Ibrahima G. Coulibaly |
| `ef97b37` | feat: checkout email modal | Ibrahima G. Coulibaly |
| `3ea17e0` | chore: avoid duplicate processing | Ibrahima G. Coulibaly |
| `7958009` | feat: send email to superadmins on fail | Ibrahima G. Coulibaly |
| `d0d85b4` | feat: send email with license | Ibrahima G. Coulibaly |
| `d78a599` | fix: keygen license creation | Ibrahima G. Coulibaly |
| `46af32a` | chore: stripe | Ibrahima G. Coulibaly |
| `afb7491` | feat: stripe checkout init | Ibrahima G. Coulibaly |
| `2b91a20` | feat: selfhosted plans | Ibrahima G. Coulibaly |
| `6b2abbe` | chore: cloud and selfhosted plans | Ibrahima G. Coulibaly |
| `2a58a2c` | chore: mobile entitlement | Ibrahima G. Coulibaly |
| `127087b` | chore: error handling | Ibrahima G. Coulibaly |

> **What changed:**
> - `5f9571e`: Updated the display names for the self-hosted license plans to include “Atlas CMMS” branding—Professional and Enterprise tiers now read “Professional Atlas CMMS license” and “Enterprise Atlas CMMS license.”  
Key file: api/src/main/java/com/grash/utils/Consts.java
> - `934acad`: Added a dedicated payment-success page that users land on after Stripe checkout.  
Backend now redirects to `/payment/success` (was `/stripe/success`) and the new page shows a localized “Payment Successful!” card with instructions to check email for license details.

Key files:  
- `api/src/main/java/com/grash/service/StripeService.java`  
- `frontend/src/content/pages/Payment/Success.tsx`
> - `ef97b37`: Added an email-collection modal during checkout to capture the customer’s email before payment.  
Backend: removed unused Keygen/Stripe webhook DTOs and the /config endpoint.  
Frontend: new EmailModal component and integration in SubscriptionPlanSelector.
> - `3ea17e0`: Added duplicate-event detection to the Stripe webhook handler: incoming events are now tracked in a 24-hour TTL map so the same checkout session can’t be processed twice. Key file: `WebhookController.java`.
> - `7958009`: Super-admins now get an immediate email alert when a license-creation fails after a Stripe checkout, so problems are noticed faster.  
Key file: `WebhookController.java`
> - `d0d85b4`: **Summary**  
Replaced the “skip email” stub with a real email dispatch after a successful Stripe checkout. The webhook now uses EmailService2 to send a styled Thymeleaf template (`checkout-complete.html`) containing the customer’s name, purchased plan, license key, and expiry date.

**Key files**  
- `api/src/main/java/com/grash/controller/WebhookController.java`  
- `api/src/main/resources/templates/checkout-complete.html`
> - `d78a599`: Fixed license creation flow by removing deprecated Keygen webhook handling and switching to email-based license creation in Stripe checkout completion. Keygen DTOs were updated to match the new API structure, and the frontend plan selector now uses the correct plan ID constant.

**Key files:**  
WebhookController.java, KeygenService.java, StripeService.java, KeygenLicense*.java, SubscriptionPlanSelector.tsx
> - `46af32a`: Stripe integration now auto-creates a Keygen license after a successful checkout and enforces validation on the checkout request (planId + email required).  
Key files: StripeController.java, WebhookController.java, CheckoutRequest.java, StripeService.java, SubscriptionPlanSelector.tsx
> - `afb7491`: Added Stripe Checkout: backend endpoint `/stripe/create-checkout-session` returns a payment URL, frontend plan selector now redirects to it.  
Key files: `StripeController`, `StripeService`, `SubscriptionPlanSelector.tsx`, `WebSecurityConfig`.
> - `2b91a20`: Added self-hosted subscription plans via Stripe + Keygen integration.  
Backend exposes `/config` for publishable keys and `/webhooks/*` to sync license events; frontend pricing page now shows self-hosted tiers and toggles between monthly/yearly.  
Key files: `WebhookController`, `ApiController`, `pricing/index.tsx`, `pricingData.ts`.
> - `6b2abbe`: Added separate pricing plans for self-hosted deployments alongside existing cloud plans. The pricing page now conditionally displays either cloud or self-hosted plan tiers based on a hardcoded type flag. Key files: frontend/src/content/pricing/index.tsx, frontend/src/content/pricing/pricingData.ts.
> - `2a58a2c`: Replaced the static `hasLicenseEntitlement` utility with a new `useLicenseEntitlement` hook across both web and mobile codebases, centralizing entitlement checks in Redux state. Key files: `frontend/src/hooks/useLicenseEntitlement.ts`, `mobile/hooks/useLicenseEntitlement.ts`, `frontend/src/App.tsx`, `mobile/screens/ScanAssetScreen.tsx`. No user-facing behavior changed.
> - `127087b`: Improved error messages across workflow and meter operations. Backend now returns a clearer “Please upgrade” hint when workflow limits are hit; frontend surfaces server-supplied error text in snackbars instead of generic failure toasts.  
Key files: WorkflowController.java, Meters/index.tsx, Settings/Workflows/index.tsx
