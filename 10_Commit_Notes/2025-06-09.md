---
title: "Commit Notes - 2025-06-09"
date: 2025-06-09
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-06-09

## Backlog.md — Push at 20:35 UTC

**Commits:** 54
**Trigger:** `b534424` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b534424` | Remove example-project from git and add to gitignore | Alex Gavrilescu |
| `98e0112` | TASK-29 - Add GitHub templates (#64) | Alex Gavrilescu |
| `8bc0d73` | TASK-27 - Add contributing guidelines (#63) | Alex Gavrilescu |
| `2d313f0` | TASK-20 - Add agent guideline to mark tasks In Progress on start (#62) | Alex Gavrilescu |
| `73b3c1b` | TASK-13.1 - CLI: Agent Instruction File Selection (#61) | Alex Gavrilescu |
| `35f678a` | TASK-4 - CLI: Task Management Commands (#60) | Alex Gavrilescu |
| `859a6b9` | TASK-32 - CLI: Hide empty 'No Status' column (#59) | Alex Gavrilescu |
| `49097e6` | TASK-31 - Update README for open source (#58) | Alex Gavrilescu |
| `45a8661` | Add TASK-32 - hide No Status column when empty (#57) | Alex Gavrilescu |
| `88b6afa` | Add TASK-27 to TASK-31 - finalize OSS prep (#56) | Alex Gavrilescu |
| `2ba5fe2` | TASK-24 - Handle subtasks in the Kanban view (#54) | Alex Gavrilescu |
| `523784f` | Add subtask 24.1: Milestone board view (#55) | Alex Gavrilescu |
| `5d218a5` | TASK-23 - CLI: Kanban board order tasks by ID ASC (#53) | Alex Gavrilescu |
| `3f64b16` | TASK-22 - CLI: Prevent double dash in task filenames (#52) | Alex Gavrilescu |
| `8006b16` | TASK-21 - Kanban board vertical layout (#51) | Alex Gavrilescu |
| `91f9ffa` | TASK-25 - CLI: Export Kanban board to README (#50) | Alex Gavrilescu |
| `2796bbc` | TASK-13 - CLI: Add Agent Instruction Prompt (#49) | Alex Gavrilescu |
| `0e990a7` | Add TASK-26 - Agents: add board export step to agent DoD (#48) | Alex Gavrilescu |
| `95121bf` | Add TASK-25 - CLI: Export Kanban board to README (#47) | Alex Gavrilescu |
| `4241792` | TASK-7.1 - CLI: Kanban board detect remote task status (#46) | Alex Gavrilescu |
| `8c53ad8` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `c20a53d` | Add space between frontmatter and task description | Alex Gavrilescu |
| `63f5f3c` | TASK-6.2 - CLI: GitHub Actions for Build & Publish (#45) | Alex Gavrilescu |
| `859590f` | Fix tasks with wrong draft status | Alex Gavrilescu |
| `b74c310` | TASK-19- CLI - fix default task status and remove Draft from statuses (#44) | Alex Gavrilescu |
| `e4c8ff7` | Add task-24 - kanban subtask indentation (#43) | Alex Gavrilescu |
| `71b88a3` | TASK-4.13 - CLI: Fix config command local/global logic (#42) | Alex Gavrilescu |
| `531c08d` | TASK-4.12 - CLI: Handle task ID conflicts across branches (#41) | Alex Gavrilescu |
| `b85413f` | Fix task IDs conflict | Alex Gavrilescu |
| `b85cbb7` | Add TASK-20 - prevent double dash filenames (#40) | Alex Gavrilescu |
| `23d507a` | Add TASK-20 - Kanban board vertical layout (#39) | Alex Gavrilescu |
| `4d5e1a3` | Add TASK-20 - Agents should mark a task to In Progress when they start (#38) | Alex Gavrilescu |
| `65c8a94` | Add TASK-20 - Add kanban board sorting by id (#37) | Alex Gavrilescu |
| `d1fef63` | Rename task files | Alex Gavrilescu |
| `d4f940a` | Update status of task 4.13 | Alex Gavrilescu |
| `7292cea` | Add TASK-19 -  CLI - fix default task status and remove Draft from statuses (#36) | Alex Gavrilescu |
| `eaec5f4` | Add subtask 4.13: CLI: Fix config command local/global logic (#35) | Alex Gavrilescu |
| `9ee0cf4` | TASK-6.1 - CLI: Local installation support for bunx/npx (#34) | Alex Gavrilescu |
| `79fb145` | Clean up bun build artifacts and prevent future tracking | Alex Gavrilescu |
| `e074296` | TASK-7 - Kanban Board: Implement CLI Text-Based Kanban Board View (#33) | Alex Gavrilescu |
| `47061b9` | Add task-15, task-16, task-17 and task-18 for improving backlog readmes (#32) | Alex Gavrilescu |
| `198691e` | Add subtask 4.12: remote id conflict solve (#31) | Alex Gavrilescu |
| `6955b66` | TASK-6 - CLI: Argument Parsing, Help, and Packaging (#30) | Alex Gavrilescu |
| `b73d24a` | TASK-5 - CLI: Implement Docs & Decisions CLI Commands (Basic) (#29) | Alex Gavrilescu |
| `d827804` | Add TASK-14 - Add gui introduction screens task (#28) | Alex Gavrilescu |
| `7de9cf0` | Add subtask 6.2: add github actions build (#27) | Alex Gavrilescu |
| `db45887` | Update task-5 requirements | Alex Gavrilescu |
| `7b6a1d3` | TASK-4.10 - CLI: enforce Agents to use backlog CLI to mark tasks Done (#26) | Alex Gavrilescu |
| `0df7705` | TASK-4.9 - CLI: Normalize task-id inputs (#25) | Alex Gavrilescu |
| `2ea874e` | Add subtask-7.1 - Add kanban board remote status (#24) | Alex Gavrilescu |
| `0d2c7a8` | TASK-4.8 - Enforce description header (#23) | Alex Gavrilescu |
| `7ac6e0b` | Fix TASK-4.6 - Allow unquoted assignee (#21) | Alex Gavrilescu |
| `1bf3f9c` | TASK-4.7 - CLI: Parse unquoted created_date (#20) | Alex Gavrilescu |
| `7e738ea` | Add subtask 13.1: backlog init agent instructions (#22) | Alex Gavrilescu |

> **What changed:**
> - `b534424`: Removed the example-project directory from version control and added it to .gitignore to prevent accidental commits of test/example data. This cleans up the repository by excluding the sample project configuration file.

**Key files modified:** .gitignore, example-project/.backlog/config.yml
> - `98e0112`: Added GitHub issue and PR templates to standardize bug reports, feature requests, and pull-request descriptions; PR template now enforces references to Backlog task IDs.  
Key files: `.github/ISSUE_TEMPLATE/*.md`, `.github/PULL_REQUEST_TEMPLATE.md`, `readme.md`
> - `8bc0d73`: Added CONTRIBUTING.md with clear steps for opening issues and submitting PRs, including running tests (`bun test`) and linting (`npx biome check .`).  
Key files: CONTRIBUTING.md, readme.md, .backlog/tasks/task-27 - add-contributing-guidelines.md
> - `2d313f0`: Updated AI agent instructions to require marking tasks "In Progress" and self-assigning before starting work. Added the rule to AGENTS.md, CLAUDE.md, and .cursorrules to prevent duplicate effort and improve task tracking.
> - `73b3c1b`: `backlog init` now shows a multi-select menu letting users pick which agent-instruction files (`.cursorrules`, `CLAUDE.md`, `AGENTS.md`, `readme.md`) to create or append; unselected files are left untouched.  
Key files: `src/cli.ts`, `src/agent-instructions.ts`, `src/test/agent-instructions.test.ts`.
> - `35f678a`: Marked the parent task TASK-4 as Done; all 13 CLI task-management subtasks (create, list, view, edit, archive, demote, etc.) are now complete.  
Key file: `.backlog/tasks/task-4 - cli-task-management-commands.md`
> - `859a6b9`: The kanban board now hides the “No Status” column when every task has a defined status, keeping the view cleaner. A new test ensures this behavior is preserved.  
Key files: src/test/board.test.ts (regression test), .backlog/tasks/task-32 – cli-hide-empty-‘no-status’-column.md (completed task record).
> - `49097e6`: README now open-source ready: added npm/bun install badges, MIT license link, contribution guide reference, and a Quick Start section with global install + sample commands.  
Key files: readme.md, .backlog/tasks/task-31 - update-readme-for-open-source.md
> - `45a8661`: Added TASK-32 to hide the “No Status” column in the CLI kanban board when no tasks are missing a status, keeping the view cleaner.  
Key file: `.backlog/tasks/task-32 - cli-hide-empty-no-status-column.md`
> - `88b6afa`: Added five backlog tasks (TASK-27–31) to prepare the repo for open-source release: CONTRIBUTING.md, CODE_OF_CONDUCT.md, GitHub issue/PR templates, CHANGELOG.md, and an updated README. These tasks define the docs and templates needed for public contributors.
> - `2ba5fe2`: Kanban board now nests subtasks beneath their parent when both share the same status, using “  |—” prefix and 6-space title indent for visual clarity. Added `max_column_width` config (default 80) to control column wrapping in both terminal and markdown exports.  
Key files: src/board.ts, .backlog/config.yml, src/types/index.ts
> - `523784f`: Added a new sub-task (24.1) to implement a `--milestones` flag for the CLI’s backlog board view, enabling tasks to be grouped by milestone.  
Key file: `.backlog/tasks/task-24.1 - cli--kanban-board-milestone-view.md`
> - `5d218a5`: Kanban board now sorts tasks within each status column by numeric ID ascending (task-2 before task-10). Added `compareIds` helper to handle both simple and decimal IDs; sorting runs after tasks are grouped. Key files: `src/board.ts`, `src/test/board.test.ts`, `.backlog/tasks/task-23 - cli-kanban-board-order-tasks-by-id-asc.md`.
> - `3f64b16`: Fixed CLI filename generation to prevent consecutive dashes by enhancing `sanitizeFilename` to collapse repeated dashes and trim leading/trailing ones. Added a regression test to ensure no double dashes appear in task filenames.

Key files: `src/file-system/operations.ts`, `src/test/filesystem.test.ts`
> - `8006b16`: Added a vertical layout option to the Kanban board: `backlog board view --layout vertical` (or `--vertical`) now stacks all status columns in a single column instead of side-by-side. Key files: `src/board.ts` (layout logic), `src/cli.ts` (new flags), `readme.md` (docs).
> - `91f9ffa`: Added `backlog board export` CLI command that appends the current Kanban board to readme.md (or a custom file via `--output`). The board is appended without overwriting existing content, and missing directories are created automatically. Key files: src/cli.ts, src/board.ts, readme.md
> - `2796bbc`: `backlog init` now asks whether to inject AI-agent instructions into the repo.  
If the user says yes, it appends Backlog-specific guidance to AGENTS.md, CLAUDE.md and .cursorrules (creating them if missing) and auto-commits the changes; saying no leaves the repo untouched.  

Key files touched: src/cli.ts, src/agent-instructions.ts, src/guidelines/*.
> - `0e990a7`: Added a new backlog task (TASK-26) to update the agent “Definition of Done” in CLAUDE.md, AGENTS.md, and .cursorrules so agents always run `backlog board export` before committing. Key file: .backlog/tasks/task-26 - docs-add-board-export-step-to-agent-dod.md
> - `95121bf`: Added backlog task TASK-25 to build a CLI command that exports the Kanban board into README.md (or a custom file), preserving existing content and creating the file if needed.  
Key file: `.backlog/tasks/task-25 - cli-export-kanban-board-to-readme.md`
> - `4241792`: CLI kanban board now fetches and merges task statuses from every remote branch (`origin/*`), so the board always shows the most advanced state of each task across the whole repo.  
Key files: `src/cli.ts`, `src/git/operations.ts`, `src/test/cli.test.ts`.
> - `8c53ad8`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `.backlog/tasks/task-6.2 - cli--github-actions-for-build-&-publish.md`, `.github/workflows/ci.yml`, `readme.md`.
> - `c20a53d`: Added a blank line between YAML frontmatter and the “## Description” header in every backlog task file and updated the markdown serializer to enforce this spacing.  
Key files: all `.backlog/tasks/*.md` task files and `src/markdown/serializer.ts`.
> - `63f5f3c`: Added a GitHub Actions CI pipeline that builds the CLI with Bun, runs tests, and auto-publishes to npm whenever a version tag (v*.*.*) is pushed. README now documents the release workflow.  
Key files: `.github/workflows/ci.yml`, `readme.md`
> - `859590f`: Corrected the status of three backlog tasks from “Draft” to “To Do” so they now appear in the active work queue.  
Files touched: task-22, task-23, task-24 under .backlog/tasks/.
> - `b74c310`: Fixed CLI so `backlog task create` now defaults to status “To Do” and stores tasks in `.backlog/tasks`; added `--draft` flag to create Draft-status tasks in `.backlog/drafts`. Removed “Draft” from the standard status list in `config.yml` and updated docs/tests.

Key files: `src/cli.ts`, `src/constants/index.ts`, `.backlog/config.yml`, `readme.md`
> - `e4c8ff7`: Added a new backlog task to implement visual indentation of subtasks in the Kanban view using pipes and em dashes.  
Key file: `.backlog/tasks/task-24 - handle-subtasks-in-the-kanban-view.md`
> - `71b88a3`: Fixed the CLI config commands so `backlog config set` now respects `--local` (writes to `.backlog/config.yml`) and `--global` (writes to `~/.backlog/.user`), while `backlog config get` reads local → global → built-in defaults.  
Key files: src/cli.ts, src/file-system/operations.ts, readme.md
> - `531c08d`: **Summary:**  
Modified `backlog task create` to scan every remote branch for existing task IDs before assigning the next one, eliminating cross-branch ID collisions. The CLI now fetches remote refs, inspects `.backlog/tasks` on each branch, and always picks an ID higher than any found.

**Key files:**  
`src/cli.ts`, `src/git/operations.ts`, `src/test/remote-id-conflict.test.ts`
> - `b85413f`: Fixed duplicate task IDs (`task-20`) in two backlog files by updating them to their correct unique IDs (`task-22` and `task-23`).  
Key files: `.backlog/tasks/task-22 - cli-prevent-double-dash-in-task-filenames.md`, `.backlog/tasks/task-23 - cli-kanban-board-order-tasks-by-id-asc.md`
> - `b85cbb7`: Added backlog item TASK-20 to stop the CLI from creating task filenames with double dashes (e.g., `task--name.md`).  
Key file: `.backlog/tasks/task-20 - cli--prevent-double-dash-in-task-filenames.md`
> - `23d507a`: Added a new backlog task (TASK-20) for implementing a vertical layout option on the Kanban board, stacking all status columns into one.  
Key file: `.backlog/tasks/task-21 - kanban-board-vertical-layout.md`
> - `4d5e1a3`: Created backlog task TASK-20 to formalize the rule that AI agents must mark a task In Progress, self-assign, and push the change as soon as they begin work.  
Key file: `.backlog/tasks/task-20 - add-agent-guideline-to-mark-tasks-in-progress-on-start.md`
> - `65c8a94`: Added backlog task TASK-20 to implement ascending-ID sorting for the CLI kanban board.  
Key file: `.backlog/tasks/task-20 - cli-kanban-board-order-tasks-by-id-asc.md`
> - `d1fef63`: Renamed three backlog task files to clearer, more descriptive names that match their content (e.g., “cli-agent-instruction-file-selection.md”).  
No functional code change—purely organizational to make future navigation easier.
> - `d4f940a`: Task 4.13 moved from Draft → To Do, signaling the CLI config-command local/global logic fix is now ready for work.  
Key file: `.backlog/tasks/task-4.13 - cli--fix-config-command-local-global-logic.md`
> - `7292cea`: Added TASK-19 to fix the CLI so new tasks default to “To Do” instead of “Draft”; Draft is now reserved for `--draft` creations in `.backlog/drafts`.  
Updated 11 existing tasks from “Draft” → “To Do” and added missing `reporter: @MrLesk` fields.  
Key files: `.backlog/tasks/task-19 …` plus tasks 13–18, 4.10–4.12.
> - `eaec5f4`: Added subtask 4.13 to fix the CLI config command so `--local` writes to `.backlog/config.yml`, `--global` to the user file, and `get` checks local → global → default.  
Key file: `.backlog/tasks/task-4.13 - cli--fix-config-command-local-global-logic.md`
> - `9ee0cf4`: Enabled local installation of the backlog.md CLI via npm/bun by removing the “private” flag, setting the package name to “backlog.md”, and adding a bin entry pointing to ./cli/index.js. Added tests and docs so `npx backlog` and `bunx backlog` now work inside any project without a global install.

Key files: package.json, readme.md, src/test/local-install.test.ts
> - `79fb145`: Cleaned up bun build artifacts by removing tracked `.*.bun-build` files and adding patterns to `.gitignore` to prevent future tracking. Key file modified: `.gitignore`.
> - `e074296`: Added a new CLI command `backlog board view` that renders a text-based Kanban board from the local task files. Tasks are grouped by status into columns with dynamic widths and the board respects the project’s configured status order. Key files: src/board.ts (core board generator), src/cli.ts (new command), src/test/board.test.ts & src/test/cli.test.ts (tests).
> - `47061b9`: Added four new backlog tasks (15-18) to improve the readme files for tasks, docs, drafts, and decisions folders. Each task focuses on replacing specific examples with generic templates and documenting the relevant CLI commands for better usability.

Key files: `.backlog/tasks/task-15` through `task-18`
> - `198691e`: Added subtask 4.12 to prevent duplicate task IDs when multiple branches exist: the CLI will now scan every remote branch for existing tasks and always pick the next sequential ID above the highest found.  
Key file: `.backlog/tasks/task-4.12 - cli--handle-task-id-conflicts-across-branches.md`
> - `6955b66`: CLI now ships with full help text, proper argument parsing, and a working standalone executable.  
package.json points `./cli/index.js` as the npm binary entry point; `bun build --compile` produces a native `cli/backlog` binary.  
Key files: package.json, src/cli.ts, src/test/build.test.ts
> - `b73d24a`: Added CLI commands to create and list documentation (`backlog doc …`) and decision logs (`backlog decision …`). Files are auto-saved under `.backlog/docs/` and `.backlog/decisions/` with incremental IDs and proper YAML front-matter. Key files touched: `src/cli.ts`, `src/core/backlog.ts`, plus new help text in readme.md and the `.backlog/` readmes.
> - `d827804`: Added a new backlog task (TASK-14) to design two GUI welcome screens: a first-launch screen for uninitialized folders and a “welcome back” screen for existing projects.  
Key file: `.backlog/tasks/task-14 - gui-introduction-screens.md`
> - `7de9cf0`: Added subtask 6.2 to track setting up GitHub Actions CI for the CLI: build with Bun, run tests, and auto-publish to npm on version tags.  
Key file: `.backlog/tasks/task-6.2 - cli-github-actions-publish.md`
> - `db45887`: Updated the CLI spec for `backlog doc create` to use a flag-based path option (`-p` / `--path`) instead of a positional argument, keeping the title as the only positional arg.  
Key file: `.backlog/tasks/task-5 - cli-docs-decisions.md`
> - `7b6a1d3`: Task 4.10 completed: agent docs (AGENTS.md, CLAUDE.md, .cursorrules) now uniformly instruct agents to run `backlog task edit <task-id> --status Done` after testing, ensuring consistent task-status workflow. Key files touched: AGENTS.md, CLAUDE.md, .cursorrules and the two backlog task records.
> - `0df7705`: The CLI now consistently normalizes task IDs across all commands, so `--parent 4` is stored as `parent_task_id: 'task-4'` in saved files. Key files changed: `src/cli.ts` (added normalization in `buildTaskFromOptions()`), `src/test/parent-id-normalization.test.ts` (updated test), and `.backlog/config.yml` (project name updated).
> - `2ea874e`: Added subtask 7.1 to enhance the Kanban board CLI so it fetches every `origin` branch, merges the freshest task status into the board, and resolves conflicts by keeping the last-seen status and its full frontmatter.  
Key file: `.backlog/tasks/task-7.1 - cli--kanban-board-detect-remote-task-status.md`
> - `0d2c7a8`: **Summary:**  
The CLI now auto-inserts a `## Description` header when creating or updating tasks if one is missing, ensuring consistent task formatting. Documentation in `.cursorrules`, `AGENTS.md`, and `CLAUDE.md` was updated to require both a Description section and relevant tests for new features.

**Key files modified:**  
`src/core/backlog.ts`, `.cursorrules`, `AGENTS.md`, `CLAUDE.md`, `src/test/core.test.ts`, `src/test/cli.test.ts`
> - `7ac6e0b`: The parser now auto-wraps bare assignee values (like @MrLesk) in quotes before YAML parsing, fixing a front-matter syntax error that prevented tasks from loading when the assignee wasn’t already quoted.

Key files: src/markdown/parser.ts, src/test/markdown.test.ts
> - `1bf3f9c`: Fixed CLI parsing of unquoted `created_date` values in task YAML frontmatter; dates without quotes are now handled correctly. Added `date_format` option to `config.yml` documentation.

Key files: `src/markdown/parser.ts`, `.backlog/docs/readme.md`
> - `7e738ea`: Added a new backlog sub-task (13.1) to replace the current yes/no prompt in `backlog init` with a multi-select menu, letting users pick which agent-instruction files (`.cursoreules`, `CLAUDE.md`, `AGENTS.md`, `readme.md`) to update.  
Key file: `.backlog/tasks/task-13.1 - cli--agent-instruction-file-selection.md`
