---
title: "Commit Notes - 2025-12-07"
date: 2025-12-07
tags: [project/Agent-Factory, project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-07

## Agent-Factory — Push at 22:52 UTC

**Commits:** 13
**Trigger:** `fef9fb1` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `fef9fb1` | feat: Phase 5 Complete - Enhanced Observability | Mikecranesync |
| `c7f74e9` | feat: Phase 1 Complete - LLM Abstraction Layer | Mikecranesync |
| `76885c6` | feat: Phase 0 complete - CLI improvements and platform documentation | Mikecranesync |
| `5217df0` | docs: Update memory files with Bob chat interface fix | Mikecranesync |
| `5562252` | fix: Add Bob to chat interface as preset agent | Mikecranesync |
| `b2fe841` | docs: Update memory files with Anti-gravity review and validation | Mikecranesync |
| `5d6e73f` | docs: Add comprehensive chat interface usage guide | Mikecranesync |
| `38d712f` | chore: Update Claude Code configuration | Mikecranesync |
| `14158fb` | docs: Update memory system with CLI and Bob progress | Mikecranesync |
| `f0e5944` | docs: Add comprehensive guides for CLI and Bob agent | Mikecranesync |
| `9b615dd` | feat: Add Bob market research agent (generated from spec) | Mikecranesync |
| `ff52a33` | feat: Add interactive agent creation and editing CLI | Mikecranesync |
| `427aae5` | feat: Add agentcli.py - CLI tool for spec-to-agent workflow | Mikecranesync |

> **What changed:**
> - `fef9fb1`: **Phase 5 Observability Complete** – Added production-ready monitoring with structured JSON logging, automatic error categorization (13 types), and multi-format metrics export (StatsD/Prometheus/Console). Key files: `logger.py`, `errors.py`, `exporters.py` plus comprehensive demo and 35 new tests.
> - `c7f74e9`: **Summary:** Phase 1 delivers a production-ready LLM abstraction layer with multi-provider routing (OpenAI/Anthropic/Google/Ollama), automatic cost tracking to $0.000001 precision, and budget monitoring. The implementation adds 1,117 lines across 8 new files including comprehensive type system, 27 new tests, and working demo while maintaining all existing functionality.

**Key files:** `agent_factory/llm/router.py`, `agent_factory/llm/tracker.py`, `agent_factory/llm/types.py`, `tests/test_llm.py`
> - `76885c6`: **Phase 0 Complete: Platform vision documented, CLI polished, ready for Phase 1**

Created comprehensive 9-file documentation suite (~530KB) covering architecture, business model, API design, and 13-week roadmap targeting $10K MRR by month 3. Enhanced CLI with improved help text, ASCII-only Windows output, and new 'roadmap' command. Updated all memory logs marking Phase 0 complete and Phase 1 (LLM abstraction layer) ready to start.

**Key files:** docs/* (9 new platform docs), agent_factory/cli/app.py, PROJECT_CONTEXT.md, NEXT_ACTIONS.md
> - `5217df0`: Bob agent is now fully accessible via the CLI chat interface—fixed the missing preset registration and corrected the docs so `poetry run agentcli chat --agent bob` works. Users can start multi-turn market-research sessions with Bob’s 10-tool suite and session-saving.  
Key files: NEXT_ACTIONS.md, PROJECT_CONTEXT.md
> - `5562252`: Added Bob as a preset chat agent: `poetry run agentcli chat --agent bob` now works and gives users a 10-tool market-research specialist with deep search, file, and research capabilities.  
Fixed all documentation to use the correct `--agent bob` syntax and added a preset-agent table plus `list-agents` command.  
Key files: `agent_factory/cli/agent_presets.py`, `CHAT_USAGE.md`.
> - `b2fe841`: **Summary:** Anti-gravity CLI integration is now fully validated and ready for use. The chat interface (poetry run agentcli chat --agent bob-1) is the recommended next step for market research, with Bob agent tested and functional. All changes have been organized into 6 logical commits with 95% constitutional alignment.

**Key files:** NEXT_ACTIONS.md, PROJECT_CONTEXT.md
> - `5d6e73f`: Added a comprehensive CHAT_USAGE.md guide that documents how to use Agent Factory’s interactive chat interface for iterative market research and agent conversations.  
Key files: CHAT_USAGE.md (new), .gitignore (updated to ignore Agent-Factory/ duplicate).
> - `38d712f`: Updated Claude Code configuration by renaming context commands to content commands (content-clear.md, content-load.md) and added auto-approvals for agentcli and bash commands in settings. Also updated .gitignore to exclude generated files. These changes streamline the Claude Code workflow and reduce approval friction for common operations.
> - `14158fb`: Updated project memory files to reflect completion of interactive CLI (wizard, editor, chat), creation of Bob market-research agent, and GitHub wiki publication; key changes include raising agent iteration limits to 25 for research tasks and implementing priority agent-editor features (tools/invariants editing) first.

Key files: PROJECT_CONTEXT.md, NEXT_ACTIONS.md, DEVELOPMENT_LOG.md, DECISIONS_LOG.md, ISSUES_LOG.md
> - `f0e5944`: Added comprehensive user documentation for the new CLI agent editor and Bob market-research agent. The six new guides cover interactive agent creation/editing, Bob’s toolset and constraints, wizard walkthrough, agent reproduction, testing workflows, and slash-command testing, letting users build and refine agents without touching code.  
Key files: AGENT_EDITING_GUIDE.md, BOB_CAPABILITIES.md, INTERACTIVE_WIZARD_DEMO.md, MARKET_RESEARCH_AGENT_INSTRUCTIONS.md, SLASH_COMMANDS_TESTING.md, TEST_BOB.md
> - `9b615dd`: Added “Bob” (unnamedagent_v1_0), a new CLI-generated market-research agent that surfaces app/agent opportunities by scraping trends, competitors, pain points and sizing MRR.  
Key files: agents/unnamedagent_v1_0.py (210-line agent), specs/bob-1.md (spec), plus quick/test scripts.
> - `ff52a33`: Added a full-featured CLI for creating and editing agents interactively. Users can now run `poetry run agentcli create` to launch an 8-step wizard that walks through agent setup, or `poetry run agentcli edit <agent-name>` to modify existing agents without touching files. The new module also includes a REPL chat session (`poetry run agentcli chat`) and a rich tool registry with templates for researcher, coder, and analyst agents.

Key files: `agent_factory/cli/interactive_creator.py`, `agent_factory/cli/agent_editor.py`, `agent_factory/cli/chat_session.py`, `agentcli.py`
> - `427aae5`: Added `agentcli.py`, a unified CLI for the Agent Factory pipeline that turns a spec into a working agent + tests with one command (`agentcli build <spec>`). Fixed filename sanitization bugs in the code and test generators so specs with special characters no longer break file creation. Key files: `agentcli.py`, `code_generator.py`, `eval_generator.py`.

## Backlog.md — Push at 17:26 UTC

**Commits:** 5
**Trigger:** `c9d8a3c` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c9d8a3c` | Cleanup done tasks | Alex Gavrilescu |
| `56bf535` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `5fcdc69` | TASK-339 - Fix tasks disappear when assignee list populated (#454) | Alex Gavrilescu |
| `d6c2ce9` | TASK-338 - Add label filter to web all-tasks page (#453) | Alex Gavrilescu |
| `4c8f9c2` | TASK-337 - Add label filter to TUI task list (#452) | Alex Gavrilescu |

> **What changed:**
> - `c9d8a3c`: Moved 7 completed tasks from the active backlog into the `backlog/completed/` archive.  
Key areas wrapped up: search refactor, TUI pane sync, resource-template error handling, editor stdio fixes, move-mode rewrite, task front-matter callbacks, and Nix flake compatibility.
> - `56bf535`: Updated the README’s auto-generated project board snapshot to v1.27.0, reflecting the latest task statuses and completion dates.  
Key files: README.md, package.json
> - `5fcdc69`: Fixed the bug where tasks vanished from `backlog task list` when a task used inline YAML like `assignee: [@user]`.  
Now the parser quotes @-handles before YAML parsing and the file-system skips unreadable task files instead of failing the whole list.  

Key files: src/markdown/parser.ts, src/file-system/operations.ts
> - `d6c2ce9`: Added a core-backed label filter to the web All Tasks page. Users can now multi-select labels; selections sync with the URL and work alongside existing status/priority/search filters without duplicating logic.  
Key files: src/core/backlog.ts, src/server/index.ts, src/web/components/TaskList.tsx
> - `4c8f9c2`: Added a multi-select label filter to the TUI task list; users can now narrow tasks by one or more labels and the filter combines cleanly with existing status/priority filters.  
Key files touched: src/ui/task-viewer-with-search.ts, src/core/search-service.ts, src/utils/label-filter.ts, plus new tests.

## cmms — Push at 23:31 UTC

**Commits:** 27
**Trigger:** `7b754c0` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `7b754c0` | perf: optimize DeleteDemoCompaniesJob | Ibrahima G. Coulibaly |
| `750844c` | chore: uncomment DeleteDemoCompaniesJob | Ibrahima G. Coulibaly |
| `ce18b4a` | fix: referrer default value | Ibrahima G. Coulibaly |
| `c13fe4f` | fix: update PM | Ibrahima G. Coulibaly |
| `61cea3a` | fix: scheduleWorkOrder creating WO on restart | Ibrahima G. Coulibaly |
| `78fc17c` | fix: PT language | Ibrahima G. Coulibaly |
| `51a21d1` | fix: PT language | Ibrahima G. Coulibaly |
| `e175a74` | fix: translations | Ibrahima G. Coulibaly |
| `5bbf5b3` | fix: default superAdmin creation | Ibrahima G. Coulibaly |
| `14febe6` | fix: delete old default roles liquibase | Ibrahima G. Coulibaly |
| `2603f11` | fix: delete old default roles liquibase | Ibrahima G. Coulibaly |
| `3c80452` | chore: delete old default roles | Ibrahima G. Coulibaly |
| `3d5a32f` | Merge pull request #119 from Grashjs/default-roles | Ibrahima G. Coulibaly |
| `9a4e328` | feat: default roles init | Ibrahima G. Coulibaly |
| `5939a10` | fix: disable update default roles | Ibrahima G. Coulibaly |
| `071a0d3` | chore: filter demo companies in default roles to update | Ibrahima G. Coulibaly |
| `45ba7cf` | chore: disable delete demo companies job | Ibrahima G. Coulibaly |
| `165c896` | chore: use SmartInitializingSingleton to ensure startup setup | Ibrahima G. Coulibaly |
| `b6eb6f8` | chore: ENABLE_MAIL_HEALTH_CHECK | Ibrahima G. Coulibaly |
| `6f67366` | fix: commandLineRunner findDefaultRoles | Ibrahima G. Coulibaly |
| `88c86f2` | refactor: ScheduleService | Ibrahima G. Coulibaly |
| `de2f04f` | fix: pm | Ibrahima G. Coulibaly |
| `d08fef7` | feat: translations | Ibrahima G. Coulibaly |
| `5212425` | Merge pull request #117 from Grashjs/schedule | Ibrahima G. Coulibaly |
| `f477dcd` | Merge branch 'main' of https://github.com/Grashjs/cmms into schedule | Ibrahima G. Coulibaly |
| `a417344` | feat: enhance scheduler | Ibrahima G. Coulibaly |
| `f0fb194` | feat: complex scheduling | Ibrahima G. Coulibaly |

> **What changed:**
> - `7b754c0`: Replaced the N+1 delete loop with a single bulk-delete query (`deleteAllByDemoTrue`) in the nightly `DeleteDemoCompaniesJob`, cutting runtime and DB round-trips. Added logging to the job and muted a noisy scheduler log line.

**Files touched:** `DeleteDemoCompaniesJob.java`, `CompanyRepository.java`, `ScheduleService.java`
> - `750844c`: Re-enabled the scheduled cleanup of demo companies by uncommenting the deletion logic in `DeleteDemoCompaniesJob`.  
Key file: `api/src/main/java/com/grash/job/DeleteDemoCompaniesJob.java`
> - `ce18b4a`: Fixed a bug where the referrer value could be stored as `null`, ensuring consistent string handling in localStorage. This prevents downstream issues when the referrer is read and expected to be a string.  
**Key file:** `frontend/src/App.tsx`
> - `c13fe4f`: Fixed a bug where updating a PreventiveMaintenance record didn’t re-enable its schedule; the schedule is now explicitly set to disabled=false before saving.  
Key file: PreventiveMaintenanceService.java
> - `61cea3a`: Fixed duplicate work-order creation on service restart by removing the “start immediately if scheduled in the past” logic; the scheduler now always uses the original startsOn date.  
Notification timing was also corrected to use the PM’s estimated-start date instead of recalculating an offset.  

Key files: ScheduleService.java, Language.java
> - `78fc17c`: Fixed the Portuguese language enum by moving `PT` to the end of the list to maintain alphabetical order. This ensures consistent ordering of language options.

**Key file modified:** `api/src/main/java/com/grash/model/enums/Language.java`
> - `51a21d1`: Added standalone Portuguese (PT) alongside existing Brazilian Portuguese (PT_BR) enum; both now map to pt-BR locale.  
Key files: Language.java, Helper.java
> - `e175a74`: Fixed hard-coded English labels in the role-permissions UI by wrapping them with the `t()` translation function so the checkbox labels now respect the user’s locale.  
Key file: `frontend/src/content/own/Settings/Roles/EditRole.tsx`
> - `5bbf5b3`: Fixed a bug where the first superAdmin couldn’t be created because the code always expected an “inviter” user. Now, if the role has companySettings (i.e., it’s the initial superAdmin), the user is linked directly to that company instead of failing to find an inviter.

Key file: api/src/main/java/com/grash/service/UserService.java
> - `14febe6`: Removes two obsolete columns (`role_code`, `role_type`) from the `own_user` table that were left behind after the earlier cleanup of default roles. Keeps the database schema tidy and avoids confusion for future queries.  
Key file: `2025_12_07_1765117872_delete_default_roles.xml`
> - `2603f11`: The liquibase script that re-links users to new default roles now skips users with role_code 6, preventing them from being incorrectly reassigned.  
Key file: `2025_12_07_1765117872_delete_default_roles.xml`
> - `3c80452`: Deleted obsolete default roles and ensured clean-up: updated own_user to point to surviving roles, then made all role-dependent tables (permissions, invitations) cascade on delete so removing a role auto-purges its linked data.  
Key files: `2025_12_07_1765117872_delete_default_roles.xml`, `master.xml`
> - `3d5a32f`: Merge pull request #119 from Grashjs/default-roles. Files: `api/src/main/java/com/grash/ApiApplication.java`, `api/src/main/java/com/grash/controller/CostCategoryController.java`, `api/src/main/java/com/grash/controller/UserController.java`, `api/src/main/java/com/grash/model/Role.java`, `api/src/main/java/com/grash/repository/RoleRepository.java` (+7 more).
> - `9a4e328`: Re-enabled the automatic creation and updating of default roles on startup by uncommenting the call to `roleService.updateDefaultRoles()` and moving the related logic into `RoleService`. Added a Liquibase migration to drop existing default roles before re-inserting them, ensuring a clean slate. Key files: `ApiApplication.java`, `RoleService.java`, and the new Liquibase changelog.
> - `5939a10`: Disabled the automatic update of default roles during startup to prevent unintended permission changes. This fixes potential role overwrites that could affect user access control.

Key file: `ApiApplication.java`
> - `071a0d3`: Default role updates now skip demo companies. Modified `RoleRepository.java` to add `AND r.companySettings.company.demo=false` so only live-company roles are returned by `findDefaultRoles()`.
> - `45ba7cf`: Disabled the scheduled cleanup that automatically deletes demo companies by commenting out the deletion logic. This prevents accidental removal of demo data while keeping the job structure intact for future re-enablement.  
Key file: `api/src/main/java/com/grash/job/DeleteDemoCompaniesJob.java`
> - `165c896`: Switched from CommandLineRunner to SmartInitializingSingleton so all Spring singletons are ready before the startup setup runs, eliminating race-condition bugs during initialization.  
Key files: ApiApplication.java (refactored startup logic), RoleRepository.java (touched).
> - `b6eb6f8`: Enabled toggling of the mail health-check via the `ENABLE_MAIL_HEALTH_CHECK` environment variable (keeps true as default).  
Key file: `api/src/main/resources/application.yml`
> - `6f67366`: Fixed the `findDefaultRoles` query to eagerly fetch all permission collections, preventing lazy-loading exceptions when the `CommandLineRunner` tries to access them.  
Key file: `RoleRepository.java`
> - `88c86f2`: Refactored ScheduleService to move the “should we actually schedule this notification?” logic out of the public method and into the private scheduleNotificationJob helper.  
Key file: api/src/main/java/com/grash/service/ScheduleService.java
> - `de2f04f`: Fixed scheduling logic for preventive-maintenance work orders: added a missing `return` to stop duplicate jobs when scheduling completion-based recurrence, and hardened date handling so jobs never start in the past and notification dates are validated against the schedule’s end date.  
Key file: `ScheduleService.java`
> - `d08fef7`: Added full i18n support to the SignaturePad component and shipped new scheduling-related translations for ES, FR, IT, PL, PT-BR, RU, SV and TR.  
The pad now uses `t('clear')` instead of hard-coded English and the redundant “Save Signature” flow was removed, so users only clear or finish drawing.  

Key files:  
- frontend/src/content/own/components/form/SignaturePad.tsx  
- frontend/src/i18n/translations/*.ts
> - `5212425`: Merge pull request #117 from Grashjs/schedule. Files: `api/src/main/java/com/grash/controller/PreventiveMaintenanceController.java`, `api/src/main/java/com/grash/controller/WorkOrderController.java`, `api/src/main/java/com/grash/dto/PreventiveMaintenancePostDTO.java`, `api/src/main/java/com/grash/dto/PreventiveMaintenanceShowDTO.java`, `api/src/main/java/com/grash/dto/SchedulePatchDTO.java` (+17 more).
> - `f477dcd`: Merge branch 'main' of https://github.com/Grashjs/cmms into schedule. Files: `api/src/main/java/com/grash/dto/WorkOrderChangeStatusDTO.java`, `api/src/main/java/com/grash/dto/WorkOrderShowDTO.java`, `api/src/main/java/com/grash/model/WorkOrder.java`, `api/src/main/java/com/grash/model/envers/WorkOrderAud.java`, `api/src/main/resources/db/changelog/2025_12_06_1765045582_signature.xml` (+22 more).
> - `a417344`: Enhanced the preventive-maintenance scheduler to support multi-week (n-weekly) recurrence: jobs now skip execution unless the current week aligns with the configured frequency.  
Key files: WorkOrderCreationJob.java, PreventiveMaintenanceMapper.java, ScheduleService.java, PMDetails.tsx, preventiveMaintenance.ts.
> - `f0fb194`: Added full support for complex recurring schedules to preventive-maintenance work orders.  
Backend now stores recurrence type (daily/weekly/monthly), the basis (start vs completion date), and optional days-of-week; frontend exposes these options in the PM create/edit forms and shows the next projected work-order date.  

Key files touched: PreventiveMaintenanceController, ScheduleService, Schedule model, DTOs, PMDetails & form components.
