---
title: "Commit Notes - 2025-04-24"
date: 2025-04-24
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-24

## cmms — Push at 23:22 UTC

**Commits:** 6
**Trigger:** `6827c58` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `6827c58` | chore: implement pageable support and sorting for various services and components | Ibrahima G. Coulibaly |
| `827acae` | refactor: simplify sorting | Ibrahima G. Coulibaly |
| `502c2ba` | fix: infinite loop while restoring table state | Ibrahima G. Coulibaly |
| `1b102ac` | fix: infinite loop while restoring table state | Ibrahima G. Coulibaly |
| `fa16c31` | fix: sorting mode | Ibrahima G. Coulibaly |
| `0ad1a45` | chore: implement pagination for location children retrieval | Ibrahima G. Coulibaly |

> **What changed:**
> - `6827c58`: Added server-side pagination & sorting: backend services (Asset, Location, etc.) now return Page<T> and accept Pageable; SearchCriteria gained sortField.  
Frontend grids were updated to request pages/sorts from the server instead of loading everything at once, keeping large lists responsive.  
Key files: SearchCriteria.java, AssetService.java, LocationService.java, AssetController.java, LocationController.java, CustomDatagrid/index.tsx, asset.ts.
> - `827acae`: Simplified the client-side sort mapping in the Locations grid: changed the mapper from `Record<string, string[]>` to `Record<string, string>` and removed the unnecessary `.map()`, directly building the single Sort string.  
Key file: `frontend/src/content/own/Locations/index.tsx`
> - `502c2ba`: Fixed an infinite loop when restoring DataGrid state by splitting the restoration flag into separate refs for columns and sorting, and adding a retry limit (20) for column restoration. This prevents the grid from repeatedly trying to restore state when columns aren't ready.

Key file: frontend/src/hooks/useGridStatePersist.ts
> - `1b102ac`: Fixed an infinite loop when restoring the data-grid’s saved column/sort state by adding a one-time guard (`hasRestoredRef`) and including sorting in the restore payload.  
Key file: `frontend/src/hooks/useGridStatePersist.ts`
> - `fa16c31`: Locations table now sorts on the client instead of the server, giving instant column sorting for name, address, createdAt and customId without extra API calls.  
Key file: frontend/src/content/own/Locations/index.tsx
> - `0ad1a45`: Added server-side pagination to the location-children endpoint so large hierarchies load faster; backend now returns a Spring Page instead of every record, and the frontend keeps its own page/size state and passes those params to the API.  
Key files: LocationController, LocationRepository, LocationService, Locations/index.tsx, location.ts slice.
