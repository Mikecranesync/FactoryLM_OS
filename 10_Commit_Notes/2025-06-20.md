---
title: "Commit Notes - 2025-06-20"
date: 2025-06-20
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-06-20

## Backlog.md — Push at 22:14 UTC

**Commits:** 21
**Trigger:** `6d7b0fd` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `6d7b0fd` | TASK-96 - Correct board filtering logic | Alex Gavrilescu |
| `a8ad2b1` | TASK-98 - Invert task order in Done column only (#132) | Alex Gavrilescu |
| `71fc0d7` | TASK-96 - Check status across archive and drafts (#131) | Alex Gavrilescu |
| `f80acdf` | TASK-97 - Cross-branch task ID checking and branch info (#130) | Alex Gavrilescu |
| `e38781e` | rename task id due to conflict | Alex Gavrilescu |
| `73203a6` | TASK-85 - Merge and consolidate loading screen functions (#129) | Alex Gavrilescu |
| `1ed9938` | Fix displaying priority | Alex Gavrilescu |
| `500c8e1` | TASK-89 - Add dependency parameter for task create and edit commands (#126) | Alex Gavrilescu |
| `cd7ec00` | TASK-94 - CLI: Show created task file path (#124) | Alex Gavrilescu |
| `a7c6ac9` | TASK-87 - Make agent guideline file updates idempotent during init (#125) | Alex Gavrilescu |
| `d48774c` | TASK-95 - Add priority field to tasks (#127) | Alex Gavrilescu |
| `9f9652f` | TASK-92 - CI: Fix intermittent Windows test failures (#128) | Alex Gavrilescu |
| `ebd6d7f` | rename task id due to conflict | Alex Gavrilescu |
| `db36c0c` | Cleanup tasks | Alex Gavrilescu |
| `dee4e69` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `f49d571` | Move task 33 to drafts | Alex Gavrilescu |
| `ec33336` | TASK-95 - Add priority field to tasks (#123) | Alex Gavrilescu |
| `f02dd48` | Add TASk-95 - Fix demoted task board visibility - check status across archive and drafts | Alex Gavrilescu |
| `46fae33` | TASK-94 - CLI: Show created task file path (#122) | Alex Gavrilescu |
| `6437222` | Converted GUI tasks to Draft for the moment | Alex Gavrilescu |
| `31c6adc` | Add TASK-93 - Cross-branch task ID checking and branch info (#115) | Alex Gavrilescu |

> **What changed:**
> - `6d7b0fd`: Fixed board filtering to correctly hide demoted/archived tasks by scanning every branch for the most-recent task/draft/archive file and only showing tasks whose latest state is “task”.  
Key files: src/cli.ts
> - `a8ad2b1`: Done column now lists tasks in descending ID order (newest first) while other columns stay ascending.  
Key files: src/board.ts, src/ui/board.ts
> - `71fc0d7`: Board view now hides any task that exists in `.backlog/drafts` or `.backlog/archive/tasks` on any local or remote branch.  
Key change: `handleBoardView` in `src/cli.ts` fetches every branch, scans those folders, and filters the displayed task list accordingly.
> - `f80acdf`: Cross-branch task ID collision prevention: `backlog task create` now scans every local and remote branch to find the highest task ID and assigns the next number globally. The board view also shows which branch last touched each task.  
Key files: src/cli.ts, src/git/operations.ts, src/core/backlog.ts, src/ui/board.ts
> - `e38781e`: Renamed the task ID from `task-94` to `task-97` to resolve a duplicate-ID conflict.  
Key file: `.backlog/tasks/task-97 - Cross-branch-task-ID-checking-and-branch-info.md`
> - `73203a6`: Refactored the loading screen implementation by consolidating duplicated code between `withLoadingScreen` and `createLoadingScreen` into a shared `createLoadingScreenBase` function, eliminating code duplication while maintaining full API compatibility. All existing loading screen functionality continues to work unchanged including TTY/non-TTY support, keyboard shortcuts, and spinner animations.

**Files modified:** `src/ui/loading.ts`
> - `1ed9938`: Replaced emoji priority indicators (🔴🟡🟢) with plain colored dots (●) in both the board list and task detail views, and ensured the priority field is now displayed in the enhanced task viewer.  
Key files: src/board.ts, src/ui/task-viewer.ts
> - `500c8e1`: Added `--depends-on/--dep` flag to `task create` and `task edit` so tasks can declare dependencies on other tasks (comma-separated or repeated flags).  
Validation ensures referenced task IDs exist before save, and dependencies now appear in both plain-text and interactive views.  

Key files: `src/cli.ts`, `src/test/cli-dependency.test.ts`, `src/test/dependency.test.ts`
> - `cd7ec00`: **CLI now shows created task file path**  
`backlog task create` and `backlog draft create` now print the absolute path to the newly-created markdown file in both plain and interactive modes. Key files touched: `src/cli.ts`, `src/core/backlog.ts`, `src/file-system/operations.ts`, and tests.
> - `a7c6ac9`: Made `backlog init` idempotent for agent guideline files: running it repeatedly no longer duplicates Backlog.md content. Each file type (CLAUDE.md, AGENTS.md, .cursorrules) now uses unique markers to detect existing guidelines and skips re-appending if present. Key files: src/agent-instructions.ts and its test suite.
> - `d48774c`: Added priority field to tasks with three levels (high/medium/low). CLI now accepts `--priority` flag for create/edit commands, and the board displays color-coded emoji indicators (🔴🟡🟢). Key files: `src/types/index.ts`, `src/cli.ts`, `src/board.ts`.
> - `9f9652f`: Fixed intermittent Windows CI test failures by eliminating race conditions in parallel test execution. Created test utilities that generate unique directories for each test and added Windows-specific retry logic, allowing tests to run reliably in parallel without conflicts.

Key files: `.github/workflows/ci.yml`, `src/test/test-utils.ts`, `src/test/core.test.ts`, `src/test/filesystem.test.ts`, `src/test/cli.test.ts`
> - `ebd6d7f`: Renamed the task ID from `task-95` to `task-96` to resolve a numbering conflict in the backlog.  
Key file: `.backlog/tasks/task-96 - Fix-demoted-task-board-visibility-check-status-across-archive-and-drafts.md`
> - `db36c0c`: Archived a completed test subtask and moved four stale “improve-README” drafts from drafts/ into the archive, cleaning up the backlog.  
Key files: `.backlog/archive/tasks/task-67.1 - test-subtask-with-p-option.md` and the four archived README-improvement drafts.
> - `dee4e69`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `.backlog/tasks/task-95 - Add-priority-field-to-tasks.md`.
> - `f49d571`: Moved task 33 (“cli-export-milestones-board-as-roadmap.md”) from the active backlog into the drafts folder, effectively pausing work on exporting GitHub milestones/boards as a roadmap until it’s ready to be resumed.  
Key file: `.backlog/drafts/task-33 - cli-export-milestones-board-as-roadmap.md`
> - `ec33336`: Added a new backlog item (TASK-95) to introduce a priority field (high/medium/low) for tasks. It will require CLI, UI, docs, and test updates.

Key file: `.backlog/tasks/task-95 - Add-priority-field-to-tasks.md`
> - `f02dd48`: Created TASK-95 to fix the board showing demoted tasks; it will now scan tasks, drafts, and archive folders across every local and remote branch before deciding what to display.  
Key file: `.backlog/tasks/task-95 - Fix-demoted-task-board-visibility-check-status-across-archive-and-drafts.md`
> - `46fae33`: Added a new backlog task to enhance the CLI so it prints the full path of the newly-created task markdown file after `backlog task create`, making the file easy to find or open.  
Key file: `.backlog/tasks/task-94 - cli-show-created-task-file-path.md`
> - `6437222`: Moved all GUI-related backlog tasks from active to draft status, effectively pausing GUI work while the team focuses on CLI and core functionality.  
Key files: `.backlog/drafts/task-{8-14}` (GUI packaging, Kanban, CRUD, intro screens, etc.).
> - `31c6adc`: Added TASK-93 to ensure new backlog tasks always get a globally-unique ID by scanning every branch for the highest existing ID, and to show on the board which branch last touched each task.  
Key file: `.backlog/tasks/task-93 - Cross-branch-task-ID-checking-and-branch-info.md`
