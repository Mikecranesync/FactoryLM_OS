---
title: "Commit Notes - 2025-11-30"
date: 2025-11-30
tags: [project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-11-30

## Backlog.md — Push at 20:56 UTC

**Commits:** 8
**Trigger:** `a9e119c` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `a9e119c` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `cdc5f5a` | TASK-327 - Fix loadTaskById to search remote branches (#442) | Alex Gavrilescu |
| `cfdb26e` | Potential fix for code scanning alert no. 1: Workflow does not contain permissions | Alex Gavrilescu |
| `473e1df` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `4652f0a` | TASK-326 - Add local branch task discovery to board loading (#441) | Alex Gavrilescu |
| `1ee996e` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `ca28457` | TASK-324 - Add browser UI initialization wizard (#440) | Alex Gavrilescu |
| `16dc3a5` | Cleanup tasks | Alex Gavrilescu |

> **What changed:**
> - `a9e119c`: Updated README.md to reflect the latest kanban board snapshot and bumped package.json to v1.25.1; no functional code changes. Key files: README.md, package.json
> - `cdc5f5a`: `task view <id>` now finds tasks in any branch—local or remote—by falling back through local-fs → other local branches → remote branches.  
Adds `findTaskInLocalBranches` and `findTaskInRemoteBranches` helpers that respect the `remoteOperations` and `activeBranchDays` config.  
Key files: `src/core/backlog.ts`, `src/core/task-loader.ts`, `src/test/find-task-in-branches.test.ts`.
> - `cfdb26e`: Added explicit `contents: read` permissions to the Shai-Hulud security-check workflow to satisfy GitHub’s code-scanning alert; the job now runs with minimal privileges instead of the default write-all token.

Key file: `.github/workflows/shai-hulud-check.yml`
> - `473e1df`: Updated README.md and package.json to reflect the latest project status (v1.25.0) and regenerated the Kanban board snapshot, showing TASK-326 (local branch task discovery) now in the Done column. No functional code changes—purely documentation housekeeping.
> - `4652f0a`: Local branch tasks are now visible from any branch; the loader indexes and hydrates tasks in unpushed branches alongside existing remote and current-branch tasks.  
Key files: `src/core/task-loader.ts`, `src/core/remote-tasks.ts`, `src/server/index.ts`, `src/web/lib/api.ts`.
> - `1ee996e`: Updated README.md and package.json to reflect the project’s new v1.24.0 status, moving several tasks from “In Progress” to “Done” (e.g., browser UI init flow, resource-templates fix, search refactor) and refreshing the generated board timestamp. Key files: README.md, package.json
> - `ca28457`: **Summary:**  
Added a browser-based initialization wizard so `backlog browser` works on fresh clones. The server now exposes `/api/status` and `/api/init` endpoints; the React UI detects an uninitialized project and walks the user through project naming, MCP vs CLI mode, client/agent setup, and optional advanced config before auto-loading the task view.

**Key files:**  
`src/server/index.ts`, `src/web/App.tsx`, `src/web/components/InitializationScreen.tsx`, `src/core/init.ts`
> - `16dc3a5`: Closed four backlog items: search refactor, TUI list/detail sync bugfix, resource-templates graceful empty list, and Kanban keyboard navigation—all moved to Done.  
Key files: backlog/tasks/task-273, task-280, task-289 and backlog/completed/task-319.

## cmms — Push at 16:33 UTC

**Commits:** 2
**Trigger:** `5cdfdd0` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5cdfdd0` | feat: barcode in asset form and dueDate columns | Ibrahima G. Coulibaly |
| `55baaf2` | feat: pagination memory | Ibrahima G. Coulibaly |

> **What changed:**
> - `5cdfdd0`: Added barcode field to asset create/edit forms and a due-date column to the Requests grid.  
Key files: `frontend/src/content/own/Assets/index.tsx`, `frontend/src/content/own/Assets/Show/index.tsx`, `frontend/src/content/own/Requests/index.tsx`
> - `55baaf2`: Added pagination memory to the grid state persistence hook. Pagination (page size & current page) is now restored from localStorage alongside column order and sorting, but only once per session to avoid loops.  
Key file: frontend/src/hooks/useGridStatePersist.ts
