---
title: "Commit Notes - 2026-01-20"
date: 2026-01-20
tags: [project/IndustrialSkillsHub, type/commit-note, project/My-Ralph, project/RideView, project/Rivet-PRO]
status: active
---

# Commit Notes - 2026-01-20

## IndustrialSkillsHub — Push at 19:26 UTC

**Commits:** 1
**Trigger:** `ada0644` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `ada0644` | feat: Add Capacitor for iOS and Android mobile apps (#1) | Mikecranesync |

> **What changed:** Added Capacitor to wrap the Next.js Industrial Skills Hub as native iOS/Android apps. Configured Next.js for static export and disabled image optimization for WebView compatibility. Key files: capacitor.config.ts, next.config.mjs, MOBILE.md, plus full iOS/Android native projects.

## IndustrialSkillsHub — Push at 15:50 UTC

**Commits:** 1
**Trigger:** `b249ce3` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b249ce3` | feat: MVP release - fix lesson unlocking and add debug tooling | Mikecranesync |

> **What changed:** Fixed lesson unlocking by seeding 125 challenges across lessons, enabling the core learning flow where users can progress through lessons by completing challenges. Added a debug script to inspect database state and track progression issues.

**Files modified:** `components/banner.tsx`, `scripts/debug-db.ts`

## IndustrialSkillsHub — Push at 15:12 UTC

**Commits:** 1
**Trigger:** `1bc8284` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1bc8284` | fix: Support track-based progress in getCourseProgress | hharp |

> **What changed:** fix: Support track-based progress in getCourseProgress. Files: `db/queries.ts`, `screenshots/homepage-new.png`, `screenshots/learn-page.png`.

## IndustrialSkillsHub — Push at 14:59 UTC

**Commits:** 1
**Trigger:** `9756e4f` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `9756e4f` | fix: Update banner to welcome message instead of Clerk limit warning | hharp |

> **What changed:** fix: Update banner to welcome message instead of Clerk limit warning. Files: `components/banner.tsx`, `homepage-screenshot.png`, `package-lock.json`, `package.json`, `run-test.cjs` (+8 more).

## IndustrialSkillsHub — Push at 14:44 UTC

**Commits:** 1
**Trigger:** `49de837` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `49de837` | feat: Add complete Electrician L1 curriculum - 125 challenges | hharp |

> **What changed:** feat: Add complete Electrician L1 curriculum - 125 challenges. Files: `scripts/seed-industrial.ts`.

## IndustrialSkillsHub — Push at 13:22 UTC

**Commits:** 1
**Trigger:** `1e2f1b0` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1e2f1b0` | fix: Add newlines between import groups for ESLint compliance | hharp |

> **What changed:** fix: Add newlines between import groups for ESLint compliance. Files: `app/(main)/glossary/list.tsx`, `app/(main)/leaderboard/leaderboard-tabs.tsx`, `app/onboarding/page.tsx`, `app/onboarding/role-selection.tsx`.

## IndustrialSkillsHub — Push at 13:13 UTC

**Commits:** 1
**Trigger:** `e608636` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `e608636` | fix: Resolve ESLint import ordering and unused variable errors | hharp |

> **What changed:** fix: Resolve ESLint import ordering and unused variable errors. Files: `app/(main)/glossary/list.tsx`, `app/(main)/leaderboard/leaderboard-tabs.tsx`, `app/(main)/leaderboard/page.tsx`, `app/(main)/learn/page.tsx`, `app/(marketing)/page.tsx` (+7 more).

## IndustrialSkillsHub — Push at 12:50 UTC

**Commits:** 1
**Trigger:** `d15b5ce` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `d15b5ce` | feat: Rebrand to Industrial Skills Hub | hharp |

> **What changed:** feat: Rebrand to Industrial Skills Hub. Files: `.gitignore`, `app/(marketing)/footer.tsx`, `app/(marketing)/header.tsx`, `app/(marketing)/page.tsx`, `app/layout.tsx` (+5 more).

## IndustrialSkillsHub — Push at 11:53 UTC

**Commits:** 1
**Trigger:** `61f6cd6` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `61f6cd6` | feat: Add gamification and role-specific features | hharp |

> **What changed:** feat: Add gamification and role-specific features. Files: `.env.example`, `actions/challenge-progress.ts`, `app/(main)/glossary/list.tsx`, `app/(main)/glossary/page.tsx`, `app/(main)/leaderboard/leaderboard-tabs.tsx` (+8 more).

## IndustrialSkillsHub — Push at 11:33 UTC

**Commits:** 1
**Trigger:** `80df9ac` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `80df9ac` | feat: Industrial Skills Hub - Duolingo for industrial maintenance | hharp |

> **What changed:** feat: Industrial Skills Hub - Duolingo for industrial maintenance. Files: `.env.example`, `.eslintrc.js`, `.github/FUNDING.yml`, `.github/ISSUE_TEMPLATE/bug_report.md`, `.github/ISSUE_TEMPLATE/feature_request.md` (+168 more).

## My-Ralph — Push at 21:34 UTC

**Commits:** 1
**Trigger:** `558b151` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `558b151` | fix(tests): add --version handler to mock claude commands in prd_import tests | Marty |

> **What changed:** Fixed hanging test suite by adding `--version` flag handling to all 7 mock `claude` commands in the prd_import tests. The mocks were blocking on `cat > /dev/null` waiting for stdin when `ralph_import.sh` checked dependencies, causing test 240 to hang indefinitely.

**Key file:** tests/integration/test_prd_import.bats

## RideView — Push at 14:46 UTC

**Commits:** 22
**Trigger:** `f7adea2` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `f7adea2` | fix: Create Kivy Window early on Android to prevent surface race condition | Mikecranesync |
| `5294c44` | fix: Add presplash, wakelock, and GL backend config for Android startup | Mikecranesync |
| `4c459de` | fix: Add Android lifecycle handlers and Kivy pause config | Mikecranesync |
| `6c4b295` | fix: Move Camera4Kivy import to on_start() to avoid Window deadlock | Mikecranesync |
| `14aa728` | fix: Skip Window import on Android to prevent initialization deadlock | Mikecranesync |
| `7c5a502` | debug: Add granular Window import logging to diagnose Android hang | Mikecranesync |
| `fa301f9` | fix: Defer Camera4Kivy import until after Kivy Window is created | Mikecranesync |
| `c472e09` | fix: Add gestures4kivy dependency for Camera4Kivy | Mikecranesync |
| `c0860ba` | debug: Show Camera4Kivy import error details | Mikecranesync |
| `f66680b` | feat: Integrate Camera4Kivy for Android camera support | Mikecranesync |
| `ad44565` | style: Fix lint errors in app.py | Mikecranesync |
| `176c005` | fix: Skip Window.size on Android to prevent GL context blocking | Mikecranesync |
| `aa01af0` | debug: Add multiple output channels for Android logcat debugging | Mikecranesync |
| `d49f975` | debug: Add granular Window import debugging to diagnose Android blocking issue | Mikecranesync |
| `3ea82e1` | fix: Add multiple camera texture capture methods for Android | Mikecranesync |
| `8cef987` | fix: Defer MainScreen import to build() to avoid Texture cascade | Mikecranesync |
| `2e6d1a6` | fix: Defer Window and Camera imports to avoid Android hang | Mikecranesync |
| `46cb725` | debug: Use stderr for debug logging (works on Android logcat) | Mikecranesync |
| `6319726` | debug: Use Android native Log API for debug messages | Mikecranesync |
| `88016b0` | debug: Add print statements to trace Android app hang location | Mikecranesync |
| `8e2a55c` | fix: Use Kivy Camera widget for Android instead of OpenCV VideoCapture | Mikecranesync |
| `1ca9cf6` | fix: Add Android runtime camera permission request | Mikecranesync |

> **What changed:**
> - `f7adea2`: Fixed Android startup freeze by creating Kivy Window before slow imports; now Window exists when SDL surface is destroyed during 6s Python init and can pause/resume correctly.  
Key file: main.py
> - `5294c44`: Fixed Android startup race condition by adding presplash screen, WAKE_LOCK permission, and explicit GL backend config to prevent SDL surface destruction during slow Python initialization. Key files: buildozer.spec, src/rideview/mobile/app.py
> - `4c459de`: Fixed Android app crashes during slow startup by adding lifecycle handlers and GL optimizations. Set KIVY_PAUSE_ON_MINIMIZE=0 and disabled multisampling to prevent GL context blocking when surface is destroyed. Added on_pause() returning True and on_resume() for proper pause/resume handling.

Key files: src/rideview/mobile/app.py
> - `6c4b295`: Fixed Android startup deadlock by deferring Camera4Kivy import from `build()` to `on_start()`. On Android, `build()` now returns a simple loading placeholder; the full UI with camera is constructed after the Window is ready. Desktop behavior unchanged.

**Key file:** `src/rideview/mobile/app.py`
> - `14aa728`: Fixed Android startup deadlock by conditionally importing Kivy's Window module only on desktop, since Android doesn't need window sizing and the import was causing initialization hangs. Moved Camera4Kivy import to Android-only path for cleaner separation.

**Key files:** src/rideview/mobile/app.py
> - `7c5a502`: Added detailed debug logging around Window import to diagnose Android initialization hangs. The logging now tracks the exact point between module import and Window object access where the hang occurs.

**Key file:** src/rideview/mobile/app.py
> - `fa301f9`: Fixed Android startup hang by deferring Camera4Kivy import until after Kivy's Window exists; moved the import from module-level into `build()` and stored the class in a module-global for later use.

Key file: src/rideview/mobile/app.py
> - `c472e09`: Fixed missing dependency causing Camera4Kivy import failures on Android by adding gestures4kivy to buildozer.spec requirements. This resolves the "No module named 'gestures4kivy'" runtime error.

Key file: buildozer.spec
> - `c0860ba`: Enhanced error reporting for Camera4Kivy import failures to aid debugging. Now captures and displays specific exception details when the camera library fails to load, distinguishing between ImportError and other exception types. Modified: src/rideview/mobile/app.py
> - `f66680b`: Replaced the deprecated Kivy Camera widget with Camera4Kivy on Android, enabling the app to use the modern CameraX API. This fixes the broken camera on Android by making the camera Preview the root widget and offloading frame processing to a background thread.

Key files: buildozer.spec, app.py, opencv_analyzer.py, camera_provider.py, main_screen.py
> - `ad44565`: Fixed lint errors in the mobile app entry point: added `# noqa` to suppress the intentional import-after-debug rule, reordered imports to satisfy the linter, and added a type hint to `_debug`.  
Key file: `src/rideview/mobile/app.py`
> - `176c005`: Fixed Android startup freeze by skipping Window.size assignment on mobile; desktop still gets 1280×720. Key file: src/rideview/mobile/app.py
> - `aa01af0`: Made Android logcat debugging more reliable by echoing debug messages to stdout, stderr, and Python’s print() so at least one channel reaches logcat.  
Key file: src/rideview/mobile/app.py
> - `d49f975`: Added step-by-step debug logging around the lazy Window import to pinpoint where the Android startup hang occurs. If the import fails, the traceback is now printed before re-raising the exception.

Key file: src/rideview/mobile/app.py
> - `3ea82e1`: Fixed unreliable camera texture capture on Android by adding fallback methods: binding to texture property changes and 30fps polling. Refactored texture processing into a shared method with debug logging to track which mechanism works.

**Key file:** src/rideview/mobile/app.py
> - `8cef987`: Fixed Android startup freeze by deferring MainScreen import until inside `build()`; this prevents Kivy's Texture→Window cascade from triggering too early.  
Key file: `src/rideview/mobile/app.py`
> - `2e6d1a6`: Fixed Android startup freeze by deferring Window and Camera imports from module-level to inside build() and _setup_kivy_camera(); added debug logging to trace execution.  
Key file: src/rideview/mobile/app.py
> - `46cb725`: Switched debug logging from Android JNI to stderr (which auto-appears in logcat) to avoid import-time jnius failures.  
Key files: main.py, src/rideview/mobile/app.py
> - `6319726`: Switched debug logging from Python print() to Android's native Log API via pyjnius, ensuring debug messages reliably appear in logcat under the 'RideView' tag. This fixes the issue where debug output was often missing from Android logs.

Key file: src/rideview/mobile/app.py
> - `88016b0`: Added immediate-flush debug prints to trace where the Android build stalls (black screen after Kivy init). Key checkpoints: module imports, RideViewApp.__init__, and every component creation inside build().  
Files: src/rideview/mobile/app.py
> - `8e2a55c`: Fixed Android camera access by replacing OpenCV VideoCapture with Kivy's native Camera widget. On Android, the app now creates a Kivy Camera widget that properly interfaces with Android's camera APIs, converts its RGBA textures to BGR numpy arrays for OpenCV processing, and continues using OpenCV VideoCapture on desktop where it works fine.

**Key files:** `src/rideview/mobile/app.py`, `src/rideview/mobile/camera_provider.py`
> - `1ca9cf6`: Fixed Android camera permission handling by adding runtime permission requests. The app now properly requests CAMERA permission on Android 6+ devices before attempting to open the camera, preventing the issue where the camera wouldn't turn on.

**Key file:** `src/rideview/mobile/app.py`

## Rivet-PRO — Push at 08:11 UTC

**Commits:** 15
**Trigger:** `320927a` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `320927a` | fix: Update test to check for actual processor methods | Mike Crane |
| `739ec4f` | feat: PRINTS-010 - Add deployment scripts and checklist | Mike Crane |
| `f70e77b` | docs: PRINTS-009 - Add electrical prints documentation | Mike Crane |
| `a5ed927` | test: PRINTS-008 - Add electrical prints test suite | Mike Crane |
| `61ffa97` | feat: PRINTS-006 - Add Telegram print handlers | Mike Crane |
| `317b494` | feat: PRINTS-007 - Add dependencies and configuration | Mike Crane |
| `000fadd` | feat: PRINTS-005 - Ralph print learning analyzer | Mike Crane |
| `f24bd68` | feat: PRINTS-003/004 - FieldInteractionHandler and MultimodalFusionEngine | Mike Crane |
| `9b58ef4` | feat: PRINTS-002 - Create ElectricalPrintProcessor service | Mike Crane |
| `9c4664c` | feat: PRINTS-001 - Create electrical prints database tables | Mike Crane |
| `1f681ef` | feat: Add batch PDF ingestion script with verified manufacturer URLs | Mike Crane |
| `5c0d996` | fix: KB worker database schema compatibility | Mike Crane |
| `31c6b1c` | feat: Switch to FREE local embeddings (sentence-transformers) | Mike Crane |
| `dbdd6b7` | fix(ralph): Add --dangerously-skip-permissions for autonomous operation | Mike Crane |
| `7ebf45c` | feat(KB-001-006): Phase 1 KB Ingestion Pipeline - Claude Direct Implementation | Mike Crane |

> **What changed:**
> - `320927a`: Fixed a broken test that was checking for a non-existent `component_patterns` attribute on the ElectricalPrintProcessor. Updated it to verify the processor has the actual required methods `process_print` and `query_print`.

**Key file:** tests/test_electrical_prints.py
> - `739ec4f`: Added a complete deployment system for the Electrical Prints feature, including a comprehensive deployment guide with pre-deployment checklist, system dependencies, and step-by-step procedures, plus a Python deployment script with migration, verification, and status checking capabilities. Key files: docs/ELECTRICAL_PRINTS_DEPLOYMENT.md and scripts/deploy_electrical_prints.py.
> - `f70e77b`: Added comprehensive documentation for the electrical prints feature, including a user guide covering schematic uploads, command usage (/prints, /worked, /didntwork), and troubleshooting workflows, plus detailed API docs for the ElectricalPrintProcessor, FieldInteractionHandler, MultimodalFusionEngine, and RalphPrintAnalyzer services. Key files: docs/ELECTRICAL_PRINTS_USER_GUIDE.md, docs/ELECTRICAL_PRINTS_API.md
> - `a5ed927`: Added comprehensive test suite for the electrical prints feature (PRINTS-008) covering PDF/image processing, multi-modal troubleshooting, diagnosis fusion, and Telegram handlers. The 814-line test file includes unit tests with mocked dependencies, integration scenarios, and edge cases like large files and malformed JSON.

Key file: tests/test_electrical_prints.py
> - `61ffa97`: Added Telegram handlers for electrical print processing: upload PDF/image schematics, ask questions about them, combine photo + print + text for troubleshooting, and give feedback. Key file: rivet_pro/adapters/telegram/print_handlers.py.
> - `317b494`: Added PDF and OCR processing capabilities for electrical schematic analysis. Configured environment variables for vision model selection, file size limits, temp storage, and Tesseract path. Key files: pyproject.toml, .env.example
> - `000fadd`: Added RalphPrintAnalyzer to learn from electrical print interactions—tracking which document types and components users query most, measuring Q&A helpfulness, detecting equipment-to-print patterns, and auto-generating daily improvement proposals for the KB. Key file: rivet_pro/core/llm/ralph_print_analyzer.py
> - `f24bd68`: Added two core services for multi-modal troubleshooting: FieldInteractionHandler orchestrates photo + print + text inputs to generate unified diagnoses, while MultimodalFusionEngine fuses all sources using Claude's chain-of-thought reasoning with confidence scoring and escalation logic. Both services store interactions for continuous learning.

Key files: `rivet_pro/core/services/field_interaction_handler.py`, `rivet_pro/core/services/multimodal_fusion_engine.py`
> - `9b58ef4`: Created ElectricalPrintProcessor service that analyzes electrical schematics (PDF/JPG/PNG) using Claude Vision to extract components, circuit sections, and voltage points. The service stores analysis results in the database and provides a Q&A interface for querying prints.

Key files: `rivet_pro/core/services/electrical_print_processor.py`
> - `9c4664c`: Created database schema for electrical prints system with four new tables: electrical_prints (stores processed schematics as JSONB), print_qa_history (tracks Q&A interactions), print_embeddings (384-dim vectors for similarity search), and user_interactions (field troubleshooting sessions). Added comprehensive implementation plan and Ralph learning stories to support the multi-modal diagnostic system.
> - `1f681ef`: Added a direct PDF ingestion script that bypasses Redis and pulls verified manuals from Siemens, Rockwell, and ABB. Successfully loaded 159 new atoms (now 206 total) at zero embedding cost.

Key file: scripts/ingest_pdfs.py
> - `5c0d996`: Fixed KB ingestion worker database compatibility issues: corrected AtlasDatabase parameter name from `connection_string` to `db_url`, updated knowledge_service INSERT to match the evolved schema with new fields (atom_id, atom_type, summary, etc.), and handled pydantic enum serialization. Key files: rivet/services/knowledge_service.py, rivet_pro/workers/kb_ingestion_worker.py.
> - `31c6b1c`: Switched from paid OpenAI embeddings to free local sentence-transformers (all-MiniLM-L6-v2, 384 dims) by default, cutting embedding costs to $0. Backend is toggled via `EMBEDDING_BACKEND=local|openai`; added migration and regenerate script to handle the dimension change from 1536 → 384.

Key files: `embedding_service.py`, `local_embedding_service.py`, `033_local_embeddings.sql`, `regenerate_embeddings.py`
> - `dbdd6b7`: **Summary:** Added `--dangerously-skip-permissions` flag to Ralph's Claude CLI invocation to enable autonomous file creation/editing without interactive permission prompts. This fixes Ralph's failure when using claude --print mode for implementing stories.

**Key files modified:** `scripts/ralph/ralph_local.py`
> - `7ebf45c`: **Summary:**  
Implemented a production-grade KB ingestion pipeline that downloads PDFs, extracts text, chunks it into 400–512-token segments, generates OpenAI embeddings with cost tracking, and converts them into searchable “knowledge atoms” with extracted manufacturer/model metadata. The full workflow is orchestrated by `kb_ingestion_worker.py`.

**Key files:**  
`kb_ingestion_worker.py`, `pdf_downloader.py`, `text_extractor.py`, `recursive_chunker.py`, `batch_embeddings.py`, `atom_creator.py`
