---
title: "Commit Notes - 2025-07-16"
date: 2025-07-16
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-07-16

## Backlog.md — Push at 21:27 UTC

**Commits:** 6
**Trigger:** `c91875a` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c91875a` | docs: update README with latest board status [skip ci] | MrLesk |
| `6278b41` | TASK-171 - Implement drafts list functionality in CLI and web UI (#225) | Alex Gavrilescu |
| `95152c2` | docs: update README with latest board status [skip ci] | MrLesk |
| `b41a1eb` | TASK-194 - Add graceful port handling for browser command (#224) | Alex Gavrilescu |
| `e6df641` | docs: update README with latest board status [skip ci] | MrLesk |
| `0648deb` | TASK-195 - Fix React Router deep linking for documentation and decisions pages (#223) | Alex Gavrilescu |

> **What changed:**
> - `c91875a`: README.md refreshed to show task-171 (drafts list) moved from “To Do” to “Done” and the board timestamp bumped to 21:27. No code changes—just a status snapshot.
> - `6278b41`: **Draft management system completed** – Added full CLI and web UI support for viewing and promoting drafts. New `draft list` command and `/api/drafts` endpoint read directly from `backlog/drafts/` folder, while `/api/drafts/:id/promote` moves drafts to `backlog/tasks/` and removes them from the drafts list.  
Key files: `src/cli.ts`, `src/server/index.ts`, `src/web/components/DraftsList.tsx`, `src/web/App.tsx`
> - `95152c2`: README.md refreshed its auto-generated Kanban snapshot: task-194 (graceful port handling) moved to Done and is now marked complete, while the timestamp bumped to 18:33.
> - `b41a1eb`: Added graceful port conflict handling to the browser command: when the default port is already in use, the server now displays a clear error message with platform-specific suggestions (lsof/netstat) and exits cleanly instead of dumping a stack trace. Users can override the port via `--port`.

Key files: `src/server/index.ts`
> - `e6df641`: README.md refreshed the auto-generated Kanban snapshot: new task-194 (graceful port handling) added to To Do, task-195 (React Router deep-link fix) moved to Done, and timestamps updated.
> - `0648deb`: Fixed React Router deep linking for documentation and decisions pages by removing race condition that prevented content loading when parent data wasn't ready. Added URL sanitization to convert messy titles like "My Awesome Title!" into clean URLs like "my-awesome-title".

Key files: DocumentationDetail.tsx, DecisionDetail.tsx, SideNavigation.tsx, urlHelpers.ts
