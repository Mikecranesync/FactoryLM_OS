---
title: "Commit Notes - 2025-07-06"
date: 2025-07-06
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-07-06

## Backlog.md — Push at 23:14 UTC

**Commits:** 14
**Trigger:** `e45034c` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `e45034c` | Create video-presentation.md | Alex Gavrilescu |
| `92897bf` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `e7930b0` | Add backlog presentation video | Alex Gavrilescu |
| `e1c6073` | Fix lint issues (#159) | Alex Gavrilescu |
| `b21335f` | Fix typo | Alex Gavrilescu |
| `c2f0405` | Update readme.md | Alex Gavrilescu |
| `3324e6a` | Add task-119 - Add documentation and decisions pages to web UI | Alex Gavrilescu |
| `3b365c8` | Add task-118 - Add side navigation menu to web UI | Alex Gavrilescu |
| `7b4fdfb` | Add task-117 - Make backlog tasks alias for task list | Alex Gavrilescu |
| `677ff49` | Tidying tasks | Alex Gavrilescu |
| `e3bee8d` | fix: move blessed back to dependencies | Alex Gavrilescu |
| `7c0939b` | Add neo-neo-bblessed as dev dependency | Alex Gavrilescu |
| `7e9c72d` | Fix failing unit tests on windows CI (#156) | Alex Gavrilescu |
| `c1f5baf` | TASK-100 - Add embedded web server to Backlog CLI (#157) | Alex Gavrilescu |

> **What changed:**
> - `e45034c`: Added a new GitHub doc that embeds a single video (user-attachments URL) for project presentation purposes.  
Key file: `.github/video-presentation.md`
> - `92897bf`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `src/test/test-helpers.ts`.
> - `e7930b0`: Added a backlog presentation video to the repo under `.github/backlog.mp4` to visually document or share the project's backlog overview. This likely serves as a reference or onboarding asset for contributors.
> - `e1c6073`: Fixed a TypeScript strict-null lint error in the task-filtering helper by replacing the non-null assertion (`!`) with optional chaining (`?.`) and a nullish-coalescing fallback (`?? ""`).  

Key file: `src/test/test-helpers.ts`
> - `b21335f`: Fixed a typo in the README: corrected the package name from `Backlog.me` to `Backlog.md` in the quick-start example.  
Key file: readme.md
> - `c2f0405`: Updated the README to clarify that the Backlog.me CLI must be installed globally before running `backlog init`, and fixed a typo in the init example.  
Key file: readme.md
> - `3324e6a`: Added task-119 to build dedicated Documentation and Decisions pages in the web UI, letting users browse, render, and edit markdown files from the `docs/` and `decisions/` folders with a shared markdown editor and new REST endpoints.

Key file: backlog/tasks/task-119 - Add-documentation-and-decisions-pages-to-web-UI.md
> - `3b365c8`: Created backlog task-118 to build a collapsible side-nav for the web UI, splitting the single-page Kanban into a multi-section app (Kanban, Documentation, Decisions) with persistent state and mobile responsiveness. Key file: backlog/tasks/task-118 - Add-side-navigation-menu-to-web-UI.md
> - `7b4fdfb`: Created task-117 to remap the CLI alias `backlog tasks` from pointing to the generic `task` command to the more specific `backlog task list`, giving users a quicker way to view their backlog.  
Key file: backlog/tasks/task-117 - Make-backlog-tasks-alias-for-task-list.md
> - `677ff49`: Archived duplicate task-117 and merged its requirements into task-101, which now governs showing absolute file paths in `backlog task/draft <id> --plain` for AI/automation use. Marked task-105 (remove dot from .backlog folder) as Done.  
Key files: backlog/tasks/task-101, backlog/archive/tasks/task-117, backlog/tasks/task-105
> - `e3bee8d`: Moved the terminal UI library `blessed` from devDependencies to dependencies so it’s available at runtime when users run the CLI.  
Key file: package.json
> - `7c0939b`: Moved the `blessed` package (aliased to `neo-neo-bblessed@1.0.3`) from runtime `dependencies` to `devDependencies`, eliminating it from the production bundle.  
Key files: `package.json`, `bun.lock`.
> - `7e9c72d`: Fixed Windows CI test failures by increasing timeout and addressing Bun memory/encoding issues. Key changes: extended test timeout to 15s, normalized Git paths to forward slashes, enforced UTF-8 env vars, and replaced Unix-only `mkdir` with cross-platform `fs.mkdir`. Modified files: package.json, readme.md, src/git/operations.ts, and 11 test files.
> - `c1f5baf`: Added an embedded web server to the Backlog CLI; `backlog browser` now spins up a local Kanban board where users can create, edit, and archive tasks without leaving the browser. The React/Tailwind UI is bundled into the single executable, so no extra runtime deps are required. Key files touched: src/cli.ts (new command), src/server/index.ts (HTTP server), src/web/* (React frontend), package.json (build wiring).
