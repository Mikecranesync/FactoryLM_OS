---
title: "Commit Notes - 2025-12-28"
date: 2025-12-28
tags: [project/Agent-Factory, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-28

## Agent-Factory — Push at 23:35 UTC

**Commits:** 10
**Trigger:** `97a17ac` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `97a17ac` | feat(rivet-pro): Phase 2/8 - Complete RAG layer | Mikecranesync |
| `0297908` | chore: Session cleanup - Phoenix integration complete | Mikecranesync |
| `27a6ed1` | Merge phoenix/production-integration into main | Mikecranesync |
| `2c4e5d6` | feat(phoenix): TAB 1 Infrastructure - Database investigation & golden dataset | Mikecranesync |
| `20d5d2d` | feat(phoenix): production integration with CI gate | Mikecranesync |
| `945976c` | feat(phoenix): TAB 2 COMPLETE - Full eval pipeline validated | Mikecranesync |
| `b3dbb58` | PHASE 4 COMPLETE: Fix landing page CEO bot references | Mikecranesync |
| `68e6ce1` | PHASE 3 COMPLETE: Configure public CMMS bot for beta | Mikecranesync |
| `d29d521` | PHASE 2 COMPLETE: Enhanced CEO bot with admin commands | Mikecranesync |
| `3de9982` | fix: resolve test import hang with lazy imports + env parsing | Mikecranesync |

> **What changed:**
> - `97a17ac`: **RAG layer complete** – added hybrid search (`search_docs`) and KB coverage estimation (`estimate_coverage`) with agent-specific configs for Siemens, Rockwell, Generic PLC and Safety agents.  
Key files: `agent_factory/rivet_pro/rag/retriever.py`, `config.py`, `filters.py` plus full test suite (6/6 passing).
> - `0297908`: Phoenix observability is now fully wired into the KB gap-detection loop: traces automatically spawn weakness tickets, bump priority on repeats, and trigger auto-research.  
Added DB schema and async logger to persist these signals, plus monitors to surface them in Rivet.  

Key files: agent_factory/core/kb_gap_logger.py, migrations/004_kb_weakness_tracking.sql, agent_factory/observability/phoenix_trace_analyzer.py
> - `27a6ed1`: Merged the production-integration branch, adding full Phoenix tracing with OpenTelemetry, an orchestrator that auto-instruments agent runs, and a GitHub Actions eval gate that blocks PRs unless local + CI tests pass. Key files: `.github/workflows/eval_gate.yml`, `phoenix_integration/phoenix_tracer.py`, `agent_factory/orchestrators/rivet_orchestrator.py`, plus the local test script and eval runners.
> - `2c4e5d6`: **Summary:**  
Switched from broken PostgreSQL (IPv6 DNS) to Supabase REST API and exported 55 real KB records, but their fault content is 71 % UNKNOWN codes, so we created a 50-case synthetic dataset with full repair steps for reliable Phoenix eval testing.  

**Key files:**  
scripts/export_golden_dataset.py, datasets/golden_dataset.jsonl, datasets/golden_from_supabase.jsonl
> - `20d5d2d`: **Summary**  
Production-ready Phoenix tracing and CI gate now block merges: every PR touching agents triggers a 4-stage eval pipeline (live tracer tests → orchestrator integration → GitHub gate → local pass/fail). Key fixes span OTel 4.x compatibility and manufacturer-specific routing (Siemens S7-1200 F47 → siemens route, 0.7 conf). All 4 evals pass thresholds; gate is green for Tab 1 & 2 integration.

**Files touched**  
`.github/workflows/eval_gate.yml`, `agent_factory/orchestrators/rivet_orchestrator.py`, `phoenix_integration/phoenix_tracer.py`, `evals/check_results.py`, `datasets/golden_from_neon.jsonl`
> - `945976c`: **Summary:**  
Completed the full Phoenix evaluation pipeline for industrial-maintenance agents. Added 7 judge templates (including retrieval_quality and response_completeness), a real RivetOrchestrator integration with Groq/OpenAI fallback, parallel eval execution with progress bars, and automated Markdown report generation. All LLM calls are traced via Phoenix; the pipeline is validated end-to-end with a mock golden dataset and is ready to ingest the real Neon DB dataset.

**Key files:**  
phoenix_integration/evals/judges.py, phoenix_integration/evals/run_eval.py, phoenix_integration/evals/generate_report.py, phoenix_integration/quick_eval.py, datasets/mock_golden_for_testing.jsonl
> - `b3dbb58`: **Landing pages now route users to the public CMMS bot**  
All references to the private CEO bot (@RivetCEO_bot) were replaced with the public CMMS bot (@RivetCMMS_bot) across pricing, success, and how-it-works pages; an env var `NEXT_PUBLIC_TELEGRAM_BOT_URL` was added for centralized control.  
**Files touched:** `pricing/page.tsx`, `success/page.tsx`, `components/HowItWorks.tsx`
> - `68e6ce1`: **Beta launch complete**: CMMS bot now runs in unlimited beta mode—rate limits disabled, all Pro features unlocked for every user. Welcome messages show a prominent beta banner listing unlimited troubleshooting, equipment tracking, and manual-library access.

**Key files**: `rivet_public_bot.py`, `public_auth.py`, `onboarding_manager.py`
> - `d29d521`: **Phase 2 CEO-bot upgrade:** grafted the full admin command suite from the archived telegram_bot.py onto the orchestrator.  
40+ new Telegram commands now expose the Admin Panel, Agent Manager, Content Review, GitHub Actions, Analytics, System Control, SCAFFOLD and LangGraph tools—everything the CEO needs to monitor and steer the factory without leaving the chat.

Key file: `agent_factory/integrations/telegram/orchestrator_bot.py`
> - `3de9982`: Fixed a 2-hour test-blocking import hang by lazy-loading langchain_anthropic components (ChatAnthropic, HumanMessage, SystemMessage) inside IntentDetector methods instead of at module level. Also corrected .env parsing syntax and a test dependency order (create user before machine).

Key files: agent_factory/integrations/telegram/public_auth.py, tests/test_backend.py, plus migration and deploy configs under migrations/public_launch/.

## cmms — Push at 19:39 UTC

**Commits:** 2
**Trigger:** `069eb87` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `069eb87` | feat: pm WO list | Ibrahima G. Coulibaly |
| `1e73d57` | fix: part consumption | Ibrahima G. Coulibaly |

> **What changed:**
> - `069eb87`: Added a “Recent Work Orders” tab to the PM detail screen.  
Backend exposes `GET /preventive-maintenances/{id}/recent-work-orders` returning the last 10 WO mini-DTOs (now with status & createdAt); frontend renders them in a new `<RecentWorkOrders />` component inside a tabbed layout.  

Key files: `PreventiveMaintenanceController.java`, `PMDetails.tsx`, `RecentWorkOrders.tsx`, `WorkOrderMiniDTO.java`
> - `1e73d57`: Fixed part-consumption logic so inventory can’t go negative and low-stock alerts fire at the right time. Moved the “not enough stock” check to run immediately after decrementing quantity, eliminating a double-check that could block valid consumption.

Key file: `PartService.java`
