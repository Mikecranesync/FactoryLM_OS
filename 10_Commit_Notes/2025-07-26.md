---
title: "Commit Notes - 2025-07-26"
date: 2025-07-26
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-07-26

## Backlog.md — Push at 20:33 UTC

**Commits:** 15
**Trigger:** `15b3e56` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `15b3e56` | Create task-210 - Refactor-Nix-packaging-to-build-node_modules-offline.md | Alex Gavrilescu |
| `464a385` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `9dd0eb7` | TASK-209 - Fix cleanup command to handle non-standard task filenames (#258) | Alex Gavrilescu |
| `98a8aed` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `02bbb53` | TASK-207 - Reorganize backlog init command questions order (#257) | Alex Gavrilescu |
| `b8ef9ce` | Create task-208 - Add-paste-as-markdown-support-in-Web-UI.md | Alex Gavrilescu |
| `ef22d31` | Create task-207 - Reorganize-backlog-init-command-questions-order.md | Alex Gavrilescu |
| `5dec085` | Update agent instructions | Alex Gavrilescu |
| `78dd923` | Create task-206 - Order-done-column-by-updatedDate-in-board-export.md | Alex Gavrilescu |
| `d9171ef` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `98ed669` | TASK-180 - Add statistics overview command to CLI with TUI interface (#253) | Alex Gavrilescu |
| `e2b5791` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `b914835` | TASK-201 - Add configurable git hooks bypass option (#252) | Alex Gavrilescu |
| `0a9a5c7` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `0103e9e` | TASK-203 - Show meaningful description in web kanban (#251) | Alex Gavrilescu |

> **What changed:**
> - `15b3e56`: Created task-210 to fix Nix builds failing offline by moving node_modules into a separate, hash-locked derivation. Goal: make `nix build/run` work without network access.

Key file: backlog/tasks/task-210 - Refactor-Nix-packaging-to-build-node_modules-offline.md
> - `464a385`: Bumped version to 1.6.2 and refreshed the auto-generated project-status table in the README with the latest counts and timestamp.  
Key files: README.md, package.json
> - `9dd0eb7`: Fixed the cleanup command to handle both standard (`task-ID - Title.md`) and non-standard (`task-ID-title.md`) task filenames. Updated `getTaskPath`, `getDraftPath`, and `getTaskFilename` in `src/utils/task-path.ts` to look for both patterns, resolving failures when processing tasks like task-196.
> - `98a8aed`: Updated README board snapshot to v1.6.1 with current task status—several items moved to Done (cleanup fixes, init UX tweaks, export sorting) and new issues added (paste-as-markdown, board export sorting).  
Key files: README.md, package.json
> - `02bbb53`: Reordered the backlog init command prompts for better UX flow, moving cross-branch checking to the top and removing the auto-commit question entirely. Key files: src/cli.ts (restructured prompt sequence), backlog/config.yml (updated defaults), and the task markdown file (marked complete).
> - `b8ef9ce`: Created new backlog task to add “paste-as-markdown” support in the Web UI, enabling automatic conversion of rich-text (Word, Docs, web) to clean markdown in task and document editors.  
Key file: backlog/tasks/task-208 - Add-paste-as-markdown-support-in-Web-UI.md
> - `ef22d31`: Created task-207 to reorder the backlog init CLI prompts for better UX—removes the auto-git-commit question from the main flow and nests cross-branch/zero-padding options. Key file: backlog/tasks/task-207 - Reorganize-backlog-init-command-questions-order.md
> - `5dec085`: Updated agent instructions to specify that task titles should be "one liner" format. Modified both the project manager backlog documentation and general agent guidelines to clarify title requirements. Key files: `.claude/agents/project-manager-backlog.md`, `src/guidelines/agent-guidelines.md`
> - `78dd923`: Created new task to sort the “Done” column in board exports by updatedDate (newest first), falling back to ID for ties.  
Key file: backlog/tasks/task-206 - Order-done-column-by-updatedDate-in-board-export.md
> - `d9171ef`: Updated README to reflect v1.6.0 release and refreshed the auto-generated Kanban board snapshot; no functional code changes.  
Key files: README.md, package.json
> - `98ed669`: Added a new `backlog overview` command that launches an interactive TUI dashboard showing project stats: task counts by status, priority breakdown, completion percentage, and recent activity. Also introduced cross-branch task tracking options (`checkActiveBranches`, `activeBranchDays`) to keep stats accurate across branches.

Key files: src/commands/overview.ts, src/ui/overview-tui.ts, src/core/statistics.ts, README.md
> - `e2b5791`: Updated README to reflect the new v1.5.10 release and refreshed the auto-generated project board snapshot.  
Key files: README.md, package.json
> - `b914835`: Added a new `bypassGitHooks` config option (default false) that, when enabled, appends `--no-verify` to every git commit so pre-commit hooks are skipped. The init wizard now asks about this when remote operations are on, and users can toggle it later via `backlog config set bypassGitHooks true`.

Key files: src/git/operations.ts, src/cli.ts, README.md
> - `0a9a5c7`: Updated README to reflect the latest project status (v1.5.9) and regenerated the embedded kanban board snapshot, moving several completed tasks into the Done column.  
Key files: README.md, package.json
> - `0103e9e`: Kanban cards now show a meaningful task preview instead of raw markdown. The TaskCard component extracts the actual description content (stripping the "## Description" header) and truncates it to ~120 characters with ellipsis.

**Files modified:** `src/web/components/TaskCard.tsx`
