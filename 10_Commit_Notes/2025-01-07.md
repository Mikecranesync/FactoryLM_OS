---
title: "Commit Notes - 2025-01-07"
date: 2025-01-07
tags: [project/Blog-writer-multi-agent, type/commit-note]
status: active
---

# Commit Notes - 2025-01-07

## Blog-writer-multi-agent — Push at 17:10 UTC

**Commits:** 5
**Trigger:** `d627dce` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `d627dce` | Update README.md | ABDUL BASIT |
| `1d557eb` | remove demo video element from README | Abdulbasit110 |
| `ef13460` | update video source path in README for correct demo video display | Abdulbasit110 |
| `369d4ee` | add demo video to README for enhanced project visibility | Abdulbasit110 |
| `d9f9e83` | add image generation feature and update blog input form; enhance blog generation page | Abdulbasit110 |

> **What changed:**
> - `d627dce`: Added a demo GIF to the README to visually showcase the multi-agent blog-writing workflow.  
Key file: README.md
> - `1d557eb`: Removed the embedded demo video from the README, likely because the file was missing or to keep the page lightweight.  
Key file: README.md
> - `ef13460`: Fixed the demo-video path in the README so the embedded `<video>` tag actually renders the clip on GitHub; added a leading slash and a label.  
Key file: README.md
> - `369d4ee`: Embedded a demo video in the README to showcase the multi-agent blog-writing workflow in action.  
Key file: README.md (plus the new MP4 asset).
> - `d9f9e83`: Added automatic AI image generation to the blog workflow: when a topic is submitted, the app now calls a new `/api/test` endpoint to fetch a cover image and displays it above the rendered post.  
Key files: `app/page.tsx` (fetches & shows image), `components/BlogInputForm.tsx` (UI tweaks), `next.config.mjs` (allow external image domains), plus dependency updates in `package*.json`.
