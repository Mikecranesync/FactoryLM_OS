---
title: "Commit Notes - 2025-03-19"
date: 2025-03-19
tags: [project/Archon, type/commit-note]
status: active
---

# Commit Notes - 2025-03-19

## Archon — Push at 14:33 UTC

**Commits:** 5
**Trigger:** `94e7608` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `94e7608` | Fixing provider update issue when switching profiles | Cole Medin |
| `5f93fed` | Enable Dependabot & CI (#66) | Juan Miguel |
| `adf96f0` | In the MCP Streamlit page, added instructios for use in Claude Code. (#64) | xactant |
| `b0658e2` | Changing location of new "Updating" section in README | Cole Medin |
| `b93cd04` | Add updating instructions; check for non running docker containers (#58) | cannuri |

> **What changed:**
> - `94e7608`: Fixed a bug where LLM/embedding providers weren’t refreshed after switching, creating, or deleting profiles; now the session cache is explicitly cleared so the new profile’s providers load immediately.  
Key file: streamlit_pages/environment.py
> - `5f93fed`: Added Dependabot config to auto-update GitHub Actions, pip, and Docker dependencies weekly, plus a matrix CI pipeline that builds and smoke-tests the project on Python 3.10-3.13 both locally and inside Docker.  
Key files: `.github/dependabot.yml`, `.github/workflows/build.yml`
> - `adf96f0`: Added Claude Code support to the MCP configuration page: new button, dedicated setup instructions, and refactored path handling.  
Key file: `streamlit_pages/mcp.py`
> - `b0658e2`: Moved the “Updating Archon” section higher in the README so users see upgrade instructions before the setup/troubleshooting content; no functional code changes.  
Key file: README.md
> - `b93cd04`: Added update instructions to README for both Docker and local Python setups.  Enhanced run_docker.py to detect and force-remove any existing Archon container (running or stopped) before starting fresh, preventing stale-container conflicts.

README.md, run_docker.py
