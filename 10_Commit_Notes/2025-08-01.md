---
title: "Commit Notes - 2025-08-01"
date: 2025-08-01
tags: [project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-08-01

## Backlog.md — Push at 20:57 UTC

**Commits:** 4
**Trigger:** `4ef7eae` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `4ef7eae` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `a63e6d2` | TASK-220 - Fix Helix editor integration with proper TUI suspension (#264) | Alex Gavrilescu |
| `9b3b3b4` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `0fb33a1` | TASK-175 - Add hour and minute to all dates (#255) | Alex Gavrilescu |

> **What changed:**
> - `4ef7eae`: Updated README.md and package.json to reflect the new v1.6.7 release, refreshing the auto-generated Kanban board snapshot with the latest task statuses (task-220 now Done) and timestamp.  
Key files: README.md, package.json
> - `a63e6d2`: Fixed Helix editor integration by implementing proper TUI suspension—added centralized `openEditor()` method in `backlog.ts` that cleanly pauses the blessed interface, launches external editors, and restores the UI without screen corruption or input conflicts. All editor contexts (Kanban board, task viewer) now use this method, eliminating overlapping displays and unresponsiveness.

**Key files:** `src/core/backlog.ts`, `src/ui/board.ts`, `src/ui/task-viewer.ts`
> - `9b3b3b4`: Updated the README’s auto-generated project board snapshot to v1.6.6 and refreshed the task status table; no functional code changes.  
Key files: README.md, package.json
> - `0fb33a1`: **Datetime precision added to all dates** — Tasks, drafts, documents and decisions now store and display time (YYYY-MM-DD HH:mm UTC) while keeping legacy date-only entries intact.  
Key files: `backlog/config.yml`, `src/types/index.ts`, `src/markdown/parser.ts`, `src/ui/task-viewer.ts`, `src/web/components/TaskCard.tsx`

## cmms — Push at 03:05 UTC

**Commits:** 2
**Trigger:** `0146c59` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `0146c59` | fix: prevent circular references in asset and location tables (main) | Ibrahima G. Coulibaly |
| `a201f7d` | fix: prevent circular references in asset and location tables (main) | Ibrahima G. Coulibaly |

> **What changed:**
> - `0146c59`: Added database triggers to block self-referential loops in the asset hierarchy (parent_asset_id) and renamed the existing location cycle-prevention routine.  
Key file: api/src/main/resources/db/changelog/2025_08_01_1754010675_prevent_cycle.xml
> - `a201f7d`: Added PostgreSQL triggers to block self-referencing parent/child loops in the asset and location tables; any insert or update that would create a cycle now raises an exception. Keeps the hierarchy clean and prevents runaway queries.  
Key files: 2025_08_01_1754010675_prevent_cycle.xml, master.xml
