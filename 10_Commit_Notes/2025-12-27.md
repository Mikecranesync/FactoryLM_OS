---
title: "Commit Notes - 2025-12-27"
date: 2025-12-27
tags: [project/Agent-Factory, type/commit-note]
status: active
---

# Commit Notes - 2025-12-27

## Agent-Factory — Push at 23:04 UTC

**Commits:** 56
**Trigger:** `4a65091` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `4a65091` | docs: Add session accomplishments to README | Mikecranesync |
| `4c934eb` | feat(mvp): Complete backlog cleanup + task-5 research pipeline | Mikecranesync |
| `870b22e` | docs: Add deployment guide for onboarding system | Mikecranesync |
| `42aa384` | feat(telegram): Register onboarding commands in bot | Mikecranesync |
| `c6e0562` | feat(telegram): Complete RIVET Telegram Onboarding System | Mikecranesync |
| `ab10396` | feat: WS-1 Backend Completion - Dual storage + Tests + Config | Mikecranesync |
| `6b8caa4` | chore: complete Phase 2 & 3 - indexers and API routers | Mikecranesync |
| `b2f7024` | feat: dual storage support (PostgreSQL + ChromaDB) | Mikecranesync |
| `49a82ca` | docs: add Vercel deployment URL for RIVET landing page | Mikecranesync |
| `d760643` | docs: Add TAB 3 completion summary | Mikecranesync |
| `01c6678` | feat(tab3): Complete TAB 3 Bot - Print/Manual Commands + Integration | Mikecranesync |
| `447d737` | RIVET Frontend Enhancement: Phases 1-7 Complete | Mikecranesync |
| `cf76dfa` | feat(landing): Add Footer component and update page layout | Mikecranesync |
| `3e1bc2b` | feat(landing): Update Features component and add session state | Mikecranesync |
| `355eb9b` | feat(landing): Redesign Hero with RIVET branding | Mikecranesync |
| `07d6244` | style: Add RIVET brand colors to Tailwind config | Mikecranesync |
| `3ec7777` | docs: Update claude.md with TAB 3 Phase 1 completion | Mikecranesync |
| `996173a` | feat(tab3): Phase 1 Context Extractor complete | Mikecranesync |
| `29bf17d` | docs: Add session state before context clear (2025-12-27) | Mikecranesync |
| `4f38ca5` | docs: Add trace enhancement completion documentation | Mikecranesync |
| `b0e0c46` | feat(route-c): Add real-time manual search to Route C | Mikecranesync |
| `4bdd678` | feat(manual-delivery): Real-time manual search and delivery for Route B | Mikecranesync |
| `f8899a8` | feat(enrichment): Phase 5 Route B KB enrichment pipeline (Steps 1-6) | Mikecranesync |
| `bcf0bd1` | feat(trace): Complete enhanced trace debugging with routing logic capture (Steps 2-6/6) | Mikecranesync |
| `cdf4a1f` | feat(trace): Enhanced trace debugging with routing logic capture (Step 1/6) | Mikecranesync |
| `1e28eb4` | feat: Add Phase 5.5 observability enhancements and deployment tools | Mikecranesync |
| `86c4e1e` | feat: Add auto-updating system map to README on every push | Mikecranesync |
| `7325200` | security: Document BFG cleanup incident and create playbook | Mikecranesync |
| `a6fd690` | security: Add ENV_FILES_GUIDE.md and DIAGNOSTIC_SESSION to .gitignore | Mikecranesync |
| `6c789f9` | feat(rivet-pro): Complete Phase 5 - Research Pipeline Integration | Mikecranesync |
| `aed2fbd` | Add integration tests directory | Mikecranesync |
| `483fa8a` | Add remaining Atlas integration files and update .env.example | Mikecranesync |
| `dfe6c16` | security: Add comprehensive .gitignore patterns for API keys and credentials | Mikecranesync |
| `1bc854d` | Add Atlas integration files and deployment status | Mikecranesync |
| `f9fab2b` | Add context handoff document for next session | Mikecranesync |
| `30e4334` | Save session state and update memory files before context clear | Mikecranesync |
| `b0f42a4` | chore: Remove langsmith info.txt with API keys from tracking | Mikecranesync |
| `29e86ed` | feat(ws3): Complete voice → orchestrator integration | Mikecranesync |
| `d898c09` | FIX: Add proper docstring quotes to observability/__init__.py | Mikecranesync |
| `8f034da` | Add comprehensive admin walkthrough for all 3 workstreams | Mikecranesync |
| `837933f` | WS-1: Phase 4 - Health & Monitoring implementation | Mikecranesync |
| `1fa2cc2` | Add session summary before context clear | Mikecranesync |
| `f589126` | Add WS-1 + WS-2 integration guide | Mikecranesync |
| `884e397` | WS-2: Add Vercel deployment configuration and guide | Mikecranesync |
| `becff74` | feat(ws-3): Integrate voice → RIVET Pro routing | Mikecranesync |
| `0382582` | WS-3: Fix LangFuseTracker import to get bot running | Mikecranesync |
| `8991898` | WS-3: Integrate WS-1 backend API client | Mikecranesync |
| `e58a01b` | WS-1: Atlas CMMS API research and documentation | Mikecranesync |
| `e939fc8` | WS-3: Add status update document | Mikecranesync |
| `057be00` | WS-3: Phase 3 - Intent clarification flow complete | Mikecranesync |
| `9c9caef` | WS-3: Phase 2 - Claude Vision for prints complete | Mikecranesync |
| `d62c7e4` | WS-2: Add deployment guide and deployment script | Mikecranesync |
| `f4537f4` | WS-2: Add README and status documentation | Mikecranesync |
| `1bd08a7` | WS-2: Add pricing page, checkout flow, success page, and env config | Mikecranesync |
| `a1540b7` | WS-2: Add landing page components (Hero, Features, HowItWorks, CTA) | Mikecranesync |
| `43bcb06` | WS-2: Initialize Next.js app with TypeScript and Tailwind | Mikecranesync |

> **What changed:**
> - `4a65091`: Updated README.md to document major project milestones: archived 38 completed tasks, finished task-5 research pipeline (now 7/7 tests passing), and established a focused 8-task MVP roadmap with 5-week timeline to Feb 2025 launch. Key files: README.md, MVP_ROADMAP.md, BACKLOG_CLEANUP_SUMMARY.md.
> - `4c934eb`: **Summary:** Completed major backlog cleanup—archived 100+ tasks to focus on 8 MVP essentials—and finished the research pipeline (task-5) with Stack Overflow/Reddit ingestion, SHA-256 deduplication, and 100% test coverage.  
**Key files:** MVP_ROADMAP.md, BACKLOG_CLEANUP_SUMMARY.md, tests/rivet_pro/test_research_pipeline.py
> - `870b22e`: **Summary:** Added comprehensive production deployment guide for the new onboarding system integration in the RIVET Telegram bot. The guide covers database migration, environment setup, three deployment methods (systemd/tmux/Docker), verification steps, and troubleshooting procedures.

**Key files modified:** DEPLOYMENT_GUIDE.md

The onboarding system now provides tier-aware welcome messages, interactive tutorials, API key authentication, and feature tours - all requiring specific database schema changes and production deployment procedures outlined in this guide.
> - `42aa384`: **Telegram bot onboarding system deployed** — Replaced basic `/start` with tier-aware onboarding flow that includes API key authentication and interactive tutorials. Added five new commands (`/tutorial`, `/tour`, `/quickstart`, `/about`, `/pricing`) plus callback handlers for inline onboarding buttons.

**Key files:** `telegram_bot.py`
> - `c6e0562`: **Summary:** Added a complete onboarding system to the RIVET Telegram bot—new users now get a guided 5-step tutorial, API-key authentication, tier-aware feature tours, and quick-reference cards. Database extended to track onboarding progress.

**Key files:**  
- `onboarding_manager.py` – core tutorial flow  
- `rivet_pro_handlers.py` – new /start, /tutorial, /tour, /quickstart commands  
- `003_add_onboarding_fields.sql` – DB migration for progress tracking
> - `ab10396`: **Backend WS-1 complete:** dual vector-storage (PostgreSQL default, ChromaDB dev) with runtime switch via `VECTOR_STORE_BACKEND`, plus full manuals API (`/api/manuals/*`) for PDF upload, semantic search, listing, and gap tracking.  
**Key files:** `agent_factory/knowledge/storage/*` (new abstraction + both stores), `tests/test_backend.py`, `.env.example`.
> - `6b8caa4`: Phase 2 & 3 deliverables are live: PDF manuals are now indexed (PyPDF2 + ChromaDB + sentence-transformers) and fully searchable via a new `/manuals` API (upload, search, list, gaps, details) with LangSmith tracing. Work-orders router is stubbed for future Atlas CMMS integration.

Key files: `pyproject.toml`, `poetry.lock`
> - `b2f7024`: Added dual vector-storage backend support: the system can now switch between PostgreSQL and ChromaDB via the `VECTOR_STORE_BACKEND` env var (defaults to postgres).  
Introduced an `AbstractVectorStore` interface plus concrete stores for each backend, wrapped in a factory so the rest of the codebase stays storage-agnostic.  
Key files: `agent_factory/knowledge/storage/__init__.py`, `abstract_vector_store.py`, `postgres_vector_store.py`, `chromadb_vector_store.py`.
> - `49a82ca`: RIVET landing page is now live at https://landing-zeta-plum.vercel.app (WS-2 TAB 2 complete). Updated both READMEs with the production URL and deployment status; stripped hard-coded env vars from vercel.json to manage via Vercel dashboard.

Key files: README.md, products/landing/README.md, products/landing/vercel.json
> - `d760643`: **Summary:** Added comprehensive documentation for TAB 3, a complete Telegram bot implementation featuring intelligent intake, voice transcription, print/manual management, and context extraction with 95% accuracy. The 448-line document details 8 implementation phases, 11 new commands, and production readiness assessment.

**Key files:** `TAB3_COMPLETE.md`
> - `01c6678`: Added full print & manual management to the Telegram bot: users can now `/add_machine`, `/upload_print`, and `/chat_print` for machine-specific electrical prints, plus `/upload_manual` and `/manual_search` across an indexed OEM-manual knowledge base. PDFs auto-route to the right handler; everything is namespaced per user and surfaced in `/help`.

Key files: `telegram_bot.py`, `print_handlers.py`, `manual_handlers.py`, `manual_indexer.py`, `manual_search.py`, `manuals.py`
> - `447d737`: Completed full-stack RIVET rebrand and subscription flow: updated Tailwind palette, Hero/Features/Footer UI, and replaced legacy pricing with Beta/Pro/Team tiers. Added user-provisioning and Telegram-notification services plus SQL schema; Stripe webhooks now auto-create users and send welcome messages.  
Key files: agent_factory/api/routers/stripe.py, agent_factory/api/services/user_provisioning.py, agent_factory/api/services/telegram_notifications.py, docs/database/migrations/rivet_users_subscriptions.sql, products/landing/app/pricing/page.tsx
> - `cf76dfa`: Added a Footer component to the landing page with contact links and RIVET branding. The footer includes support email, privacy/terms links, and copyright info.

**Files modified:** `products/landing/app/page.tsx`, `products/landing/components/Footer.tsx`
> - `3e1bc2b`: Updated the landing page Features component with new RIVET branding styling and added session state tracking documentation. The visual changes align the landing page with updated brand guidelines while the session state file captures backend infrastructure progress for development continuity.

Key files: `products/landing/components/Features.tsx`, `SESSION_STATE_TAB1.md`
> - `355eb9b`: Redesigned the landing page Hero component with RIVET branding—dark gradient background, orange CTAs, wrench logo, and feature pills highlighting voice commands and print analysis. Added lucide-react dependency for icons and updated CTA buttons to "Start Free Trial" and "Try on Telegram".

**Key files:** `products/landing/components/Hero.tsx`, `products/landing/package.json`, `products/landing/package-lock.json`
> - `07d6244`: Added RIVET brand colors to Tailwind config with orange (#FF6B35) for primary CTAs, dark (#1a1a2e) for main backgrounds, gray (#16213e) for secondary backgrounds, and light (#e8e8e8) for hover states. This enables consistent theming across the landing page using the new `rivet` color namespace.

**Key file:** products/landing/tailwind.config.ts
> - `3ec7777`: TAB 3 Phase 1 Context Extractor is now complete, boosting equipment detection to 95% and fault-code extraction to 98%.  
WS-3 Telegram Bot readiness jumps from 75% → 80% production-ready; Phase 2 Response Synthesizer work can begin.

Key files: `agent_factory/rivet_pro/context_extractor.py`, `tests/test_context_extractor.py`, `agent_factory/rivet_pro/intent_detector.py`
> - `996173a`: **Summary:**  
Added a deep context extractor that boosts equipment/fault detection from 70% to 95% by scraping fault codes, part numbers, symptoms, and manufacturer info from text, images, and voice. It plugs into IntentDetector when confidence < 0.7 and can be toggled off.

**Key files:**  
`agent_factory/rivet_pro/context_extractor.py`, `agent_factory/rivet_pro/intent_detector.py`, `tests/test_context_extractor.py`
> - `29bf17d`: Added real-time manual search to Route C queries (no KB coverage) via 3-way parallel execution, ensuring users always receive documentation links. Queued 5 Fuji Electric manuals (218 pages) for VPS ingestion to expand KB coverage. Modified orchestrator.py to include manual web search in Route C responses.

Key files: orchestrator.py
> - `4f38ca5`: Added comprehensive documentation confirming all 6 trace enhancement steps are complete and production-ready. The new TRACE_ENHANCEMENT_COMPLETE.md file validates the enhanced RequestTrace methods, orchestrator integration, LangGraph workflow tracing, VPS log monitoring, admin trace messages, and /trace command functionality.

Key files: TRACE_ENHANCEMENT_COMPLETE.md
> - `b0e0c46`: Route C now runs manual web search in parallel with gap detection and LLM fallback, so users without KB coverage (e.g., Fuji Electric queries) instantly receive 1–3 manual URLs inside the chat response.  
Key file: agent_factory/core/orchestrator.py
> - `4bdd678`: Route B now runs a parallel web search for manufacturer manuals while the SME agent answers, adding 1s to deliver 1–3 clickable PDF links in Telegram.  
Key files: agent_factory/core/orchestrator.py, agent_factory/services/manual_finder.py
> - `f8899a8`: **Summary:**  
Built an automatic KB enrichment pipeline for Route B (thin coverage) queries. When Route B triggers, gaps are logged with priority scoring, then an hourly worker discovers manufacturer URLs and queues them to a VPS Redis queue for crawling. Added `/kb_enrichment` Telegram command to monitor pending gaps and queued sources.

**Key files:**  
`agent_factory/core/orchestrator.py`, `agent_factory/workers/enrichment_worker.py`, `agent_factory/core/kb_gap_logger.py`, `docs/database/migrations/002_enrichment_tracking.sql`
> - `bcf0bd1`: **Enhanced trace debugging system completed** - Added comprehensive tracing across the entire request pipeline with routing decisions, agent reasoning, and VPS log monitoring. Key files: `orchestrator.py` (routing traces), `langgraph_handlers.py` (workflow traces), `log_monitor.py` (VPS SSH monitoring), `orchestrator_bot.py` (enhanced admin messages + /trace command).
> - `cdf4a1f`: Enhanced the RequestTrace logger with comprehensive debugging methods to capture routing decisions, agent reasoning, research pipeline status, LangGraph execution, and KB retrieval scores. This foundational step adds 8 new trace methods and corresponding getters to enable deep visibility into system decision-making processes.

Key files: `agent_factory/core/trace_logger.py`
> - `1e28eb4`: Added comprehensive observability to Phase 5.5: trace_logger.py now captures every routing decision, agent reasoning (KB atoms used, confidence), research-pipeline status, and LangGraph workflow steps.  Delivered deployment tooling (SQL verify, REST/schema deploy scripts) and consolidated sprint docs.  Key files: agent_factory/core/trace_logger.py, docs/PHASE5_DEPLOYMENT_GUIDE.md, scripts/deployment/*.
> - `86c4e1e`: Added automatic system map generation that updates the README on every push. The system scans 7 major component areas (Core, Memory, LLM, RIVET, PLC, Infrastructure, Observability) and tracks 40+ subcomponents with their build status. Currently shows 100% completion (7/7 major components built).

Key files: `scripts/generate_system_map.py`, `scripts/update_readme_system_map.py`, `.githooks/pre-push`, `README.md`, `data/system_map.json`, `data/system_map.md`
> - `7325200`: Documented a security incident where API keys were accidentally committed to git history and created a comprehensive BFG Repo-Cleaner playbook for emergency secret removal. The playbook includes step-by-step instructions for using BFG to purge sensitive data from entire git history, with prerequisites, backup procedures, and multiple removal strategies. Key files modified: `docs/patterns/GIT_SECRET_REMOVAL.md` (new playbook) and `docs/security/INCIDENT_2025-12-27.md` (incident report).
> - `a6fd690`: Added documentation files containing credentials to .gitignore to prevent accidental commits. This follows a BFG cleanup of git history to remove previously committed sensitive files.

**Key file modified:** `.gitignore`
> - `6c789f9`: **Phase 5 Research Pipeline Integration Complete**  
The system now autonomously grows its knowledge base when users ask about uncovered topics (Route C). It scrapes Stack Overflow/Reddit in background threads, deduplicates via URL fingerprints, and queues new sources for ingestion without blocking user responses.

**Key files:**  
- `docs/database/phase5_research_pipeline_migration.sql` – new `source_fingerprints` table  
- `agent_factory/rivet_pro/research/research_pipeline.py` – background ingestion threads  
- `agent_factory/integrations/telegram/orchestrator_bot.py` – user-facing “searching forums…” message
> - `aed2fbd`: Added a new `tests/integrations/atlas/` directory with comprehensive unit tests for the Atlas CMMS client. The test suite validates token caching/refresh logic, authentication flows, and API error handling, ensuring the integration behaves correctly before real-world deployment.
> - `483fa8a`: Added the missing Atlas CMMS integration module (`agent_factory/integrations/atlas/`) and expanded `.env.example` with Atlas-specific config (URL, credentials, token TTL, feature flag).  
Key files: `.env.example`, `agent_factory/integrations/atlas/__init__.py`, `agent_factory/integrations/atlas/client.py`.
> - `dfe6c16`: Added 50+ new .gitignore patterns to block API keys and credentials (Langchain, Stripe, Supabase, Neon) from ever being committed, closing gaps that previously let secrets slip into GitHub.  
Key file: `.gitignore`
> - `1bc854d`: Added the Atlas CMMS integration skeleton: a Pydantic-based config module that pulls API URL, credentials, timeout, retry, and JWT-refresh settings from env vars, plus custom exceptions and a deployment-status note.  
Key files: agent_factory/integrations/atlas/config.py, agent_factory/integrations/atlas/exceptions.py, deploy/atlas/DEPLOYMENT_STATUS.md
> - `f9fab2b`: Created a comprehensive context handoff document for the next development session, documenting that GitHub push is blocked due to a secret scanning issue with an old Langchain API key in commit history. The document provides two resolution options and outlines the current project status with all three workstreams nearly complete. Key files: sprint/CONTEXT_HANDOFF.md
> - `30e4334`: **Session checkpoint before context clear:** Saved comprehensive work on WS-1/WS-2 deployment configs, integration guide, and a 2,600-line admin walkthrough. Updated CLAUDE.md with session accomplishments and added Atlas CMMS local deployment docs. Key files: claude.md, sprint/ADMIN_WALKTHROUGH.md, deploy/atlas/README.md, products/landing/package.json
> - `b0f42a4`: Removed tracked API key file by adding it to .gitignore. Prevents accidental exposure of LangSmith credentials in version control.

**Key file modified:** .gitignore
> - `29e86ed`: **Voice messages now route through RivetOrchestrator** with full KB search and safety warnings. Added `handle_voice()` to orchestrator_bot.py to transcribe voice → Whisper → A/B/C/D routing system, resolving bot token conflicts and circular import issues.

**Key files:** orchestrator_bot.py, voice_orchestrator_handler.py, bot.py
> - `d898c09`: Fixed a syntax error by converting a bare string to a proper module-level docstring in the observability package.  
**File touched:** `agent_factory/observability/__init__.py`
> - `8f034da`: Added a 2.6k-line admin walkthrough doc that consolidates setup, testing and ops instructions for all three Rivet MVP workstreams (FastAPI backend, Next.js frontend, Telegram bot).  
Key file: sprint/ADMIN_WALKTHROUGH.md
> - `837933f`: **Health & monitoring upgrade**: Enhanced `/health` endpoint now tests live Stripe API connectivity (not just config) and aggregates overall status (healthy/degraded/unhealthy) with response-time metrics. Added LangSmith tracing via new `@trace_endpoint` decorator on all user and Stripe webhook routes for production observability.  
**Key files**: `agent_factory/api/main.py`, `agent_factory/observability/langsmith_config.py`, `agent_factory/api/routers/users.py`, `agent_factory/api/routers/stripe.py`, `.env.example`
> - `1fa2cc2`: Added a comprehensive session summary documenting the completion of WS-1 (backend) and WS-2 (frontend) for the Rivet MVP. Both workstreams are now code-complete with deployment guides ready for Railway/Render (backend) and Vercel (frontend).

Key files: sprint/SESSION_SUMMARY.md
> - `f589126`: Added a comprehensive integration guide for deploying the Rivet MVP, covering both the FastAPI backend (WS-1) and Next.js frontend (WS-2). The guide includes deployment workflows for Railway/Render and Vercel, environment variable configurations, testing steps, and troubleshooting. Key file: sprint/INTEGRATION_GUIDE.md
> - `884e397`: **Summary:** Added complete Vercel deployment configuration for the Rivet landing page, including `vercel.json` with Next.js settings and a comprehensive deployment guide. The frontend is now ready for deployment with environment variables for Stripe integration and backend connection.

**Key files:** `products/landing/vercel.json`, `products/landing/VERCEL_DEPLOY.md`, `sprint/STATUS_WS2.md`
> - `becff74`: Voice messages now flow through RIVET Pro: after Whisper transcribes the audio, IntentDetector classifies it as TROUBLESHOOTING or INFORMATION and routes to the matching RIVET handler; TIER0Handlers accepts an optional rivet_handlers param to enable this.  
Key files: agent_factory/integrations/telegram/tier0_handlers.py, telegram_bot.py
> - `0382582`: Disabled the unimplemented LangFuseTracker import so the bot starts; wired the user-provision and get-user endpoints to the real PostgreSQL backend (RIVETProDatabase) and added a DB-aware health check.  
Key files: agent_factory/api/main.py, agent_factory/api/routers/users.py, agent_factory/integrations/telegram/langgraph_handlers.py
> - `8991898`: Added a new `RivetAPIClient` that lets the Telegram bot talk to the WS-1 backend: it can create work orders and search assets to resolve ambiguous equipment names.  
The RIVET Pro handlers now call this client, so voice/text messages can turn into real work orders and the bot can ask “which pump?” when needed.  
Key files: `api_client.py` (new client), `rivet_pro_handlers.py` (wired to use it).
> - `e58a01b`: Added comprehensive API documentation for Atlas CMMS integration, covering authentication, work orders, assets, users, and Docker deployment configuration. This enables future agent development to interact with the self-hosted maintenance management system. Key file: agent_factory/integrations/atlas/ATLAS_API_DOCUMENTATION.md
> - `e939fc8`: Added a comprehensive status update document for WS-3 (Bot + AI Features), documenting completion of Phases 1-3: voice message handling with Whisper transcription, Claude Vision for schematic analysis, and intelligent intent clarification flows. Key files: sprint/STATUS_WS3.md
> - `057be00`: **Summary:**  
Added an intent clarification flow that intercepts vague troubleshooting questions and asks targeted follow-ups (equipment type, symptoms, error codes) before proceeding. The bot now stores the pending clarification in user context and routes the user’s answer back through a dedicated handler to refine the intent and retry the troubleshoot flow.

**Key files:**  
`agent_factory/integrations/telegram/handlers.py`, `agent_factory/integrations/telegram/rivet_pro_handlers.py`, `agent_factory/rivet_pro/clarification.py`
> - `9c9caef`: Added Claude Vision support to the Telegram bot so users can now upload schematic/print photos and get AI analysis. The bot uses Claude Sonnet 4 to extract component details, wiring info, circuit function, and troubleshooting tips from technical drawings. Key files: bot.py (adds photo handler), claude_provider.py (new vision provider), photo_handler.py (routes images), print_analyzer.py (analysis service).
> - `d62c7e4`: Added voice-message support to the Telegram bot: users can now send audio that is transcribed with Whisper and routed through RIVET Pro intent detection.  
New deployment guide and one-line deploy script make it trivial to ship the landing page.  

Key files:  
- agent_factory/integrations/telegram/bot.py (voice handler wiring)  
- agent_factory/integrations/telegram/voice/handler.py (new voice flow)  
- products/landing/DEPLOYMENT.md & deploy.sh
> - `f4537f4`: Added voice-message support to the Telegram bot: new WhisperTranscriber converts OGG audio to text via OpenAI and AudioConverter handles the required OGG→WAV conversion with ffmpeg.  
Key files: agent_factory/integrations/telegram/voice/{transcriber,audio_utils}.py, products/landing/README.md, sprint/STATUS_WS2.md
> - `1bd08a7`: Added a pricing page with three tiers (Basic/Pro/Enterprise), a checkout API that proxies to the FastAPI backend, and a success page. Users can now select a plan, enter email, and be redirected to Stripe checkout (or email sales for Enterprise). Key files: `pricing/page.tsx`, `api/checkout/route.ts`, `success/page.tsx`.
> - `a1540b7`: Replaced the placeholder landing page with a full marketing site: Hero, Features, How-It-Works, and CTA sections. Users now see a polished pitch for voice-first CMMS and can jump straight to pricing.  
Key files: `products/landing/app/page.tsx`, `products/landing/components/{Hero,Features,HowItWorks,CTA}.tsx`
> - `43bcb06`: Scaffolded a new Next.js 14 landing-page project under `products/landing/` with TypeScript strict mode, Tailwind CSS, and a minimal “Coming soon” placeholder.  
Key files created: `package.json`, `tsconfig.json`, `tailwind.config.ts`, `app/layout.tsx`, `app/page.tsx`, plus the usual Next.js boilerplate and sprint prompt docs.
