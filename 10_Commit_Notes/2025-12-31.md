---
title: "Commit Notes - 2025-12-31"
date: 2025-12-31
tags: [project/Agent-Factory, project/My-Ralph, type/commit-note]
status: active
---

# Commit Notes - 2025-12-31

## Agent-Factory — Push at 16:20 UTC

**Commits:** 6
**Trigger:** `fb8aa00` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `fb8aa00` | docs: Add comprehensive summary of all 7 bugs fixed | Mikecranesync |
| `f492107` | fix: Change atom_type from 'research' to 'concept' | Mikecranesync |
| `56518f6` | fix: Reuse DatabaseManager singleton in test | Mikecranesync |
| `eb4e720` | fix: Change placeholders from PostgreSQL ($1) to psycopg3 (%s) format | Mikecranesync |
| `256b075` | debug: Add SQL logging to investigate empty query | Mikecranesync |
| `4f6aae9` | fix: Fix 5 critical research integration bugs | Mikecranesync |

> **What changed:**
> - `fb8aa00`: Research integration is now production-ready: all 7 critical bugs blocking atom insertion into Neon have been fixed, including schema mismatches, missing required fields, connection pool exhaustion, silent errors, query parameter format issues, PostgreSQL placeholder syntax, and Neon CHECK constraints. Debug logging was removed and a comprehensive summary of all fixes was documented.

Key files: ALL_BUGS_FIXED_SUMMARY.md, agent_factory/core/database_manager.py, agent_factory/tools/response_gap_filler.py
> - `f492107`: Fixed a Neon DB constraint violation by changing the hard-coded atom_type from 'research' to 'concept' when inserting new atoms.  
Files touched: agent_factory/tools/response_gap_filler.py, test_single_insert_vps.py
> - `56518f6`: Fixed test creating a second DatabaseManager instance, which was exhausting the connection pool. Now reuses the singleton from the filler object.

Key file: test_single_insert_vps.py
> - `eb4e720`: Fixed SQL placeholder syntax for psycopg3 compatibility by replacing PostgreSQL-style `$1, $2…` with psycopg3 `%s` format in all CRUD queries.  
Added debug logging and two new database providers (VPS KB and Atlas CMMS) to the manager.  
Key files: `database_manager.py`, `response_gap_filler.py`, `test_minimal_insert.py`.
> - `256b075`: Added debug logging to trace the SQL INSERT statement and parameter count before the database call, helping diagnose why the query appears empty.  
Key file: agent_factory/tools/response_gap_filler.py
> - `4f6aae9`: Fixed 5 critical research integration bugs that were blocking atom insertion into the database. Implemented singleton DatabaseManager to prevent connection pool exhaustion, wrapped query parameters in tuples for correct SQL formatting, mapped equipment_type to product_family for schema compatibility, added all missing required database fields (summary, difficulty, source_pages, source_url), and modified error handling to return failures list instead of silently swallowing errors.

Key files: agent_factory/tools/response_gap_filler.py, test_single_insert_vps.py

## My-Ralph — Push at 23:49 UTC

**Commits:** 12
**Trigger:** `6c647ef` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `6c647ef` | Merge pull request #9 from frankbria/fix/cross-platform-date-compatibility | Frank Bria |
| `70279c0` | fix(date): ensure Linux date utility returns UTC timestamps | frankbria |
| `3d7db2c` | feat(date): add cross-platform date compatibility for macOS and Linux | frankbria |
| `42b3a1e` | Merge pull request #8 from frankbria/claude/update-docs-x8G2u | Frank Bria |
| `38aa53c` | docs: update CLAUDE.md and README.md with recent improvements | Claude |
| `8158e38` | Merge pull request #6 from frankbria/fix/circuit-breaker-false-positives | Frank Bria |
| `63590b3` | fix(circuit-breaker): fix multi-line error matching in detect_stuck_loop | frankbria |
| `890720a` | fix(circuit-breaker): fix detect_stuck_loop function and add tests | frankbria |
| `90fb558` | fix(circuit-breaker): address CodeRabbit review feedback | frankbria |
| `8fc5375` | fix(circuit-breaker): eliminate JSON field false positives in error detection | frankbria |
| `e370c1e` | Merge pull request #4 from ikanc/fix/install-lib-directory | Frank Bria |
| `a98a167` | fix(install): add lib/ directory copy for response_analyzer.sh | ikabalzam |

> **What changed:**
> - `6c647ef`: Merge pull request #9 from frankbria/fix/cross-platform-date-compatibility. Files: `lib/circuit_breaker.sh`, `lib/date_utils.sh`, `lib/response_analyzer.sh`, `ralph_loop.sh`.
> - `70279c0`: Fixed Linux date utility to return UTC timestamps by adding `-u` flag, matching macOS behavior and function documentation. Ensures consistent UTC ISO format across platforms.

**Key file:** lib/date_utils.sh
> - `3d7db2c`: Added a cross-platform date utility (`lib/date_utils.sh`) that auto-detects macOS vs Linux and uses the correct `date` syntax, eliminating ISO-timestamp and date-arithmetic failures on macOS.  
Key files touched: `lib/circuit_breaker.sh`, `lib/response_analyzer.sh`, `ralph_loop.sh` (all now call `get_iso_timestamp()` and `get_next_hour_time()`).
> - `42b3a1e`: Merge pull request #8 from frankbria/claude/update-docs-x8G2u. Files: `CLAUDE.md`, `README.md`.
> - `38aa53c`: Updated documentation to reflect the new modular architecture with lib/ components (circuit_breaker.sh and response_analyzer.sh) and the two-stage error detection system that eliminates false positives. Key files: CLAUDE.md, README.md.
> - `8158e38`: Merge pull request #6 from frankbria/fix/circuit-breaker-false-positives. Files: `lib/response_analyzer.sh`, `ralph_loop.sh`, `tests/test_error_detection.sh`, `tests/test_stuck_loop_detection.sh`.
> - `63590b3`: Fixed multi-line error matching in `detect_stuck_loop()` so Ralph now correctly flags stuck loops only when **all** current error lines repeat across **all** recent outputs.  
Key files: `lib/response_analyzer.sh`, `tests/test_stuck_loop_detection.sh`
> - `890720a`: Fixed the circuit breaker’s `detect_stuck_loop()` to use the same two-stage error filtering as `analyze_response()`, eliminating false positives from JSON field names like `"is_error": false`. Removed unused `STUCK_INDICATORS` array and added a 7-case test suite to lock in the behavior.

Key files: `lib/response_analyzer.sh`, `tests/test_stuck_loop_detection.sh`
> - `90fb558`: Fixed circuit-breaker false positives by aligning error-detection regex between `lib/response_analyzer.sh` and `ralph_loop.sh`; Stage-1 now strips any JSON field containing “error” and Stage-2 no longer flags bare “cannot/unable”. Added test 10a to prove prose with those words is ignored; all 13 tests pass.
> - `8fc5375`: Fixed circuit-breaker false positives by adding two-stage JSON filtering before error detection. The change prevents JSON field names like `"is_error": false` from triggering premature circuit-breaker trips while still catching real errors.

**Key files:** `lib/response_analyzer.sh`, `ralph_loop.sh`, `tests/test_error_detection.sh`
> - `e370c1e`: Merge pull request #4 from ikanc/fix/install-lib-directory. Files: `install.sh`.
> - `a98a167`: Fixed install.sh to properly copy the lib/ directory containing response_analyzer.sh and circuit_breaker.sh, preventing ralph_loop.sh failures. Added directory creation, file copying, and executable permissions for lib scripts.

**Key file:** install.sh
