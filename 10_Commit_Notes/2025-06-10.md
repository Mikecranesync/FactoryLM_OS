---
title: "Commit Notes - 2025-06-10"
date: 2025-06-10
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-06-10

## Backlog.md — Push at 20:21 UTC

**Commits:** 22
**Trigger:** `010e204` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `010e204` | TASK-35 - Finalize package.json metadata for publishing (#78) | Alex Gavrilescu |
| `7c4261e` | TASK-34 - Split readme.md for users and contributors (#77) | Alex Gavrilescu |
| `b97c5af` | TASK-36 - CLI: Prompt for project name in init (#76) | Alex Gavrilescu |
| `eff15ca` | TASK-38 - CLI: Improved Agent Selection for Init (#75) | Alex Gavrilescu |
| `48ed56f` | TASK-39 - CLI: fix empty agent instruction files on init (#74) | Alex Gavrilescu |
| `4d11ca3` | TASK-40 - CLI: Board command defaults to view (#73) | Alex Gavrilescu |
| `ac61e2a` | backlog: Demote task 30 | Alex Gavrilescu |
| `7b2c65f` | backlog: Demote task 28 | Alex Gavrilescu |
| `56135d4` | backlog: Demote task 26 | Alex Gavrilescu |
| `87cf8d7` | Add build config for windows | Alex Gavrilescu |
| `39539c3` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `230da36` | Add space in between sub tasks (it's cleaner) | Alex Gavrilescu |
| `b1cf34d` | Add task-40 - board alias for board view (#72) | Alex Gavrilescu |
| `542a21a` | Improve terminal board view for subtasks | Alex Gavrilescu |
| `f680a85` | Add task-39 - CLI: fix empty agent instruction files on init (#65) | Alex Gavrilescu |
| `3c2d7b9` | Add Task-38 - CLI: Improved Agent Selection for Init (#66) | Alex Gavrilescu |
| `7ce2d9d` | Add Task-37 - CLI: Board view open tasks in IDE (#68) | Alex Gavrilescu |
| `a12a199` | Task-36 - CLI: Prompt for project name in init (#67) | Alex Gavrilescu |
| `b227b4d` | Add Task-35 - CLI: Export milestones board as roadmap (#69) | Alex Gavrilescu |
| `c67a1d2` | Add Task-34 - docs: add metadata finalization task (#70) | Alex Gavrilescu |
| `79cd78b` | Add task-33 split README (#71) | Alex Gavrilescu |
| `7fb7e6f` | rename cursorrules file | Alex Gavrilescu |

> **What changed:**
> - `010e204`: Added all required npm metadata (author, repo, bugs, homepage, keywords, license) so the package can be published without warnings; also fixed the bin path and marked the backlog task complete.  
Key files: package.json, .backlog/tasks/task-35 - finalize-package.json-metadata-for-publishing.md
> - `7c4261e`: Split the monolithic README into a user-focused readme.md and a contributor-focused DEVELOPMENT.md; the former now only covers installation and usage, while the latter holds local-dev, test, and release instructions. Cross-links keep the two docs connected.

Key files: readme.md, DEVELOPMENT.md, .backlog/tasks/task-34 - split-readme.md-for-users-and-contributors.md
> - `b97c5af`: `backlog init` now works without a project name argument; when omitted, the CLI interactively prompts for one before proceeding. Updated CLI command signature and added test coverage.

Key files: `src/cli.ts`, `src/test/cli.test.ts`
> - `eff15ca`: Replaced the numeric agent picker with an interactive checkbox UI (`prompts` lib) so users can now select multiple agents for `backlog init` via space/enter.  
Key files: `src/cli.ts`, `package.json`, `bun.lock`, `.backlog/tasks/task-38…md`.
> - `48ed56f`: Fixed `backlog init` so the generated agent instruction files (AGENTS.md, CLAUDE.md, .cursorrules) now contain their default guideline text instead of being empty. Added a `_loadAgentGuideline` helper that reads the bundled guideline constants as files when necessary, and updated tests to verify non-empty content.

Key files: src/agent-instructions.ts, src/index.ts, src/test/agent-instructions.test.ts
> - `4d11ca3`: The `backlog board` command now defaults to displaying the kanban board, matching the behavior of `backlog board view`. This change refactors the CLI to share the board rendering logic between the root command and the view subcommand, maintaining backward compatibility. Key files modified: `src/cli.ts` and `src/test/cli.test.ts`.
> - `ac61e2a`: Moved task 30 from active drafts back to the backlog; the changelog creation item is no longer in progress.  
Key file: `.backlog/drafts/task-30 - create-changelog.md`
> - `7b2c65f`: Demoted task 28 (add-code-of-conduct.md) from active backlog to drafts, effectively pausing work on the Code-of-Conduct addition.  
Key file: `.backlog/drafts/task-28 - add-code-of-conduct.md`
> - `56135d4`: Moved task 26 from active drafts back to the backlog; the “add board-export step to agent DoD” documentation update is no longer prioritized.  
Key file: `.backlog/drafts/task-26 - docs-add-board-export-step-to-agent-dod.md`
> - `87cf8d7`: Added Windows-compatible build pipeline: replaced inline shell commands with a cross-platform Node script that spawns Bun to build both JS bundle and native executable, correctly handles `.exe` extension and skips shebang/chmod on Windows. Updated package.json scripts and tests to match.  
Key files: `scripts/build.js`, `package.json`, `src/test/build.test.ts`
> - `39539c3`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `.backlog/tasks/task-40 - cli-board-command-defaults-to-view.md`.
> - `230da36`: Kanban board layout now always inserts an empty row between tasks, even when the next task is a subtask. This removes the special-case logic that kept parent/child tasks visually adjacent, trading compactness for cleaner separation.  
Key file: src/board.ts
> - `b1cf34d`: Added backlog task-40 to make the bare `backlog board` command default to showing the kanban board (currently requires `backlog board view`). Key file: `.backlog/tasks/task-40 - cli-board-command-defaults-to-view.md`
> - `542a21a`: Subtasks in the terminal kanban board now render with proper tree lines: “└─” for the last child and “|—” for earlier ones, making the hierarchy visually clearer. Updated the logic that decides when to insert blank rows so it recognizes both styles. Key files: src/board.ts, src/test/board.test.ts
> - `f680a85`: Added task-39 to fix the CLI bug where `backlog init` creates empty agent-instruction files instead of populating them with default content; also corrected the id in task-37.  
Key files: `.backlog/tasks/task-39 - cli-fix-empty-agent-instruction-files-on-init.md`, `.backlog/tasks/task-37 - cli-board-view-open-tasks-in-ide.md`
> - `3c2d7b9`: Added backlog task for enhancing the CLI’s `init` command with an interactive checkbox-style agent selector, replacing the current prompt.  
Key file: `.backlog/tasks/task-38 - cli-improved-agent-selection-for-init.md`
> - `7ce2d9d`: Added a new backlog task (Task-37) to make task IDs in the CLI board view clickable links that open the corresponding task file in the user’s IDE, with a `--no-links` flag to disable the feature.  
Key file: `.backlog/tasks/task-37 - cli-board-view-open-tasks-in-ide.md`
> - `a12a199`: Added a backlog task to make `backlog init` interactive: if no project name is supplied, the CLI will now prompt for one instead of failing.  
Key file: `.backlog/tasks/task-36 - cli-prompt-for-project-name-in-init.md`
> - `b227b4d`: Added two new backlog tasks: #33 for adding a CLI command to export milestone boards as roadmaps (with optional custom title), and #35 for finalizing package.json metadata for publishing. Key files: `.backlog/tasks/task-33 - cli-export-milestones-board-as-roadmap.md`, `.backlog/tasks/task-35 - finalize-package.json-metadata-for-publishing.md`.
> - `c67a1d2`: Added two new backlog tasks: Task-33 outlines the steps to finalize package.json metadata (author, repo, bugs, homepage, keywords, license) so the package can be published to npm without warnings, while Task-34 (correctly ID’d) covers splitting the README into user and contributor sections.  
Key files: `.backlog/tasks/task-33 …` and `.backlog/tasks/task-34 …`
> - `79cd78b`: Added a new backlog task (#33) to split the monolithic README into separate user and contributor guides, improving onboarding clarity. Key file: `.backlog/tasks/task-33 - split-readme.md-for-users-and-contributors.md`.
> - `7fb7e6f`: Renamed the Cursor IDE rules file from `cursorrules.md` to `.cursorrules.md` (dot-prefix) so Cursor automatically recognizes it. Updated the import path in `src/guidelines/index.ts` to match the new filename. Key files: `src/guidelines/.cursorrules.md`, `src/guidelines/index.ts`.
