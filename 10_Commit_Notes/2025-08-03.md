---
title: "Commit Notes - 2025-08-03"
date: 2025-08-03
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-08-03

## Backlog.md — Push at 20:34 UTC

**Commits:** 15
**Trigger:** `8cfc231` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `8cfc231` | Create task-223 - Optimize-statistics-dashboard-loading-performance.md | Alex Gavrilescu |
| `14e276c` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `d374668` | TASK-181 - Add statistics dashboard to web UI (#270) | Alex Gavrilescu |
| `0a20acd` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `162731e` | TASK-206 - Order done column by updatedDate in board export (#269) | Alex Gavrilescu |
| `344d5af` | task 212 leftovers | Alex Gavrilescu |
| `a17b30b` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `940f1d9` | TASK-212 - Improve board export UI formatting and readability (#268) | Alex Gavrilescu |
| `8c2dd7e` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `787eec3` | Task 221 add favicon (#267) | Alex Gavrilescu |
| `94d582a` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `150883a` | TASK-211 - Add version number display to browser UI (#266) | Alex Gavrilescu |
| `67d73d8` | Cleanup tasks | Alex Gavrilescu |
| `5351296` | Create task-222 - Improve-task-and-subtask-visualization-in-web-UI.md | Alex Gavrilescu |
| `bb72e41` | Create task-221 - Add-favicon-to-web-interface.md | Alex Gavrilescu |

> **What changed:**
> - `8cfc231`: Created high-priority task to cut statistics-dashboard load time from 15s to <3s via caching, lazy loading, or background processing.  
Key file: backlog/tasks/task-223 - Optimize-statistics-dashboard-loading-performance.md
> - `14e276c`: Updated README to v1.7.0, moving TASK-181 (statistics dashboard) to Done and removing the completed Claude Code integration task from the board.  
Key files: README.md, package.json
> - `d374668`: Added a full-featured statistics dashboard to the web UI: new `/statistics` route shows status & priority charts, completion %, recent activity, and export options, all fed by a new `/api/statistics` endpoint that reuses existing CLI logic. Key files touched: `src/web/components/Statistics.tsx`, `src/server/index.ts`, `src/web/App.tsx`, `src/web/components/SideNavigation.tsx`.
> - `0a20acd`: Updated README board snapshot and bumped version to 1.6.11. Several tasks moved to Done: version display, board export formatting, done-column sorting, favicon, Helix fix, date-time precision, server refactor, and Nix packaging. Key files: README.md, package.json
> - `162731e`: Board export now sorts the Done column by updatedDate (newest first), falling back to createdDate when missing; ties break by descending task ID. Other columns keep their existing ID-based order. Key files: src/board.ts (sorting logic), src/test/board.test.ts (new tests).
> - `344d5af`: Board-export formatting now uses bold uppercase task IDs, bracketed assignees, and italic #labels on their own line; the task-212 ticket is closed. Key files: `src/board.ts` (formatting logic), `backlog/tasks/task-212…md` (status update), `src/server/index.ts` (minor import tweak).
> - `a17b30b`: Updated README board snapshot to v1.6.10 with current task statuses and reformatted task IDs to uppercase; no functional code changes.  
Key files: README.md, package.json
> - `940f1d9`: Board export now renders tasks in a cleaner, more scannable format: task IDs are bold and uppercase, assignees appear in brackets with @-prefixes (only when present), and labels sit on a new italicized line with # prefixes. Empty assignees/labels are omitted entirely, and the same styling applies to both regular and README exports. Key files touched: `src/board.ts`, `src/test/board.test.ts`, `src/test/cli.test.ts`.
> - `8c2dd7e`: Updated README.md and package.json to reflect the latest project status and bump version to 1.6.9. The board snapshot shows task-221 (favicon) moved to Done and the timestamp refreshed. Key files: README.md, package.json.
> - `787eec3`: Added a Backlog.md-branded favicon to the web UI: the server now serves `/favicon.png` with proper headers and the HTML template links to it, so the icon appears in every browser tab.  
Key files: `src/server/index.ts`, `src/web/index.html`, `src/web/favicon.png`.
> - `94d582a`: Updated README to reflect v1.6.8 board status, showing new tasks like improved task/subtask visualization and a completed web UI paste-as-markdown feature. Key files: README.md, package.json.
> - `150883a`: Added a subtle version label (“Backlog.md – v1.6.2”) to the far right of the Settings item in the expanded sidebar; it’s hidden when the sidebar is collapsed and is also injected as a data-version attribute on the document body for quick debugging.  
Key files: src/web/components/SideNavigation.tsx, src/server/index.ts (new /api/version endpoint), src/web/utils/version.ts.
> - `67d73d8`: Archived one pending task and marked twelve completed tasks as done, covering CLI improvements (stats TUI, version export, git hooks bypass), web UI fixes (ordering, kanban descriptions, overflow), editor integrations (Helix), and Claude Code agent fixes. Key files: backlog/archive/tasks/task-172.md and the twelve completed task files.
> - `5351296`: Created backlog task-222 to enhance the web UI’s handling of parent/child tasks: parent cards will show subtask counts and progress, subtasks will nest and collapse, and the board gains toggles for showing/hiding them.  
Key file: backlog/tasks/task-222 - Improve-task-and-subtask-visualization-in-web-UI.md
> - `bb72e41`: Created backlog task #221 to add a favicon to the web UI for better tab recognition and branding. Key file: backlog/tasks/task-221 - Add-favicon-to-web-interface.md
