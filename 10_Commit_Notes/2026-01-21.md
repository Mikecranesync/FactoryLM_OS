---
title: "Commit Notes - 2026-01-21"
date: 2026-01-21
tags: [project/IndustrialSkillsHub, type/commit-note, project/My-Ralph]
status: active
---

# Commit Notes - 2026-01-21

## IndustrialSkillsHub — Push at 00:32 UTC

**Commits:** 1
**Trigger:** `13f815e` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `13f815e` | feat: Fix mobile app WebView loading and Gradle compatibility (#2) | Mikecranesync |

> **What changed:** Fixed the mobile app's WebView to load the production URL directly and keep navigation inside the app, eliminating the need for a local dev server. Updated Gradle to 8.9.1 and wrapper to 9.0 for compatibility. Key files: MainActivity.java, capacitor.config.ts, android/build.gradle.

## My-Ralph — Push at 14:56 UTC

**Commits:** 3
**Trigger:** `5ce9bc8` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5ce9bc8` | fix(analyzer): handle Claude CLI JSON array output format | zerone0x |
| `9b19d70` | feat(structure): migrate Ralph files to .ralph/ subfolder (#109) | Frank Bria |
| `0e95f67` | Add timeout command support for macOS (#108) | Frank Bria |

> **What changed:**
> - `5ce9bc8`: Fixed JSON parsing to handle Claude CLI's array output format (issue #112). The analyzer now detects and extracts the "result" message from `[{type: "system"}, {type: "assistant"}, {type: "result"}]` arrays, normalizing it to the expected object format while preserving session continuity.

**Key files**: `lib/response_analyzer.sh`, `CLAUDE.md`, `tests/unit/test_json_parsing.bats`
> - `9b19d70`: **Ralph v0.10.0** restructures the project by moving all Ralph-specific files into a hidden `.ralph/` subfolder, keeping the root clean and improving tooling compatibility. This is a **breaking change** that requires running `ralph-migrate` on existing projects to relocate configuration files, logs, and state tracking.

**Key files modified:** `ralph_loop.sh`, `migrate_to_ralph_folder.sh`, `setup.sh`, `tests/unit/test_cli_modern.bats`, `templates/PROMPT.md`, `.gitignore`, `README.md`, `CLUUDE.md`
> - `0e95f67`: Added cross-platform timeout support for macOS by creating a `lib/timeout_utils.sh` module that auto-detects and uses `gtimeout` (Homebrew coreutils) on macOS or standard `timeout` on Linux. Updated `install.sh` to check for coreutils, `ralph_loop.sh` to use the new portable timeout wrapper, and documentation to reflect the new macOS dependency (`brew install coreutils`).
