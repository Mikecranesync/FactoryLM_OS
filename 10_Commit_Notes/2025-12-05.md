---
title: "Commit Notes - 2025-12-05"
date: 2025-12-05
tags: [project/Agent-Factory, project/Thefuture, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-05

## Agent-Factory — Push at 22:58 UTC

**Commits:** 4
**Trigger:** `6e0032f` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `6e0032f` | PHASE 2 COMPLETE: Structured Outputs with Pydantic Schemas | Mikecranesync |
| `e00515a` | PHASE 1 COMPLETE: Multi-agent orchestration with comprehensive tests | Mikecranesync |
| `26276ca` | feat: Constitutional system with hybrid documentation | Mikecranesync |
| `2feab12` | CHECKPOINT: Phase 1 orchestration complete | Mikecranesync |

> **What changed:**
> - `6e0032f`: **Summary:** Phase 2 introduces Pydantic schemas for type-safe agent responses. Agents can now specify response schemas (ResearchResponse, CodeResponse, etc.) when created, with automatic validation and parsing in the orchestrator. This maintains backward compatibility while enabling IDE autocomplete and structured data validation.

**Key files:** `agent_factory/schemas/`, `agent_factory/core/agent_factory.py`, `agent_factory/core/orchestrator.py`
> - `e00515a`: **Summary:** Phase 1 of the Agent Factory project is complete with a working multi-agent orchestration system that routes queries via keywords → LLM → fallback. Added comprehensive test coverage (24 tests) and a context-load command to resume sessions from memory files, plus Phase 5 specification for a future digital twin system.

**Key files:** `agent_factory/examples/orchestrator_demo.py`, `docs/PHASE5_SPEC.md`, `.claude/commands/context-load.md`, `tests/test_callbacks.py`
> - `26276ca`: **Summary**: Introduced a constitutional code-generation system where markdown specs drive Python code creation via a factory. The factory parses REQ-* requirements from specs and generates annotated, test-covered modules (callbacks, orchestrator) with PLC-style comments and full traceability.

**Key files**: `factory.py`, `specs/*.md`, `agent_factory/core/{callbacks,orchestrator}.py`, `agentcli`
> - `2feab12`: **Phase 1 orchestration complete** – built a multi-agent router that picks the right specialist agent for each query.  
Hybrid routing first tries a fast keyword match, then falls back to an LLM decision; all moves are logged to a pub/sub EventBus for observability.  
Key files: `orchestrator.py` (router), `callbacks.py` (EventBus), `agent_factory.py` (factory method).

## Thefuture — Push at 21:07 UTC

**Commits:** 3
**Trigger:** `dabaea9` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `dabaea9` | fix: Platform-agnostic PAI_DIR placeholder for cross-platform installs | Daniel Miessler |
| `79913df` | docs: Update README and QUICKSTART for v0.9.1 patch release | Daniel Miessler |
| `ba76c8a` | fix: setup.sh now automatically configures PAI_DIR in settings.json | Daniel Miessler |

> **What changed:**
> - `dabaea9`: Fixed cross-platform installation failures by replacing macOS-specific `/Users/YOURNAME/.claude` placeholder with platform-agnostic `__HOME__/.claude` that resolves to the actual home directory on any OS. Added robust home directory detection using `${HOME:-$(eval echo ~)}` to handle edge cases where `$HOME` is unset.

**Files modified:** `.claude/settings.json`, `.claude/setup.sh`, `PAI_CONTRACT.md`, `PAI_SYNC_GUIDE.md`, `README.md`, `docs/QUICKSTART.md`
> - `79913df`: Updated documentation for v0.9.1 patch release that fixes PAI_DIR auto-configuration in setup.sh. Added troubleshooting guide for "YOURNAME" hook errors and updated version badge to 0.9.1.

**Files modified:** README.md, docs/QUICKSTART.md
> - `ba76c8a`: Fixed setup.sh to automatically configure PAI_DIR with the actual home directory path instead of leaving the placeholder "/Users/YOURNAME/.claude" that was causing hook failures. The script now copies all configuration files and updates the path in settings.json during installation.

**Key files:** `.claude/setup.sh`, `.claude/settings.json`

## cmms — Push at 13:41 UTC

**Commits:** 4
**Trigger:** `1e931f6` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1e931f6` | feat: improve footer and cta | Ibrahima G. Coulibaly |
| `f3f8b50` | chore: improve navbar | Ibrahima G. Coulibaly |
| `0c1d859` | chore: improve navbar | Ibrahima G. Coulibaly |
| `c3324dc` | feat: demo link | Ibrahima G. Coulibaly |

> **What changed:**
> - `1e931f6`: Updated the site-wide footer and the CTA section on the pricing page; both now use a cleaner layout and consistent branding.  
Key files: `Footer/index.tsx`, `SubscriptionPlanSelector.tsx`, plus dependency bumps in `package*.json`.
> - `f3f8b50`: Increased the navbar spacer height to prevent content overlap and replaced the demo button’s JS redirect with a native `href` for better UX and SEO.  
Key file: `frontend/src/components/NavBar/index.tsx`
> - `0c1d859`: Made the navbar sticky (fixed to top) and added a spacer so page content no longer slides under it; also cleaned up responsive breakpoints.  
Key file: frontend/src/components/NavBar/index.tsx
> - `c3324dc`: Added a configurable “Book Demo” button to the NavBar that opens an external demo link.  
Key files: frontend/src/components/NavBar/index.tsx, frontend/src/config.ts, plus env/docker-compose plumbing for DEMO_LINK.
