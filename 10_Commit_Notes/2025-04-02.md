---
title: "Commit Notes - 2025-04-02"
date: 2025-04-02
tags: [project/Archon, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-02

## Archon — Push at 23:52 UTC

**Commits:** 2
**Trigger:** `a16c09e` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `a16c09e` | Fixing up f strings for agent prompts | Cole Medin |
| `efa348f` | Fix issue #67: MCP container could not resolve host 'host.docker.internal' on linux (#82) | BarelElbaz |

> **What changed:**
> - `a16c09e`: Cleaned up f-string formatting in agent system prompts by removing stray `\n` escapes and replacing them with literal newlines for cleaner prompt text.  
Files touched: `advisor_agent.py`, `pydantic_ai_coder.py`, `tools_refiner_agent.py` (and their v6-tool-library-integration copies).
> - `efa348f`: Fixed Docker networking issue on Linux by adding `--add-host=host.docker.internal:host-gateway` to ensure containers can reach the host machine. Updated both the Docker run command for Cursor IDE and the shared docker_config template.

Key file: streamlit_pages/mcp.py

## cmms — Push at 22:49 UTC

**Commits:** 9
**Trigger:** `ec099a8` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `ec099a8` | chore: login automatically on signup if invited | Ibrahima G. Coulibaly |
| `348f36a` | Merge branch 'main' of https://github.com/Grashjs/cmms | Ibrahima G. Coulibaly |
| `a700dfa` | fix: coming wo notifications sent on patch trigger | Ibrahima G. Coulibaly |
| `30e64ee` | Merge pull request #21 from peanutyost/main | Ibrahima G. Coulibaly |
| `176c37e` | Update README.MD | peanutyost |
| `f798e09` | Update README.MD | peanutyost |
| `7acd335` | Rename .env to .env.example | peanutyost |
| `bbc689d` | Create .gitignore | peanutyost |
| `a200373` | chore: send mails to admins for PMs | Ibrahima G. Coulibaly |

> **What changed:**
> - `ec099a8`: Automatically logs the user in after signup when they’ve been invited to a company, instead of leaving them on the disabled list.  
Key file: `api/src/main/java/com/grash/service/UserService.java`
> - `348f36a`: Merge branch 'main' of https://github.com/Grashjs/cmms. Files: `.env.example`, `.gitignore`, `README.MD`.
> - `a700dfa`: Fixed a bug where coming-work-order notifications were sent immediately instead of waiting until the scheduled trigger date. The timer now correctly calculates the next occurrence after the notification lead time.  
Key file: `ScheduleService.java`
> - `30e64ee`: Merge pull request #21 from peanutyost/main. Files: `.env.example`, `.gitignore`, `README.MD`.
> - `176c37e`: Fixed the broken link in the setup instructions so it now correctly points to `.env.example` instead of the non-existent `.env` file.  
Key file: README.MD
> - `f798e09`: Clarified setup instructions: users must now copy `.env.example` to `.env` before editing, replacing the outdated reference to a non-existent `.env` file.  
Key file: `README.MD`
> - `7acd335`: Renamed `.env` to `.env.example` to provide a template for environment setup; no functional code changed.  
Key file: `.env.example`
> - `bbc689d`: Added a `.gitignore` file to prevent the `.env` file (likely containing sensitive environment variables) from being tracked by Git. This keeps secrets out of version control.

Key file: `.gitignore`
> - `a200373`: Added logic to automatically include company admins (users with SETTINGS permission) in the email notification list for upcoming preventive-maintenance work orders, ensuring they stay informed even if not explicitly assigned.

Key file: api/src/main/java/com/grash/service/ScheduleService.java
