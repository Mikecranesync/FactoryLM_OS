---
title: "Commit Notes - 2024-08-23"
date: 2024-08-23
tags: [project/frame_realtime_gemini_voicevision, type/commit-note]
status: active
---

# Commit Notes - 2024-08-23

## frame_realtime_gemini_voicevision — Push at 11:32 UTC

**Commits:** 2
**Trigger:** `b7956e1` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b7956e1` | added \|navigation to android:configChanges | CitizenOneX |
| `9c7cab2` | updated brilliant bluetooth | CitizenOneX |

> **What changed:**
> - `b7956e1`: Added `navigation` to the `android:configChanges` list so the app won’t restart when the system navigation type changes (e.g., gesture vs. 3-button). Keeps Flutter state intact during those transitions.  
Key file: `android/app/src/main/AndroidManifest.xml`
> - `9c7cab2`: Added two new BLE send methods: `sendDataRaw()` for zero-copy writes when caller supplies the 0x01 header, and `sendMessage()` that auto-chunks large payloads (≤64 kB) into a custom 0x01-flagged protocol so Frame can reassemble them.  
Key file: lib/brilliant_bluetooth.dart
