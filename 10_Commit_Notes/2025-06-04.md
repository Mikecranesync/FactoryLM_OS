---
title: "Commit Notes - 2025-06-04"
date: 2025-06-04
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-06-04

## cmms — Push at 16:03 UTC

**Commits:** 4
**Trigger:** `c56a393` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c56a393` | chore: remove `hasChildren` field from `Asset` and `Location` entities and drop corresponding columns | Ibrahima G. Coulibaly |
| `f592e26` | fix: logo selection logic in email templates to check for both null and empty values | Ibrahima G. Coulibaly |
| `1ec2f1f` | fix: logo selection logic in email templates to check for both null and empty values | Ibrahima G. Coulibaly |
| `02fa6e2` | refactor: logo rendering logic for dynamic branding in email templates | Ibrahima G. Coulibaly |

> **What changed:**
> - `c56a393`: Removed the unused `hasChildren` boolean from Asset and Location entities and dropped the corresponding `has_children` columns via Liquibase migration, simplifying the data model and keeping code and schema in sync.  
Key files: Asset.java, Location.java, 2025_06_04_1749052788_drop_has_children.xml
> - `f592e26`: Fixed email logo selection by switching to `#strings.isEmpty()` to safely handle both null and empty white-label logo paths, preventing broken images in branded emails.  
Files: `main-layout.html`, `work-order-report.html`
> - `1ec2f1f`: Fixed email logo logic to check both null and empty for `white-labeling.logo-paths`, preventing template errors when the property is undefined.  
Key files: `main-layout.html`, `work-order-report.html`
> - `02fa6e2`: Email templates now switch logos only when the white-label path list is non-empty, fixing cases where an empty list was incorrectly treated as a custom brand.  
Key files: `main-layout.html`, `work-order-report.html`
