---
title: "Commit Notes - 2025-02-28"
date: 2025-02-28
tags: [project/Archon, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-02-28

## Archon — Push at 20:03 UTC

**Commits:** 3
**Trigger:** `53cfd5e` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `53cfd5e` | Self Agent Execution version plans | Cole Medin |
| `a8f6b10` | Future enhancements section | Cole Medin |
| `ee5d70c` | Docker support for Archon V4 | Cole Medin |

> **What changed:**
> - `53cfd5e`: Inserted “V9: Self Agent Execution” into the roadmap, shifting later milestones down one slot.  
Key files: README.md (roadmap list) and future_enhancements.py (detailed Streamlit expander).  
Impact: Archon will soon sandbox-test generated agents locally before release, improving reliability.
> - `a8f6b10`: Added a new “Future Enhancements” tab in the Streamlit UI that walks through the next 8 roadmap versions (V5–V12) with collapsible cards, diagrams, and code snippets.  
Updated the README roadmap and bumped the theme color; key files touched: future_enhancements.py (new), streamlit_ui.py, README.md, .streamlit/config.toml.
> - `ee5d70c`: Added full Docker support to Archon V4: a slim Python 3.12 image runs the Streamlit UI on port 8501 and the Archon service on 8100, while a second container hosts the MCP server. A new `run_docker.py` script orchestrates the multi-container build/start, letting users skip local Python setup. Key files touched: root `Dockerfile`, `run_docker.py`, `README.md`, plus identical Docker assets under `iterations/v4-streamlit-ui-overhaul/` and `mcp/`.

## cmms — Push at 02:43 UTC

**Commits:** 1
**Trigger:** `893c5b2` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `893c5b2` | feat: minio init | Ibrahima G. Coulibaly |

> **What changed:** Added MinIO as a pluggable storage backend.  
Introduced `STORAGE_TYPE=MINIO` env var and a factory that switches between GCP and MinIO; default switched to MinIO.  
Key files: `docker-compose.yml`, `StorageServiceFactory.java`, `MinioService.java`, `application.yml`, `.env`.
