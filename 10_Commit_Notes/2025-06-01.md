---
title: "Commit Notes - 2025-06-01"
date: 2025-06-01
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-06-01

## cmms — Push at 20:36 UTC

**Commits:** 14
**Trigger:** `8a23cb1` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `8a23cb1` | fix: formatting in Terms of Service: add spaces before links and adjust line breaks | Ibrahima G. Coulibaly |
| `413e2a8` | Merge pull request #51 from Grashjs/white-labeling | Ibrahima G. Coulibaly |
| `c6708aa` | docs: add reference to SQL command guide in subscription plan configuration docs | Ibrahima G. Coulibaly |
| `df238ee` | docs: add developer documentation for configuring subscription plan pricing | Ibrahima G. Coulibaly |
| `ab6e972` | fix: remove unused license slice extraReducer and minor cleanup in LogoSign | Ibrahima G. Coulibaly |
| `0ccd478` | refactor: legal documents to conditionally show Intelloop/Moroccan law references for original cloud only | Ibrahima G. Coulibaly |
| `2da49df` | refactor: use dynamic brand logo for favicon based on brand configuration | Ibrahima G. Coulibaly |
| `42d9650` | refactor: replace hardcoded "Atlas" references with dynamic brand variables | Ibrahima G. Coulibaly |
| `2622720` | refactor: update branding references for dynamic brand support and test data consistency | Ibrahima G. Coulibaly |
| `2f0d5f3` | refactor: notification message properties for dynamic branding support | Ibrahima G. Coulibaly |
| `32985d7` | refactor: notification email templates to use shared branding and layout | Ibrahima G. Coulibaly |
| `87911da` | refactor: unify notification email templates to use shared main layout fragment | Ibrahima G. Coulibaly |
| `42e17ac` | feat: add `BRAND_CONFIG` env to docker-compose and update overview page to use license validity from store | Ibrahima G. Coulibaly |
| `0ceed57` | refactor: translations to use dynamic brand variables instead of hardcoded "Atlas" | Ibrahima G. Coulibaly |

> **What changed:**
> - `8a23cb1`: Fixed spacing and line breaks in the Terms of Service component to prevent URLs from running into surrounding text, improving readability without altering content or logic.  
Key file: frontend/src/content/terms-of-service/index.tsx
> - `413e2a8`: Merge pull request #51 from Grashjs/white-labeling. Files: `.env.example`, `.gitignore`, `.idea/inspectionProfiles/Project_Default.xml`, `README.MD`, `api/.github/ISSUE_TEMPLATE/bug_report.md` (+96 more).
> - `c6708aa`: Added a link to the SQL command guide so white-label licensees who don’t know how to run SQL against the Atlas database can quickly find the prerequisite steps.  
Key file: dev-docs/Configure subscription plans.md
> - `df238ee`: Added a developer doc (`dev-docs/Configure subscription plans.md`) that shows how to set custom pricing for white-labeled deployments by running the provided SQL updates against the `subscription_plan` table and verifying the changes.
> - `ab6e972`: Removed dead code: deleted the unused `extraReducers` block in the license slice that was listening for the never-dispatched `revertAll` action, and cleaned up a stray blank line in LogoSign.  
Key files: `frontend/src/slices/license.ts`, `frontend/src/components/LogoSign/index.tsx`
> - `0ccd478`: Added `IS_ORIGINAL_CLOUD` flag so Privacy Policy & Terms-of-Service only mention Intelloop s.a.r.l and Moroccan law for the original cloud deployment; white-labeled/self-hosted builds now show generic legal text.  
Key files: `frontend/src/content/privacyPolicy/index.tsx`, `frontend/src/content/terms-of-service/index.tsx`, `api/.gitignore`
> - `2da49df`: Favicon now dynamically switches to the brand’s dark logo when white-labeling is on and the license is valid, replacing the previous hard-coded icon.  
Key file: frontend/src/App.tsx
> - `42d9650`: Replaced every hard-coded “Atlas” string in UI, legal pages, translations, and Redux with dynamic variables from `brandConfig` (`name`, `website`, `{{shortBrandName}}`) so the app can be white-labeled without recompiling.  
Key files: `App.tsx`, `LogoSign/index.tsx`, `Status500`, `privacyPolicy`, `terms-of-service`, German translations, `license.ts`.
> - `2622720`: Replaced hard-coded brand names in emails, API docs, and OAuth flows with dynamic values from BrandingService so the app can be white-labeled at runtime.  
Also standardized test data emails to “testcmms” accounts across all language import templates and deleted obsolete GitHub workflows/templates.

Key files: EmailService2.java, SwaggerConfig.java, OAuth2AuthenticationSuccessHandler.java, all import-template CSVs
> - `2f0d5f3`: Replaced hard-coded brand name “Grash” with parameterized placeholders like `{0}` in all localized mail/message bundles, letting the app inject the correct brand at runtime.  
**Files touched:** every `mailMessages_*.properties` and `messages_*.properties` under `api/src/main/resources/`.
> - `32985d7`: All notification emails now pull brand name, colors and contact details from a single `BrandConfig` bean, letting the same deployment white-label itself by changing only the YAML config.  
Key files: `BrandingService.java`, `BrandConfig.java`, `EmailService2.java`, plus the Thymeleaf templates (`invite.html`, `reset-password.html`, `main-layout.html`).
> - `87911da`: All notification emails now share a single `main-layout` Thymeleaf fragment, guaranteeing the same header, footer and dynamic background color everywhere. A new `BrandingService` centralizes the color logic, eliminating duplication in controllers and `EmailService2`. Key files: `BrandingService.java`, `EmailService2.java`, `main-layout.html`, and every email template under `templates/`.
> - `42e17ac`: Added `BRAND_CONFIG` env var to docker-compose for runtime white-labeling and updated the overview page to check Redux store `isLicenseValid` before allowing access, replacing the hard-coded cloud check.  
Key files: docker-compose.yml, frontend/src/content/overview/index.tsx
> - `0ceed57`: Replaced hard-coded “Atlas” with dynamic `{{shortBrandName}}` / `{{brandName}}` placeholders in all translation files, enabling white-labeling without touching UI code.  
Key files: frontend/src/i18n/translations/*.ts (ar, de, es, fr, pl, pt_BR, tr)
