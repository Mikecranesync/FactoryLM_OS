---
title: "Commit Notes - 2025-04-22"
date: 2025-04-22
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-22

## cmms — Push at 20:17 UTC

**Commits:** 4
**Trigger:** `92c7828` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `92c7828` | chore: cache analytics controller | Ibrahima G. Coulibaly |
| `1f7b1ac` | chore: use caffeine cache and cache asset analytics controller | Ibrahima G. Coulibaly |
| `7274b1e` | chore: cache analytics endpoints and show login page if not cloud | Ibrahima G. Coulibaly |
| `58eb517` | chore: remove restart unless-stopped | Ibrahima G. Coulibaly |

> **What changed:**
> - `92c7828`: Added `@Cacheable` to all analytics endpoints in the four analytics controllers (Part, Request, User, WO).  
These endpoints now cache results keyed by user ID and date range, cutting repeated database hits and speeding up dashboard loads.
> - `1f7b1ac`: Added Caffeine caching to all analytics controllers (Asset, Part, Request, User, WO) and enabled Spring’s @Cacheable on their heavy date-range queries.  
Key files: AssetAnalyticsController.java, ApiApplication.java, pom.xml, CacheKeyUtils.java
> - `7274b1e`: Added HTTP caching headers to all analytics endpoints (Asset, Part, Request, User, WO) via Helper.withCache() to reduce repeated queries.  
Frontend now shows a login page instead of the overview when the instance is not cloud-hosted.  
Key files: the five analytics controllers and frontend/src/content/overview/index.tsx
> - `58eb517`: Removed the `restart: unless-stopped` policy from all four services (postgres, api, frontend, minio) in docker-compose.yml. Containers will now stay stopped if manually halted, giving tighter control during local development and debugging.
