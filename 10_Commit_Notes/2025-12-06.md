---
title: "Commit Notes - 2025-12-06"
date: 2025-12-06
tags: [project/Agent-Factory, project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-06

## Agent-Factory — Push at 23:51 UTC

**Commits:** 15
**Trigger:** `d72d929` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `d72d929` | feat: Phase 3 complete - Spec to Agent Generation Pipeline | Mikecranesync |
| `97dd833` | feat: GitHub integration - create agents from issues | Mikecranesync |
| `85c404b` | docs: Update progress tracking and create memory files for Phase 0-2 | Mikecranesync |
| `f3bbb8d` | feat: Phase 2 complete - PLC-style heavy commenting (40% density) | Mikecranesync |
| `f2b5481` | feat: Phase 1 Complete - AGENTS.md Constitution (Spec-First Foundation) | Mikecranesync |
| `25b3567` | feat: Phase 0 Complete - OpenHands Integration (Dec 15th Deadline Met) | Mikecranesync |
| `0365835` | feat: Add GitHub Actions integration for @claude mentions | Mikecranesync |
| `b4d6d59` | feat: Add interactive agent builder CLI | Mikecranesync |
| `a92ee7c` | docs: Create memory system for session continuity | Mikecranesync |
| `4810b37` | feat: Phase 5 complete - Project Twin digital codebase mirror | Mikecranesync |
| `0931ea3` | docs: Update memory files with Phase 4 completion | Mikecranesync |
| `855569d` | feat: Phase 4 complete - Deterministic tools with safety & caching | Mikecranesync |
| `c8a784f` | docs: Update PROGRESS.md with factory testing completion | Mikecranesync |
| `280c574` | feat: Add comprehensive AgentFactory tests (22 tests) | Mikecranesync |
| `1f778f1` | PHASE 3 COMPLETE: Production Observability System | Mikecranesync |

> **What changed:**
> - `d72d929`: **Summary:** Completed Phase 3 - the spec-to-agent generation pipeline that converts markdown specifications into deployable LangChain agents. The system parses structured specs, generates complete agent code with anti-sycophancy tests, and maintains the philosophy that "specs are eternal, code is ephemeral."

**Key files:** `agent_factory/codegen/spec_parser.py`, `agent_factory/codegen/code_generator.py`, `agent_factory/codegen/eval_generator.py`, `agent_factory/examples/codegen_demo.py`

**Impact:** Enables rapid agent development by treating specifications as the source of truth - change a spec, regenerate the code instantly. All 193 tests passing with industrial-grade commenting standards.
> - `97dd833`: **Summary:** Added GitHub integration to automatically create agents from GitHub issues via new CLI commands (`github-create`, `github-init`, `github-sync`). The system parses both template-based and freeform issues to extract agent configurations, with 31 new tests added and all 193 tests passing.

**Key files modified:** `agent_factory/cli.py`, `agent_factory/github/github_client.py`, `agent_factory/github/issue_parser.py`, `tests/test_github.py`, `pyproject.toml`
> - `85c404b`: **Summary:**  
Project documentation updated to reflect completion of Phases 0-2: OpenHands integration ($200/mo savings), AGENTS.md constitution (10 Articles), and PLC-style heavy commenting (40% density, 162 tests passing). Created `.memory/` files for session restoration, task tracking, and chronological history.

**Key files:**  
PROGRESS.md, .memory/PROJECT_CONTEXT.md, .memory/NEXT_ACTIONS.md, .memory/DEVELOPMENT_LOG.md
> - `f3bbb8d`: Added comprehensive PLC-style documentation to core modules—40% comment density achieved per AGENTS.md Article IV. Enhanced orchestrator, callbacks, and schema files with PURPOSE/WHY/PLC-ANALOGY blocks for every class/method, turning the codebase into self-documenting “ladder logic.” No functional changes; all 162 tests still pass.
> - `f2b5481`: **Summary:** Established the foundational AGENTS.md constitution and spec template, enforcing a spec-first development philosophy where specifications are eternal and code is ephemeral. The system now mandates anti-sycophancy protocols, quality gates with 95% eval compliance, and a 6-agent swarm architecture targeting $10K MRR.

**Key files:** AGENTS.md, specs/template.md
> - `25b3567`: **OpenHands Integration Complete** - Successfully integrated OpenHands autonomous coding agent before the Dec 15th deadline, avoiding a $200/month subscription fee. The integration adds a new `create_openhands_agent()` factory method that spawns Docker-contained AI coders capable of writing, testing, and debugging code autonomously.

**Key Files Modified:** `agent_factory/core/agent_factory.py` (added factory method), `agent_factory/workers/openhands_worker.py` (580-line worker implementation), `agent_factory/examples/openhands_demo.py` (comprehensive demos), `agent_factory/workers/__init__.py` (module exports)
> - `0365835`: Added GitHub Actions integration that lets users summon Claude via @claude mentions in issues/PRs or by applying a 'claude' label. The workflow triggers Claude to analyze the repo and respond with code insights, reviews, or changes. Requires adding ANTHROPIC_API_KEY to repository secrets.

Key files: `.github/workflows/claude.yml`, `.github/GITHUB_SETUP.md`
> - `b4d6d59`: Added interactive agent builder CLI with `agentcli create` command for creating custom agents via step-by-step prompts. Users can now configure agents with specific tools (research/coding/file/twin), LLM providers (OpenAI/Anthropic/Google), and save/load configs from `~/.agent_factory/agents/`. The REPL was updated to load these custom agents alongside built-ins. Key files: `agent_factory/cli.py`, `agent_factory/cli_builder.py`, `agent_factory/examples/agent_builder_demo.py`.
> - `a92ee7c`: Created a memory system to preserve project state across sessions. The system tracks project context, next actions, development history, issues, and architectural decisions in five markdown files under `docs/memory/`. This enables seamless session continuity with `/context-load` after context clears.
> - `4810b37`: **Project Twin digital codebase mirror completed** – adds a full digital twin system that mirrors the entire codebase structure with AST-based analysis, dependency graphs, and natural language querying. The system can automatically scan Python files, infer semantic purposes, track dependencies, and answer questions like “Where is AgentFactory defined?” using an LLM interface.

**Key files:** `agent_factory/refs/project_twin.py`, `agent_factory/refs/twin_agent.py`, `agent_factory/refs/code_analyzer.py`, `agent_factory/refs/knowledge_graph.py`, `agent_factory/examples/twin_demo.py`
> - `0931ea3`: Phase 4 wrapped: all 27 file tools now pass tests after fixing Pydantic v1 instantiation issues by moving validators into `_run()`. Added 10 MB file-size guardrails and atomic writes via temp-file renames.

Key files: `DECISIONS_LOG.md`, `DEVELOPMENT_LOG.md`, `ISSUES_LOG.md`, `NEXT_ACTIONS.md`, `PROJECT_CONTEXT.md`
> - `855569d`: Phase 4 wraps with production-grade file tools: safe read/write/list/search operations guarded against path-traversal and oversized files, plus an in-memory TTL cache (LRU eviction, @cached_tool decorator).  
Key files touched: agent_factory/tools/file_tools.py, agent_factory/tools/cache.py, agent_factory/tools/validators.py, tests/test_file_tools.py, tests/test_cache.py.
> - `c8a784f`: Factory testing is now complete with 22 new tests added, bringing the total to 92 passing tests. The factory module now has comprehensive test coverage for initialization, agent creation, LLM configuration, orchestrator creation, integration, error handling, and metadata storage.

**Key file modified:** PROGRESS.md
> - `280c574`: Added comprehensive test suite for AgentFactory with 22 new tests covering factory initialization, agent creation, LLM configuration, orchestrator creation, workflows, error handling, and metadata storage. This brings total test coverage to 92 tests across all components, making the factory module production-ready.

Key files: tests/test_factory.py
> - `1f778f1`: **Phase 3 Complete: Production Observability System**

Added comprehensive monitoring with request tracing, performance metrics, and cost tracking. Every request now gets a unique trace_id with spans for debugging, plus real-time metrics (latency percentiles, success rates) and cost breakdowns by provider/agent. Integrated into orchestrator via `enable_observability=True` flag with <5ms overhead.

**Key files:** `agent_factory/observability/` (tracer.py, metrics.py, cost_tracker.py), `agent_factory/core/orchestrator.py`

## Backlog.md — Push at 21:26 UTC

**Commits:** 4
**Trigger:** `ba3a349` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `ba3a349` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `f8474b2` | TASK-335 - Fix CLI to find Backlog.md root from subfolders (#450) | Alex Gavrilescu |
| `f18f8b9` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `20e8a47` | TASK-336 - Rollback GitHub workflows to Bun 1.2.x to avoid websocket CPU regression (#451) | Alex Gavrilescu |

> **What changed:**
> - `ba3a349`: Bumped version to 1.26.4 and refreshed the auto-generated project-status table in the README with the latest timestamp.  
Key files: README.md, package.json
> - `f8474b2`: Fixed CLI so commands run from any repo subfolder now correctly locate and use the Backlog.md project root instead of spawning a new backlog/ directory. Added `findBacklogRoot()` utility that walks up the tree looking for backlog.json or backlog/ and falls back to git root, plus a `requireProjectRoot()` helper that exits with “No Backlog.md project found. Run backlog init to initialize.” when nothing is found.

Key files: src/utils/find-backlog-root.ts, src/cli.ts
> - `f18f8b9`: README.md refreshed to show v1.26.3 board snapshot (2025-12-06) with latest task moves—TASK-336 now Done, TASK-335 In Progress, and several new items added.  
package.json bumped to 1.26.3 to match the documented release.
> - `20e8a47`: Pinned CI to Bun 1.2.23 to dodge the 1.3.x websocket CPU spike that was maxing out macOS machines. All GitHub workflows (ci.yml, release.yml) now lock to this version and cache keys follow suit. Added task note documenting the temporary rollback.

## cmms — Push at 21:51 UTC

**Commits:** 14
**Trigger:** `3e3f19f` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `3e3f19f` | chore: take into account schedule new columns | Ibrahima G. Coulibaly |
| `aba5aee` | chore: schedule new columns | Ibrahima G. Coulibaly |
| `2a75ae7` | refactor: use quartz | Ibrahima G. Coulibaly |
| `42a4779` | Merge pull request #116 from Grashjs/signature | Ibrahima G. Coulibaly |
| `7b7e5f1` | feat: manual signature | Ibrahima G. Coulibaly |
| `84aebb6` | Merge pull request #95 from Grashjs/configure-ios | Ibrahima G. Coulibaly |
| `150cacb` | Merge branch 'main' of https://github.com/Grashjs/cmms into configure-ios | Ibrahima G. Coulibaly |
| `d932396` | fix: tsc | Ibrahima G. Coulibaly |
| `e04c9fa` | feat: use DropDownSheet.tsx in task | Ibrahima G. Coulibaly |
| `a371774` | fix: misc | Ibrahima G. Coulibaly |
| `4c854e6` | fix: spinner size | Ibrahima G. Coulibaly |
| `24cf97f` | chore: improve pages layouts | Ibrahima G. Coulibaly |
| `83d7bab` | chore: improve landing | Ibrahima G. Coulibaly |
| `49cf2a6` | fix: referrer | Ibrahima G. Coulibaly |

> **What changed:**
> - `3e3f19f`: Updated scheduling logic to respect new DB columns (`recurrenceBasedOn`, `daysOfWeek`, etc.). When a work-order tied to a PM schedule is completed, the next occurrence is now queued via `scheduleNextWorkOrderJobAfterCompletion`. Key files: `WorkOrderController.java`, `ScheduleService.java`.
> - `aba5aee`: Added recurrence support to the Schedule entity: new columns for recurrence type (daily/weekly/monthly/yearly), whether recurrence is based on scheduled or completed date, and a list of weekday numbers.  
Key files: Schedule.java, RecurrenceType.java, RecurrenceBasedOn.java, 2025_12_06_1765053020_add_schedule_recurrence.xml
> - `2a75ae7`: Switched from Java TimerTask to Quartz-scheduled jobs for preventive-maintenance reminders and work-order creation.  
New QuartzJobBean classes now handle the same logic (notify users, create WO from PM) but run inside the Quartz scheduler, giving better visibility, clustering, and cron control.  

Files touched: PreventiveMaintenanceNotificationJob.java, WorkOrderCreationJob.java, ScheduleService.java
> - `42a4779`: Merge pull request #116 from Grashjs/signature. Files: `api/src/main/java/com/grash/dto/WorkOrderChangeStatusDTO.java`, `api/src/main/java/com/grash/dto/WorkOrderShowDTO.java`, `api/src/main/java/com/grash/model/WorkOrder.java`, `api/src/main/java/com/grash/model/envers/WorkOrderAud.java`, `api/src/main/resources/db/changelog/2025_12_06_1765045582_signature.xml` (+22 more).
> - `7b7e5f1`: Replaced the WorkOrder signature from a File entity to a plain text field so users can now draw a manual signature on both web and mobile. Added a SignaturePad component and database migration to store the signature as text instead of a file reference.

Key files: WorkOrder.java, WorkOrderShowDTO.java, SignaturePad.tsx (web + mobile), 2025_12_06_1765045582_signature.xml
> - `84aebb6`: Merge pull request #95 from Grashjs/configure-ios. Files: `mobile/App.tsx`, `mobile/android/.gitignore`, `mobile/android/app/BUCK`, `mobile/android/app/build.gradle`, `mobile/android/app/build_defs.bzl` (+111 more).
> - `150cacb`: Merge branch 'main' of https://github.com/Grashjs/cmms into configure-ios. Files: `.env.example`, `api/scripts/generate-liquibase.js`, `api/src/main/java/com/grash/controller/SubscriptionController.java`, `api/src/main/java/com/grash/dto/UserSignupRequest.java`, `api/src/main/java/com/grash/dto/UtmParams.java` (+43 more).
> - `d932396`: Fixed TypeScript compilation errors across the mobile app by updating import paths for `IconSource`, correcting navigation parameter types, and aligning type references (`RootStackParamList` → `RootParamList`). Key files touched: `App.tsx`, `RootNavigation.ts`, and most component files under `mobile/components/` and `mobile/screens/`.
> - `e04c9fa`: Replaced the inline dropdown picker in SingleTask with a reusable DropDownSheet action sheet, triggered by a read-only TextInput. This unifies the UI and removes z-index issues on mobile. Key files: SingleTask.tsx, DropDownSheet.tsx, sheets.tsx.
> - `a371774`: Fixed Android file-upload permissions and iOS encryption flag, and updated the audio-recorder package.  
Key files: mobile/components/FileUpload.tsx, mobile/app.config.ts, mobile/package.json
> - `4c854e6`: Fixed an oversized loading spinner on the work-order status selector by setting it to 2rem.  
Key file: frontend/src/content/own/WorkOrders/Details/WorkOrderDetails.tsx
> - `24cf97f`: Simplified layout markup across asset, work-order, and other “own” pages by replacing nested Grid wrappers with flatter Box components and removing redundant spacing props; no user-visible behavior changed.  
Key files: `Assets/index.tsx`, `WorkOrders/index.tsx`, and seven other own-section pages plus the pricing plan selector.
> - `83d7bab`: Landing page CTA buttons now stack vertically on mobile and sit side-by-side on desktop via MUI Stack; register carousel auto-advances every 2s and has tighter vertical padding.  
Key files: frontend/src/content/overview/Hero/index.tsx, frontend/src/content/pages/Auth/Register/Cover/index.tsx
> - `49cf2a6`: Fixed a bug where the referrer data wasn’t being sent in the correct shape during signup; it’s now nested inside `utmParams` instead of as a top-level field.  
Key file changed: `frontend/src/contexts/JWTAuthContext.tsx`
