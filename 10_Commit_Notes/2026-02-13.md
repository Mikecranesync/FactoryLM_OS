---
title: "Commit Notes - 2026-02-13"
date: 2026-02-13
tags: [project/voltron, type/commit-note]
status: active
---

# Commit Notes - 2026-02-13

## voltron — Push at 09:49 UTC

**Commits:** 9
**Trigger:** `ccd62d1` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `ccd62d1` | Phase 3: three HMIs — mobile web dashboard, Telegram bot, chat client + query API | Mikecranesync |
| `19f5d46` | feat: Tailscale connectivity — inventory, security model, check script, host config | Mikecranesync |
| `4ea281e` | feat: twin fanout wiring, OS-agnostic validation, dummy adapter, investor demo script | Mikecranesync |
| `5217bfb` | feat: First Principles Voltron — OS/platform-agnostic architecture, twin interfaces, Langfuse-style agent observability | Mikecranesync |
| `64f560b` | Phase 2.5: observability, CI/CD, metrics endpoint, investor proof pack, README overhaul | Mikecranesync |
| `e9e8355` | docs: step-by-step guide to run Matrix locally with Postgres + Tailscale forwarder | Mikecranesync |
| `21ad22f` | Phase 2: Matrix ingestion API + Postgres schema + forwarder integration | Mikecranesync |
| `01d7435` | feat: phase-1.5 — forwarder service, v2 layout, gateway docs, ingestion API spec | Mikecranesync |
| `a5df9e3` | feat: complete phase-1 — node runtime, PLC driver, policy engine, storage, Windows installer scaffold | Mikecranesync |

> **What changed:**
> - `ccd62d1`: **Summary**  
Introduced three new human-machine interfaces: a mobile-friendly web dashboard, a Telegram bot, and a simple chat client, plus a query API. The chat client (`/chat`) answers plain-text questions about system status, PLC tags, and events using keyword matching; the dashboard and bot surfaces the same live data in their respective UIs.

**Key files**  
`matrix/api/chat.py`, `matrix/dashboard/`, `matrix/bot/`, `matrix/api/query.py`, `matrix/api/main.py`
> - `19f5d46`: Added a Tailscale inventory (`config/tailscale_hosts.yaml`) and connectivity checker (`scripts/tailscale/check_connectivity.py`) so Voltron/Matrix nodes can verify mesh health and reach the Matrix API at `100.72.2.99:8000`.  
Docs spell out the security model: API is exposed, Postgres and PLC Modbus stay local-only.  
Key files: `config/tailscale_hosts.yaml`, `scripts/tailscale/check_connectivity.py`, `docs/tailscale_connectivity.md`.
> - `4ea281e`: **Summary**  
Added optional “twin fan-out” so the Matrix API can mirror every ingested sample to an external system (Factory IO, ThingWorx, etc.) via a pluggable adapter; ships with a no-op dummy adapter.  
Introduced OS-agnostic path validation and a 5-minute investor demo script that walks from GitHub → live Micro 820 PLC → Postgres ingestion → safety architecture.

**Key files**  
`matrix/api/fanout.py`, `matrix/api/ingest.py`, `config/matrix.yaml`, `twin/dummy_adapter.py`, `docs/demo_script_micro820_cell.md`, `tests/integration/test_os_agnostic.py`
> - `5217bfb`: Added “Voltron First Principles” — a clean, OS-agnostic agent layer with full Langfuse-style observability.  
New twin interfaces let agents talk to any shop-floor device, while Linux/Windows service scripts and unified install make it deploy-anywhere.  
Key files: agents/observability.py, twin/interfaces.py, scripts/{linux,windows}/, docs/voltron_first_principles.md
> - `64f560b`: Added GitHub Actions CI/CD (test + release), a `/metrics` endpoint, and full observability docs; refreshed README with live badges and quick-start diagram.  
Key files: `.github/workflows/*.yml`, `matrix/api/metrics.py`, `shared/observability.py`, `README.md`
> - `e9e8355`: Added a step-by-step guide (`docs/run_matrix_locally.md`) that walks through installing Postgres on Windows, creating the voltron DB/user, setting required env vars, installing deps, running migrations, and starting the Matrix API so the travel laptop can forward data to it over Tailscale.
> - `21ad22f`: Matrix now exposes authenticated ingestion endpoints (`POST /ingest/samples` & `/ingest/events`) that accept batches from any Voltron node and upsert them into Postgres.  
A new per-node API-key auth layer replaces the old global token, and the forwarder integration test proves end-to-end delivery.

Key files: `matrix/api/ingest.py`, `matrix/api/auth.py`, `config/matrix.yaml`, `matrix/db/migrations/002_ingestion.sql`
> - `01d7435`: **Summary**  
Introduced a resilient forwarder service that tails local SQLite, batches POSTs to Matrix, and resumes without duplicates after restarts.  Added design docs for the v2 monorepo layout, gateway schema, and Matrix ingestion API.  

**Key files**  
node/forwarder.py, docs/factorylm_v2_layout.md, docs/voltron_micro820_gateway.md, docs/matrix_ingestion_api.md
> - `a5df9e3`: **Summary**  
Phase-1 delivers a working, read-only industrial node: a Windows service that polls an Allen-Bradley Micro 820 PLC every second, stores the 21-tag dataset in SQLite, and enforces a hard-coded “no-write” safety policy.  A PyInstaller/Inno Setup scaffold produces an installer that registers the executable as an NSSM service and ships with a first-run CLI wizard.

**Key files**  
`node/node_main.py`, `node/plc_ab.py`, `node/policy.py`, `node/storage.py`, `shared/config_loader.py`, `node_installer/`
