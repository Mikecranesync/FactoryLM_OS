---
title: "Commit Notes - 2026-01-19"
date: 2026-01-19
tags: [project/RideView, project/Rivet-PRO, type/commit-note]
status: active
---

# Commit Notes - 2026-01-19

## RideView — Push at 23:19 UTC

**Commits:** 6
**Trigger:** `6e5cd14` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `6e5cd14` | fix: Use Android app-private storage for recordings | Mikecranesync |
| `8ae0d54` | fix: Add filetype dependency for Kivy image loading on Android | Mikecranesync |
| `97c41d6` | fix: Android APK missing pyyaml and rideview module import errors | Mikecranesync |
| `cba95fa` | chore: ignore APK binaries in git | Mikecranesync |
| `95c4669` | fix: Android APK startup crash - path resolution and dependencies | Mikecranesync |
| `9d7dfe9` | feat: add local Windows build script | Mikecranesync |

> **What changed:**
> - `6e5cd14`: Fixed Android permission crash by switching from external `/sdcard/` to app-private storage (`getFilesDir()`) for recordings, eliminating the need for runtime permissions on Android 11+.  

**Key file:** `src/rideview/mobile/app.py`
> - `8ae0d54`: Fixed Android app crash by adding the missing `filetype` dependency to buildozer.spec. Kivy 2.3.1 requires this package for image loading functionality.

Key file modified: buildozer.spec
> - `97c41d6`: Fixed Android APK crashes by adding missing pyyaml dependency and improving module import paths. The camera now works on-device via OpenCV after replacing the stub provider.

Key files: buildozer.spec, main.py, src/rideview/mobile/camera_provider.py
> - `cba95fa`: Added `*.apk` to `.gitignore` so Android build artifacts are no longer tracked by git.  
Key file: `.gitignore`
> - `95c4669`: Fixed Android APK startup crash by adding missing pydantic dependency and ensuring config/config files are bundled. Updated main.py with Android-specific import handling and config.py with fallback defaults when YAML files aren't found. Key files: buildozer.spec, main.py, src/rideview/core/config.py.
> - `9d7dfe9`: Added a one-click Windows build script that checks for Python 3.11, installs required packages, bundles the app with PyInstaller into a single-folder executable, and compresses it into a ready-to-share ZIP.  
Key file: build_windows_local.bat

## Rivet-PRO — Push at 23:04 UTC

**Commits:** 2
**Trigger:** `3c170b8` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `3c170b8` | Merge branch 'laptop-setup' | Mike Crane |
| `e0ba48b` | fix: Set bot to single instance for Telegram polling compatibility | Mikecranesync |

> **What changed:**
> - `3c170b8`: Merge branch 'laptop-setup'. Files: `rivet_pro/adapters/telegram/bot.py`, `rivet_pro/core/services/manual_download_manager.py`, `rivet_pro/core/utils/encoding.py`, `rivet_pro/troubleshooting/fallback.py`.
> - `e0ba48b`: Switched the Fly.io deployment from multi-instance to single-instance by replacing the default `http_service` with a process-scoped `services` block that only targets the bot process and allows machines to scale to zero. This prevents duplicate Telegram polling updates when multiple instances are running.  

Key file: fly.toml
