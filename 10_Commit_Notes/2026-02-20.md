---
title: "Commit Notes - 2026-02-20"
date: 2026-02-20
tags: [project/mikecranesync, project/JarvisTLaptop, project/factorylm-cosmos-cookoff, type/commit-note]
status: active
---

# Commit Notes - 2026-02-20

## factorylm-cosmos-cookoff — Push at 23:59 UTC

**Commits:** 1
**Trigger:** `28cd78d` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `28cd78d` | docs: Add 3-minute demo video script for Cosmos Cookoff submission | Mikecranesync |

> **What changed:** Added a 2:55 demo-video script for the Cosmos Cookoff submission that walks through FactoryLM Vision’s multimodal AI diagnosis: technician texts Telegram bot → system fuses live camera + PLC data → Cosmos Reason2-8B returns root-cause in <20 s.  
Key file: cookoff/DEMO_VIDEO_SCRIPT.md

## factorylm-cosmos-cookoff — Push at 23:03 UTC

**Commits:** 1
**Trigger:** `69facf3` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `69facf3` | Initial commit: FactoryLM Vision — Cosmos Cookoff submission | Mikecranesync |

> **What changed:** Initial commit: FactoryLM Vision — Cosmos Cookoff submission. Files: `.env.example`, `.gitignore`, `LICENSE`, `README.md`, `config/cosmos.yaml` (+31 more).

## JarvisTLaptop — Push at 22:49 UTC

**Commits:** 1
**Trigger:** `e9a5915` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `e9a5915` | feat: initial commit — Jarvis Telegram bot resurrection | Mikecranesync |

> **What changed:** Resurrected a unified Telegram bot from three legacy implementations, now running as a systemd service on VPS 100.68.120.99. It merges photo diagnosis, voice transcription, Claude CLI text routing, and CMMS work-order stubs under one bot with session management and rate limiting.

Key files: bot.py, handlers/photo.py, handlers/voice.py, handlers/text.py, integrations/claude_bridge.py, integrations/cmms.py, systemd/jarvis-telegram.service

## mikecranesync — Push at 22:21 UTC

**Commits:** 1
**Trigger:** `3a4f827` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `3a4f827` | Correct name from 'Harpenau' to 'Harper' | Mikecranesync |

> **What changed:** Fixed the founder’s last name from “Harpenau” to “Harper” in the README header.  
Key file: README.md

## mikecranesync — Push at 06:50 UTC

**Commits:** 1
**Trigger:** `bee8413` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `bee8413` | chore: update profile activity | github-actions[bot] |

> **What changed:** chore: update profile activity. Files: `README.md`.

## factorylm — Push at 23:10 UTC

**Commits:** 12
**Trigger:** `9c8052c` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `9c8052c` | fix(jarvis-telegram): pass photo captions to diagnosis, improve Claude bridge error logging | Mikecranesync |
| `8cff594` | fix(jarvis-telegram): replace Gemini with Groq, add rate limiting and typing indicators | Mikecranesync |
| `e4891b2` | fix(jarvis-telegram): use absolute imports + add ops trace | Mikecranesync |
| `ddce940` | feat(jarvis-telegram): restore unified Telegram bot from jarvis-workspace | Mikecranesync |
| `b3f1659` | Merge branch 'main' of https://github.com/Mikecranesync/factorylm | Mikecranesync |
| `a70e695` | feat(tools): add video-to-story montage pipeline (#63) | Mikecranesync |
| `c587673` | feat(tools): add video-to-story montage pipeline | Mikecranesync |
| `1f50766` | Merge branch 'main' of https://github.com/Mikecranesync/factorylm | Mikecranesync |
| `2ef344e` | feat(tools): add batch video processing with ThreadPoolExecutor (#62) | Mikecranesync |
| `132dad0` | feat(tools): add batch video processing with ThreadPoolExecutor | Mikecranesync |
| `d84c8ce` | Merge branch 'main' of https://github.com/Mikecranesync/factorylm | Mikecranesync |
| `7ecbdda` | feat(tools): add video-to-frames pipeline (#61) | Mikecranesync |

> **What changed:**
> - `9c8052c`: Telegram photo handler now includes user captions as technician notes in Groq prompts, improving diagnostic context. Claude bridge error logging now captures return codes and stdout when stderr is empty, eliminating blank error messages.

**Files modified:**  
- services/jarvis-telegram/handlers/photo.py  
- services/jarvis-telegram/integrations/claude_bridge.py
> - `8cff594`: **Summary:**  
Emergency swap from Gemini to Groq after API key leak; all AI calls now async via AsyncGroq. Added per-user rate-limiting (10/min) and typing indicators for photo/voice handlers.  

**Key files:**  
bot.py, config.py, handlers/photo.py, handlers/voice.py, integrations/groq_client.py
> - `e4891b2`: Fixed import errors that prevented the Jarvis Telegram bot from starting when run directly (not as a package) by converting all relative imports to absolute imports in handlers/ and integrations/. Added deployment documentation for the new jarvis-telegram service on the VPS.

Key files: handlers/photo.py, handlers/text.py, handlers/voice.py, integrations/gemini.py, docs/ops/traces/2026-02-20-jarvis-telegram-deploy.md
> - `ddce940`: Restored the unified Telegram bot by merging three production bots into a single modular service under services/jarvis-telegram/.  All 21 capabilities—photo diagnosis, voice commands, Claude CLI bridge, work-order creation, ops commands, etc.—are now available via one bot instance.

Key files: services/jarvis-telegram/bot.py, handlers/*.py, integrations/*.py
> - `b3f1659`: Merge branch 'main' of https://github.com/Mikecranesync/factorylm.
> - `a70e695`: Added a 4-stage video-to-story montage pipeline that turns extracted frames into a narrated documentary video. The new `tools/story_pipeline.py` script describes frames via Gemini Vision, generates a narrative, builds a montage plan, and assembles the final video with ffmpeg. Also introduced a deterministic troubleshooting engine for guided diagnosis via Telegram and batch video processing with parallel workers.
> - `c587673`: Added a 4-stage video-to-story montage pipeline that turns extracted frames into a narrated documentary video. It describes frames with Gemini Vision, writes a narrative, plans the montage, and assembles montage.mp4 via ffmpeg. Key file: tools/story_pipeline.py
> - `1f50766`: Merge branch 'main' of https://github.com/Mikecranesync/factorylm.
> - `2ef344e`: Added parallel batch processing to the video pipeline: drop a folder of videos and it’ll spin up 3 workers to extract frames/clips for every `.mp4/.avi…` it finds, skipping corrupt files and writing a single manifest for the whole run.  
CLI now uses sub-commands (`run` for single file, `batch` for folder) while keeping the old one-file syntax intact.

Key file: `tools/video_pipeline.py`
> - `132dad0`: Added batch video processing via `batch` subcommand that parallelizes frame/clips extraction across a folder (default 3 workers).  
CLI now uses subparsers (`run` for single file, `batch` for folder) while keeping old single-file syntax intact; skips corrupt/zero-byte files and writes both per-video and overall manifests.  
Key file: `tools/video_pipeline.py`
> - `d84c8ce`: Merge branch 'main' of https://github.com/Mikecranesync/factorylm. Files: `tools/video_pipeline.py`.
> - `7ecbdda`: Added a standalone video-to-frames pipeline tool (`tools/video_pipeline.py`) that extracts frames and scene-based clips from local videos or YouTube URLs, outputting a `manifest.json` for downstream agents. It reuses existing ffmpeg patterns and supports both frame extraction and clip segmentation with configurable thresholds.
