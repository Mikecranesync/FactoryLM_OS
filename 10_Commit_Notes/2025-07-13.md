---
title: "Commit Notes - 2025-07-13"
date: 2025-07-13
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-07-13

## Backlog.md — Push at 21:31 UTC

**Commits:** 19
**Trigger:** `fabcf46` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `fabcf46` | Ensure Claude is only triggered by MrLesk | Alex Gavrilescu |
| `e833930` | Claude PR Assistant workflow (#209) | Alex Gavrilescu |
| `5eaa8fc` | docs: update README with latest board status [skip ci] | MrLesk |
| `fec03d0` | TASK-188 - Add support for zero-padded IDs (#206) | Mark Smith |
| `d9b4488` | TASK-187 - fix(cli): respect autoCommit setting for all commands (#204) | Mark Smith |
| `c5391fe` | TASK-186 - Ensure IDs for new items increment correctly (#203) | Mark Smith |
| `b94499d` | docs: update README with latest board status [skip ci] | MrLesk |
| `f09b0d1` | TASK-177 - Automate Kanban board updates in README.md on release (#208) | Alex Gavrilescu |
| `06093ad` | TASK-185 - Fix task listing incorrectly including README.md files (#207) | Alex Gavrilescu |
| `29ec9df` | Create task-185 - Fix-task-listing-incorrectly-including-README.md-files.md | Alex Gavrilescu |
| `4e4c097` | TASK-184 - Fix Windows bug with task edit status command (#205) | Alex Gavrilescu |
| `6dc8550` | TASK-178 - Enhance backlog init with comprehensive configuration and re-initialization support (#202) | Alex Gavrilescu |
| `6f404df` | Update README.md | Alex Gavrilescu |
| `d77b7e1` | Create task-183 - Add-ordinal-field-for-custom-task-ordering-in-web-UI.md | Alex Gavrilescu |
| `7d6ed33` | TASK-182 - Document agents command and reorganize README CLI reference tables (#201) | Alex Gavrilescu |
| `c752d73` | TASK-176 - Add Priority Filtering/Sorting to Task List (#200) | Alex Gavrilescu |
| `14522cf` | Update agent-guidelines.md | Alex Gavrilescu |
| `b2bf98b` | Create task-182 - Document-agents-upgrade-command-and-reorganize-README-CLI-reference-into-purpose-based-tables.md | Alex Gavrilescu |
| `479a3d8` | TASK-116 - Add dark mode toggle to web UI (#199) | Alex Gavrilescu |

> **What changed:**
> - `fabcf46`: **Summary:** Restricted the Claude GitHub Action to only respond when triggered by the user "MrLesk" across all event types (issue comments, PR comments, reviews, and issues). This prevents unauthorized users from activating the Claude bot.

**Key file modified:** `.github/workflows/claude.yml`
> - `e833930`: Added Claude AI assistant to GitHub via new workflow that triggers on @claude mentions in issues, PRs, and reviews. The bot can read repo contents and CI results to provide contextual help. Key file: .github/workflows/claude.yml
> - `5eaa8fc`: README.md’s auto-generated Kanban board refreshed: three newly-closed items (zero-padded IDs, autoCommit respect, duplicate-ID fix) moved to Done, timestamp bumped to 2025-07-13 20:00:15.  
Key file: README.md
> - `fec03d0`: Added optional zero-padding for all IDs (tasks, docs, decisions) via new `zeroPaddedIds` config (0–10 digits; default 0 = off).  
Sub-task IDs always get 2-digit decimals (e.g. task-001.01) while parent portion respects the setting; full backward compatibility kept.  
Key files: src/cli.ts, backlog/config.yml, README.md, src/test/cli-zero-padded-ids.test.ts
> - `d9b4488`: Fixed CLI commands ignoring the `autoCommit` config by removing hard-coded `true` parameters from `core.createDocument`, `core.createDecision`, and `core.archiveDraft`. Now the user’s `config.yml` setting controls whether git commits happen after doc/decision/draft operations. Added E2E tests to verify commit count and repo state for both `autoCommit` modes.

Key files: `src/cli.ts`, `src/test/cli-commit-behaviour.test.ts`
> - `c5391fe`: Fixed unreliable ID generation that was creating duplicate task/doc/decision IDs by narrowing file-scanning patterns (e.g., decision-*.md) and normalizing IDs before filename creation. Added E2E CLI tests to verify IDs now increment correctly.  
Key files: src/file-system/operations.ts, src/test/cli-incrementing-ids.test.ts
> - `b94499d`: README.md refreshed its auto-generated project board: timestamp bumped and several completed tasks (docs pages, side nav, folder rename, API & bundling work) were removed from the Done column.  
Key file: README.md
> - `f09b0d1`: Added GitHub workflow to auto-update README.md with a Kanban board snapshot on every release. The board export now supports a `--readme` flag that injects a markdown table between `<!-- BOARD_START -->` and `<!-- BOARD_END -->` markers (creating the section if absent) and commits the change via the github-actions bot.

Key files: `.github/workflows/release.yml`, `src/cli.ts`, `src/readme.ts`, `README.md`
> - `06093ad`: Fixed task listing bug where README.md files were incorrectly parsed as broken tasks. Updated `src/utils/task-path.ts` to use strict `task-*.md` pattern instead of broad `*.md` glob, ensuring only actual task files are discovered across all listing operations (active, drafts, archived, completed).
> - `29ec9df`: Created new task to stop README.md files from being mis-identified as broken tasks in the task listing.  
Key file: backlog/tasks/task-185 - Fix-task-listing-incorrectly-including-README.md-files.md
> - `4e4c097`: Fixed Windows argument parsing bug in `backlog task edit` by removing conflicting options from the fallback command handler. The CLI now correctly handles status edits like `task edit 123 -s "In progress"` on Windows without throwing "too many arguments" errors.

**Files changed:** `src/cli.ts`, `src/test/cli-parent-filter.test.ts`, `backlog/tasks/task-184 - Investigate-Windows-bug-with-task-edit-status-command.md`
> - `6dc8550`: Enhanced `backlog init` with interactive configuration prompts for auto-commit, editor, offline mode, and web UI settings. Re-initializing now preserves existing values and lets users selectively update them. Key files: `src/cli.ts`, `README.md`, `src/test/enhanced-init.test.ts`.
> - `6f404df`: Added Homebrew installation option to README; no functional code changes.  
Key file: README.md
> - `d77b7e1`: Created new high-priority task to add custom task ordering via an ordinal field in the web UI, enabling drag-and-drop reordering within columns and CLI-based ordinal management.  
Key file: backlog/tasks/task-183 - Add-ordinal-field-for-custom-task-ordering-in-web-UI.md
> - `7d6ed33`: Added the new `backlog agents --update-instructions` command to sync AI-agent instruction files (.cursorrules, CLAUDE.md, etc.) and reorganized the README’s CLI reference into purpose-based tables (Task, Draft, Board, Config, Doc, Decision, Agent, Maintenance) for faster lookup.  
Key file: README.md
> - `c752d73`: Added priority filtering and sorting to the `task list` CLI: `--priority high|medium|low` and `--sort priority`. Plain output now shows `[HIGH]/[MEDIUM]/[LOW]` tags so AI tools can read them. Works alongside existing filters and is fully tested.

Key files: `src/cli.ts`, `src/utils/task-sorting.ts`, `src/test/cli-priority-filtering.test.ts`, `src/test/task-sorting.test.ts`
> - `14522cf`: Clarified when to fill out the Implementation Plan (after moving task to “in progress”) and Implementation Notes (after finishing work) to keep task tracking accurate.  
Key file: src/guidelines/agent-guidelines.md
> - `b2bf98b`: Created backlog task to document the new `backlog agents --update-instructions` command and reorganize the README’s unwieldy CLI reference into smaller, purpose-grouped tables for easier navigation.  
Key file: backlog/tasks/task-182 - Document-agents-upgrade-command-and-reorganize-README-CLI-reference-into-purpose-based-tables.md
> - `479a3d8`: Added a complete dark mode toggle to the web UI: a sun/moon button in the nav persists the choice to localStorage and switches every component (boards, forms, modals, etc.) between light and dark themes using Tailwind’s dark: classes. Key files touched: ThemeContext.tsx, ThemeToggle.tsx, Navigation.tsx, and the full component set for consistent styling.
