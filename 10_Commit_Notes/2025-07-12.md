---
title: "Commit Notes - 2025-07-12"
date: 2025-07-12
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-07-12

## Backlog.md — Push at 20:16 UTC

**Commits:** 15
**Trigger:** `c4e932a` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c4e932a` | Update task-177 - Automate-Kanban-board-updates-in-README.md-on-release.md | Alex Gavrilescu |
| `7aff161` | Create task-181 - Add-statistics-dashboard-to-web-UI.md | Alex Gavrilescu |
| `4fe535d` | Create task-180 - Add-statistics-overview-command-to-CLI-with-TUI-interface.md | Alex Gavrilescu |
| `f422d0b` | Create task-179 - Add-Settings-page-to-web-UI-with-configuration-management.md | Alex Gavrilescu |
| `6803c3f` | Update task-171 - Implement-drafts-list-functionality-in-CLI-and-web-UI.md | Alex Gavrilescu |
| `2e60fb4` | Create task-178 - Enhance-backlog-init-with-comprehensive-configuration-and-re-initialization-support.md | Alex Gavrilescu |
| `1a08623` | Create task-177 - Automate-Kanban-board-updates-in-README.md-on-release.md | Alex Gavrilescu |
| `fd1a8ee` | TASK-174 - Implement backlog cleanup command with completed folder management (#198) | Alex Gavrilescu |
| `2f7c2d0` | Add TASK-176 - Add Priority Filtering/Sorting to Task List. Especially with the --plain flag | Alex Gavrilescu |
| `c88485d` | Add TASK-175 - Add hour and minute to all dates in drafts, tasks, documents, decisions | Alex Gavrilescu |
| `4454943` | TASK-173 - Add CLI command to export Kanban board to markdown (#197) | Alex Gavrilescu |
| `dd50f4e` | Add TASK-173 - Add CLI command to export Kanban board to markdown | Alex Gavrilescu |
| `11f40ec` | Add TASk-172 - Order tasks by status and ID in both web and CLI lists | Alex Gavrilescu |
| `8722375` | Bugfix: allow scrolling on the task list page | Alex Gavrilescu |
| `8732469` | Feature: handle docs & decisions in the web view (#196) | Alex Gavrilescu |

> **What changed:**
> - `c4e932a`: Added task-173 as a dependency for the Kanban-board-automation task, signalling it must complete first.  
Key file: backlog/tasks/task-177 - Automate-Kanban-board-updates-in-README.md-on-release.md
> - `7aff161`: Created backlog task-181 to add a statistics dashboard to the web UI, defining the full scope, acceptance criteria, and implementation plan for visualizing project metrics, charts, and activity timelines.

Key file: backlog/tasks/task-181 - Add-statistics-dashboard-to-web-UI.md
> - `4fe535d`: Added a new backlog task (#180) to create a `backlog overview` CLI command that will surface live project stats—task counts by status/priority, completion %, recent activity—through an interactive TUI dashboard.  
Key file: backlog/tasks/task-180 - Add-statistics-overview-command-to-CLI-with-TUI-interface.md
> - `f422d0b`: Added a new backlog task (#179) to build a Settings page in the web UI. It defines the full spec: new /settings route, GET/PUT config API endpoints, grouped config controls (toggles, selects, inputs), validation, and save/cancel UX. Key file: backlog/tasks/task-179 - Add-Settings-page-to-web-UI-with-configuration-management.md
> - `6803c3f`: Expanded the scope of task-171 from simply listing drafts to a full draft-management feature: CLI and web UI now support both viewing and promoting drafts, turning them into tasks.  
Key file: backlog/tasks/task-171 - Implement-drafts-list-functionality-in-CLI-and-web-UI.md
> - `2e60fb4`: Created a new high-priority task to overhaul the backlog init command so it prompts for all key workflow settings (auto-commit, editor, offline mode, web UI) and safely re-initializes projects without clobbering existing config.  
Key file: backlog/tasks/task-178 - Enhance-backlog-init-with-comprehensive-configuration-and-re-initialization-support.md
> - `1a08623`: Added backlog task-177 to automate README Kanban updates on release. The task defines a GitHub Action that injects the current Kanban board into README.md between HTML comment markers whenever a release is created. Key file: backlog/tasks/task-177 - Automate-Kanban-board-updates-in-README.md-on-release.md
> - `fd1a8ee`: Added an interactive `backlog cleanup` command that moves old Done tasks into a new `backlog/completed/` folder, cutting board clutter while keeping tasks reachable via `listAllTasks()`.  Age filter (1 day–1 year), pre-move preview, and Git-aware renames keep history intact.

Key files: src/cli.ts, src/core/backlog.ts, src/file-system/operations.ts, src/git/operations.ts, README.md
> - `2f7c2d0`: Added TASK-176 to backlog: enhance the `backlog task list` command with priority filtering (`--priority`) and sorting (`--sort priority`), ensuring it works with `--plain` for AI workflows. Also covers web UI priority indicators and Kanban filtering.
> - `c88485d`: Created backlog task TASK-175 to add hour/minute precision to all dates (currently YYYY-MM-DD only). This is flagged as a breaking change requiring a full migration strategy for existing markdown files, UI updates, and timezone handling.
> - `4454943`: Added `backlog board export` CLI command that writes the current Kanban board to a markdown file (default Backlog.md). The export includes a timestamped header and a clean table view of tasks grouped by status, replacing any existing file unless `--force` is used. Key files: src/cli.ts, src/board.ts, README.md, backlog.md
> - `dd50f4e`: Added a new CLI task (TASK-173) to export the Kanban board as a markdown table, defaulting to `Backlog.md`, with overwrite protection and a `--force` flag.  
Also updated TASK-172’s checklist to ensure the new `--plain` list option follows the same status-then-ID ordering as the main CLI list.  

Key files:  
- `backlog/tasks/task-173 - Add-CLI-command-to-export-Kanban-board-to-markdown.md` (new)  
- `backlog/tasks/task-172 - Order-tasks-by-status-and-ID-in-both-web-and-CLI-lists.md`
> - `11f40ec`: Added task TASk-172 to standardize task ordering across the web UI and CLI. Tasks will now be grouped by status (To Do → In Progress → Done) and sorted by ID descending within each group. Key file: backlog/tasks/task-172 - Order-tasks-by-status-and-ID-in-both-web-and-CLI-lists.md
> - `8722375`: Fixed a scrolling issue on the task list page by changing `overflow-hidden` to `overflow-auto` in the main container, allowing content to scroll when it exceeds the viewport. This resolves the bug where users couldn't scroll through long task lists.

**Key file modified:** `src/web/components/Layout.tsx`
> - `8732469`: Added a collapsible side navigation to the web UI that exposes Documentation and Decisions alongside the Kanban Board; routes are now client-side and state persists in localStorage. Key files: src/web/App.tsx, src/web/components/SideNavigation.tsx, src/web/components/Layout.tsx.
