---
title: "Commit Notes - 2025-06-19"
date: 2025-06-19
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-06-19

## Backlog.md — Push at 21:49 UTC

**Commits:** 14
**Trigger:** `c1ac98e` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c1ac98e` | TASK-93 - Fix Windows agent instructions file reading hang (#121) | Alex Gavrilescu |
| `65b2603` | TASK-91 - Fix Windows issues: empty task list and weird Q character (#119) | Alex Gavrilescu |
| `41b8472` | Add TASK-92 - CI: Fix intermittent Windows test failures (#120) | Alex Gavrilescu |
| `0dc5b01` | TASK-90 - Fix task list scrolling behavior - selector should move before (#118) | Alex Gavrilescu |
| `ce17645` | Add TASK-91 - Fix Windows issues: empty task list and weird Q character | Alex Gavrilescu |
| `f86ebc3` | Add TASK-90 - Fix task list scrolling behavior - selector should move before scrolling | Alex Gavrilescu |
| `123195a` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | Alex Gavrilescu |
| `5e0e2a2` | Add TASK-89 - Add dependency parameter for task create and edit commands | Alex Gavrilescu |
| `0390046` | TASK-88 - Fix missing metadata and implementation plan in task view (#117) | Alex Gavrilescu |
| `7f3e85f` | TASK-76 - Add Implementation Plan section (#114) | Alex Gavrilescu |
| `b048041` | Add TASK-88 - Fix missing metadata and implementation plan in task view command | Alex Gavrilescu |
| `f3b68cb` | TASK-84 - Add -ac flag for acceptance criteria in task create/edit (#113) | Alex Gavrilescu |
| `47ffd8e` | TASK-83: Add case-insensitive status filter support (#112) | Alex Gavrilescu |
| `f665124` | Add task-87: Make agent guideline file updates idempotent during init | Alex Gavrilescu |

> **What changed:**
> - `c1ac98e`: Fixed Windows hanging issue when reading agent instruction files by adding existsSync() check and platform-specific fallback using readFileSync() on Windows. Modified src/agent-instructions.ts to handle file operations differently on Windows while maintaining cross-platform compatibility. Added markdown type declarations in src/types/markdown.d.ts.
> - `65b2603`: Fixed Windows-specific parsing bugs that left the task list empty and showed a stray “Q” character. Updated line-ending handling in the markdown parser and swapped label padding to Unicode non-breaking spaces in the UI.  
Key files: src/markdown/parser.ts, src/ui/task-viewer.ts
> - `41b8472`: Added a backlog task to eliminate flaky Windows CI runs by forcing sequential test execution; no code changes yet, just planning. Key file: `.backlog/tasks/task-92 - CI-Fix-intermittent-Windows-test-failures.md`.
> - `0dc5b01`: Fixed task list scrolling to match board view behavior: cursor now moves through visible items before scrolling begins. Updated generic-list.ts navigation from `up()/down()` to `select(index)` and disabled auto-scroll, aligning keyboard handling with the board view.

**Files modified:** `src/ui/components/generic-list.ts`, `src/ui/task-viewer.ts`
> - `ce17645`: Created backlog task to fix Windows-only regression: task list appears empty and a spurious “Q” character shows next to the Tasks title after the task-88 scrolling changes. Plan is to reinstate the Unicode non-breaking-space fix and make the new key-handling settings Windows-safe.
> - `f86ebc3`: Added TASK-90 to fix the task list’s scrolling logic so the cursor moves through visible items before the view scrolls, matching the smoother board-view behavior.  
Key file: `.backlog/tasks/task-90 - Fix-task-list-scrolling-behavior-selector-should-move-before-scrolling.md`
> - `123195a`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `.backlog/tasks/task-88 - Fix-missing-metadata-and-implementation-plan-in-task-view-command.md`, `src/cli.ts`, `src/test/cli-plain-output.test.ts`, `src/ui/board.ts`, `src/ui/components/generic-list.ts` (+1 more).
> - `5e0e2a2`: Added backlog task TASK-89 to implement `--depends-on` / `--dep` CLI flags for `task create` and `task edit`, letting users set task dependencies without manual markdown edits.  
Key file: `.backlog/tasks/task-89 - Add-dependency-parameter-for-task-create-and-edit-commands.md`
> - `0390046`: Fixed the `backlog task <id>` view to display all task metadata (status, assignee, labels, dates) plus Implementation Plan and Notes sections in both plain (`--plain`) and interactive modes. Key files: `src/cli.ts`, `src/ui/task-viewer.ts`, `src/test/cli-plain-output.test.ts`.
> - `7f3e85f`: Added an optional “Implementation Plan” section to task files, positioned after Acceptance Criteria and before Implementation Notes. The CLI now supports `--plan` on `task create`/`edit` to pre-fill this section, and all guideline docs have been updated to encourage using it.

Key files touched: `src/cli.ts`, `src/markdown/serializer.ts`, `.backlog/tasks/readme.md`, `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, plus a new test suite.
> - `b048041`: Created backlog task TASK-88 to fix the task view command so it displays all metadata (status, assignee, labels, dates) and any Implementation Plan/Notes sections, ensuring both plain and interactive modes show complete information.
> - `f3b68cb`: Added `--ac` and `--acceptance-criteria` flags to `backlog task create/edit` so tasks can now include acceptance criteria as markdown checkboxes. Multiple criteria can be comma-separated or passed via repeated flags; existing `-d` description flag remains unchanged.  
Key files: `src/cli.ts`, `src/test/acceptance-criteria.test.ts`, plus new sample tasks in `.backlog/archive/tasks/`.
> - `47ffd8e`: Added case-insensitive status filtering to the CLI's `task list` command. Users can now filter with `--status done`, `-s DONE`, or any case variation and still match tasks regardless of how the status is capitalized. Key files: `src/cli.ts` (filter logic), `src/test/cli.test.ts` (tests), and task backlog.
> - `f665124`: Added task-87 to make `backlog init` idempotent when injecting Backlog.md guidelines into agent files (.cursorrules, CLAUDE.md, AGENTS.md). The fix will use unique markers to detect and skip re-appending content on subsequent runs. Key file: `.backlog/tasks/task-87 - Make-agent-guideline-file-updates-idempotent-during-init.md`
