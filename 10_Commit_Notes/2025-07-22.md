---
title: "Commit Notes - 2025-07-22"
date: 2025-07-22
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-07-22

## Backlog.md — Push at 21:26 UTC

**Commits:** 6
**Trigger:** `24d1a01` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `24d1a01` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `c42e3a9` | TASK-199 - Add version parameter to board export command (#246) | Alex Gavrilescu |
| `555e46a` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `2341416` | Add correct version to board export in the readme file | Alex Gavrilescu |
| `f071c9c` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `ef6da42` | TASK-198 - Fix markdown content overflow beyond screen width (#245) | Alex Gavrilescu |

> **What changed:**
> - `24d1a01`: Updated README to reflect the new v1.5.5 release and refreshed the auto-generated Kanban board snapshot, showing task-199 (board export version param) now completed and a few other minor board changes. Bumped package.json version to match.

Key files: README.md, package.json
> - `c42e3a9`: Added `--export-version` flag to the `backlog board export` command so users can stamp a custom version string into the exported board header; renamed from `--version` to avoid a clash with Commander’s built-in flag.  
Key files: src/cli.ts (flag logic), README.md (usage docs), .github/workflows/release.yml (updated CI call).
> - `555e46a`: Bumped patch version to 1.5.4 in package.json to reflect the latest board-status and README updates; no functional code changes.  
Key file: package.json
> - `2341416`: The release workflow now injects the actual Git tag version into the README board export instead of the hard-coded CLI version.  
Key files: `.github/workflows/release.yml`, `src/cli.ts`, `src/readme.ts`
> - `f071c9c`: README.md refreshed with latest project board snapshot (v0.0.0) and package.json version bumped; Done column now shows two new completed tasks (markdown overflow & decision-page blank-screen fixes) while To-Do and In-Progress lists remain unchanged.
> - `ef6da42`: Fixed markdown content overflow by adding CSS rules to force text wrapping in MDEditor components, eliminating horizontal scrolling on mobile. Also cleaned up build config and removed unused health-check endpoint. Key files: `src/web/styles/source.css`, `src/server/index.ts`, and multiple React components.
