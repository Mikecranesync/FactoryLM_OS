---
title: "Commit Notes - 2025-09-10"
date: 2025-09-10
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-09-10

## Backlog.md — Push at 21:09 UTC

**Commits:** 5
**Trigger:** `ceb6430` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `ceb6430` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `6b7bc33` | TASK-263 - Enforce agent instruction selection during init (#339) | Alex Gavrilescu |
| `fb77e17` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `1756107` | Cleanup tasks | Alex Gavrilescu |
| `a5678a7` | TASK-256 - Add CLI command to append implementation notes (#340) | Alex Gavrilescu |

> **What changed:**
> - `ceb6430`: Updated README.md to reflect the latest project board snapshot (v1.12.0) and bumped package.json version to 1.12.0. Key files: README.md, package.json.
> - `6b7bc33`: Enforced explicit AI-agent instruction selection during `backlog init`: interactive mode now requires at least one choice (with a “none/danger” option) and ignores “none” if real agents are also picked; CLI flag `--agent-instructions` validates cursor|claude|agents|gemini|copilot|none and errors helpfully.  
Key files: src/cli.ts, src/agent-instructions.ts, README.md
> - `fb77e17`: Updated README.md to reflect the current project board status and bumped version to 1.11.0; no functional code changes—just documentation refresh.  
Key files: README.md, package.json
> - `1756107`: Moved four completed tasks from the active backlog to the completed folder. These tasks covered a web UI acceptance-criteria editor, CLI newline handling, CJK Unicode rendering in the TUI, and splitting Domain description/plan/notes into first-party Task fields.  
Key files: all four `backlog/completed/task-*` markdown files.
> - `a5678a7`: Added `--append-notes` flag to `backlog task edit` so you can tack on new implementation notes without overwriting the old ones; the command inserts the section in the right place and normalizes spacing.  
Key files touched: `src/cli.ts`, `src/core/backlog.ts`, `src/markdown/serializer.ts`, plus docs (`README.md`, `AGENTS.md`) and a pile of new tests.
