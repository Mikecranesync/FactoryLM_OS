---
title: "Commit Notes - 2025-03-15"
date: 2025-03-15
tags: [project/Archon, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-03-15

## Archon — Push at 18:34 UTC

**Commits:** 2
**Trigger:** `e967b1d` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `e967b1d` | Fixing couple bugs for using the right provider when crawling docs | Cole Medin |
| `2275c15` | Fixing the LLM client for crawling docs | Cole Medin |

> **What changed:**
> - `e967b1d`: Fixed provider-selection bugs in the doc-crawler: ensured the correct LLM client is used when re-crawling Pydantic-AI docs, and added a small delay to avoid rate-limiting.  
Key files: `archon/crawl_pydantic_ai_docs.py`, `utils/utils.py`
> - `2275c15`: Fixed the LLM client configuration in the doc crawler by properly initializing an AsyncOpenAI client using environment variables for provider, base URL, and API key. The key fix was switching from the incorrect `embedding_client` to the correct `llm_client` when generating titles and summaries.

**Key file:** `archon/crawl_pydantic_ai_docs.py`

## cmms — Push at 21:01 UTC

**Commits:** 2
**Trigger:** `5d590cf` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5d590cf` | feat: check ui config for tables and form fields | Ibrahima G. Coulibaly |
| `f7bbcf7` | feat: ui configuration | Ibrahima G. Coulibaly |

> **What changed:**
> - `5d590cf`: Added UI-config checks to tables and forms company-wide.  
Columns now hide/show per `uiConfigKey` (e.g., `locations`) pulled from `CompanySettingsContext`, letting each tenant control visible fields without code changes.

**Key files:**  
`CustomDatagrid/index.tsx`, `WorkOrders/index.tsx`, `Assets/index.tsx`, `Meters/index.tsx`, `PreventiveMaintenance/index.tsx`, plus the four Analytics `WOModal.tsx` files.
> - `f7bbcf7`: Added full UI-configuration feature: backend CRUD endpoints, DB table, and a new settings page where admins can toggle color themes and field visibility.  
Key files: `UiConfigurationController`, `UiConfigurationService`, `UiConfiguration/index.tsx`, `SettingsLayout.tsx`, plus the new Liquibase changelog.
