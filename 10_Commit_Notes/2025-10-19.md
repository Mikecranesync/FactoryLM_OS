---
title: "Commit Notes - 2025-10-19"
date: 2025-10-19
tags: [project/Thefuture, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-10-19

## Thefuture — Push at 21:15 UTC

**Commits:** 14
**Trigger:** `debe1e0` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `debe1e0` | Updated readme | Daniel Miessler |
| `4bd8a3f` | Updates | Daniel Miessler |
| `20993c6` | Sync voice server configuration with authoritative system | Daniel Miessler |
| `df087c1` | Updated pai. | Daniel Miessler |
| `7715ec1` | Revert: Restore PAI_DIR to settings.json env section | Daniel Miessler |
| `4980035` | Remove redundant PAI_DIR from settings.json env section | Daniel Miessler |
| `2606997` | Remove duplicate MCP server configuration from settings.json | Daniel Miessler |
| `d9e97bd` | Add automatic PAI context loading system | Daniel Miessler |
| `d1c9cd1` | Remove always-init skill - not working as intended | Daniel Miessler |
| `be96298` | feat: Add always-init universal task initializer skill | Daniel Miessler |
| `23b3bff` | Add Fabric repository setup check to skill documentation | Daniel Miessler |
| `9f4f0e2` | fix: Complete migration from context/ to skills-based architecture | Daniel Miessler |
| `67253fe` | feat: Enhanced documentation updater with intelligent content updates | Daniel Miessler |
| `6880a89` | Merge pull request #42 from org-axiopisty/main | Daniel Miessler 🛡️ |

> **What changed:**
> - `debe1e0`: Added two dated tip entries to the README—Oct 19 touts the new session-start hook for auto-loading the PAI skill, and Oct 18 notes broad repo fixes.  
Key file: README.md
> - `4bd8a3f`: Added two new changelog entries to README.md: the October 19 note about switching to a session-start hook for loading the PAI skill, and the October 18 note acknowledging broad repo fixes. These updates document the recent shift to the skills-based architecture and the cleanup work that stabilized the codebase.
> - `20993c6`: Updated voice system documentation to reflect actual ElevenLabs API implementation instead of non-existent macOS native system. Fixed voice assignments (engineer: Zoe, writer: Serena, added artist) and updated all configuration files with correct ElevenLabs voice IDs and API requirements.

Key files: voice-server/voices.json, documentation/voice-system.md, README.md
> - `df087c1`: Refactored the PAI skill into a single, always-active template that bakes identity, contacts, stack prefs, security rules and response format directly into the skill description; deleted the five satellite docs (contacts, prefs, response-format, security-detailed, voice-ids) to eliminate lazy-loading overhead.  
README and skills-system.md now log the 2025-10-19 automated update showing 1 skill modified and 5 removed.
> - `7715ec1`: Reverted removal of PAI_DIR environment variable in settings.json, restoring the explicit configuration pointing to `$HOME/.claude`. This ensures the Claude environment directory is properly defined for the application.

**Files modified:** settings.json, README.md
> - `4980035`: Removed duplicate PAI_DIR environment variable from settings.json since it's already exported in .zshrc. This eliminates configuration redundancy while maintaining functionality through shell environment inheritance.

**Files modified:** settings.json
> - `2606997`: **MCP Configuration Cleanup**  
Removed the duplicate `mcpServers` block from `settings.json` (72 lines) so all MCP servers are now configured solely in `.mcp.json`.  
No functional change—HTTP servers (httpx, pai, naabu…) and CLI servers (brightdata, stripe, Ref, apify) continue to work exactly as before.
> - `d9e97bd`: **PAI context now auto-loads at session start** via two new hooks: `load-core-context.ts` injects your `skills/PAI/SKILL.md` preferences into Claude’s context before `initialize-pai-session.ts` sets voice & tab titles.  
Replaces the old single `session-start-hook.ts`; update `settings.json` to list the new hooks in order and delete the old file.  

Key files: `hooks/load-core-context.ts`, `hooks/initialize-pai-session.ts`, `settings.json`, `documentation/pai-context-loading.md`
> - `d1c9cd1`: Removed the broken always-init skill that was supposed to automatically load PAI context for every request but wasn't working. This eliminates the failed attempt at universal task initialization until a better approach is implemented.

**Files modified:** skills/always-init/SKILL.md
> - `be96298`: Added a new `always-init` skill that automatically loads PAI context (contacts, preferences, protocols) before any user request is processed. This universal context loader replaces the previous `begin-task` skill and ensures consistent personalization across all operations.

**Key files modified:** `skills/always-init/SKILL.md`, `README.md`, `documentation/skills-system.md`
> - `23b3bff`: Added a setup verification step to the Fabric skill documentation that automatically checks for and clones the Fabric repository if missing. This ensures users have access to pattern definitions before attempting to use the intelligent pattern selection functionality.

**Key files:** `skills/fabric/SKILL.md`, `README.md`, `documentation/skills-system.md`
> - `9f4f0e2`: Completed migration from deprecated context/ directory to skills-based PAI architecture (v0.5.0). Updated all researcher agents and documentation to reference PAI.md instead of global/KAI.md or context/CLAUDE.md, with confirmation messages now showing "PAI Context Loading Complete".

Key files: agents/claude-researcher.md, agents/gemini-researcher.md, agents/perplexity-researcher.md, commands/load-dynamic-requirements.md, documentation/ufc-context-system.md
> - `67253fe`: **Enhanced pre-commit hook now writes real change summaries instead of just timestamps.**  
It parses git diff stats to create “Recent Changes” sections in README.md and docs/*.md with counts of added/modified/deleted lines and human-readable summaries like “Skills: 1 new, 2 updated”.  

**Key file:** `hooks/update-documentation.ts`
> - `6880a89`: Merge pull request #42 from org-axiopisty/main. Files: `settings.json`.

## cmms — Push at 20:58 UTC

**Commits:** 2
**Trigger:** `57e2a29` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `57e2a29` | fix: Account soft deletion | amb00u |
| `3df8d60` | fix: Account soft deletion | amb00u |

> **What changed:**
> - `57e2a29`: Removed the company-owner branch from the soft-delete logic so only global admins can disable users; company-level owners now receive a permission error instead of silently succeeding.  
Key file: `UserController.java`
> - `3df8d60`: Fixed the soft-delete endpoint URL and tightened permissions: users with SETTINGS permission can now soft-delete themselves, and the error message now correctly says “User not found”.  
Key file: `UserController.java`
