---
title: "Commit Notes - 2025-04-29"
date: 2025-04-29
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-29

## cmms — Push at 23:03 UTC

**Commits:** 11
**Trigger:** `5239d10` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5239d10` | fix: stale preventive maintenance retrieval by sorting work orders by creation date | Ibrahima G. Coulibaly |
| `1677a4f` | chore: diminish icon padding | Ibrahima G. Coulibaly |
| `5771864` | fix: Guest request page | Ibrahima G. Coulibaly |
| `dc9c225` | chore: localeCompare currencies | Ibrahima G. Coulibaly |
| `bb7bd1a` | chore: exhaustive list of currencies | Ibrahima G. Coulibaly |
| `95370e1` | fix: checklist tasks order | Ibrahima G. Coulibaly |
| `04de79a` | chore: part units mobile | Ibrahima G. Coulibaly |
| `325f1fd` | fix: missing translations | Ibrahima G. Coulibaly |
| `4e4a87c` | feat: implement password reset confirmation flow with token-based verification | Ibrahima G. Coulibaly |
| `d133c7e` | chore: enhance downtime management for assets and their hierarchies | Ibrahima G. Coulibaly |
| `58fd8d0` | refactor: cost and quantity data types to double for increased precision | Ibrahima G. Coulibaly |

> **What changed:**
> - `5239d10`: Fixed stale preventive maintenance data by ensuring the most recent work orders are returned first—now sorted by creation date descending in `findLastByPM`.  
Key file: `WorkOrderService.java`.
> - `1677a4f`: Shrunk the app icon on the login screen from 200 px to 180 px and regenerated the adaptive/icon PNGs to match, tightening the visual padding around the logo.  
Key files: LoginScreen.tsx, adaptive-icon.png, icon.png
> - `5771864`: Fixed a routing bug in the guest wrapper: authenticated users are now redirected to the correct dashboard based on their role (requesters → /app/requests, others → /app/work-orders).  
Key file: frontend/src/components/Guest/index.tsx
> - `dc9c225`: Currency dropdown in Settings → General is now alphabetically sorted by currency name instead of the arbitrary server order.  
Only file touched: `frontend/src/content/own/Settings/General/index.tsx`.
> - `bb7bd1a`: Added a complete currency reference table (`currencies_temp`) covering every country/territory with ISO code, symbol, and decimal places.  
Key files: `2025_04_29_1745944996_currencies.xml`, `master.xml`
> - `95370e1`: Fixed task ordering in checklist updates by changing the task collection type from `Collection` to `List` to preserve sequence.  
Key file: `api/src/main/java/com/grash/dto/ChecklistPatchDTO.java`
> - `04de79a`: Added per-unit cost display and unit labels (pcs, ft, etc.) to part quantities across the mobile app. Quantity input now accepts decimals and shows the correct cost per unit instead of total cost. Key files: `PartQuantities.tsx`, `formatters.ts`, `PartDetails.tsx`.
> - `325f1fd`: Fixed missing or garbled translations in all localized email property files (AR, DE, ES, FR, PL, PT-BR, TR).  
Key files: `mailMessages_*.properties` under `api/src/main/resources/`.  
Users will now receive correctly worded password-reset and welcome emails in their chosen language.
> - `4e4a87c`: Password reset now requires email confirmation: the reset request stores a temporary password in a new `payload` column of `verification_token`, emails a link to `/auth/reset-pwd-confirm`, and only applies the change after the user clicks it.  
Key files: `AuthController`, `UserService`, `VerificationToken`, `VerificationTokenService`, plus Liquibase migration and security config update.
> - `d133c7e`: Asset downtime logic now cascades: stopping downtime also stops it for all child assets, and triggering downtime walks up the parent chain so every ancestor is marked down. Added safety check for missing assets and extracted helper methods for clarity.  
Key file: api/src/main/java/com/grash/service/AssetService.java
> - `58fd8d0`: Changed cost and quantity fields from int/long to double across backend DTOs, models, and DB schema for sub-cent precision in financial and inventory data. Added a unit field to parts and updated frontend formatters to handle the new double values. Key files: Part.java, Asset.java, PartQuantity.java, 2025_04_28_1745037504_part_quantity.xml, formatters.ts.
