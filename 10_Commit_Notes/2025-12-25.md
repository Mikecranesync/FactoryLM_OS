---
title: "Commit Notes - 2025-12-25"
date: 2025-12-25
tags: [project/Agent-Factory, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-25

## Agent-Factory — Push at 22:48 UTC

**Commits:** 37
**Trigger:** `742c5f3` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `742c5f3` | fix: Deploy missing ingestion tables and fix success status | Mikecranesync |
| `5601b07` | docs(memory): Update handoff with database discovery and shutdown status | Mikecranesync |
| `965f47b` | fix(ingestion): Complete KB observability troubleshooting and documentation | Mikecranesync |
| `2461d12` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `706d603` | fix: Add robust JSON extraction from LLM responses (handles markdown code blocks + better logging) | Mikecranesync |
| `47ea782` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `7d50726` | fix: Gracefully degrade when source_fingerprints table missing (allows ingestion to continue) | Mikecranesync |
| `ed738ca` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `20a8500` | fix: Use %s placeholders for psycopg in immediate write query | Mikecranesync |
| `0dec25d` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `be1406b` | fix: Use plain text instead of Markdown for batch summaries (fixes 400 error) | Mikecranesync |
| `7dd9133` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `d3eedf4` | fix: Write ingestion metrics immediately instead of background queue | Mikecranesync |
| `8c19681` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `2d4c1df` | fix: Make batch notifications read from database not in-memory queue | Mikecranesync |
| `1b67817` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `16cd206` | fix: Add persistent KB observability batch timer to orchestrator bot | Mikecranesync |
| `43c177d` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `553afff` | feat: Integrate KB observability into ingestion pipeline | Mikecranesync |
| `b733d30` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `929d63c` | docs: Add comprehensive Telegram Observability implementation guide | Mikecranesync |
| `a527961` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `fc71483` | docs: Add KB Observability Platform system map and test files | Mikecranesync |
| `816faa1` | docs: Update README with latest metrics [skip ci] | Mikecranesync |
| `610c7e1` | fix: Use temp file instead of pipe for hook reliability | Mikecranesync |
| `8577ca8` | fix: Simplify hook pipe for Windows compatibility | Mikecranesync |
| `c2af690` | fix: Add DB_SKIP mode for fast local hook execution | Mikecranesync |
| `e795901` | feat: Complete Phase 2.3 - KB Observability Platform with Telegram Integration | Mikecranesync |
| `498f9b7` | test: Second verification of pre-push hook | Mikecranesync |
| `a3c4224` | test: Verify pre-push hook triggers README update | Mikecranesync |
| `6981a70` | Merge branch 'test-readme-automation' - README Auto-Update Feature Complete | Mikecranesync |
| `ff7172f` | feat: README auto-update automation (Phase 1-5 complete) | Mikecranesync |
| `a30955d` | feat(automation): Add README auto-update on git push | Mikecranesync |
| `7d2db5d` | feat: Phase 1 Complete - KB Observability Platform Database Schema | Mikecranesync |
| `a44dadb` | chore: Update project tracking logs | Mikecranesync |
| `97efa70` | docs: Add comprehensive persistent state completion report | Mikecranesync |
| `a3e4774` | feat(persistence): Integrate rock-solid conversation state into library.py add_machine flow | Mikecranesync |

> **What changed:**
> - `742c5f3`: Fixed the silent failure in the ingestion pipeline by deploying 5 missing PostgreSQL tables (source_fingerprints, ingestion_logs, failed_ingestions, human_review_queue, atom_relations) and correcting the success status logic to properly report failures when atoms_created=0 and errors exist. Key file modified: agent_factory/workflows/ingestion_chain.py.
> - `5601b07`: Updated session handoff documentation to reflect the discovery of two separate Neon databases—main with 21 atoms and VPS with 1,964+ atoms—and confirmed the KB observability pipeline is now fully operational. Added shutdown status and next-session priority to consolidate databases or deploy observability to the main DB.

**Key file:** `.claude/memory/SESSION_HANDOFF_2025-12-25.md`
> - `965f47b`: Fixed 5 critical ingestion pipeline bugs that prevented atom creation: missing source_fingerprints table (now gracefully skips deduplication), Telegram Markdown parsing errors (switched to plain text), database schema mismatch (added 15 missing columns to ingestion_metrics_realtime), PostgreSQL placeholder syntax ($1→%s), and LLM JSON wrapped in markdown blocks (now extracts JSON before parsing). Key file: agent_factory/workflows/ingestion_chain.py
> - `2461d12`: README.md updated to reflect the latest commit metrics—added an entry for the JSON-extraction fix (24 lines added, 2 removed).  
Key file: README.md
> - `706d603`: Fixed brittle JSON extraction from LLM responses by adding support for markdown code blocks (` ```json … ``` `) and improved error logging when parsing fails. This prevents ingestion failures when the model wraps its JSON output in markdown.

Key file: agent_factory/workflows/ingestion_chain.py
> - `47ea782`: Updated the README to log the latest hot-fix that lets ingestion proceed even when the source_fingerprints table is absent.  
Key file: README.md
> - `7d50726`: **Summary:**  
Made the ingestion pipeline resilient to a missing `source_fingerprints` table by wrapping both the duplicate-check and fingerprint-write operations in try/except blocks. If the table is absent, the workflow logs a warning and continues processing instead of crashing, treating deduplication as optional.

**Key file:**  
`agent_factory/workflows/ingestion_chain.py`
> - `ed738ca`: README updated to log two recent hot-fixes: psycopg placeholder fix and plain-text batch-summary fix.  
Key file: README.md
> - `20a8500`: Fixed a SQL placeholder mismatch: switched from asyncpg-style `$n` to psycopg-style `%s` placeholders in the immediate-write query so metrics are correctly inserted into the Postgres table.

Key file: `agent_factory/observability/ingestion_monitor.py`
> - `0dec25d`: Updated README with the latest 2025-12-25 fixes: batch summaries now use plain text to avoid 400 errors and metrics are written synchronously instead of via a background queue.  
Key file: README.md
> - `be1406b`: Fixed Telegram 400 errors by switching batch summaries from Markdown to plain text. Removed all formatting characters (*, _) and made parse_mode optional in the sender.

Key file: agent_factory/observability/telegram_notifier.py
> - `7dd9133`: README updated to reflect synchronous metric writes (no more background queue) and a new KB-gap schema that tracks equipment, route, and confidence for autonomous ingestion.  
Key files: README.md, agent_factory/core/kb_gap_logger.py, docs/SESSION_2025-12-25_KB_INGESTION.md
> - `d3eedf4`: Fixed ingestion metrics loss by writing directly to DB instead of queuing for background flush. Added synchronous `_write_metric_immediately()` that bypasses the background writer and falls back to file logging if DB is down.  
Key file: `agent_factory/observability/ingestion_monitor.py`
> - `8c19681`: Updated README with the latest batch-notification fix: TelegramNotifier now reads from the DB and marks sessions as notified to eliminate duplicates across CLI, Redis worker, and bot processes. Key file: README.md
> - `2d4c1df`: Fixed batch Telegram notifications to read from the shared `ingestion_metrics_realtime` table instead of a process-local queue, enabling cross-worker deduplication. Added a `db_manager` parameter to `TelegramNotifier` and a SQL query to pull the last 5 min of sessions, marking them as notified to prevent duplicates.

Key files: `telegram_notifier.py`, `orchestrator_bot.py`
> - `1b67817`: Added a 5-minute batch timer to the orchestrator bot for persistent KB observability summaries that survives restarts. README now reflects this 24/7 monitoring capability.  
Key file: README.md
> - `16cd206`: Fixed the knowledge-base observability batch timer so it now runs continuously inside the Telegram orchestrator bot instead of dying when scripts exit. A 5-minute background task now survives restarts and quietly retries on errors.

Key file: agent_factory/integrations/telegram/orchestrator_bot.py
> - `43c177d`: Updated README with the latest ingestion-pipeline observability work: new IngestionMonitor/TelegramNotifier, 5-min batch summaries, session tracking across all stages, and an end-to-end test.  
Key file: README.md
> - `553afff`: Added real-time observability to the KB ingestion pipeline: every run now gets a session ID that is tracked through all seven stages and, if Telegram is configured, a 5-minute batch summary is pushed.  
Key files: `agent_factory/workflows/ingestion_chain.py` (monitor wired in), `scripts/test_observability_e2e.py` (validation script).
> - `b733d30`: Added a comprehensive Telegram observability guide to the README, documenting the new 956-line implementation that supports multiple notification modes (basic, BATCH, error-only, multi-chat) with copy-paste patterns and production deployment templates. Key file: README.md
> - `929d63c`: Added comprehensive Telegram observability implementation guide (~25KB) with step-by-step bot setup, 4 production patterns (basic/BATCH/error-only/multi-chat), and production deployment templates. Key files: docs/TELEGRAM_OBSERVABILITY_IMPLEMENTATION.md
> - `a527961`: README now shows the latest 2025-12-25 11:44 UTC metrics for the KB Observability Platform—944 new lines across 4 files, including full system docs, integration tests, and a failover log example.  
Key file: README.md
> - `fc71483`: Added missing observability documentation and test files for Phase 2.1-2.3 implementation. The main addition is a comprehensive system architecture document (25 KB) covering the IngestionMonitor and TelegramNotifier components, plus a real database integration test and sample failover metrics data demonstrating graceful degradation when the database is unavailable.

Key files: docs/SYSTEM_MAP_OBSERVABILITY.md, test_ingestion_monitor.py
> - `816faa1`: Updated the README to document a recent reliability fix that switches from pipes to temp files for hook execution, preventing Windows stdout/stderr mixing. README.md now shows the 2025-12-25 11:42 UTC entry with cross-platform metrics.
> - `610c7e1`: Fixed Git hook reliability by replacing pipe-based metrics→README workflow with temp file approach to avoid Windows stderr/stdout mixing. Metrics now extracted to `$LOG_DIR/metrics_temp.json`, then fed to README updater. Modified `.claude/hooks/pre_push.sh` and `.githooks/pre-push`.
> - `8577ca8`: Fixed Windows compatibility issues in pre-push hooks by removing the bash -c wrapper that was breaking pipe operations. Simplified the command flow to directly pipe metrics extraction into README update while properly exporting DB_SKIP=1 to skip database operations for speed.

**Files changed:** `.claude/hooks/pre_push.sh`, `.githooks/pre-push`
> - `c2af690`: Pre-push hooks now skip database queries locally via `DB_SKIP=1`, cutting runtime from 15-30s to <1s and avoiding Windows timeouts; GitHub Actions still collects DB metrics with automatic failover.  
Key files: `.claude/hooks/pre_push.sh`, `.githooks/pre-push`, `scripts/update_readme_metrics.py`.
> - `e795901`: **Summary:**  
Completed Phase 2.3 by wiring the new TelegramNotifier into IngestionMonitor; every ingestion session now auto-sends notifications (immediate or 5-min batch) without ever crashing the pipeline. Added quiet-hours, rate-limiting, ASCII formatting, and three-tier fallback logging.

**Key files:**  
`agent_factory/observability/telegram_notifier.py` (new), `agent_factory/observability/ingestion_monitor.py` (+30 lines), plus three new test files.
> - `498f9b7`: The pre-push hook was re-verified by appending “Test 2” to the tracking file.  
Key file: test_hook_installed.txt
> - `a3c4224`: Added a test file to confirm the pre-push hook is installed and will auto-update the README.  
Key file: test_hook_installed.txt
> - `6981a70`: Merge branch 'test-readme-automation' - README Auto-Update Feature Complete. Files: `.claude/hooks/config.yml`, `.claude/hooks/pre_push.sh`, `.github/workflows/update-readme.yml`, `README.md`, `scripts/update_readme.py` (+2 more).
> - `ff7172f`: **Summary:** Implemented a 5-phase automation system that automatically updates the README with project metrics on every push. The system extracts git stats and database atom counts, injects them into a "Latest Updates" section (keeping only 10 entries), and runs via both local git hooks and GitHub Actions backup with Telegram notifications.

**Key files:** `scripts/update_readme_metrics.py`, `scripts/update_readme.py`, `.claude/hooks/pre_push.sh`, `.github/workflows/update-readme.yml`, `.claude/hooks/config.yml`
> - `a30955d`: Added a CI hook that rewrites the repo’s README after every push: it pulls fresh git stats, counts DB atoms, and appends a plain-English summary of the new commits in reverse order. A local pre-push hook runs first, with GitHub Actions as fallback.  
Key file: test_readme_automation.txt
> - `7d2db5d`: **Summary:** Phase 1 of the KB Observability Platform is complete with a production-ready PostgreSQL schema deployed to a VPS. The migration created 3 metrics tables (real-time, hourly, daily) with 13 optimized indexes and a rollup function, establishing the foundation for monitoring a 7-stage ingestion pipeline.

**Key Files:**  
- `docs/database/observability_migration.sql` (schema definition)  
- `scripts/run_observability_migration.py` (automated deployment)  
- `.claude/memory/observability_session_2025-12-25.md` (technical decisions & next steps)

**Next:** Phase 2 begins with implementing `IngestionMonitor` to capture per-source metrics and `TelegramNotifier` for real-time alerts.
> - `a44dadb`: **Summary**: Auto-updated project logs capture the switch to git worktrees so four critical RivetCEO fixes (36 s Route C latency, zero KB atoms, unknown gap detector, broken OCR flow) could be developed in parallel. The logs also document the asyncio.gather() refactor that cut Route C latency from 36 s to ~5 s and the KB-population fix that restored missing knowledge-base atoms.

**Files touched**: DECISIONS_LOG.md, DEVELOPMENT_LOG.md, ISSUES_LOG.md, PROJECT_CONTEXT.md, SYSTEM_MAP.md
> - `97efa70`: Persistent conversation state is now fully implemented with 4-tier database failover (Neon → Supabase → Railway → Local SQLite) ensuring zero data loss. The system saves state after every user input during machine addition and allows seamless resume from any interruption point.

Key files: `docs/PERSISTENT_STATE_COMPLETE.md`, `NEXT_ACTIONS.md`
> - `a3e4774`: Added rock-solid conversation persistence to the Telegram add-machine flow: every user input is now saved to a tiered backend (Neon → Supabase → Railway → local SQLite) and the bot can resume exactly where it left off after any disconnect or restart.  
Key file: `agent_factory/integrations/telegram/library.py`

## cmms — Push at 23:08 UTC

**Commits:** 1
**Trigger:** `751911b` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `751911b` | fix: license front | Ibrahima G. Coulibaly |

> **What changed:** Fixed license-related UI issues: removed unused tutorial link constant, enabled Enter key to submit email modal, disabled button during loading, and added null-check before Stripe redirect.  
Key files: Consts.java, EmailModal.tsx, SubscriptionPlanSelector.tsx
