---
title: "Commit Notes - 2025-04-14"
date: 2025-04-14
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-14

## cmms — Push at 22:33 UTC

**Commits:** 10
**Trigger:** `110ff36` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `110ff36` | chore: update default credentials and reorganize backup scripts | Ibrahima G. Coulibaly |
| `2448e39` | fix(api): use custom id in wo pdf | Ibrahima G. Coulibaly |
| `ff3630d` | chore: atlas-backup.sh | Ibrahima G. Coulibaly |
| `e9cc03d` | chore: overwrite atlas bucket | Ibrahima G. Coulibaly |
| `bbc0730` | feat: backup and restore powershell db | Ibrahima G. Coulibaly |
| `bfc106f` | refactor: dtos and update environment configuration for flexibility and clarity | Ibrahima G. Coulibaly |
| `1dce9c9` | chore: update contact email in README for support and inquiries | Ibrahima G. Coulibaly |
| `0c9a853` | Merge pull request #26 | Ibrahima G. Coulibaly |
| `1e19698` | Remove .DS_Store files | Ibrahima G. Coulibaly |
| `0b85f7d` | chore: update .gitignore to include `.DS_Store` files | Ibrahima G. Coulibaly |

> **What changed:**
> - `110ff36`: Reorganized backup scripts into `scripts/backup/` and hardened default credentials in both PowerShell and Bash versions. Added a comprehensive backup/restore guide (`dev-docs/Backup.md`) with OS-specific usage and optional skip flags for DB or file storage. Updated README link text to “Super admin docs.”
> - `2448e39`: Fixed work-order PDF header to display the user-friendly customId instead of the internal database id.  
Key file: api/src/main/resources/templates/work-order-report.html
> - `ff3630d`: Added a new `atlas-backup.sh` script that automates full or partial backups and restores of the Atlas CMMS stack (Postgres DB + MinIO files).  
Key files: `atlas-backup.sh`
> - `e9cc03d`: Added `--overwrite` flag to the `mc mirror` command so the nightly Atlas backup job will now replace any existing objects in the MinIO bucket instead of skipping them, ensuring the backup stays current.  
Key file: atlas-backup.ps1
> - `bbc0730`: Added a PowerShell backup/restore script for the Atlas CMMS stack on Windows. The new atlas-backup.ps1 can snapshot the Postgres DB and MinIO file store into a timestamped zip under atlas_backups/, and later restore from it. Updated .gitignore to keep these backups out of version control.
> - `bfc106f`: Refactored DTOs to centralize ID fields and added new Task/FloorPlan DTOs for cleaner controller returns.  
Switched docker-compose and .env to use env vars for DB and MinIO credentials, boosting security and deploy flexibility.  

Key files: TaskController.java, FloorPlanController.java, TaskShowDTO.java, FloorPlanShowDTO.java, docker-compose.yml, .env.example
> - `1dce9c9`: Updated the support and professional-services contact email in the README from a personal Gmail address to the official Atlas-CMMS domain.  
Key file: README.MD
> - `0c9a853`: Merge pull request #26. Files: `.gitignore`, `api/src/main/resources/mailMessages_es_ES.properties`.
> - `1e19698`: Cleaned up macOS metadata files (.DS_Store) that had been accidentally committed to the repo; no functional code changes.  
Key files removed: .DS_Store and its nested copies under api/ and subdirectories.
> - `0b85f7d`: Updated `.gitignore` to exclude macOS `.DS_Store` files from version control, preventing clutter from Finder metadata.  
Key file: `.gitignore`
