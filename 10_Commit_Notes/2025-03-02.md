---
title: "Commit Notes - 2025-03-02"
date: 2025-03-02
tags: [project/Archon, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-03-02

## Archon — Push at 22:18 UTC

**Commits:** 1
**Trigger:** `2a1b820` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `2a1b820` | Updating the intro to Archon | Cole Medin |

> **What changed:** Re-branded Archon as the world’s first “Agenteer” in the README and fixed import order in both the main and v4-streamlit-ui-overhaul branches so `utils.utils` is imported after the path append.  
Key files: README.md, archon/archon_graph.py, archon/pydantic_ai_coder.py, iterations/v4-streamlit-ui-overhaul/archon/archon_graph.py, iterations/v4-streamlit-ui-overhaul/archon/pydantic_ai_coder.py

## cmms — Push at 22:57 UTC

**Commits:** 2
**Trigger:** `9bf3e53` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `9bf3e53` | fix: gcp setup | Ibrahima G. Coulibaly |
| `a912021` | fix: docker-compose.yml | Ibrahima G. Coulibaly |

> **What changed:**
> - `9bf3e53`: Fixed the GCP setup instructions by replacing the insecure “grant allUsers read access” step with the correct “fine-grained access control” option when creating the bucket.  
Key file: GCP-setup.md
> - `a912021`: Fixed docker-compose.yml by renaming the dependency from `mc` to `minio` and removed the now-redundant `mc` service that created buckets on startup; MinIO now runs with explicit address flags. Added IDE config selecting Claude 3.7 Sonnet model.

Key files: docker-compose.yml
