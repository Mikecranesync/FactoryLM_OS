---
title: "Commit Notes - 2026-02-03"
date: 2026-02-03
tags: [project/jarvis-workspace, type/commit-note, project/Rivet-PRO, project/factorylm, project/factorylm-core, project/factorylm-landing, project/pi-gateway]
status: active
---

# Commit Notes - 2026-02-03

## jarvis-workspace — Push at 19:13 UTC

**Commits:** 1
**Trigger:** `29a8534` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `29a8534` | docs: Add Commandments + Constitution blurb to AGENTS.md | Jarvis AI |

> **What changed:** Added the “Engineering Commandments” and “Constitution” summaries to AGENTS.md so every agent sees the ground-rules (issue-first workflow, no direct main pushes, Mike-approval gate, etc.) right in their workspace.  
Key file: AGENTS.md

## jarvis-workspace — Push at 18:57 UTC

**Commits:** 1
**Trigger:** `6c0d03c` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `6c0d03c` | docs: AGENTS.md references FactoryLM Vision v0.25 | Jarvis AI |

> **What changed:** Added a prominent banner to AGENTS.md that forces every contributor to first read the FactoryLM Vision v0.25 architecture (layers 0-3) and remember that intelligence must be converted into code.  
Key file: AGENTS.md

## jarvis-workspace — Push at 17:28 UTC

**Commits:** 1
**Trigger:** `7b305ca` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `7b305ca` | feat: v4.0 React Dashboard + EtherNet/IP Gateway | Jarvis AI |

> **What changed:** **Summary:**  
Upgraded the PLC gateway to v4.0 with a unified FastAPI backend (`api_v4.py`) that auto-connects to a Micro820 via EtherNet/IP and exposes REST/WebSocket endpoints for real-time I/O. A new React dashboard (`frontend/`) replaces the old Flask UI, offering live I/O tables and clickable outputs. Everything now runs in a single Docker container with simplified networking.

**Key files:**  
`infrastructure/balena/plc-gateway/app/api_v4.py`, `infrastructure/balena/plc-gateway/frontend/src/`, `infrastructure/balena/plc-gateway/Dockerfile`

## jarvis-workspace — Push at 17:05 UTC

**Commits:** 1
**Trigger:** `486c4fa` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `486c4fa` | v3.0.0 - Micro820 PLC Integration Complete | Jarvis AI |

> **What changed:** **Summary:**  
FactoryLM Edge v3.0 now ships with full Allen-Bradley Micro820 PLC support: auto-detects the PLC’s network, joins it, and exposes live I/O plus read/write tags over EtherNet/IP and Modbus TCP.  
Tested against real hardware; gateway boots in <1 min and survives power cycles without manual IP tweaks.

**Key files:**  
`infrastructure/balena/plc-gateway/app/micro820.py`, `infrastructure/balena/plc-gateway/app/main.py`, `docs/micro820/QUICK-REFERENCE.md`, `scripts/test-micro820.sh`

## jarvis-workspace — Push at 15:05 UTC

**Commits:** 1
**Trigger:** `dcb97ef` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `dcb97ef` | 🚀 FactoryLM Edge v2.0 - True Plug-and-Play | Jarvis AI |

> **What changed:** FactoryLM Edge v2.0 now auto-detects the subnet of any connected device and self-configures to join that network—no manual setup, just plug-and-play. A new network_detect.py engine scans the link, applies the matching IP range, and keeps it alive, falling back to its own DHCP server if no peer is found. Key files: network_detect.py (detection logic), start.sh (orchestration), dnsmasq.conf (fallback DHCP).

## jarvis-workspace — Push at 14:31 UTC

**Commits:** 1
**Trigger:** `5c14aa8` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5c14aa8` | 🔌 TRUE PLUG-AND-PLAY: Added DHCP server to Pi | Jarvis AI |

> **What changed:** Added a DHCP server (dnsmasq) to the Pi so any device automatically gets an IP (192.168.1.100-200) when plugged into ethernet—no manual network config needed. Replaced hard-coded IP setup with a start.sh script that brings up the interface and launches both dnsmasq and the gateway app.  
Key files: Dockerfile, start.sh, dnsmasq.conf, main.py

## jarvis-workspace — Push at 14:28 UTC

**Commits:** 1
**Trigger:** `018c2c2` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `018c2c2` | 🔌 Auto-configure ethernet for PLC subnet on startup | Jarvis AI |

> **What changed:** The Pi now boots with eth0 fixed at 192.168.1.1/24, so any PLC or laptop plugged straight in gets an instant link—no manual network setup.  
Key files: infrastructure/balena/plc-gateway/app/main.py (new DHCP/static logic), plus routine knowledge-base & log updates.

## jarvis-workspace — Push at 14:21 UTC

**Commits:** 1
**Trigger:** `484db20` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `484db20` | 📝 Add PLC Gateway API details to NETWORK.md | Jarvis AI |

> **What changed:** Added the new REST API surface for the Micro820 PLC gateway (port 5000) to the network docs, listing every endpoint and noting the PLC is expected at 192.168.1.10.  
Key file: NETWORK.md

## jarvis-workspace — Push at 14:13 UTC

**Commits:** 1
**Trigger:** `5d50d27` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5d50d27` | 🤖 Deploy PLC Gateway to Raspberry Pi | Jarvis AI |

> **What changed:** Deployed a new PLC Gateway service on the Pi at 100.97.210.121:5000 that bridges Allen-Bradley Micro820 PLCs to the cloud via REST (port 5000) and MQTT.  
Key files: infrastructure/balena/plc-gateway/Dockerfile, app/main.py, balena.yml, docker-compose.yml

## jarvis-workspace — Push at 13:56 UTC

**Commits:** 1
**Trigger:** `179cfc4` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `179cfc4` | 🛡️ Bulletproof network configuration | Jarvis AI |

> **What changed:** **Network hardening:** Created a device registry (NETWORK.md) and automated health-check script to monitor all Tailscale endpoints every 30 min, with alerts if any node drops.  
**Operational fix:** Documented the static-IP outage that killed the PLC laptop and added DHCP-only rule; all four devices (Pi Edge, PLC & travel laptops, VPS) are now verified online.  

Key files: NETWORK.md, scripts/network-health-check.sh, HEARTBEAT.md

## jarvis-workspace — Push at 12:33 UTC

**Commits:** 1
**Trigger:** `0aae693` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `0aae693` | 🔐 Add Balena API credentials | Jarvis AI |

> **What changed:** Added Balena Cloud credentials for the factorylm-edge fleet (ID 2332816) to enable device provisioning. The API token and fleet details are now stored in .env.balena and documented in KEYMASTER.md.

## jarvis-workspace — Push at 12:18 UTC

**Commits:** 1
**Trigger:** `306e8ba` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `306e8ba` | 🥧 Add Raspberry Pi zero-touch deployment scripts | Jarvis AI |

> **What changed:** Added zero-touch Raspberry Pi deployment: Balena Cloud API script, auto-Tailscale firstrun.sh, and Wi-Fi template so flashing an SD card now brings a Pi online with no manual steps.  
Key files: infrastructure/raspberry-pi/boot-files/firstrun.sh, infrastructure/balena/setup.py, infrastructure/raspberry-pi/README.md

## jarvis-workspace — Push at 10:38 UTC

**Commits:** 2
**Trigger:** `e7f6380` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `e7f6380` | 📁 Add Flowise config file | Jarvis AI |
| `01eb512` | 🌊 Flowise configured programmatically | Jarvis AI |

> **What changed:**
> - `e7f6380`: Added a Flowise configuration file that stores connection details, API keys, and flow IDs for the local and external Flowise instances. This enables the app to authenticate and interact with specific AI flows like “voice_to_code_logger” and Anthropic services.

Key file: config/flowise.json
> - `01eb512`: Flowise is now live on the VPS with a new “Voice to Code Logger” flow; Anthropic credentials and API keys were injected into both the UI and the config file.  
Key files touched: `.env.flowise`, `.env.master`, `KEYMASTER.md` (now trimmed to only Flowise & VPS access).

## jarvis-workspace — Push at 10:29 UTC

**Commits:** 1
**Trigger:** `9bea82f` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `9bea82f` | 📋 Add Telegram log exporter and conversation archival | Jarvis AI |

> **What changed:** Added a Telegram log exporter (`scripts/export_telegram_logs.py`) that archives daily conversation history, workflow stats, and media files into timestamped markdown logs. It auto-generates three logs per day: full chat, summary with git commits, and inbound media inventory.

## jarvis-workspace — Push at 10:28 UTC

**Commits:** 1
**Trigger:** `5fb154e` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5fb154e` | 📍 Checkpoint before Balena/Edge integration | Jarvis AI |

> **What changed:** Checkpoint commit captures the swarm’s current state: Celery backend and S7-1200 data collector are working, but YouTube API integration is stuck and sales pages are unfinished.  
Key files: brain/automaton/evolution-log.md (new cycle entry + 5 follow-up tasks), brain/synthetic-kb/2026-02-03.jsonl (fresh apprentice Q&A on ladder logic).

## jarvis-workspace — Push at 10:02 UTC

**Commits:** 1
**Trigger:** `8af1f46` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `8af1f46` | 📋 Add Foundational Workflow Documentation Process | Jarvis AI |

> **What changed:** Established a mandatory workflow documentation process requiring every AI automation to be visually mapped in Flowise, logged with proof-of-work, and committed to GitHub for human review. The new process includes a Python client for programmatic Flowise access and automatic logging of all Jarvis commands.

Key file: processes/WORKFLOW_DOCUMENTATION.md

## jarvis-workspace — Push at 09:53 UTC

**Commits:** 1
**Trigger:** `571b13c` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `571b13c` | 🔧 Configure Flowise + Add Workflow Proof of Work Logger | Jarvis AI |

> **What changed:** Flowise AI builder is now live on port 3001 with basic auth (mike/password) and a fresh API key.  
Every voice-to-code request is now logged to `/evidence/workflows/` as JSONL plus a summary table, giving us a full audit trail of what Clawdbot executes.

## jarvis-workspace — Push at 09:40 UTC

**Commits:** 1
**Trigger:** `d1bc7a3` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `d1bc7a3` | 🆘 Add emergency access documentation | Jarvis AI |

> **What changed:** Created comprehensive emergency access documentation: a disaster recovery guide (EMERGENCY_ACCESS.md) and a stripped-down quick-reference (CLAUDE.md) with VPS IPs, SSH commands, service restart scripts, and Telegram contact. Updated the knowledge base and evolution log. Key files: EMERGENCY_ACCESS.md, CLAUDE.md, brain/synthetic-kb/2026-02-03.jsonl, brain/automaton/evolution-log.md

## jarvis-workspace — Push at 02:18 UTC

**Commits:** 1
**Trigger:** `c0fa8d8` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c0fa8d8` | Add master network map and credentials registry | Jarvis AI |

> **What changed:** Added a living network map and credential registry: NETWORK_MAP.md now documents every Tailscale node and service endpoint, while KEYMASTER.md catalogs where every secret lives (without exposing values). The update-network-map.sh script keeps the topology current, and .env.reference serves as the master checklist for required credentials.

## jarvis-workspace — Push at 01:37 UTC

**Commits:** 1
**Trigger:** `72ebf93` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `72ebf93` | Build out RemoteMe infrastructure - scripts, docs, installers | Jarvis AI |

> **What changed:** **Summary:**  
Laid the foundation for a distributed “RemoteMe” fleet by adding a Windows SSH key installer, a Pi first-boot script that auto-enables camera/GPIO, a network-health watchdog, and a Python orchestrator that can push code/config to every node.  Companion docs and a master plan map out how the monolithic FactoryLM codebase will be split into a single source-of-truth package and rolled out to field devices.

**Key files touched:**  
`infrastructure/MASTER_PLAN.md`, `infrastructure/scripts/orchestrate.py`, `infrastructure/installers/raspberry-pi/first-boot.sh`, `infrastructure/scripts/setup-ssh-key.ps1`, `infrastructure/scripts/health-check.sh`, `jarvis_node.py`

## Rivet-PRO — Push at 19:13 UTC

**Commits:** 2
**Trigger:** `21bf56c` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `21bf56c` | docs: Add Commandments + Constitution blurb | FactoryLM Bot |
| `c345a03` | docs: CLAUDE.md references FactoryLM Vision v0.25 | FactoryLM Bot |

> **What changed:**
> - `21bf56c`: Added a concise engineering commandments and constitution summary to CLAUDE.md, codifying the dev workflow (issue-first, branch-main, approval gates) and mission/boundaries for the Rivet-PRO project.  
Key file: CLAUDE.md
> - `c345a03`: The CLAUDE.md file was rewritten to align Rivet-PRO with the FactoryLM Vision v0.25, replacing the previous RIVET Pro documentation. It now positions Rivet-PRO as the knowledge-gathering component feeding Layer 0 of the FactoryLM stack, removing all prior CMMS extraction and Ralph autonomous agent instructions.

Key file modified: CLAUDE.md

## factorylm — Push at 19:13 UTC

**Commits:** 7
**Trigger:** `e4d807a` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `e4d807a` | docs: Add Commandments + Constitution blurb to CLAUDE.md | Claude Agent |
| `9fdae15` | docs: Add Engineering Commandments and Constitution to README | FactoryLM Bot |
| `8f7b31b` | docs: copilot-instructions references FactoryLM Vision v0.25 | Claude Agent |
| `60b854a` | docs: CLAUDE.md references FactoryLM Vision v0.25 | Claude Agent |
| `6ec7b5d` | docs: FactoryLM Vision v0.25 - THE canonical architecture document | FactoryLM Bot |
| `3cb2971` | Merge remote main with local development | Mikecranesync |
| `7219360` | feat: Add core infrastructure for Catapult demo | Mikecranesync |

> **What changed:**
> - `e4d807a`: Added a concise summary of the team’s 10 Engineering Commandments and 6-point Constitution to the project README so newcomers immediately see the rules for branching, review, and release.  
Key file: CLAUDE.md
> - `9fdae15`: Added two new governance docs to the README: “Engineering Commandments” (10 workflow rules—issue-first, PR-only, no direct main pushes, Mike approval required) and “AI Constitution” (10 articles defining agent autonomy, speed mandate, boundaries, and human-in-the-loop oversight).  
Key file: README.md
> - `8f7b31b`: Updated the GitHub Copilot instructions to mandate reading the FactoryLM Vision (v0.25) before any work begins.  
Key file: `.github/copilot-instructions.md`
> - `60b854a`: Added CLAUDE.md to anchor all contributors to the FactoryLM Vision v0.25 architecture.  
It mandates reading the main README, codifies the 4-layer intelligence stack (deterministic KB → edge Pi → local GPU → cloud AI), and declares WhatsApp the primary interface.  

Key file: CLAUDE.md
> - `6ec7b5d`: README.md replaced with the canonical FactoryLM architecture: a 4-layer “intelligence-downward” stack (Layer 0 deterministic code/KB → Layer 3 cloud AI) and multi-channel UI (WhatsApp, phone, Slack, etc.).  
Key change: codifies the goal of moving every query from expensive AI (Layer 3) to instant deterministic code (Layer 0) over time.
> - `3cb2971`: Merge remote main with local development. Files: `.claude/skills/factorylm/SKILL.md`, `.github/copilot-instructions.md`, `.gitignore`, `MIGRATION.md`, `README.md` (+295 more).
> - `7219360`: **Summary:**  
Built the full Catapult demo stack: an LLM abstraction layer supporting GROQ/Claude/DeepSeek, a Factory I/O + Micro 820 PLC client, and a Telegram→PLC→LLM diagnosis bridge. Added autonomous dev-loop API (My-Ralph) and multi-device config distributor, all wired so Clawdbot can route factory questions to the diagnosis service running on Jarvis.

**Key files:**  
core/, plc-client-factoryio/, My-Ralph/, scripts/diagnosis_service.py, scripts/deploy_mission_brief.py

## factorylm-core — Push at 19:13 UTC

**Commits:** 2
**Trigger:** `c07723e` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c07723e` | docs: Add Commandments + Constitution blurb | FactoryLM Bot |
| `21f9da6` | docs: CLAUDE.md references FactoryLM Vision v0.25 | FactoryLM Bot |

> **What changed:**
> - `c07723e`: Added a concise “Engineering Commandments” checklist and a three-line “Constitution” to CLAUDE.md, codifying the team’s git workflow and release gate (Mike must approve).  
Key file: CLAUDE.md
> - `21f9da6`: Added a project-level CLAUDE.md that positions FactoryLM Core as the LLM abstraction layer, explaining its provider-agnostic routing, cost optimization, and the overarching goal of shrinking LLM usage as patterns harden into code.  
Key file: CLAUDE.md

## factorylm-landing — Push at 22:23 UTC

**Commits:** 4
**Trigger:** `c9fe562` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c9fe562` | 🔌 USB-Style Product Configurator v2 (#7) | Mikecranesync |
| `516a626` | 🧩 Drag & Drop Puzzle Configurator (#5) | Mikecranesync |
| `2335f3c` | docs: Add Commandments + Constitution blurb | FactoryLM Bot |
| `83e23d9` | docs: CLAUDE.md references FactoryLM Vision v0.25 | FactoryLM Bot |

> **What changed:**
> - `c9fe562`: USB-style product configurator v2 landed: 3D USB-stick tiles with color-coded LED animations (blue pulse while dragging, yellow flicker near slot, green solid when connected) plus drag-and-drop snap behavior, floating popovers, and live cart state. Corsair-grade polish added—cleaner panels, hover badges, gateway counter, toast alerts, and silky animations.  
Key files: edge/css/{animations,configurator,gateway,usb-products}.css, edge/js/{cart,configurator,drag-drop,popover}.js, edge/index.html
> - `516a626`: **Summary:**  
Launched a dark-themed FactoryLM Edge product site with two key pages: a hero page (`/edge`) featuring a 3D-orbit animation of the device ecosystem, and an interactive configurator (`/edge/configurator`) that walks users through PLC brand, fieldbus, and I/O choices before auto-generating a priced bundle. A later commit replaced the wizard with a drag-and-drop puzzle: users must first drop the mandatory Phone App tile, then snap additional hardware tiles onto the vertically-rotated Edge gateway while prices update live.

**Key files:**  
- `edge/index.html` – hero & orbit animation  
- `edge/configurator.html` – step-by-step wizard → drag-and-drop puzzle
> - `2335f3c`: Added a concise “Engineering Commandments” checklist and a three-line “Constitution” to CLAUDE.md, clarifying the dev workflow (issue → branch → PR → approval) and that Mike must approve anything that ships.  
Key file: CLAUDE.md
> - `83e23d9`: Added a concise project brief (CLAUDE.md) that anchors the marketing site to the FactoryLM v0.25 vision: industrial, edge-first, air-gapped, read-only AI. It lists the key pages and mandates a dark, fast, dependency-free design.

## pi-gateway — Push at 19:13 UTC

**Commits:** 2
**Trigger:** `3b9c450` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `3b9c450` | docs: Add Commandments + Constitution blurb | FactoryLM Bot |
| `3611a93` | docs: CLAUDE.md references FactoryLM Vision v0.25 | FactoryLM Bot |

> **What changed:**
> - `3b9c450`: Added a concise “Engineering Commandments” checklist and a three-line “Constitution” to CLAUDE.md, codifying the team’s git workflow and release gate (Mike must approve).  
Key file: CLAUDE.md
> - `3611a93`: Added a prominent warning banner to CLAUDE.md that directs contributors to read the FactoryLM Vision v0.25 spec before making changes, clarifying that Pi-Gateway only implements Layers 0-1 of that stack.  
Key file: CLAUDE.md
