---
title: "Commit Notes - 2025-11-19"
date: 2025-11-19
tags: [project/Thefuture, project/cmms, project/jarvis-unified, type/commit-note]
status: active
---

# Commit Notes - 2025-11-19

## Thefuture — Push at 07:43 UTC

**Commits:** 5
**Trigger:** `506c935` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `506c935` | docs(hook-system): update branding from Kai to PAI | Daniel Miessler |
| `b69a20b` | fix: remove PAI_DIR from settings.json to avoid expansion issues | Daniel Miessler |
| `1f818c2` | fix(critical): fix SessionStart hook and remove private skill references | Daniel Miessler |
| `aade312` | docs: fix all broken documentation links and references | Daniel Miessler |
| `94f1e22` | feat(skills): add system-createskill and system-createcli meta-skills | Daniel Miessler |

> **What changed:**
> - `506c935`: Updated hook system documentation to rebrand the main agent from "Kai" to "PAI" throughout, including voice server notifications and agent-specific voice mappings. No functional changes—voice IDs and behavior remain identical.

**Key file:** `.claude/skills/CORE/hook-system.md`
> - `b69a20b`: Fixed hook execution failures by removing PAI_DIR from settings.json since Claude Code doesn't expand `$HOME` in JSON env vars. Hooks now automatically default to `~/.claude` without needing PAI_DIR set.

**Files modified:** `.claude/settings.json`, `docs/QUICKSTART.md`
> - `1f818c2`: Fixed critical onboarding blockers: updated the SessionStart hook to reference the correct CORE skill (was incorrectly pointing to non-existent PAI skill), and removed the private system-mcp routing section from CORE/SKILL.md that was causing 401 errors. These changes allow new users to successfully load PAI without errors.

**Files changed:** `.claude/hooks/load-core-context.ts`, `.claude/skills/CORE/SKILL.md`
> - `aade312`: Fixed all broken documentation links in README.md and docs/QUICKSTART.md by converting absolute paths to relative paths and removing references to non-existent private files (agent-personalities.md, stack-preferences.md). Updated voice-server link from non-existent USAGE.md to README.md. All 11 documentation links now functional.

Key files: README.md, docs/QUICKSTART.md
> - `94f1e22`: Added two meta-skills that teach PAI architecture: `system-createskill` (complete skill creation/validation framework) and `system-createcli` (production TypeScript CLI generation with three complexity tiers). Both are sanitized from private Kai infrastructure and serve as reference implementations for deterministic code wrapped by AI orchestration.

Key files: `.claude/skills/system-createcli/SKILL.md`, `.claude/skills/system-createskill/SKILL.md`, and their associated workflow files.

## cmms — Push at 23:02 UTC

**Commits:** 1
**Trigger:** `2f3a5bb` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `2f3a5bb` | fix: email encoding | Ibrahima G. Coulibaly |

> **What changed:** Fixed email encoding issues by switching to a UTF-8 ClassLoaderTemplateResolver and explicitly setting UTF-8 on the message source.  
Key file: api/src/main/java/com/grash/configuration/EmailConfiguration.java

## jarvis-unified — Push at 23:34 UTC

**Commits:** 4
**Trigger:** `1c8751c` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `1c8751c` | Merge pull request #2 from Mikecranesync/add-claude-github-actions-1763593418789 | Mikecranesync |
| `ac0f2be` | "Claude Code Review workflow" | Mikecranesync |
| `ad7eb3f` | "Claude PR Assistant workflow" | Mikecranesync |
| `cf2001f` | feat(mobile): Initial Expo voice interface app - simple working TTS | Mikecranesync |

> **What changed:**
> - `1c8751c`: Merge pull request #2 from Mikecranesync/add-claude-github-actions-1763593418789. Files: `.github/workflows/claude-code-review.yml`, `.github/workflows/claude.yml`.
> - `ac0f2be`: Added GitHub Actions workflow to automatically run Claude Code reviews on pull requests. The workflow triggers on PR open/update and uses Claude to analyze code quality, bugs, performance, security, and test coverage, then posts feedback as PR comments.
> - `ad7eb3f`: Added a GitHub Actions workflow that lets team members summon Claude by typing “@claude” in issues, PRs, or reviews. The bot receives read access to repo content and can optionally update PR descriptions or run custom prompts. Key file: .github/workflows/claude.yml.
> - `cf2001f`: Created a minimal Expo voice assistant app that eliminates Android navigation crashes by removing React Navigation entirely. The app provides 5 voice commands for email management using TTS, with a single-screen interface and mock data for testing.

Key files: `apps/jarvis-mobile-expo/App.tsx`, `apps/jarvis-mobile-expo/src/hooks/useVoiceCommands.ts`, `apps/jarvis-mobile-expo/src/screens/VoiceScreen.tsx`
