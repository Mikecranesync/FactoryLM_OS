---
title: "Commit Notes - 2026-01-04"
date: 2026-01-04
tags: [project/Rivet-PRO, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2026-01-04

## Rivet-PRO — Push at 22:14 UTC

**Commits:** 6
**Trigger:** `b6b127a` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b6b127a` | Add missing print_analyzer module for telegram bot | Mikecranesync |
| `db0b35f` | Add multi-bot launcher script (run_bots.py) | Mikecranesync |
| `4a8c6ef` | Bot command registration: Add Telegram command discovery | Mikecranesync |
| `fc31c70` | Atlas CMMS: Complete extraction and productization | Mikecranesync |
| `d45c6ec` | Harden: Photo OCR bug fix + VPS deployment hardening | Mikecranesync |
| `c0db51e` | Fix: Bots now respond to photos | Mikecranesync |

> **What changed:**
> - `b6b127a`: Added the missing `PrintAnalyzer` module that enables the Telegram bot to analyze technical drawings and wiring diagrams using vision AI (GPT-4o/Claude). Key file: `rivet/workflows/print_analyzer.py`.
> - `db0b35f`: Added a single launcher script (`run_bots.py`) that spawns all three Rivet Telegram bots—Local Dev, Main AI, and CMMS—in parallel processes and keeps them alive, replacing the need to start each bot manually.  
Key file: `run_bots.py`.
> - `4a8c6ef`: Added Telegram command registration to all three bots, enabling autocomplete and discoverability. Local dev bot gets 6 commands, main bot gets 3, and CMMS bot gets 5. Also enhanced photo handling to detect schematics via caption keywords and route them to a new PrintAnalyzer workflow. Key files: telegram.py, telegram_rivet_bot.py, telegram_cmms_bot.py
> - `fc31c70`: **Atlas CMMS fully extracted and production-ready**  
Migrated the complete maintenance-management system out of Agent Factory into a standalone Rivet Pro module with its own PostgreSQL schema, async connection pooling, fuzzy equipment matcher (85 % threshold), technician registration, and work-order auto-creation pipeline.  
Key files: `rivet/integrations/atlas.py` (high-level client), `rivet/atlas/database.py` (pooled asyncpg), `rivet/atlas/models.py` (Pydantic schemas), plus four SQL migrations and the new service layers.
> - `d45c6ec`: Fixed Windows Unicode display issues in config.py and resolved Telegram bot photo OCR failures by relaxing validation thresholds (0.80→0.90) and confidence requirements (0.7→0.5). Hardened VPS deployment by eliminating duplicate bot services and adding comprehensive logging with rollback procedures.

**Key files:** `rivet/config.py`, `BUGFIX_2026-01-04.md`, `.env.example`
> - `c0db51e`: **Summary:** Fixed bots silently rejecting photos by relaxing image quality validation (0.80→0.90) and lowering confidence thresholds (0.7→0.5). Added comprehensive photo processing logging, 30s download timeouts, and better error classification.

**Key files:** `rivet/integrations/telegram*.py`, `rivet/workflows/ocr.py`, `rivet/rivet_pro/confidence_scorer.py`

## cmms — Push at 23:51 UTC

**Commits:** 23
**Trigger:** `379ca7f` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `379ca7f` | chore: show license errors in front | Ibrahima G. Coulibaly |
| `06aea76` | feat: add aspect-ratio styles and eager loading for images in Hero component | Ibrahima G. Coulibaly |
| `4ad3b6d` | update: replace logo and logo-white image files in static assets | Ibrahima G. Coulibaly |
| `e6068fc` | fix: handle navigation after successful plan upgrade and adjust BUSINESS plan condition check | Ibrahima G. Coulibaly |
| `46e472d` | feat: add `fromString` method to `Language` enum for case-insensitive deserialization | Ibrahima G. Coulibaly |
| `78d1281` | fix: update People table to show disabled user status | Ibrahima G. Coulibaly |
| `144f3b1` | fix: refine downgrade and upgrade logic in PaddleService | Ibrahima G. Coulibaly |
| `b781a20` | fix: refine subscription user count validation in PaddleService and SubscriptionController | Ibrahima G. Coulibaly |
| `ee99df6` | fix: improve checkout flow and user count validation in subscription and Paddle services | Ibrahima G. Coulibaly |
| `f5a9140` | refactor: rename `stopScheduleTimers` to `stopScheduleJobs` in `ScheduleService` for clarity | Ibrahima G. Coulibaly |
| `8d12c68` | fix: prevent scheduling of stale preventive maintenance tasks in ScheduleService | Ibrahima G. Coulibaly |
| `8e18d94` | refactor: relocate trial period banner logic from Sidebar to SidebarMenu for better component organization | Ibrahima G. Coulibaly |
| `f33bc3f` | fix: use `state.getEntitlements()` for entitlement validation in `LicenseService` | Ibrahima G. Coulibaly |
| `3d4253b` | feat: reintroduce and secure schedule management endpoints | Ibrahima G. Coulibaly |
| `e8f5197` | fix: update default license users count to 5,000,000 in `LicenseService` | Ibrahima G. Coulibaly |
| `2160b2f` | refactor: simplify work order scheduling logic and optimize active schedule query | Ibrahima G. Coulibaly |
| `38617ee` | feat: add Quartz table creation and active schedule filtering | Ibrahima G. Coulibaly |
| `6c35906` | feat: implement subscription auto-reset job with Quartz scheduling | Ibrahima G. Coulibaly |
| `26bb426` | fix: resolve duplicate key issue in English translations file | Ibrahima G. Coulibaly |
| `aab10b4` | Merge branch 'licensing' of https://github.com/Grashjs/cmms | Ibrahima G. Coulibaly |
| `1ff1047` | feat: improve subscription upgrade flow with new handling for business plans and enhanced feedback | Ibrahima G. Coulibaly |
| `f7c5fca` | feat: introduce configurable Paddle environment for improved flexibility | Ibrahima G. Coulibaly |
| `53d9412` | feat: add trial banner for cloud users and extend `Company` model with audit fields | Ibrahima G. Coulibaly |

> **What changed:**
> - `379ca7f`: License entitlement checks now surface user-friendly errors in the UI instead of failing silently. Backend services (Asset, Location, Part, WorkOrder, PreventiveMaintenance, AssetDowntime) were updated to throw descriptive exceptions when limits are hit, and the frontend modals and list views now catch and display these messages. Key files: AssetDowntimeService, AssetService, LocationService, plus ~15 frontend components.
> - `06aea76`: Hero images now load instantly (`loading="eager"`) and scale without layout shift via fixed `aspect-ratio` (1920×922 desktop, 720×1600 mobile).  
File touched: `frontend/src/content/overview/Hero/index.tsx`
> - `4ad3b6d`: Replaced the main and white variants of the application logo in the static assets folder. The change updates the brand image shown in the header and other UI components.  
Key files: `frontend/public/static/images/logo/logo.png`, `frontend/public/static/images/logo/logo-white.png`
> - `e6068fc`: Fixed BUSINESS plan upgrade flow: now checks both `selectedPlan` and `selectedPlanObject.code` to reliably trigger upgrade request, and after any successful upgrade the user is redirected to `/app/work-orders`.  
Key file: `frontend/src/content/own/Subscription/Plans/index.tsx`
> - `46e472d`: Added a case-insensitive `fromString` factory to the `Language` enum so Jackson can deserialize language codes like “en”, “FR”, etc.; unmatched values default to English.  
Key file: `api/src/main/java/com/grash/model/enums/Language.java`
> - `78d1281`: Disabled users now show “(disabled)” after their name and appear in gray, making their status instantly visible in the People table.  
Key file: frontend/src/content/own/PeopleAndTeams/People.tsx
> - `144f3b1`: Fixed Paddle subscription upgrade/downgrade logic to correctly handle all user-count scenarios (fewer, equal, or more than the plan limit). Now explicitly sets both flags so no edge case is left ambiguous.  
Key file: PaddleService.java
> - `b781a20`: Fixed subscription user-count logic to only include paid, enabled users when checking downgrade eligibility. Consolidated the downgrade flag assignment in PaddleService and aligned the controller’s validation to use the same paid-user filter.

**Files changed:**  
- `SubscriptionController.java`  
- `PaddleService.java`
> - `ee99df6`: Fixed checkout race-conditions and user-count mismatches: added a `checkoutComplete` flag so the UI only reloads after a successful Paddle checkout, blocked the “FREE” plan from being selected, and tightened backend validation to only count paid/enabled users when checking seat limits.  
Key files: SubscriptionController.java, PaddleService.java, frontend/src/content/own/Subscription/Plans/index.tsx
> - `f5a9140`: Renamed `stopScheduleTimers` to `stopScheduleJobs` throughout `ScheduleService` to better reflect that it halts Quartz jobs, not just timers.  
Key files: `ScheduleService.java`, `PreventiveMaintenanceController.java`.
> - `8d12c68`: Preventive-maintenance schedules now auto-disable when the last 5 generated work-orders have never been started (stale), stopping pointless future work-order creation.  
Key file: api/src/main/java/com/grash/service/ScheduleService.java
> - `8e18d94`: Moved the trial-period banner from the top-level Sidebar into the SidebarMenu component; no user-facing change, just cleaner component boundaries.  
Key files: `SidebarMenu/index.tsx`, `Sidebar/index.tsx`.
> - `f33bc3f`: Fixed a bug where `hasEntitlement()` was checking an outdated `cachedEntitlements` set instead of the current entitlements from `state.getEntitlements()`, ensuring accurate entitlement validation.  
**File changed:** `api/src/main/java/com/grash/service/LicenseService.java`
> - `3d4253b`: Re-enabled the getById, patch and delete endpoints for schedules, now with authorization checks that verify the user owns the related preventive-maintenance asset.  Removed the old startup rescheduler and made the preventiveMaintenance field mandatory.  
Files touched: ScheduleController.java, Schedule.java, ApiApplication.java
> - `e8f5197`: Updated the default license user limit from 500 to 5,000,000 in `LicenseService`, effectively removing the cap for free-tier deployments.  
Key file: `api/src/main/java/com/grash/service/LicenseService.java`
> - `2160b2f`: Simplified work-order scheduling by removing the stale-check that auto-disabled schedules after 10 un-reacted work orders; the same guard-rail is now enforced in one optimized native query in `ScheduleRepository`.  
Key files: `ScheduleService.java`, `ScheduleRepository.java`
> - `38617ee`: Added Liquibase migration to create Quartz scheduler tables and wired Quartz via application.yml.  Only active schedules are now loaded at startup and exposed through a new `findActive()` method; unused REST endpoints are commented out.  
Key files: `2026_01_04_1767500000_quartz_tables.xml`, `ScheduleService.java`, `ScheduleRepository.java`, `application.yml`, `ScheduleController.java`.
> - `6c35906`: Added a Quartz-scheduled job (`SubscriptionEndJob`) that automatically downgrades paid subscriptions to FREE when they expire. Only paid, time-limited subscriptions are now scheduled for expiration checks, cutting startup noise.

Key files: `SubscriptionEndJob.java`, `SubscriptionService.java`, `SubscriptionRepository.java`, `ApiApplication.java`
> - `26bb426`: Fixed a duplicate key (`days_of_week`) in the English locale file that was causing build/runtime errors.  
**File:** `frontend/src/i18n/translations/en.ts`
> - `aab10b4`: Merge branch 'licensing' of https://github.com/Grashjs/cmms. Files: `.env.example`, `README.MD`, `api/pom.xml`, `api/src/main/java/com/grash/ApiApplication.java`, `api/src/main/java/com/grash/configuration/WebMvcConfig.java` (+169 more).
> - `1ff1047`: Business-plan upgrades now bypass Stripe and trigger a manual request; all other plans still auto-checkout. The request path adds proper loading states, success/error toasts, and a clearer “we’ll contact you” message.  
Key files: frontend/src/content/own/Subscription/Plans/index.tsx, frontend/src/i18n/translations/en.ts
> - `f7c5fca`: Added `PADDLE_ENVIRONMENT` config (defaults to sandbox) so the same image can run against Paddle’s sandbox or production endpoints without rebuilds. Key files: docker-compose.yml, frontend/.env.example, frontend/src/config.ts, and the two subscription plan components that now read the runtime value instead of hard-coding “sandbox”.
> - `53d9412`: **Summary:**  
Cloud users now see a trial banner in the sidebar showing days left and a CTA to upgrade. The `Company` model inherits new audit fields (created/updated timestamps). Pricing tabs are temporarily hidden for future rework.

**Key files:**  
`Sidebar/index.tsx`, `company.ts`, `pricing/index.tsx`
