---
title: "Commit Notes - 2024-12-23"
date: 2024-12-23
tags: [project/frame_realtime_gemini_voicevision, type/commit-note]
status: active
---

# Commit Notes - 2024-12-23

## frame_realtime_gemini_voicevision — Push at 23:51 UTC

**Commits:** 6
**Trigger:** `f24d371` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `f24d371` | WIP | CitizenOneX |
| `88dc0f8` | handling code for specific events, including interruption | CitizenOneX |
| `aa33e36` | audio conversations working | CitizenOneX |
| `47ae65f` | close to bidirectional audio, but not yet | CitizenOneX |
| `4a7f56c` | renamed application | CitizenOneX |
| `6b62347` | renamed application | CitizenOneX |

> **What changed:**
> - `f24d371`: Increased the raw-sound buffer from 1 KB to 32 KB to stop very short Gemini audio clips from being rejected; cleaned up stale TODO comments and changed the interruption log marker for clarity.  
Key file: lib/main.dart
> - `88dc0f8`: Added handling for server-side events: interruption (stops audio), turn-complete, and setup-complete.  
Key file: lib/main.dart
> - `aa33e36`: Enabled full-duplex audio conversations with Gemini by switching the response modality from text+audio to audio-only and fixing the WebSocket handshake (now waits for `_channel.ready`).  
Key file: `lib/main.dart`
> - `47ae65f`: Added infrastructure for bidirectional audio: new extractor pulls base64 audio from Gemini responses and upsampler converts 8 kHz → 16 kHz PCM16.  
Android minSdk bumped to 26 for raw_sound plugin; Gradle namespace fix for newer builds.  
Key files: audio_data_extractor.dart, audio_upsampler.dart, android/app/build.gradle.
> - `4a7f56c`: Renamed the Flutter app from `frame_realtime_openai_voice` to `frame_realtime_gemini_voicevision` and updated its description to reflect the switch from OpenAI Realtime API to Gemini Realtime API with added camera support. Bumped the `simple_frame_app` dependency from 2.0.0 to 2.0.3.

Key files: `pubspec.yaml`, `pubspec.lock`
> - `6b62347`: Renamed the Flutter app from “Frame Realtime OpenAI Voice” to “Frame Realtime Gemini Voice and Vision,” updating the package name, display names, and API key prompt to reflect the switch from OpenAI to Gemini.  
Key files: android/app/build.gradle, AndroidManifest.xml, MainActivity.kt, ios/Runner/Info.plist, lib/main.dart, README.md
