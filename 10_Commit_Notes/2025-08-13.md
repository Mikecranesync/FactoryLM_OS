---
title: "Commit Notes - 2025-08-13"
date: 2025-08-13
tags: [project/Archon, type/commit-note]
status: active
---

# Commit Notes - 2025-08-13

## Archon — Push at 23:59 UTC

**Commits:** 6
**Trigger:** `c929042` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c929042` | Speeding up CI | Cole Medin |
| `e020e33` | Bringing in first version of CI | Cole Medin |
| `bb64af9` | Archon onboarding, README updates, and MCP/global rule expansion for more coding assistants | Cole Medin |
| `8d189b9` | Delete CNAME | Cole Medin |
| `ba00d34` | Create CNAME | Cole Medin |
| `5908403` | The New Archon (Beta) - The Operating System for AI Coding Assistants! | Cole Medin |

> **What changed:**
> - `c929042`: Removed the full Docker Compose integration-test job from CI to cut runtime; only unit-level frontend/backend and build checks remain.  
Key file: `.github/workflows/ci.yml`
> - `e020e33`: Added the project’s first CI pipeline (`.github/workflows/ci.yml`) that runs on pushes/PRs to main. It currently executes Python backend tests with pytest + coverage and has a placeholder for React/Vitest frontend tests that will be enabled after a frontend overhaul.
> - `bb64af9`: Archon now ships with a guided onboarding flow that walks new users through connecting their first LLM provider and setting up global coding rules. The MCP server and IDE global-rules pages were expanded so Claude, Cursor, Windsurf, etc. can pull richer project context. Key files: archon-ui-main/src/pages/OnboardingPage.tsx, archon-ui-main/src/components/settings/IDEGlobalRules.tsx, python/src/server/api_routes/projects_api.py.
> - `8d189b9`: Removed the custom domain (archon.diy) from the repository by deleting the CNAME file, reverting the site back to its default GitHub Pages URL.  
Key file: CNAME
> - `ba00d34`: Added a CNAME file to configure the custom domain "archon.diy" for the GitHub Pages site. This enables the site to be served from the custom domain instead of the default GitHub Pages URL.

**Key file:** CNAME
> - `5908403`: **Summary:** Introduces “Archon V2 Alpha,” a complete rewrite of the AI-coding-assistant platform.  
The repo now ships a React/TypeScript/Vite frontend (`archon-ui-main/`) and a FastAPI/PydanticAI backend (`original_archon/`), plus Docker, docs, and GitHub automation. Key files added: `archon-ui-main/src/App.tsx`, `original_archon/archon/archon_graph.py`, `docker-compose.yml`, and a full test-suite scaffold.
