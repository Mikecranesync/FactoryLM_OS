---
title: "Commit Notes - 2025-04-09"
date: 2025-04-09
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-09

## cmms — Push at 21:56 UTC

**Commits:** 6
**Trigger:** `d40e8ce` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `d40e8ce` | fix: docker compose | Ibrahima G. Coulibaly |
| `9e2772f` | fix: longitude, latitude data types | Ibrahima G. Coulibaly |
| `94ff144` | fix: asset model patch | Ibrahima G. Coulibaly |
| `7f3542d` | fix: plans page | Ibrahima G. Coulibaly |
| `4d939d1` | fix: schedule subscription end | Ibrahima G. Coulibaly |
| `b08a9ab` | feat: cloud version var | Ibrahima G. Coulibaly |

> **What changed:**
> - `d40e8ce`: Added the missing `CLOUD_VERSION: false` environment variable to the Docker Compose config, ensuring the app correctly identifies itself as the self-hosted (non-cloud) edition.  
Key file: docker-compose.yml
> - `9e2772f`: Fixed location coordinates to use proper decimal types: changed longitude/latitude from `Long` to `Double` in DTOs and entity, and migrated DB columns to `float`.  
Key files: `Location.java`, `LocationPatchDTO.java`, `LocationShowDTO.java`, `LocationImportDTO.java`, plus Liquibase changelog `2025_04_10_1744226393_update_location.xml`.
> - `94ff144`: Added the missing `model` field to AssetPatchDTO so asset updates can now patch the model value.  
Key file: `api/src/main/java/com/grash/dto/AssetPatchDTO.java`
> - `7f3542d`: Plans page now sorts tiers by price and shows “Custom pricing” for the BUSINESS plan instead of a numeric cost.  
Key file: frontend/src/content/own/Subscription/Plans/index.tsx
> - `4d939d1`: Fixed a bug in subscription-end scheduling: the timer was incorrectly skipped when the end date was in the past; the date check is now removed so the task is always queued for non-FREE plans.  
Added Arabic translations for all e-mail and in-app messages.  

Key files: SubscriptionService.java, mailMessages_ar_AR.properties, messages_ar_AR.properties
> - `b08a9ab`: Introduced a `cloud-version` toggle that switches between SaaS (10-user/15-day trial) and self-hosted (100-user/unlimited) defaults; drops subscription prices to $10/$100 Starter and $15/$150 Professional; surfaces the flag via `/config` and UI copy.  
Key files: `api/src/main/resources/application.yml`, `frontend/src/config.ts`, `UserService.java`, `SubscriptionController.java`, `CompanyPlan.tsx`, `i18n/*.ts`.
