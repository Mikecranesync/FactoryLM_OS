---
title: "Commit Notes - 2025-06-13"
date: 2025-06-13
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-06-13

## Backlog.md — Push at 22:57 UTC

**Commits:** 14
**Trigger:** `cb6156a` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `cb6156a` | TASK-53 - Fix blessed load on bun (#85) | Alex Gavrilescu |
| `500c4fc` | bump package.json | Alex Gavrilescu |
| `6da9c22` | Fix npm publish error by removing prepare script | Alex Gavrilescu |
| `de532b6` | Add standalone executable with multi-arch support (#84) | Alex Gavrilescu |
| `c7ed5c8` | TASK-52 - CLI: Filter tasks list by status or assignee (#82) | Alex Gavrilescu |
| `d87e5f1` | Update readme.md | Alex Gavrilescu |
| `69437ef` | Add better guidelines for AI agents | Alex Gavrilescu |
| `0f8a646` | Update readme.md file | Alex Gavrilescu |
| `f9a419b` | Improve board and task detail views | Alex Gavrilescu |
| `e60912f` | Improve board ui | Alex Gavrilescu |
| `44947dd` | Improve loading performance of tasks | Alex Gavrilescu |
| `df4c9b0` | Mark task 24.1 as in progress | Alex Gavrilescu |
| `3b3fe1c` | fix task 24.1 filename | Alex Gavrilescu |
| `b0c103e` | TASK-41 to 51 - Migrate to blessed TUI (#80) | Alex Gavrilescu |

> **What changed:**
> - `cb6156a`: Fixed the `blessed` screen-loading crash when the CLI is installed globally via Bun by switching the load order: dynamic import first (works in Bun), falling back to `createRequire`. Added a regression test and updated README to note Bun 1.0+ support.  
Key files: `src/ui/tui.ts`, `src/test/line-wrapping.test.ts`, `readme.md`
> - `500c4fc`: Bumped the package version from 0.1.1 to 0.1.3 to prepare for a new release; no functional code changes.  
Key file: package.json
> - `6da9c22`: Fixed npm publish failures by stripping the `prepare`, `preinstall`, `postinstall` scripts and `devDependencies` from the package that gets shipped to the registry, eliminating the “husky: not found” error.  
Key file: `.github/workflows/release.yml`
> - `de532b6`: Switched the build pipeline to produce ~50 MB standalone executables via `bun build --compile`, eliminating the need for users to have Bun/Node installed and cutting startup time.  
A new release workflow now ships auto-selected native binaries for Linux/macOS/Windows on x64 & ARM64, while CI tests these builds on every push.  

Key files: `.github/workflows/release.yml`, `.github/workflows/ci.yml`, `package.json`, `scripts/build-standalone.js`, `src/cli.ts`, `src/utils/version.ts`
> - `c7ed5c8`: Added `--status` and `--assignee` flags to `backlog task list` so users can now filter tasks by exact status or assignee; both filters can be combined.  
Key files touched: `src/cli.ts` (filter logic), `src/test/cli.test.ts` (new tests), plus docs and CI tweaks.
> - `d87e5f1`: Added support for creating sub-tasks under a parent task and demoting tasks back to drafts; updated the Kanban command from `export` to `view`.  
Key file: readme.md
> - `69437ef`: Added comprehensive, CLI-driven workflow instructions for AI agents working on Backlog.md projects. The guidelines now detail task lifecycle, definition-of-done checklist, recommended task anatomy, and handy CLI commands. Key files: src/guidelines/.cursorrules.md, src/guidelines/AGENTS.md, src/guidelines/CLAUDE.md, src/test/cli.test.ts
> - `0f8a646`: Rewrote the README from a verbose manual into a concise marketing page: clearer tag-line, compact install commands, and a 5-minute tour that replaces the old step-by-step instructions.  
Key file: readme.md
> - `f9a419b`: Enhanced the Kanban board and task detail views with color-coded assignees/labels, bold task IDs, and improved acceptance-criteria rendering that preserves checkbox states and uses Unicode symbols.  
Key files: src/ui/board.ts, src/ui/task-viewer.ts
> - `e60912f`: Fixed column layout math in the TUI board so the last column now stretches to the right edge and removed the cumulative left-offset loop. Also tightened spacing (borders, padding, footer gap) so task lists sit properly inside their columns.  
Key file: src/ui/board.ts
> - `44947dd`: **Performance boost for task loading**  
Replaced sequential Git/FS calls with parallel Promise.all() fetches and added a loading spinner so the board view opens faster and feels responsive.  
Key files: `src/cli.ts`, `src/core/remote-tasks.ts`, `src/ui/loading.ts`
> - `df4c9b0`: Marked task 24.1 (CLI Kanban board milestone view) as “In Progress” and assigned it to @codex.  
Key file: `.backlog/tasks/task-24.1 - cli-kanban-board-milestone-view.md`
> - `3b3fe1c`: Fixed the filename for task 24.1 to correctly reference the CLI kanban board milestone view.  
Key file: `.backlog/tasks/task-24.1 - cli-kanban-board-milestone-view.md`
> - `b0c103e`: Migrated the entire CLI from prompts-based UI to blessed TUI with split-pane layout (40 % task list, 60 % detail), arrow-key navigation, sticky headers, and graceful non-TTY fallbacks.  
Key files: src/ui/tui.ts (core TUI wrapper), src/ui/board.ts (board renderer), src/cli.ts (integrated --tui flags), package.json (blessed dep).
