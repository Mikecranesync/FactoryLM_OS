---
title: "Commit Notes - 2025-11-25"
date: 2025-11-25
tags: [project/Thefuture, type/commit-note]
status: active
---

# Commit Notes - 2025-11-25

## Thefuture — Push at 21:15 UTC

**Commits:** 13
**Trigger:** `09a73c7` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `09a73c7` | fix: standardize paths to use Skill("CORE") and ${PAI_DIR} | Daniel Miessler |
| `65d1551` | feat(research): add comprehensive research skill with 10 workflows | Daniel Miessler |
| `b3631ae` | fix: remove references to non-existent commands in researcher agents | Daniel Miessler |
| `7e04e51` | fix: add debouncing to prevent duplicate SessionStart voice notifications | Daniel Miessler |
| `22c9b67` | fix: update ElevenLabs model from deprecated eleven_monolingual_v1 | Daniel Miessler |
| `9647d9e` | fix: restore PAI_DIR for location-agnostic installation | Daniel Miessler |
| `b54f29a` | fix: correct Fabric update instructions in SKILL.md | Daniel Miessler |
| `4f4d80c` | fix: simplify permissions format in settings.json | Daniel Miessler |
| `b49778d` | fix: replace ${PAI_DIR} with ~/.claude in settings.json | Daniel Miessler |
| `ae669c0` | feat: add commands directory with README and example | Daniel Miessler |
| `64f5f30` | docs: rewrite philosophy in casual voice | Daniel Miessler |
| `af28b84` | docs: add mission statement to README philosophy section | Daniel Miessler |
| `bd2973d` | fix: update broken paths from context/ to skills/CORE/ architecture | Daniel Miessler |

> **What changed:**
> - `09a73c7`: Standardized agent configurations to use `Skill("CORE")` instead of hardcoded paths, ensuring consistent context loading across architect, designer, engineer, pentester, and gemini-researcher agents. Updated SKILL-STRUCTURE-AND-AND-ROUTING.md to use `${PAI_DIR}` variable and removed incorrect mcp_settings.json registration instructions.

**Key files modified:** `.claude/agents/architect.md`, `.claude/agents/designer.md`, `.claude/agents/engineer.md`, `.claude/agents/gemini-researcher.md`, `.claude/agents/pentester.md`, `.claude/skills/CORE/SKILL-STRUCTURE-AND-ROUTING.md`
> - `65d1551`: Added a comprehensive research skill with 10 workflows (conduct, enhance, extract-knowledge, fabric, interview, perplexity-research, retrieve, web-scraping, youtube) and updated all researcher agents to use the unified `Skill("research")` pattern instead of individual commands. Fixed broken references to non-existent research commands and sanitized personal references for public template use.

**Key files:** `.claude/skills/research/`, `.claude/agents/*researcher.md`
> - `b3631ae`: Fixed broken researcher agent configs by removing references to non-existent Kai-specific commands (web-research, perform-perplexity-research, perform-claude-research). Updated agents to use standard WebSearch/WebFetch tools directly and fixed context loading to use Skill("CORE") instead of missing PAI.md file.

**Files modified:** `.claude/agents/claude-researcher.md`, `.claude/agents/perplexity-researcher.md`, `.claude/agents/researcher.md`
> - `7e04e51`: Fixed duplicate voice notifications during session start by adding a 2-second debounce mechanism using a lockfile in `/tmp`. The hook now silently skips duplicate SessionStart events fired by the IDE extension.

**Key file:** `.claude/hooks/initialize-pai-session.ts`
> - `22c9b67`: Updated ElevenLabs integration from deprecated `eleven_monolingual_v1` to recommended `eleven_multilingual_v2` model. Added `ELEVENLABS_MODEL` environment variable for model customization and improved error handling for model-related API failures.

**Files modified:** `.claude/.env.example`, `.claude/voice-server/server.ts`
> - `9647d9e`: Fixed hardcoded installation path by introducing `PAI_DIR` environment variable. All hook commands now use `${PAI_DIR}` instead of `~/.claude`, allowing users to install PAI anywhere. Updated settings.json and added setup instructions to QUICKSTART.md.

Key files: `.claude/settings.json`, `docs/QUICKSTART.md`
> - `b54f29a`: Fixed broken update instructions in the Fabric skill documentation by replacing non-functional `git pull` commands with two working alternatives: cloning fresh and copying over, or using a local Fabric installation. Updated all `${PAI_DIR}` references to the hardcoded `~/.claude` path.

**Key file:** `.claude/skills/fabric/SKILL.md`
> - `4f4d80c`: Simplified Claude's permissions format in settings.json by removing unnecessary wildcard syntax (e.g., Read(*) → Read) and eliminating the invalid LS(*) permission. This change cleans up the configuration and resolves schema validation warnings.

Key file: .claude/settings.json
> - `b49778d`: Fixed broken hook paths by replacing `${PAI_DIR}` with `~/.claude` in settings.json, ensuring hooks run out-of-box without env setup. Updated docs to remove obsolete PAI_DIR references.

**Files changed:** `.claude/settings.json`, `.claude/voice-server/QUICKSTART.md`, `docs/QUICKSTART.md`
> - `ae669c0`: Added a `.claude/commands/` directory with documentation and an example to enable custom slash commands in Claude Code. Users can now create markdown files here that become executable commands like `/example`.

Key files: `.claude/commands/README.md`, `.claude/commands/example.md`
> - `64f5f30`: Rewrote the README's philosophy section in a more casual, direct voice to emphasize individual empowerment over corporate AI dominance. Key file: README.md
> - `af28b84`: Updated README.md to add a mission statement and expand the philosophy section, emphasizing democratizing AI access and personal customization over corporate-controlled solutions.
> - `bd2973d`: Fixed broken file paths across all PAI agents after the architecture migration from `context/` to `skills/CORE/` structure. Updated bootloader references in architect, designer, engineer, and pentester agents to point to the correct `~/.claude/skills/CORE/SKILL.md` file, and replaced the broken `/conduct-research` command with inline workflow instructions in the research skill.

**Key files modified:** `.claude/agents/architect.md`, `.claude/agents/designer.md`, `.claude/agents/engineer.md`, `.claude/agents/pentester.md`, `.claude/skills/research/SKILL.md`
