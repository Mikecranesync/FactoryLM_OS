---
title: "Commit Notes - 2025-12-04"
date: 2025-12-04
tags: [project/Agent-Factory, project/Backlog.md, project/Thefuture, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-04

## Agent-Factory — Push at 16:09 UTC

**Commits:** 4
**Trigger:** `ae49774` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `ae49774` | docs: add memory system and agent building guide | Mikecranesync |
| `74eee9f` | fix: Windows compatibility - replace Unicode with ASCII | Mikecranesync |
| `cb18089` | fix: remove langgraph dependency causing conflict | Mikecranesync |
| `ed79f2f` | Initial commit: Agent Factory framework | Mikecranesync |

> **What changed:**
> - `ae49774`: **Summary:** Added a comprehensive memory system using timestamped markdown files to preserve project context across sessions, replacing the previous MCP-based approach due to token usage concerns. Also created a detailed guide for building custom agents with three different methods.

**Key files:** PROJECT_CONTEXT.md, ISSUES_LOG.md, DEVELOPMENT_LOG.md, DECISIONS_LOG.md, NEXT_ACTIONS.md, HOW_TO_BUILD_AGENTS.md
> - `74eee9f`: Fixed Windows console Unicode encoding errors by replacing Unicode characters (box-drawing, emoji) with ASCII equivalents ([OK], [ERROR], [WARNING]) and added sys.path fix for proper module imports when running as standalone script.

**Key files:** `agent_factory/examples/demo.py`
> - `cb18089`: Removed the LangGraph dependency from pyproject.toml to resolve a version conflict between LangGraph 0.0.26 (which requires langchain-core <0.2.0) and langchain 0.2.1 (which requires langchain-core >=0.2.0). This unblocks poetry installation since LangGraph isn't currently used in the codebase.

Key file: pyproject.toml
> - `ed79f2f`: Initial commit: Agent Factory framework. Files: `.claude/settings.local.json`, `.env.example`, `.gitattributes`, `.gitignore`, `LICENSE` (+17 more).

## Backlog.md — Push at 20:31 UTC

**Commits:** 4
**Trigger:** `c6aaadb` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `c6aaadb` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `9a67197` | TASK-328 - Make filename sanitization stricter by default (#443) | Alex Gavrilescu |
| `81e5161` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `6fb272e` | TASK-334 - Fix task numbering reset when all tasks archived (#449) | Eduard Kracmar |

> **What changed:**
> - `c6aaadb`: Bumped version to 1.26.2 and refreshed the auto-generated status board in the README with the latest counts and timestamp.  
Key files: README.md, package.json
> - `9a67197`: Stricter filename sanitization is now the single default: punctuation like quotes, parentheses, commas, etc. is stripped and dashes collapsed for tasks/drafts/decisions/docs. Legacy decision files are auto-cleaned on save so the new sanitized name stays canonical while existing items remain loadable by ID.

Key files: src/file-system/operations.ts, src/test/filesystem.test.ts
> - `81e5161`: README.md refreshed to v1.26.1, bumping the auto-generated Kanban board snapshot and package.json version.  
Key files: README.md, package.json
> - `6fb272e`: Fixed task ID reset bug: new tasks now continue numbering from the highest ID across active, archived, and completed tasks instead of restarting at task-1. Added `listArchivedTasks()` and updated `generateNextId()` to scan all directories, plus comprehensive tests.

Key files: `src/core/backlog.ts`, `src/file-system/operations.ts`, `src/test/id-generation.test.ts`

## Thefuture — Push at 20:44 UTC

**Commits:** 14
**Trigger:** `ca9d331` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `ca9d331` | style: TitleCase skill directories and update README references | Daniel Miessler |
| `6f3dc2e` | style: Standardize file naming to TitleCase and update QUICKSTART for three platforms | Daniel Miessler |
| `d2f6e41` | feat: Add detailed PAI infrastructure architecture diagram | Daniel Miessler |
| `b25c358` | feat: Add Windows PowerShell support for PAI setup | Daniel Miessler |
| `2b00466` | feat: Add comprehensive PAI overview diagram | Daniel Miessler |
| `71e1c39` | feat: Regenerate PAI overview diagram with proper technical aesthetic | Daniel Miessler |
| `a985e78` | feat: Replace skills diagram with full PAI architecture diagram | Daniel Miessler |
| `63d0164` | Update README to improve phrasing of AI availability | Daniel Miessler 🛡️ |
| `c9da8cf` | feat: Native Fabric patterns - run 248 patterns directly in Claude Code | Daniel Miessler |
| `40419fd` | fix: Regenerate principle diagrams with correct #0C1117 background | Daniel Miessler |
| `ffd5c7b` | feat: Add technical diagrams for all 13 founding principles | Daniel Miessler |
| `b9b84ef` | style: Clean up README header formatting below video section | Daniel Miessler |
| `e6c88af` | feat: Add interactive setup wizard for PAI configuration | Daniel Miessler |
| `2b236ab` | chore: Remove stale GITHUB_ISSUES_REPORT.md | Daniel Miessler |

> **What changed:**
> - `ca9d331`: Renamed all skill directories from lowercase to TitleCase (Art, BrightData, Fabric, etc.) and updated README links accordingly. Removed the non-interactive setup section and simplified the setup tip to clarify what the wizard configures.

**Key files:** `.claude/skills/*/SKILL.md`, `.claude/skills/README.md`
> - `6f3dc2e`: Standardized all markdown filenames to TitleCase and overhauled QUICKSTART.md with collapsible, platform-specific install steps for macOS/Linux/Windows plus troubleshooting sections.  
Key files: docs/QUICKSTART.md (major rewrite) and 50+ renamed skill/agent docs under .claude/.
> - `d2f6e41`: Added a comprehensive system architecture diagram to the README that visualizes all PAI components (Skills, Agents, Hooks, History, CLI Tools, Routing) using a dark GitHub-consistent background. This helps users quickly understand the system structure in the "What is PAI?" section.

**Files modified:** README.md, docs/images/pai-infrastructure.png
> - `b25c358`: Added Windows PowerShell support to the PAI setup system by creating a bootstrap.ps1 script that checks for Bun/Claude Code prerequisites and runs the setup wizard. Updated configurators to handle PowerShell profile templates with proper environment variable expansion for Windows paths (USERPROFILE). Key files: bootstrap.ps1, configurators/index.ts, templates/powershell.ts, validators/index.ts.
> - `2b00466`: Added a new comprehensive PAI overview diagram (pai-overview.png) that visualizes the full "Human Goals → PAI Framework → Human Flourishing" flow and displays all 13 founding principles in a scaffolding-style graphic. Updated README.md to reference the new diagram and expanded setup instructions with Windows PowerShell support alongside existing macOS/Linux commands.

Key files: README.md
> - `71e1c39`: Replaced the low-quality PAI overview diagram with a high-resolution, architect-style version that visually aligns with the 13 principle diagrams. The new image clearly depicts the transformation pipeline from Human Goals through the PAI Framework to Human Flourishing.

**Key file:** `docs/images/pai-overview.png`
> - `a985e78`: Replaced the skills-only diagram with a complete PAI (Principle AI) architecture visualization that positions Claude Code at the center, connecting Skills, Agents, Hooks, and History components on a dark-themed background.  
Key file: docs/images/skills-architecture.png
> - `63d0164`: Re-phrased the README’s tagline from a bolded sentence to a top-level heading, tightening the statement about democratizing AI.  
Key file: README.md
> - `c9da8cf`: **Native Fabric integration**: 248 patterns now run directly inside Claude Code without CLI spawning, using your Claude subscription's full context and intelligence. Only external CLI calls retained for YouTube transcripts (-y) and pattern updates (-U).

**Key files**: `.claude/skills/fabric/SKILL.md` (complete rewrite), plus 248 new pattern definition files under `.claude/skills/fabric/tools/patterns/*/system.md`
> - `40419fd`: Regenerated all 13 principle diagrams with GitHub dark mode background (#0C1117) to fix visual consistency issues. The diagrams now properly integrate with the dark theme instead of having mismatched white/black backgrounds.

**Files modified:** All 13 `docs/images/principle-*.png` files
> - `ffd5c7b`: Added 13 visual diagrams to illustrate PAI's founding principles, replacing the text-only architecture section with rich Excalidraw-style illustrations. Each principle now has a dedicated diagram with descriptions, making the system's philosophy more accessible. Key files: README.md and 13 new PNG diagrams in docs/images/.
> - `b9b84ef`: **Summary:** Cleaned up README formatting by removing excessive line breaks and consolidating the mission statement into a single bold sentence for better visual hierarchy below the video section.

**Key file modified:** README.md
> - `e6c88af`: Added an interactive setup wizard that replaces hardcoded paths with a guided configuration process. The wizard creates user profiles, configures the voice server, and updates shell profiles with automatic rollback on failure. Key files: `tools/setup/setup.ts`, `bootstrap.sh`, and voice server install scripts.
> - `2b236ab`: Removed the outdated GITHUB_ISSUES_REPORT.md file that contained stale issue tracking info, including "pending" fixes that were already resolved. This eliminates confusion from misleading documentation.

**Key file deleted:** GITHUB_ISSUES_REPORT.md

## cmms — Push at 20:25 UTC

**Commits:** 7
**Trigger:** `a44ebfa` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `a44ebfa` | fix: appendUtmParameters | Ibrahima G. Coulibaly |
| `6a10e8f` | fix: requestUpgrade | Ibrahima G. Coulibaly |
| `6a07724` | fix: appendUtmParameters | Ibrahima G. Coulibaly |
| `0410f74` | feat: use referrer | Ibrahima G. Coulibaly |
| `450d221` | fix: check if utmParams null | Ibrahima G. Coulibaly |
| `7fdca1a` | chore: improve superadmin mail | Ibrahima G. Coulibaly |
| `f417cdb` | fix: UserMapper toModel | Ibrahima G. Coulibaly |

> **What changed:**
> - `a44ebfa`: Fixed the `appendUtmParameters` method so it only appends the marketing-attribution block when at least one UTM/tracking value is actually present. Added a helper method `hasAnyParam()` to `UtmParams` to check all fields in one call.

Files touched: `UtmParams.java`, `UserService.java`
> - `6a10e8f`: Fixed the upgrade-request endpoint so demo-company owners can’t spam support with upgrade requests. Key file: SubscriptionController.java
> - `6a07724`: Fixed a bug where UTM marketing parameters were being appended to registration emails even in self-hosted (non-cloud) deployments. Now the attribution block only appears when running the cloud version and UTM data is present.  
Key file: `UserService.java`
> - `0410f74`: Added referrer tracking to user sign-ups: frontend now captures `document.referrer` in localStorage and sends it with the registration payload; backend stores it in the `own_user` table and includes it in the welcome email.  
Key files: `OwnUser.java`, `UserService.java`, `JWTAuthContext.tsx`, `App.tsx`, plus new DB changelog.
> - `450d221`: Added a null check for `utmParams` before appending marketing attribution data to registration emails, preventing potential NPEs during user signup.

Key file: `UserService.java`
> - `7fdca1a`: Refactored the super-admin registration email in UserService: extracted messy inline string-building into clean helper methods that now include UTM/gclid attribution data for marketing tracking.  
Key file: api/src/main/java/com/grash/service/UserService.java
> - `f417cdb`: Fixed the parameter order in UserMapper’s @AfterMapping method so UTM parameters from UserSignupRequest are correctly copied into the OwnUser model instead of the reverse.  
Key file: api/src/main/java/com/grash/mapper/UserMapper.java
