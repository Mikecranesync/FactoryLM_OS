---
title: "Commit Notes - 2025-09-17"
date: 2025-09-17
tags: [project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-09-17

## Backlog.md — Push at 21:14 UTC

**Commits:** 1
**Trigger:** `37004b8` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `37004b8` | TASK-266 - Fix Implementation Notes heading truncation in web UI (#358) | Alex Gavrilescu |

> **What changed:** Fixed the web UI truncation bug by wrapping Implementation Notes in sentinel comments (`<!-- SECTION:NOTES:BEGIN/END -->`) so nested H2+ headings are preserved; parser now honors these markers and serializer avoids duplicating them. Key files touched: `src/markdown/parser.ts`, `src/markdown/serializer.ts`, `src/ui/task-viewer.ts`, `src/web/components/TaskCard.tsx` and related tests.

## cmms — Push at 22:29 UTC

**Commits:** 5
**Trigger:** `cb7108d` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `cb7108d` | feat: Add Work Order import functionality (main) | Ibrahima G. Coulibaly |
| `4bb4875` | feat: Add import service for various entities (main) | Ibrahima G. Coulibaly |
| `122fca5` | feat: Add lombok annotations and default values to DTOs (main) | Ibrahima G. Coulibaly |
| `6caf077` | feat: Delete demo companies (main) | Ibrahima G. Coulibaly |
| `c52f5d3` | Fix: Handle null demo field in user registration | Ibrahima G. Coulibaly |

> **What changed:**
> - `cb7108d`: Added CSV import for Work Orders: demo data now loads WorkOrders.csv, parses each row into WorkOrderImportDTO, and persists it.  
Key files: DemoController.java, WorkOrders.csv
> - `4bb4875`: Added ImportService to bulk-create/update Work Orders, Assets, Locations, Meters and Parts for a company; it returns counts of created vs. updated records.  
Key file: api/src/main/java/com/grash/service/ImportService.java
> - `122fca5`: Added Lombok boilerplate and default collection values to all import DTOs to prevent NPEs.  
Introduced a new ImportService and wired the DemoController to auto-load telecom demo data (locations, assets, meters, parts) when a demo account is created.  
Key files: ImportController.java, DemoController.java, *ImportDTO.java, telecom/*.csv.
> - `6caf077`: Added a Liquibase changelog to delete demo companies and updated the master changelog to include it. The change modifies the foreign key constraint on verification_token to cascade on delete, enabling cleanup of related demo data.

Key files:  
- api/src/main/resources/db/changelog/2025_09_17_1758107551_delete_demo_companies.xml  
- api/src/main/resources/db/master.xml
> - `c52f5d3`: Fixed a null-pointer risk during signup by safely checking the optional `demo` flag; users with an unset demo field no longer break registration.  
Key file: `api/src/main/java/com/grash/service/UserService.java`
