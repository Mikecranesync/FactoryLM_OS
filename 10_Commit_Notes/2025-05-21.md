---
title: "Commit Notes - 2025-05-21"
date: 2025-05-21
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-05-21

## cmms — Push at 16:57 UTC

**Commits:** 5
**Trigger:** `8650f5b` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `8650f5b` | refactor: custom ID generation logic for Asset, WorkOrder, and Location services | Ibrahima G. Coulibaly |
| `ec2f839` | chore: update reset password email to use "Confirm reset" button in all supported languages | Ibrahima G. Coulibaly |
| `918cd98` | chore(mobile): remove patches | Ibrahima G. Coulibaly |
| `3f2e2d0` | chore(mobile): versioning | Ibrahima G. Coulibaly |
| `94ab289` | fix: tsc and download permissions | Ibrahima G. Coulibaly |

> **What changed:**
> - `8650f5b`: Refactored custom ID generation for assets, work orders, and locations into private helper methods (`getAssetNumber`, `getWorkOrderNumber`, `getLocationNumber`) within their respective services. This centralizes ID creation, ensuring consistency across create, update, and import operations while eliminating code duplication.  
Key files: `AssetService.java`, `LocationService.java`, `WorkOrderService.java`.
> - `ec2f839`: Password-reset emails now show a “Confirm reset” button instead of a bare link across all supported languages.  
Key files: every `mailMessages_*.properties` and `reset-password.html`.
> - `918cd98`: Removed local patches for expo-modules-core and react-native, including the workaround that switched the Boost download mirror.  
AndroidManifest.xml was also cleaned up—redundant comments stripped but no permission behavior changed.  
Key files: mobile/patches/* (deleted), mobile/android/app/src/main/AndroidManifest.xml
> - `3f2e2d0`: Bumped the mobile app version from 1.0.27 to 1.0.28 in both the Expo config and Android build.gradle, preparing for the next release.  
Key files: mobile/android/app/build.gradle, mobile/app.config.ts
> - `94ab289`: Fixed TypeScript compilation errors by removing deprecated PropTypes from context providers and corrected theme hook usage in AssetsScreen. Updated Android download logic to skip storage permission requests on API 29+ (scoped storage) and properly await download completion.  
Key files: AuthContext.tsx, CompanySettingsContext.tsx, AssetsScreen.tsx, WODetailsScreen.tsx
