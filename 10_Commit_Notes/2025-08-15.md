---
title: "Commit Notes - 2025-08-15"
date: 2025-08-15
tags: [project/Archon, type/commit-note]
status: active
---

# Commit Notes - 2025-08-15

## Archon — Push at 21:39 UTC

**Commits:** 11
**Trigger:** `41c58e5` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `41c58e5` | Merge pull request #219 from coleam00/fix/respect-log-level-env-var | Wirasm |
| `8743c05` | Merge pull request #218 from coleam00/fix/filter-binary-files-from-crawl | Wirasm |
| `f96a9a4` | Merge pull request #213 from coleam00/fix/consolidate-concurrency-settings | Wirasm |
| `e2e51f3` | Fixing persona issue in PRP docs | Cole Medin |
| `4a4663b` | Disabling reranking by default so the server container isn't so big | Cole Medin |
| `ade4397` | Suppress noisy third-party library debug logs | Rasmus Widing |
| `caefacc` | Fix trailing whitespace (ruff formatting) | Rasmus Widing |
| `e9a19ff` | Fix LOG_LEVEL environment variable not being respected | Rasmus Widing |
| `8157670` | Fix crawler attempting to navigate to binary files | Rasmus Widing |
| `e98f52a` | Address code review feedback: improve error handling and documentation | Rasmus Widing |
| `aab0721` | Fix crawler concurrency configuration to prevent memory crashes | Rasmus Widing |

> **What changed:**
> - `41c58e5`: Merge pull request #219 from coleam00/fix/respect-log-level-env-var. Files: `python/src/server/config/logfire_config.py`.
> - `8743c05`: Merge pull request #218 from coleam00/fix/filter-binary-files-from-crawl. Files: `python/src/server/services/crawling/helpers/url_handler.py`, `python/src/server/services/crawling/strategies/recursive.py`, `python/tests/test_url_handler.py`.
> - `f96a9a4`: Merge pull request #213 from coleam00/fix/consolidate-concurrency-settings. Files: `.env.example`, `python/src/server/services/crawling/crawling_service.py`, `python/src/server/services/crawling/strategies/batch.py`, `python/src/server/services/crawling/strategies/recursive.py`.
> - `e2e51f3`: Fixed a rendering bug in the PRP persona cards by adding `Array.isArray()` checks before mapping over `goals` and `pain_points`, preventing crashes when those fields aren’t arrays.  
Key file: `archon-ui-main/src/components/prp/sections/PersonaSection.tsx`
> - `4a4663b`: Disabled the optional reranking RAG feature by default to shrink the Archon Server container image: commented-out the heavy `sentence-transformers`, `torch`, and `transformers` packages in `requirements.server.txt` and wrapped their imports in try/except blocks so the server starts without them. Users can re-enable reranking by uncommenting those three lines. Key files: `python/requirements.server.txt`, `python/src/server/main.py`, `python/src/server/services/search/rag_service.py`, `python/src/server/services/search/reranking_strategy.py`.
> - `ade4397`: Reduced log noise by setting hpack and httpcore loggers to WARNING, filtering out excessive HTTP/2 and low-level protocol debug output.  
Key file: `python/src/server/config/logfire_config.py`
> - `caefacc`: Fixed trailing whitespace in the Logfire config module to comply with Ruff formatting rules. No functional changes—purely cosmetic cleanup.  
Key file: `python/src/server/config/logfire_config.py`
> - `e9a19ff`: Fixed the LOG_LEVEL environment variable so debug logs actually appear when set. Now reads LOG_LEVEL from env with safe string-to-constant conversion, defaulting to INFO if missing or invalid.

Key file: python/src/server/config/logfire_config.py
> - `8157670`: Added binary-file filtering to the crawler so it no longer tries to navigate ZIP, PDF, images, etc., eliminating net::ERR_ABORTED errors.  
Key files: url_handler.py (new is_binary_file helper), recursive.py (skip binary URLs in crawl queue).
> - `e98f52a`: Crawler now fails fast on bad config (ValueError/KeyError/TypeError) while still falling back to safe defaults for transient DB or network hiccups; added env docs for the four new crawler knobs.  
Key files: `.env.example`, `batch.py`, `recursive.py`.
> - `aab0721`: Fixed memory crashes by removing hardcoded 20-page concurrency limit for docs sites; all crawls now respect the single `CRAWL_MAX_CONCURRENT` DB setting (default 10).  
Key files: `crawling_service.py`, `strategies/batch.py`
