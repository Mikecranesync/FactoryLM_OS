---
title: "Commit Notes - 2025-05-31"
date: 2025-05-31
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-05-31

## cmms — Push at 21:45 UTC

**Commits:** 9
**Trigger:** `5ebc249` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5ebc249` | refactor(frontend): replace `useLogo` and direct branding with unified `useBrand` hook for white-labeling | Ibrahima G. Coulibaly |
| `b331097` | refactor: backend handles logo management; remove frontend logo copy logic | Ibrahima G. Coulibaly |
| `9f0432c` | refactor: switch to backend-driven logo management and remove frontend logo copy logic | Ibrahima G. Coulibaly |
| `54ddd47` | chore: improve white-labeling robustness: ensure logo fallback, branding display, and license validity checks | Ibrahima G. Coulibaly |
| `f2b43e0` | feat: add license validity check and logo fallback logic to support secure white-labeling | Ibrahima G. Coulibaly |
| `9c14c5e` | feat: enhance white-labeling with multi-logo and color support via environment variables | Ibrahima G. Coulibaly |
| `0931347` | refactor: work order email template to use shared main layout fragment | Ibrahima G. Coulibaly |
| `f6e27c6` | feat: add dynamic email background color support via customColors | Ibrahima G. Coulibaly |
| `e0c65b9` | feat: add support for custom logo and colors via environment variables for white-labeling | Ibrahima G. Coulibaly |

> **What changed:**
> - `5ebc249`: Replaced hard-coded brand info with a new `useBrand` hook that pulls name/logo/contact from runtime config, enabling full white-label deployments via the optional `BRAND_CONFIG` env var. Moved license and logo resolution to the backend; frontend now only consumes the resolved branding state.

Key files: `frontend/src/hooks/useBrand.ts`, `frontend/src/config.ts`, `api/src/main/java/com/grash/controller/LicenseController.java`, `.env.example`, `docker-compose.yml`
> - `b331097`: Backend now copies and serves custom logos from `/app/static/images`, eliminating the need for frontend logo-copy logic. Users place `dark.png`/`white.png` in a `logo` folder beside `docker-compose.yml` and reference only the filenames in `LOGO_PATHS`.  
Key files: `api/src/main/java/com/grash/utils/LogoSetupComponent.java`, `README.MD`
> - `9f0432c`: Backend now owns logo lifecycle: copies dark & white variants into `/app/static/images` at startup and serves them at `/images/**`; frontend simply fetches from those URLs and only shows logos when the license is valid.  
Removed frontend build-time copy script and git-ignored the shared `logo` volume, enabling runtime logo swaps in Docker without rebuilds.  
Key files: `LogoSetupComponent.java`, `WebMvcConfig.java`, `docker-compose.yml`, `useLogo.ts`
> - `54ddd47`: White-labeling now only activates when the license is valid: backend creates the logo folder before copying, frontend waits for license check (`null` → true/false) before swapping logos, and the “Powered by Intelloop” badge is hidden when white-label is on.  
Key files: `useLogo.ts`, `Sidebar/index.tsx`, `JWTAuthContext.tsx`, `LogoSetupComponent.java`, `license.ts`.
> - `f2b43e0`: Added license-gated white-labeling: backend caches 12h license validity via LicenseController; frontend uses Redux + `useLogo` hook to show custom logo/colors only when valid, falling back to defaults and hiding “Powered by Intelloop” if not.  
Key files: LicenseController.java, LicenseService.java, useLogo.ts, license.ts, PureLightTheme.ts.
> - `9c14c5e`: White-labeling now supports multiple logos (dark/white) and custom colors via `LOGO_PATHS` and `CUSTOM_COLORS` env vars. Backend and frontend updated to serve and fallback to defaults; docs and compose files reflect the new structure.  
Key files: `.env.example`, `docker-compose.yml`, `LogoSetupComponent.java`, `index.tsx`, `config.ts`
> - `0931347`: Refactored the work-order email to share a new `main-layout.html` fragment, giving all notification emails a consistent branded look and dynamic background color. Centralizes layout maintenance; key files: `main-layout.html` (new shared fragment) and `new-work-order.html` (now uses the fragment).
> - `f6e27c6`: Added dynamic email theming: the `white-labeling.custom-colors` JSON is parsed in `EmailService2` and the `emailColors` value is injected into Thymeleaf as `backgroundColor`, letting the new-work-order template (and future ones) render branded backgrounds/buttons. Docker-compose now defaults `CUSTOM_COLORS` and `LOGO_PATH` to empty to prevent unset-var errors.
> - `e0c65b9`: Added white-labeling: set `LOGO_PATH` and `CUSTOM_COLORS` env vars to swap the app logo and color scheme without touching code.  
Logo is copied at startup and injected into email templates; vars are documented in `.env.example`, `README`, and Docker configs.  
Key files: `.env.example`, `README.MD`, `docker-compose.yml`, `application.yml`, `LogoSetupComponent.java`, email templates.
