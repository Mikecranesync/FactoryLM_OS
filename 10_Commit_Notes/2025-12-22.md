---
title: "Commit Notes - 2025-12-22"
date: 2025-12-22
tags: [project/Agent-Factory, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-22

## Agent-Factory — Push at 23:45 UTC

**Commits:** 17
**Trigger:** `d9d974d` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `d9d974d` | docs: session 2025-12-22 - KB search column fix | Mikecranesync |
| `ce687db` | docs: session 2025-12-22 - Phase 1 KB gap logging deployed | Mikecranesync |
| `d85c374` | fix: KB search column name mismatch (vendor → manufacturer) | Mikecranesync |
| `3a2a5e2` | fix: JSON parsing and Route C orchestrator bugs | Mikecranesync |
| `49f4a1d` | feat: Add photo OCR handler for equipment nameplates | Mikecranesync |
| `d38b43c` | feat: Add KB gap logging (Phase 1) | Mikecranesync |
| `d79e06d` | docs: Session 2 complete - Two-message pattern + CI/CD audit | Mikecranesync |
| `a84787b` | docs: Add SYSTEM_MANIFEST.md documenting CI/CD pipelines and VPS deployment | Mikecranesync |
| `f2ec4bd` | feat: Add source attribution and admin debug traces to bot | Mikecranesync |
| `9e2761d` | feat: Split bot responses into two messages - clean user response + admin debug trace | Mikecranesync |
| `2161da0` | feat: Add Groq LLM fallback for Routes C & D | Mikecranesync |
| `e29cd1f` | fix: Change SQL placeholders from $N to %s for psycopg3 compatibility | Mikecranesync |
| `9cf96de` | fix: Convert VendorType enum to string value in RivetIntent creation | Mikecranesync |
| `a95fe79` | fix: Change EquipmentType.GENERIC to UNKNOWN in kb_evaluator | Mikecranesync |
| `339e078` | fix: Correct search_docs() function signature in kb_evaluator | Mikecranesync |
| `685915b` | fix: Add Markdown escaping and RAG layer to Telegram bot | Mikecranesync |
| `fff05c0` | feat: Add VPS deployment setup for 24/7 Telegram bot | Mikecranesync |

> **What changed:**
> - `d9d974d`: Fixed KB search by correcting column references in retriever.py/filters.py and mapping 9 vendor names to manufacturers; 1,964 atoms now load without SQL errors. Updated docs to mark KB search as working and queued vendor-detection Phase 2 plus research Phase 3.  
Key files: CURRENT_STATE.md, NEXT_ACTIONS.md
> - `ce687db`: **Summary:**  
Phase 1 KB-gap logging is live: the orchestrator now writes every unanswered query to the new `kb_gaps` table (Neon PG) and increments a 7-day frequency counter for duplicates. All docs are updated—new CURRENT_STATE.md gives a traffic-light status board, SYSTEM_MAP.md shows the full 4-route flow, and PHASE1_KB_GAP_TEST.md walks through the 5-step validation. Bot service restarted cleanly; next action is user testing before wiring the research pipeline in Phase 2.

**Key files touched:**  
CURRENT_STATE.md, DECISIONS_LOG.md, NEXT_ACTIONS.md, SYSTEM_MAP.md, docs/testing/PHASE1_KB_GAP_TEST.md
> - `d85c374`: Fixed a critical KB search bug where queries returned zero results due to column name mismatch: code was filtering on non-existent 'vendor' column instead of 'manufacturer'. Updated retriever.py and filters.py to use correct column name and added vendor-to-manufacturer mapping for 9 vendor types. Removed equipment_type filter as that column also doesn't exist in DB. Key files: agent_factory/rivet_pro/rag/filters.py, agent_factory/rivet_pro/rag/retriever.py
> - `3a2a5e2`: Fixed two runtime errors: (1) GPT-4o Vision sometimes wraps JSON in markdown fences, so the Telegram bot now strips ```json/``` before parsing; (2) Route C orchestrator was trying to access a non-existent intent attribute on RoutingDecision, so removed the KB-gap logging call that relied on it.  
Key files: agent_factory/core/orchestrator.py, agent_factory/integrations/telegram/orchestrator_bot.py
> - `49f4a1d`: The bot now accepts photos of equipment nameplates, uses GPT-4o Vision to read the manufacturer, model, serial and any fault code, then routes that data through the existing troubleshooting orchestrator.  
Key file: agent_factory/integrations/telegram/orchestrator_bot.py
> - `d38b43c`: Added KB gap logging to capture when Route C (knowledge-base fallback) returns zero atoms.  A new `kb_gaps` table and `KBGapLogger` class now record the query, intent, vendor, equipment and increment a frequency counter if the same query re-appears within 7 days; gaps can later be marked resolved once matching atoms are ingested.  
Key files: `agent_factory/core/kb_gap_logger.py`, `agent_factory/core/orchestrator.py`, `docs/database/migrations/001_kb_gaps_table.sql`
> - `d79e06d`: **Summary:**  
Implemented a two-message pattern in the Telegram bot: users now receive clean responses without debug metadata, while admins get a separate trace message with route and confidence. Discovered GitHub Actions deploys outdated bot code from a different workflow than the systemd service runs. Key files: `orchestrator_bot.py`, `DEVELOPMENT_LOG.md`, `ISSUES_LOG.md`.
> - `a84787b`: Added SYSTEM_MANIFEST.md to capture the current deployment reality: GitHub Actions is broken because it still references the legacy telegram_bot.py and rivet-pro.service while the live VPS runs orchestrator_bot.py under orchestrator-bot.service. The doc lists every VPS service, pinpoints the workflow mismatch, and gives a clear manual-deploy fallback until CI is fixed.
> - `f2ec4bd`: Added source attribution to Telegram bot responses: answers now show whether they came from the Knowledge Base (with atom count/titles) or were AI-generated, plus the route and confidence.  Admins also get a richer debug trace (KB coverage, vendor, agent) after every response.  
Key file: agent_factory/integrations/telegram/orchestrator_bot.py
> - `9e2761d`: Bot now splits every reply: users see only the clean answer, while a second message containing route & confidence is quietly sent to the hard-coded admin chat (8445149012).  
Key file: `agent_factory/integrations/telegram/orchestrator_bot.py`
> - `2161da0`: Added Groq Llama 3.1 70B as the primary fallback for Routes C & D, replacing static “check back later” messages with AI-generated answers. A 3-tier chain (Groq → GPT-3.5 → hardcoded) keeps responses flowing even if upstream LLMs fail.  
Key files: agent_factory/core/orchestrator.py, agent_factory/llm/config.py, agent_factory/llm/types.py
> - `e29cd1f`: Fixed SQL placeholder syntax for psycopg3: switched from `$N` to `%s` format and corrected the keyword ILIKE clause to append three copies of each keyword (one per field) instead of one.  
Key file: `agent_factory/rivet_pro/rag/retriever.py`
> - `9cf96de`: Fixed a validation error when creating RivetIntent by mapping the routing-layer VendorType enum to the corresponding rivet_pro string values and adding the missing required kb_coverage field. Key file: agent_factory/routers/kb_evaluator.py
> - `a95fe79`: Fixed a runtime error by changing the fallback equipment type from the non-existent `EquipmentType.GENERIC` to the correct `EquipmentType.UNKNOWN` enum value in the kb_evaluator. This ensures the evaluator can handle requests without explicit equipment types without crashing.

**Key file:** `agent_factory/routers/kb_evaluator.py`
> - `339e078`: Fixed the kb_evaluator by updating the search_docs() call to use the new signature: create a RivetIntent from the request and pass intent/config/db instead of the old query/vendor/top_k. Also switched to reading similarity scores from RetrievedDoc objects.  
Key file: agent_factory/routers/kb_evaluator.py
> - `685915b`: Telegram bot now safely formats responses with Markdown escaping to prevent parse errors, and initializes the orchestrator with a database-backed RAG layer for confident knowledge-base answers. Falls back gracefully if the database is unavailable.

Key file: `agent_factory/integrations/telegram/orchestrator_bot.py`
> - `fff05c0`: Added VPS deployment infrastructure for 24/7 Telegram bot operation with systemd service management scripts and monitoring tools. Integrated RivetOrchestrator into Telegram via new `/rivet` command that routes industrial maintenance queries through 4-route intelligent system (A/B/C/D) with safety warnings and citations.

Key files: `deploy/vps/` (setup, deploy, service files), `agent_factory/integrations/telegram/rivet_orchestrator_handler.py`, `bot.py`

## cmms — Push at 02:55 UTC

**Commits:** 3
**Trigger:** `7b9cbba` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `7b9cbba` | fix: use shared IsOriginalCloud | Ibrahima G. Coulibaly |
| `8e89899` | chore: improve UI | Ibrahima G. Coulibaly |
| `7cefa0b` | fix: user role patch | Ibrahima G. Coulibaly |

> **What changed:**
> - `7b9cbba`: Replaced scattered `!isWhiteLabeled && isCloudVersion` checks with a single exported constant `IS_ORIGINAL_CLOUD` to simplify and unify the “original cloud” detection logic. Also cleaned up an unused license check and fixed a deprecated `getBytes("UTF-8")` call in the fingerprint generator.  
Key files: `frontend/src/config.ts`, `frontend/src/content/overview/index.tsx`, `frontend/src/content/terms-of-service/index.tsx`, `api/src/main/java/com/grash/utils/FingerprintGenerator.java`
> - `8e89899`: Improved UI responsiveness by adding loading states to asset detail views and enabling tracking of Microsoft Click ID in analytics. Key files: `AssetDetails.tsx`, `AssetWorkOrders.tsx`, `App.tsx`.
> - `7cefa0b`: Fixed role patching logic by correcting the method name from `belongsOnlyToCompany` to `belongsToCompany`, ensuring users can only be assigned roles that belong to their company. This prevents unauthorized role assignments across company boundaries.

**Key file modified:** `UserController.java`
