---
title: "Commit Notes - 2025-07-04"
date: 2025-07-04
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-07-04

## Backlog.md — Push at 22:58 UTC

**Commits:** 7
**Trigger:** `7b41a9e` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `7b41a9e` | TASK-108 - Fix bug: Acceptance criteria removed when updating description (#153) | Alex Gavrilescu |
| `335e003` | TASK-107 - Add agents --update-instructions command (#152) | Alex Gavrilescu |
| `6f84f98` | Fix Windows CI compatibility by forcing bash shell | Alex Gavrilescu |
| `1a9d279` | Fix build conflicts: output executables to dist/ folder | Alex Gavrilescu |
| `d79a144` | Add new backlog folder to git instead of .backlog | Alex Gavrilescu |
| `a7add0c` | TASK-106 - Add --desc alias for description flag (#150) | Alex Gavrilescu |
| `d768e7a` | TASK-105 - Remove dot from .backlog folder name (#149) | Alex Gavrilescu |

> **What changed:**
> - `7b41a9e`: Fixed a bug where updating a task’s description wiped out other sections (acceptance criteria, plan, notes). Added comprehensive tests to ensure each edit command only touches its target section and leaves the rest intact.  
Key files: `src/test/task-edit-preservation.test.ts`, `src/test/update-task-description.test.ts`
> - `335e003`: Added a new `agents --update-instructions` CLI command that bulk-updates all AI-agent instruction files (.cursorrules, CLAUDE.md, etc.) with the latest project conventions.  
Key files: src/cli.ts (new command), src/file-system/operations.ts (update logic), .cursorrules (now includes backlog task guidelines).
> - `6f84f98`: Fixed Windows CI failure by explicitly setting the shell to bash for the “Check build output” step, ensuring the `ls -la` command runs cross-platform.  
Key file: `.github/workflows/release.yml`
> - `1a9d279`: Build outputs now land in `dist/` to avoid clashing with the source `backlog/` folder; CI and release workflows updated to expect binaries there and rename `backlog-bin` → `backlog` before packaging.  
Key files: `.github/workflows/release.yml`, `package.json`
> - `d79a144`: Replaced the hidden `.backlog` folder with a visible `backlog` directory so the entire task hierarchy (tasks, drafts, milestones, decisions, archive) is now tracked in git.  
Key files touched: `.gitignore` (removed `backlog` entry), `package.json`, `biome.json`, and the new `backlog/` tree.
> - `a7add0c`: Added `--desc` as an alias for the `--description` flag in task and draft commands. Users can now use `-d`, `--description`, or `--desc` interchangeably; help text and tests cover all variants. Key files: `src/cli.ts`, `src/markdown/serializer.ts`, `src/test/desc-alias.test.ts`.
> - `d768e7a`: Renamed the `.backlog` folder to `backlog` (dropping the dot) and made the directory name configurable via `backlogDirectory` in config.yml. Existing installs keep `.backlog` for backward compatibility, while new ones default to `backlog`. All code, tests, and docs updated to match.

Key files: `src/core/backlog.ts`, `src/constants/index.ts`, `config.yml`, plus every `.backlog/*` path now lives under `backlog/*`.
