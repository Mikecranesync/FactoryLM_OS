---
title: "Commit Notes - 2025-09-13"
date: 2025-09-13
tags: [project/Thefuture, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-09-13

## Thefuture — Push at 18:05 UTC

**Commits:** 1
**Trigger:** `aaa5d7f` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `aaa5d7f` | Replace hardcoded paths with PAI_HOME environment variable | Michael Vöhringer |

> **What changed:** Replaced all hardcoded `~/.claude` paths with `${PAI_HOME}` environment variable across documentation, commands, and TypeScript files. This enables flexible installation directories and custom deployment paths for the Claude context system.

Key files: `.claude/commands/capture-learning.md`, `.claude/commands/capture-learning.ts`, `.claude/commands/load-dynamic-requirements.md`, `.claude/context/CLAUDE.md`, `.claude/hooks/load-dynamic-requirements.ts`, `README.md`

## cmms — Push at 23:46 UTC

**Commits:** 12
**Trigger:** `f3dd325` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `f3dd325` | feat: Add demo user registration option (123) | Ibrahima G. Coulibaly |
| `50513f0` | fix: Remove unnecessary company check in email sending (123) | Ibrahima G. Coulibaly |
| `9887ed0` | Fix: Allow undefined headers in api util (#1) | Ibrahima G. Coulibaly |
| `0c11383` | feat: Improve API types and add auth header to demo | Ibrahima G. Coulibaly |
| `018034c` | Fix: Prevent sending emails for demo companies | Ibrahima G. Coulibaly |
| `a7271d0` | Fix: Delete demo companies job | Ibrahima G. Coulibaly |
| `4d23430` | feat: Add demo column to company table (main) | Ibrahima G. Coulibaly |
| `5dc1572` | feat: Add demo column to company table (main) | Ibrahima G. Coulibaly |
| `367620a` | feat: Update bucket4j dependency (main) | Ibrahima G. Coulibaly |
| `825346e` | fix(main): Change notification position to fixed | Ibrahima G. Coulibaly |
| `f1dd237` | Fix: Only show demo alert if user is logged in (#123) | Ibrahima G. Coulibaly |
| `dc79449` | feat: Implement demo mode and automatic deletion (main) | Ibrahima G. Coulibaly |

> **What changed:**
> - `f3dd325`: Added a demo-registration path: when `userReq.getDemo()` is true the new account is immediately activated and no verification e-mail is sent.  
Key file: `api/src/main/java/com/grash/service/UserService.java`
> - `50513f0`: Registration emails to super-admins are now sent for every enabled user, regardless of company demo status; the demo-company guard was moved inside the mailer method so demo users are silently skipped instead of blocking the flow.  
Key file: `UserService.java`
> - `9887ed0`: Made the `headers` property optional in the API utility’s `Options` type so callers can omit it when no custom headers are needed.  
Key file: frontend/src/utils/api.ts
> - `0c11383`: Added auth header to the demo account generation call so the request now carries the JWT token, fixing a silent 401.  
Tightened the API helper types (Options) for safer option passing across get/post/patch/delete.

Files: frontend/src/content/overview/Hero/index.tsx, frontend/src/utils/api.ts
> - `018034c`: **Summary:**  
Registration emails are now only sent to super admins if the user’s company is not a demo company, preventing unnecessary notifications for test data.

**Key file:**  
- `api/src/main/java/com/grash/service/UserService.java`
> - `a7271d0`: Re-enabled the scheduled cleanup of demo companies by uncommenting the deletion logic in `DeleteDemoCompaniesJob`.  
Key file: `api/src/main/java/com/grash/job/DeleteDemoCompaniesJob.java`
> - `4d23430`: Added a boolean `demo` column to the `company` table and disabled the nightly job that deletes demo companies.  
Key files: `2025_09_13_1757719340_demo.xml`, `DeleteDemoCompaniesJob.java`
> - `5dc1572`: Added a `demo` boolean column (default false) to the company table via a new Liquibase changeset so the system can flag demo companies.  
Key file: `api/src/main/resources/db/changelog/2025_09_13_1757719340_demo.xml`
> - `367620a`: Upgraded the rate-limiting library bucket4j from 8.7.0 to 8.2.0 and switched to the JDK-8-specific artifact (`bucket4j_jdk8-core`) for better compatibility.  
Key file: `api/pom.xml`
> - `825346e`: Notification now stays anchored to the viewport instead of scrolling with the page, so alerts remain visible while users navigate.  
Key file: frontend/src/App.tsx
> - `f1dd237`: DemoAlert now only renders when a user is authenticated (`user` exists), eliminating the flash of the banner on the pre-login splash.  
Key file: frontend/src/App.tsx
> - `dc79449`: Added a demo mode that flags companies as demo=true and auto-deletes them every hour via a Quartz job; cascades were added to all FKs so child records vanish cleanly.  
Key files: api/src/main/java/com/grash/job/DeleteDemoCompaniesJob.java, api/src/main/java/com/grash/configuration/QuartzConfig.java, api/src/main/resources/db/changelog/2025_09_13_1757723543_cascade_delete.xml, frontend/src/App.tsx
