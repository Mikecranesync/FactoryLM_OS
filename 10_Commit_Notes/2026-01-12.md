---
title: "Commit Notes - 2026-01-12"
date: 2026-01-12
tags: [project/Rivet-PRO, type/commit-note]
status: active
---

# Commit Notes - 2026-01-12

## Rivet-PRO — Push at 22:29 UTC

**Commits:** 12
**Trigger:** `03facf6` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `03facf6` | feat(FEEDBACK-LOOP): complete user feedback loop implementation | Mikecranesync |
| `da0ef6d` | fix: restart n8n service to apply secure cookie setting | Mikecranesync |
| `52d5559` | docs: add updated import guide with refreshed URLs | Mikecranesync |
| `8d72cad` | docs: add session resume status and tunnel refresh reports | Mikecranesync |
| `ba8ce7c` | feat: Add Groq and DeepSeek fallback providers for URL validation | Mikecranesync |
| `050714c` | debug: add comprehensive logging to URL validator | Mikecranesync |
| `d318236` | feat(RIVET-016): Add LLM URL validator to prevent search pages | Mikecranesync |
| `0f9464e` | feat: add direct Tavily API integration for manual search | Mikecranesync |
| `238cc2f` | feat(RIVET-012,013,014,015): add manual delivery to photo flow | Mikecranesync |
| `d56b4de` | docs(CHORE-001): system audit and MVP roadmap | Mikecranesync |
| `5b646ac` | test(RIVET-010): fix bot handler test imports and mocks | Mikecranesync |
| `ca8d24e` | feat: Complete RIVET-008, 010, 011 - Production readiness | Mikecranesync |

> **What changed:**
> - `03facf6`: **User feedback loop fully implemented** - Users can now reply to bot messages with issue reports, receive AI-generated fix proposals via Telegram, and approve/reject them with emoji buttons. The system captures feedback, processes it through Ralph, and provides real-time updates as fixes are implemented.

**Key files modified:** `rivet_pro/core/services/feedback_service.py`, `rivet_pro/migrations/014_feedback_loop.sql`, `rivet-n8n-workflow/user_feedback_loop.json`, `rivet_pro/adapters/telegram/bot.py`
> - `da0ef6d`: Restarted the n8n systemd service so the previously-set `N8N_SECURE_COOKIE=false` now takes effect, eliminating the browser “secure cookie” error and restoring access on port 5678.  
Key file: `N8N_ACCESS_FIXED.md`
> - `52d5559`: Added a fresh import guide (IMPORT_NOW_UPDATED.md) that replaces stale URLs and tokens with the current HTTPS tunnel, Telegram bot, Anthropic, and Neon credentials.  
Key files: IMPORT_NOW_UPDATED.md
> - `8d72cad`: Added two status docs: SESSION_RESUMED_STATUS.md confirms the HTTPS tunnel was refreshed (old URL timed out, new one verified) and the Telegram webhook updated; TUNNEL_REFRESHED.md duplicates that refresh log. Key files: SESSION_RESUMED_STATUS.md, TUNNEL_REFRESHED.md.
> - `ba8ce7c`: Added Groq and DeepSeek as fallback LLM providers for URL validation in Manual Hunter, expanding the chain to Claude → OpenAI → Groq → DeepSeek → Reject. This fixes Claude 404 and OpenAI quota errors by ensuring URLs still get validated when primary providers fail. Modified settings.py and manual_service.py to support the new API keys and integration logic.
> - `050714c`: Added comprehensive debug logging to the manual URL validator to expose why LLM validation silently fails. Key changes include logging API key presence, response status codes, LLM content before JSON parsing, and specific exception handlers for HTTP/JSON/timeout errors.

**Modified file:** `rivet_pro/core/services/manual_service.py`
> - `d318236`: **Summary:** Added an LLM-powered URL validator to prevent users from receiving search pages instead of direct PDF manuals. The system uses Claude/GPT-4o to analyze URLs and provide confidence scores, warning users when a link might not be a direct PDF download.

**Key files modified:** `manual_service.py`, `response_formatter.py`, `URL_VALIDATOR_IMPLEMENTATION.md`

**Functional impact:** Users now get validated manual links with confidence indicators, reducing frustration from clicking through to search pages instead of direct PDFs.
> - `0f9464e`: Added direct Tavily API integration for manual searches, removing the n8n dependency. ManualService now calls Tavily directly (3-8s) when TAVILY_API_KEY is set, with n8n webhook as fallback (5-15s). Search queries are optimized with filetype:pdf filter and trusted manufacturer domains. Key files: settings.py, manual_service.py
> - `238cc2f`: **Summary:**  
Rivet Pro now delivers PDF equipment manuals alongside photo recognition. A new `ManualService` searches a local cache first (< 100 ms) or falls back to an n8n/Tavily web-search workflow; results are cached in `manual_cache` to cut external calls by 60 %. Responses are formatted as rich Markdown with clickable PDF links in Telegram.

**Key files:**  
`manual_service.py` | `013_manual_cache.sql` | `response_formatter.py` | `N8N_MANUAL_HUNTER_INTEGRATION.md`
> - `d56b4de`: **Summary:**  
Completed a full system audit and documented the hybrid Python + n8n architecture for AI agents. Established key patterns: n8n webhooks trigger Python business logic, and equipment is the mandatory center of all work orders. MVP readiness scored 6/10—foundation solid but n8n integration is the critical blocker. Queued 7 new tickets for Ralph.

**Key files:**  
AGENTS.md (new), MVP_ROADMAP.md, STATUS_REPORT.md
> - `5b646ac`: Fixed broken bot handler tests by correcting import paths and mock setups, bringing test coverage to 24/24 passing. Key fixes: removed non-existent Photo import, updated service mock paths to core.services, and corrected test data structures.

**Files changed:** tests/test_bot_handlers.py
> - `ca8d24e`: **Production deployment readiness complete** — Bot now supports secure HTTPS webhooks (vs. polling), 30+ comprehensive tests cover all handlers, and 800-line deployment guide enables VPS setup. Key files: bot.py (webhook mode), settings.py (new env vars), DEPLOYMENT.md, test_bot_handlers.py.
