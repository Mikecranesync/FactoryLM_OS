---
title: "Commit Notes - 2025-06-21"
date: 2025-06-21
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-06-21

## Backlog.md — Push at 12:32 UTC

**Commits:** 4
**Trigger:** `3276467` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `3276467` | TASK-99 - Fix loading screen border rendering and improve UX (#135) | Alex Gavrilescu |
| `70aa651` | TASK-86 - Update agent guidelines to emphasize outcome-focused acceptance criteria (#133) | Alex Gavrilescu |
| `11ebd4a` | TASK-96 - Fix demoted task board visibility (#134) | Alex Gavrilescu |
| `b2f3251` | Fix bug preventing tasks to load | Alex Gavrilescu |

> **What changed:**
> - `3276467`: Fixed loading-screen layout bugs: right border now stays visible, content gets proper padding, and long text no longer wraps. Added a title-bar spinner and ensured the screen reliably appears on Windows by replacing setImmediate with a short setTimeout.  
Key files: src/ui/loading.ts, src/cli.ts
> - `70aa651`: Updated all AI agent instruction files to emphasize writing outcome-focused acceptance criteria that are testable and verifiable, while clearly separating them from implementation plans. Added concrete examples of good vs bad acceptance criteria across CLAUDE.md, AGENTS.md, and .cursorrules.

**Key files modified:** CLAUDE.md, AGENTS.md, .cursorrules
> - `11ebd4a`: Fixed board visibility so demoted/archived tasks no longer appear.  
Introduced `cross-branch-tasks.ts` to scan every local & remote branch, compare commit timestamps for each task in tasks/drafts/archive folders, and keep only the most-recent state; `handleBoardView` now filters the board to “active” tasks only.  
Key files: `src/core/cross-branch-tasks.ts`, `src/cli.ts`.
> - `b2f3251`: Fixed a bug where tasks failed to load by replacing the complex Git-based state resolution with a simpler local-first merge strategy. The new code loads local tasks first, then fetches and merges remote ones using a configurable conflict-resolution strategy. Key file: src/cli.ts.
