---
title: "Commit Notes - 2025-07-07"
date: 2025-07-07
tags: [project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-07-07

## Backlog.md — Push at 21:17 UTC

**Commits:** 10
**Trigger:** `cd38d1f` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `cd38d1f` | TASK-115 - Add live health check system to web UI (#180) | Alex Gavrilescu |
| `f9558fc` | TASK-114 - cli: filter task list by parent task (#179) | Alex Gavrilescu |
| `2ef5ec2` | TASK-101 - Show task file path in plain view (#178) | Alex Gavrilescu |
| `d4394ca` | Add instructions for creating task with --notes | Alex Gavrilescu |
| `d55fd00` | TASK-166 - Audit and fix autoCommit behavior across all commands (#176) | Alex Gavrilescu |
| `99bf6f7` | TASK-165 - Fix BUN_OPTIONS environment variable conflict (#175) | Alex Gavrilescu |
| `033c4e9` | TASK-164 - Add auto_commit config option with default false (#173) | Alex Gavrilescu |
| `e887a4b` | TASK-163 - Fix intermittent git failure in task edit (#172) | Alex Gavrilescu |
| `509142e` | TASK-120 - Add offline mode configuration for remote operations (#170) | Alex Gavrilescu |
| `99a5b79` | Update task-114 - cli-filter-task-list-by-parent-task.md | Alex Gavrilescu |

> **What changed:**
> - `cd38d1f`: Added a live health-check system: the server now exposes `/api/health`, the React UI polls it every 30s, and a banner/toast warns when the connection drops and confirms when it’s restored; status survives page reloads.  
Key files: `src/server/index.ts`, `src/web/hooks/useHealthCheck.tsx`, `src/web/components/HealthIndicator.tsx`, `src/web/App.tsx`.
> - `f9558fc`: Added parent-task filtering to `backlog task list` via new `-p/--parent <task-id>` flag; only child tasks of the given parent are shown, with validation that the parent exists.  
Key files touched: `src/cli.ts` (filter logic), `src/test/cli-parent-filter.test.ts` (new tests), `README.md` (usage docs).
> - `2ef5ec2`: **Summary:** Added `--plain` flag support to `backlog draft <id>` and enhanced `backlog task <id> --plain` to output the absolute markdown file path as the first line. This enables AI agents and automation scripts to locate task/draft files for processing. Backward compatibility is maintained, and comprehensive unit tests were added.

**Key files modified:** `src/cli.ts`, `src/ui/task-viewer.ts`, `src/utils/task-path.ts`, `src/file-system/operations.ts`, `src/test/cli-plain-output.test.ts`
> - `d4394ca`: Added `--notes` flag to the CLI examples in README so users can see how to attach free-form notes when creating or editing tasks.  
Key file: README.md
> - `d55fd00`: Fixed inconsistent autoCommit behavior: `backlog init` and `addAgentInstructions` now respect the user’s autoCommit config instead of always committing. All git-triggering commands were audited and updated to check the config setting. Key files changed: `src/cli.ts`, `src/core/backlog.ts`, `src/agent-instructions.ts`, and related tests.
> - `99bf6f7`: Fixed the CLI crash when `BUN_OPTIONS` is set by temporarily clearing it while Commander.js parses arguments, then restoring it so downstream scripts still see the variable.  
Key files: `src/cli.ts` (save/restore logic), `src/test/bun-options.test.ts` (new test suite).
> - `033c4e9`: Added an `autoCommit` config flag (default: false) so users can decide whether task changes auto-commit to git. Core, CLI and config migration updated; new CLI flag `backlog config set autoCommit true`. Key files: src/core/backlog.ts, src/cli.ts, src/types/index.ts, backlog/config.yml
> - `e887a4b`: Fixed the intermittent git failures in `backlog task edit` by isolating commits to only the modified task file. The solution resets the git index before staging, ensuring no other staged/unstaged changes are accidentally committed, and adds retry logic for transient failures.

Key files changed: `src/git/operations.ts` (core git isolation logic), `src/test/git-isolation.test.ts` (new test coverage).
> - `509142e`: Added an offline mode: new `remoteOperations` config flag (default true) lets you disable all remote git fetches. When off, the board, view, and export commands fall back to local branches only and show clear warnings if the network is down. Key files: backlog/config.yml, src/core/remote-tasks.ts, src/git/operations.ts, src/cli.ts.
> - `99a5b79`: Added a short `-p` alias for the `--parent` flag in the CLI spec for task-114.  
Key file: backlog/tasks/task-114 - cli-filter-task-list-by-parent-task.md

## cmms — Push at 19:39 UTC

**Commits:** 1
**Trigger:** `062e19b` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `062e19b` | fix: annual savings | Ibrahima G. Coulibaly |

> **What changed:** Fixed the displayed annual savings percentage from 20% to 17% in the subscription plan selector. This corrects the pricing UI to reflect the actual discount offered for annual billing.

**Key file:** `SubscriptionPlanSelector.tsx`
