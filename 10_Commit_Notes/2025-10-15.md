---
title: "Commit Notes - 2025-10-15"
date: 2025-10-15
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-10-15

## Backlog.md — Push at 21:10 UTC

**Commits:** 11
**Trigger:** `d4baa35` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `d4baa35` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `747c69e` | TASK-297 - Fix Windows MCP git fetch hang during task_create (#403) | Alex Gavrilescu |
| `3101efc` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `da4497b` | TASK-290 - Improve MCP status field to use enum dropdown with config values (#402) | Alex Gavrilescu |
| `1607745` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `1560dd8` | TASK-289 - Implement resource templates list handler to return empty list instead of error (#401) | Alex Gavrilescu |
| `aad7230` | Merge branch 'main' of https://github.com/MrLesk/Backlog.md | MrLesk |
| `3a3fcbc` | Create task-290 - Improve-MCP-status-field-to-use-enum-dropdown-with-config-values.md | MrLesk |
| `cfd5aff` | Create task-289 - Implement-resource-templates-list-handler-to-return-empty-list-instead-of-error.md | MrLesk |
| `bb031a2` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `61323b0` | TASK-288 - Fix browser UI error display and make priority optional (#400) | Alex Gavrilescu |

> **What changed:**
> - `d4baa35`: Updated README to reflect the latest project board status and bumped version to v1.16.4. The board now shows TASK-297 (Windows MCP git fetch fix) as completed and refreshes the timestamps.

Key files: README.md, package.json
> - `747c69e`: Fixed a Windows-specific deadlock where `git fetch` would hang during `task_create` when run through the MCP stdio transport. Replaced the Bun-shell invocation with `Bun.spawn` that explicitly ignores stdin, preventing the inherited MCP pipe from blocking the command.  
Key file: `src/git/operations.ts`
> - `3101efc`: Updated README to reflect v1.16.3 and refreshed the auto-generated Kanban snapshot, moving TASK-290 (MCP enum dropdown) to Done and bumping the package.json version.  
Key files: README.md, package.json
> - `da4497b`: MCP task schemas now expose status as a proper enum dropdown populated from Backlog config.statuses, with the first status as the default; case-insensitive normalization is preserved in the validator.  
Key files: schema-generators.ts, validators.ts, mcp-tasks.test.ts
> - `1607745`: Updated the README’s auto-generated project board snapshot to v1.16.2, reflecting the latest task statuses and timestamps.  
Key files: README.md, package.json
> - `1560dd8`: Fixed the MCP server’s “List Templates” endpoint so it now returns an empty array instead of throwing an error when no resource templates exist.  
Key files touched: `src/mcp/server.ts` (added handler), `src/mcp/types.ts` (added result type), plus tests and TUI wiring.
> - `aad7230`: Merge branch 'main' of https://github.com/MrLesk/Backlog.md. Files: `README.md`, `package.json`.
> - `3a3fcbc`: Created task-290 to replace the plain-text MCP status field with an enum dropdown populated from config.statuses, matching the browser UI. Key file: backlog/tasks/task-290 - Improve-MCP-status-field-to-use-enum-dropdown-with-config-values.md
> - `cfd5aff`: Added a low-priority task to stub out the missing MCP `resources/templates/list` handler so it returns an empty array instead of throwing “Method not found,” eliminating the error in MCP Inspector.  
Key file: `backlog/tasks/task-289 - Implement-resource-templates-list-handler-to-return-empty-list-instead-of-error.md`
> - `bb031a2`: Updated README.md and package.json to reflect the latest project board status and bumped version to v1.16.1. The board now shows TASK-288 (browser UI error fix) as completed and refreshes the timestamp to 2025-10-15 18:10:11.
> - `61323b0`: Fixed two UI issues: validation errors now display to users instead of only logging to console, and the task modal stays open on errors so users can fix them. Also made priority optional by treating empty strings as undefined. Key files: `src/web/App.tsx`, `src/web/components/TaskDetailsModal.tsx`, `src/core/backlog.ts`.
