---
title: "Commit Notes - 2026-01-03"
date: 2026-01-03
tags: [project/Rivet-PRO, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2026-01-03

## Rivet-PRO — Push at 17:21 UTC

**Commits:** 1
**Trigger:** `fbd397d` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `fbd397d` | Phase 1: Foundation + OCR workflow | Mikecranesync |

> **What changed:** **Summary:**  
Laid the production-grade foundation for RIVET Pro 2.0: a cost-cascading OCR pipeline (Groq → Gemini → Claude → GPT-4o) that extracts equipment tags with 95 % accuracy, backed by Pydantic settings that enforce Beta/Pro/Team tier limits and full Phoenix + LangSmith observability. Key files added: `rivet/workflows/ocr.py`, `rivet/integrations/llm.py`, `rivet/config.py`, `rivet/models/ocr.py`, plus 13 tests.

## cmms — Push at 23:32 UTC

**Commits:** 9
**Trigger:** `b3a15e3` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b3a15e3` | feat: enhance Paddle subscription management with pause, resume, and scheduled changes handling | Ibrahima G. Coulibaly |
| `5c1ec18` | refactor: remove PaddleCloudController and migrate subscription management to PaddleController and WebhookController | Ibrahima G. Coulibaly |
| `b4845d2` | feat: migrate from FastSpring to Paddle for subscription management | Ibrahima G. Coulibaly |
| `906cc5a` | feat: add temporary licensing state generation for testing purposes | Ibrahima G. Coulibaly |
| `991ba77` | fix: update threshold types to `Long` for license-based limit checks and related repositories | Ibrahima G. Coulibaly |
| `f0ebeb9` | feat: send email notifications on license renewal with purchase details | Ibrahima G. Coulibaly |
| `60dd5f0` | refactor: make `LicenseService` a final dependency and remove redundant setters | Ibrahima G. Coulibaly |
| `e85f299` | feat: enforce license-based limits for locations, assets, parts, checklists, PM schedules, and work orders | Ibrahima G. Coulibaly |
| `5d4b42a` | feat: enforce license entitlements in service methods | Ibrahima G. Coulibaly |

> **What changed:**
> - `b3a15e3`: Added pause/resume controls for Paddle subscriptions and wired up scheduled-change webhooks so the UI instantly reflects plan downgrades/upgrades that are queued for the next cycle.  
Key files: PaddleController.java, WebhookController.java, Subscription.java, frontend plan components.
> - `5c1ec18`: Removed the dedicated PaddleCloudController and folded its subscription lifecycle logic into the existing PaddleController and WebhookController.  
Security config and frontend auth context were updated to drop the now-unused `/paddle/cloud/**` endpoints.  
Key files: PaddleCloudController.java (deleted), PaddleController.java, WebhookController.java, WebSecurityConfig.java, JWTAuthContext.tsx
> - `b4845d2`: Switched subscription provider from FastSpring to Paddle.  
Key changes: removed FastSpring webhook controller, added Paddle webhook controller, updated security config to allow Paddle endpoints, and added DB migration to store Paddle subscription IDs.  
Files touched: WebSecurityConfig.java, PaddleCloudController.java, Subscription.java, PaddleService.java, 2026_01_03_1767462867_subscription_paddle.xml, and frontend Plans page.
> - `906cc5a`: Added a hard-coded “always valid” licensing state for local testing, bypassing the normal cache/validation flow.  
Key file: api/src/main/java/com/grash/service/LicenseService.java
> - `991ba77`: Changed all license-limit threshold parameters from `int` to `Long` so the repository `hasMoreThan` queries accept the same type returned by the usage-based license map.  
Key files: every *Repository.java and its matching *Service.java for Asset, Checklist, Location, Part, PreventiveMaintenance and WorkOrder.
> - `f0ebeb9`: Added email notifications when a license auto-renews: customers now get a “checkout-complete” message showing plan, seat count, key and new expiry.  
Key files: WebhookController.java (send logic), checkout-complete.html (template).
> - `60dd5f0`: Refactored `VendorService` and `WorkOrderService` to inject `LicenseService` only once via the constructor, eliminating the extra setter method and making the field final.  
Key files: `VendorService.java`, `WorkOrderService.java`
> - `e85f299`: Added license-based limits for locations, assets, parts, checklists, PM schedules, and active work orders. Each service now checks the company’s license entitlement before allowing new records to be created, returning a 403 if the limit is exceeded. Key files: PartController, all *Service classes, LicenseEntitlement enum, and repository interfaces with new hasMoreThan queries.
> - `5d4b42a`: Added license-entitlement checks to all major service methods so features like field configs, file uploads, work orders, assets, PMs, etc. are blocked when the tenant’s license doesn’t include them.  
Key files: WorkOrderController, FileController, FieldConfigurationController, plus every *Service.java touched to call licenseService.hasEntitlement().
