---
title: "Commit Notes - 2025-06-14"
date: 2025-06-14
tags: [project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-06-14

## Backlog.md — Push at 14:40 UTC

**Commits:** 9
**Trigger:** `e338dc4` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `e338dc4` | Add TASK-60 - Migrate from blessed to Ink (#93) | Alex Gavrilescu |
| `de5b9cf` | Remove readme.md from the agents guidelines selection | Alex Gavrilescu |
| `4195094` | TASK-59 Simplify init command (#92) | Alex Gavrilescu |
| `2f6ae5c` | Fix version command (#91) | Alex Gavrilescu |
| `635f99e` | TASK-58 - Unify task list view to use task viewer component (#90) | Alex Gavrilescu |
| `041b364` | Bump version | Alex Gavrilescu |
| `23f1717` | TASK-56 - Simplify TUI blessed import (#88) | Alex Gavrilescu |
| `c9822b8` | TASK-57 - Fix version command to support -v flag and display correct … (#89) | Alex Gavrilescu |
| `edf3db9` | TASK-54 - CLI: fix init prompt colors (#86) | Alex Gavrilescu |

> **What changed:**
> - `e338dc4`: Added the full backlog for TASK-60: replace the CLI’s blessed-based terminal UI with Ink/React components.  
Covers dependency swap, new scrollable, board/task/list views, spinner, and final cleanup.
> - `de5b9cf`: The CLI no longer offers `readme.md` as a selectable agent-guidelines file when running `init`.  
Key file: `src/cli.ts`
> - `4195094`: Replaced the heavy `blessed` TUI with a lightweight `prompts`-based questionnaire for `backlog init`, giving plain text prompts for project/reporter names and arrow-key checkboxes for agent selection.  
Key files: `src/cli.ts`, `package.json` (added `prompts` dep).
> - `2f6ae5c`: Fixed the `backlog --version` command so it now prints the correct package version instead of `undefined`.  
The build scripts (CI, release, and local) now inject the version from `package.json` into the compiled executable via a `__EMBEDDED_VERSION__` define, and the CLI reads this constant instead of trying to locate `package.json` at runtime.

Key files: `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `scripts/build-standalone.js`, `scripts/build.js`, `src/cli.ts`, `src/utils/version.ts`
> - `635f99e`: Unified the task list UI with the task viewer: `backlog task list` now renders a split-pane layout (task list left, full detail right) reusing the existing `generateDetailContent` logic. All navigation, filtering, and `--plain` output remain intact.  
Key files: `src/cli.ts`, `src/ui/task-viewer.ts`, `src/ui/task-list.ts`.
> - `041b364`: Bumped the package version from 0.1.4 to 0.1.5 in package.json to prepare for the next release.
> - `23f1717`: Switched the TUI modules (`src/ui/*.ts`) from dynamic to static `blessed` imports and added `--external blessed` to every build command so the library is excluded from the compiled binary.  
Key files: `src/ui/tui.ts`, `src/ui/task-list.ts`, `src/ui/task-viewer.ts`, plus all build scripts and CI workflows.
> - `c9822b8`: Fixed the CLI version flag: added `-v` alias (now supports `-v`, `-V`, `--version`) and ensured the compiled binary reports the correct 0.1.4 version instead of the stale 0.1.0 fallback.  
Key files: src/cli.ts, src/utils/version.ts
> - `edf3db9`: Fixed the unreadable `backlog init` prompt by switching from the blessed TUI to a plain readline prompt, ensuring text is visible on dark terminals. Also added a clearer error message when Git user config is missing during auto-commit.  
Key files: src/ui/tui.ts, src/git/operations.ts

## cmms — Push at 17:13 UTC

**Commits:** 1
**Trigger:** `bd4d97b` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `bd4d97b` | docs: licensing | Ibrahima G. Coulibaly |

> **What changed:** Atlas CMMS switched from permissive MIT to GPL-3.0, obliging any derivative work to stay open-source. A new paid “Commercial License” file was added, letting customers buy out of GPL obligations (white-label, priority support).  
Key files: LICENSE, COMMERCIAL_LICENSE.MD, README.MD
