---
title: "Commit Notes - 2025-11-28"
date: 2025-11-28
tags: [project/Backlog.md, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-11-28

## Backlog.md — Push at 21:04 UTC

**Commits:** 4
**Trigger:** `a531e5b` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `a531e5b` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `b48b698` | TASK-318 - Add status change callback task (#434) | Alex Gavrilescu |
| `21315fe` | docs: update README with latest board status and version [skip ci] | MrLesk |
| `dea566f` | TASK-320 - Refactor and fix move mode implementation (#437) | Alex Gavrilescu |

> **What changed:**
> - `a531e5b`: Updated README.md and package.json to reflect v1.23.0 release and refreshed the auto-generated Kanban board snapshot with latest task statuses.  
Key files: README.md, package.json
> - `b48b698`: Added status-change hooks: global config and per-task frontmatter can now define shell commands that run whenever a task’s status changes, with injected variables (`$TASK_ID`, `$OLD_STATUS`, `$NEW_STATUS`, `$TASK_TITLE`). Failures are reported but don’t block the status update.

Key files: `src/utils/status-callback.ts`, `src/core/backlog.ts`, `src/types/index.ts`, `README.md`
> - `21315fe`: Updated README to reflect v1.22.1 release and refreshed the auto-generated Kanban board snapshot, moving TASK-320 (move-mode refactor) to Done and bumping the package version. Key files: README.md, package.json
> - `dea566f`: Fixed task-moving bugs in the Kanban board: tasks no longer vanish when moved out of Done, and moves between columns now update instantly without flicker. The `completed/` folder is now strictly archival; only active tasks are handled during reordering.

Key files: `src/ui/board.ts`, `src/core/backlog.ts`

## cmms — Push at 02:21 UTC

**Commits:** 6
**Trigger:** `5ed714e` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `5ed714e` | feat: ga4 tracking | Ibrahima G. Coulibaly |
| `0c75ca0` | feat: ga4 tracking | Ibrahima G. Coulibaly |
| `4ef260e` | fix: ga4 tracking | Ibrahima G. Coulibaly |
| `bea812b` | feat: ga4 tracking | Ibrahima G. Coulibaly |
| `779f2fc` | feat: ga4 tracking | Ibrahima G. Coulibaly |
| `c3378a8` | fix: ga4 tracking | Ibrahima G. Coulibaly |

> **What changed:**
> - `5ed714e`: Added GA4 tracking support while tightening the session-storage conversion flag so it only sets when a conversionKey is actually provided.  
Key file: frontend/src/utils/overall.ts
> - `0c75ca0`: Added GA4 event tracking across key user flows: live demo, contact-us, first asset, work-order creation, pricing views and upgrade requests.  
Key files: `Hero/index.tsx`, `Assets/index.tsx`, `WorkOrders/index.tsx`, `Subscription/Plans/index.tsx`, `overall.ts` (new `fireGa4Event` helper).
> - `4ef260e`: Fixed duplicate GA4 sign-up events by adding sessionStorage deduplication key per email. Now tracks each user’s conversion only once per session.  
Key file: frontend/src/contexts/JWTAuthContext.tsx
> - `bea812b`: Added GA4 event tracking while fixing a bug: conversion flags are now only set when a conversion key is actually provided, preventing unwanted sessionStorage writes.  
Key file: frontend/src/utils/overall.ts
> - `779f2fc`: Added Google Analytics 4 tracking across key user flows: live demo, contact-us, first-asset creation, work-order creation, pricing views and upgrade requests.  
Key files: overall.ts (new helper), Hero, Assets, WorkOrders, Subscription/Plans and pricing pages.
> - `c3378a8`: Fixed duplicate GA4 sign-up conversion events by adding sessionStorage guard keyed to user email. Now tracks only once per session, preserving UA event while preventing over-counting.

Key file: frontend/src/contexts/JWTAuthContext.tsx
