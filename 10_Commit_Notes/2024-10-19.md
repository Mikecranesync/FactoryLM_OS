---
title: "Commit Notes - 2024-10-19"
date: 2024-10-19
tags: [project/frame_realtime_gemini_voicevision, type/commit-note]
status: active
---

# Commit Notes - 2024-10-19

## frame_realtime_gemini_voicevision — Push at 05:53 UTC

**Commits:** 10
**Trigger:** `cec11c0` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `cec11c0` | Update README.md with framecast videos | CitizenOneX |
| `430df52` | updated framecast videos in docs | CitizenOneX |
| `62638dc` | updated README and docs | CitizenOneX |
| `52ee688` | re-point simple_frame_app dependency to pub.dev | CitizenOneX |
| `55959eb` | updated restarts with the google speech API to every 2 minutes, with a 500ms transition buffer | CitizenOneX |
| `e19c75b` | added INTERNET permission to allow the use of the cloud-based APIs | CitizenOneX |
| `933380c` | tidy up of todos | CitizenOneX |
| `7a2915d` | tweaked interim and final text box sizes and made them expand more flexibly | CitizenOneX |
| `1598464` | rough working implementation of google speech transcription from Frame audio | CitizenOneX |
| `f765b5d` | fixed bug in frameside code, remember to break out of audio sending loop once we know we are not streaming anymore | CitizenOneX |

> **What changed:**
> - `cec11c0`: Added two demo videos (“framecasts”) to the README to show the e-paper frame in action.  
Key file: README.md
> - `430df52`: Replaced the demo videos in the docs folder with fresh captures to reflect the current UI.  
Key files: docs/framecast1.mp4, docs/framecast2.mp4
> - `62638dc`: Updated README.md with clearer Google Cloud setup steps, pricing notes, language/model limits, and added new screenshots/diagrams to reflect the Frame microphone audio path. Key files: README.md, docs/*.png, docs/*Frame Microphone.svg
> - `52ee688`: Switched the `simple_frame_app` dependency from a local path to the official pub.dev version 0.0.9, ensuring the app uses the published package instead of a local development copy.  
Key files: `pubspec.yaml`, `pubspec.lock`.
> - `55959eb`: - Extended Google Speech API restart interval from 60s to 120s and trimmed the audio overlap buffer from 2s to 500ms, cutting server churn and data usage.  
- Cleared the default “N/A” placeholder for partial results so the UI starts blank.  

Key file: lib/main.dart
> - `e19c75b`: Added the INTERNET permission so the Android app can reach Google Cloud’s Speech-to-Text API.  
Key file: `android/app/src/main/AndroidManifest.xml`
> - `933380c`: Clean-up commit: removed two stale TODO comments—one about temporary logging filters (now permanent) and a no-longer-needed delay after stopping audio recognition.  
Key file: lib/main.dart
> - `7a2915d`: Made the transcript list and partial-result text areas expand responsively instead of using fixed heights; transcript now fills available space while partial-result grows up to 5 lines. Also added auto-scroll to bottom on each partial update.  
Key file: lib/main.dart
> - `1598464`: Added real-time Google Cloud Speech-to-Text transcription: double-tap the Frame to start/stop streaming 8 kHz PCM audio and see partial & final results in the app.  
Key file: lib/main.dart
> - `f765b5d`: Fixed a bug where the audio-sending loop kept running after streaming stopped, wasting CPU and possibly sending stale data. Added a `break` to exit the loop immediately once `streaming` is set to false.  
Key files: `assets/frame_app.lua`, `assets/frame_app.min.lua`
