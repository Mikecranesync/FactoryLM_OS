---
title: "Commit Notes - 2025-10-13"
date: 2025-10-13
tags: [project/Backlog.md, type/commit-note]
status: active
---

# Commit Notes - 2025-10-13

## Backlog.md — Push at 22:22 UTC

**Commits:** 8
**Trigger:** `63d040c` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `63d040c` | Fix tests | MrLesk |
| `805c0d9` | Improve agent guidelines for MCP usage | MrLesk |
| `6726a5f` | Fix windows CI tests | MrLesk |
| `a70c735` | Fix unit tests on windows CI | MrLesk |
| `9845df6` | Update bun.nix | MrLesk |
| `a823d7c` | Update bun.lock | MrLesk |
| `1243971` | Fix tests and cleanup old tasks | MrLesk |
| `049a9af` | TASK 287 - Add MCP support for agent integration (#355) | Richard Adleta |

> **What changed:**
> - `63d040c`: Added robust error handling to all file watchers in ContentStore to prevent silent failures. Errors are now logged to console when DEBUG env var is set. Key file: src/core/content-store.ts
> - `805c0d9`: **Summary**: Updated agent guidelines to use the new Backlog.md MCP resource system (`backlog://workflow/overview`) instead of the deprecated `read_backlog_instructions` tool. This streamlines onboarding by directing agents to a single, persistent resource that explains when and how to track work, removing the old mandatory discovery step.

**Key files**: `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `src/guidelines/mcp/*`, `src/mcp/server.ts`
> - `6726a5f`: Fixed Windows CI failures by adding missing Git mock methods to test stubs and wrapping blessed screen errors to prevent crashes when Windows file-system watcher events surface as plain strings instead of Error objects.  
Key files: src/test/board-config-simple.test.ts, src/ui/tui.ts
> - `a70c735`: Fixed a failing Windows CI test by explicitly setting `remoteOperations: false` in the mock settings object used in `server-search-endpoint.test.ts`. This ensures consistent behavior across platforms by preventing unintended remote calls during the test.

**Key file:** `src/test/server-search-endpoint.test.ts`
> - `9845df6`: Downgraded the top-level `cookie` package from 1.0.2 → 0.7.2 and moved the newer 1.0.2 version under `react-router/node_modules`, removing the duplicate that was previously under `express/node_modules`.  
Only `bun.nix` touched; no runtime behaviour change, just dependency relocation for consistency.
> - `a823d7c`: Moved the MCP SDK from runtime dependencies to devDependencies and downgraded the cookie package from 1.0.2 to 0.7.2 to resolve a transitive conflict introduced by react-router.  
Key file: bun.lock
> - `1243971`: Fixed failing tests and removed obsolete validation that blocked combining `--notes` with `--append-notes`; the two flags now work together (replace-then-append). Added “Acceptance Criteria” as a recognized markdown section and cleaned up completed backlog tasks. Key files: `src/cli.ts`, `src/markdown/section-titles.ts`, assorted test files.
> - `049a9af`: Added comprehensive Model Context Protocol (MCP) support, enabling AI agents (Claude Code, Gemini, Codex) to manage tasks via 75+ tools and dynamic resources. The implementation is a pure protocol wrapper around existing Core APIs with localhost-only security, triple transport modes (stdio/HTTP/SSE), and 1,067 passing tests. Key files: `src/mcp/server.ts`, `src/mcp/tools/`, `src/commands/mcp.ts`.
