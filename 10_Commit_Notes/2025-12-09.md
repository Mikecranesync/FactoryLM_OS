---
title: "Commit Notes - 2025-12-09"
date: 2025-12-09
tags: [project/Agent-Factory, project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-12-09

## Agent-Factory — Push at 06:53 UTC

**Commits:** 2
**Trigger:** `ee48134` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `ee48134` | docs: Add task sizing guidelines for Claude GitHub Action | Mikecranesync |
| `27b8d3c` | fix: Add id-token permission to Claude workflow | Mikecranesync |

> **What changed:**
> - `ee48134`: Added task sizing guidelines to help users break large tasks into 30-minute chunks that Claude can complete without timing out. The documentation includes concrete examples of oversized vs. properly-sized tasks and a step-by-step approach for creating sub-issues.

**Key file modified:** `.github/GITHUB_SETUP.md`
> - `27b8d3c`: Fixed OIDC authentication error in the Claude workflow by adding the missing `id-token: write` permission. This enables the workflow to generate OIDC tokens for secure authentication with external services.

**Key file:** `.github/workflows/claude.yml`

## cmms — Push at 19:49 UTC

**Commits:** 13
**Trigger:** `b474dd7` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b474dd7` | fix: default roles liquibase script | Ibrahima G. Coulibaly |
| `1ed9c98` | fix: default roles | Ibrahima G. Coulibaly |
| `d0ebdf9` | chore: set default roles company_settings to null | Ibrahima G. Coulibaly |
| `ce55f68` | feat: DemoCleaningAlert | Ibrahima G. Coulibaly |
| `e3ab49d` | feat: use CompanyCreatedEvent | Ibrahima G. Coulibaly |
| `a987e9f` | feat: link api to front for demo data deletion | Ibrahima G. Coulibaly |
| `e1aa277` | feat: delete demo data | Ibrahima G. Coulibaly |
| `7f02dbb` | fix: add created_by to demo | Ibrahima G. Coulibaly |
| `d2b0d22` | fix: demo data | Ibrahima G. Coulibaly |
| `cf8288d` | fix: demo data | Ibrahima G. Coulibaly |
| `834e748` | feat: demo data on signup | Ibrahima G. Coulibaly |
| `fefbbf0` | feat: demo data on signup | Ibrahima G. Coulibaly |
| `91d3d09` | Add Hungarian, Dutch, and Chinese (Simplified) language translations | atlas-cmms |

> **What changed:**
> - `b474dd7`: Fixed Liquibase migration that assigns default roles to users by using MIN(r.id) to ensure only one role match is returned, preventing “sub-query returns more than one row” failures. Added `<validCheckSum>ANY</validChecksum>` so the revised script will run on already-deployed databases.

Key file: `2025_12_07_1765117872_delete_default_roles.xml`
> - `1ed9c98`: Fixed the default-roles deletion script so it no longer deletes the wrong set: now it targets the six lowest-numbered codes (0-5) instead of the first six IDs, preventing accidental removal of customer roles.  
Key file: `2025_12_07_1765117872_delete_default_roles.xml`
> - `d0ebdf9`: Migration clears the company_settings_id on the six oldest “default” roles (role_type = 1) so they are no longer tied to a specific company.  
Key file: `2025_12_07_1765117872_delete_default_roles.xml`
> - `ce55f68`: Added a one-time banner that reminds new demo-company owners they can purge sample data via Settings.  
Key files: `UserResponseDTO.java` (added `createdAt`), `App.tsx` (new `DemoCleaningAlert` component).
> - `e3ab49d`: Introduced a CompanyCreatedEvent to decouple demo-data creation from user signup; UserService now publishes the event after a company-owning user registers, and DemoDataService listens asynchronously to create demo data only after the transaction commits.  
Key files: CompanyCreatedEvent.java, DemoDataService.java, UserService.java
> - `a987e9f`: Added a “Delete demo data” button to Settings → General that calls the new `DELETE /demo/demo-data` endpoint.  
Backend wires `DemoDataService.deleteDemoData(companyId)` to the controller and opens the endpoint to any authenticated user.  
Key files: `DemoController.java`, `frontend/src/content/own/Settings/General/index.tsx`, `en.ts` (for the button label).
> - `e1aa277`: Added repository methods to bulk-delete demo data flagged with `isDemo=true` for every major entity (assets, work orders, parts, locations, etc.).  
Key files: 20 repository interfaces plus `DemoDataService.java` and `ScheduleService.java`.
> - `7f02dbb`: Fixed NPE risk in delete checks by reversing the equals() call so the potentially-null `createdBy` field is on the right side.  
Also ensured demo data is stamped with a real user id so the ownership check passes.  

Key files: AssetController, CustomerController, LocationController, RequestController, VendorController, DemoDataService
> - `d2b0d22`: Fixed the demo data so the “Quarterly HVAC Inspection” now repeats every 90 days instead of once a year, aligning the schedule with its name.  
Key file: `DemoDataService.java`
> - `cf8288d`: Fixed demo data so the first work order now starts in IN_PROGRESS instead of OPEN, giving new users a more realistic initial view.  
Key file: DemoDataService.java
> - `834e748`: New accounts now get a realistic demo dataset—categories, assets, work orders, PMs, parts, vendors, customers—so users can explore the app without starting from scratch.  
Key files: DemoDataService.java (creates the data), 2025_12_09_1765200000_add_is_demo_column.xml (marks companies as demo).
> - `fefbbf0`: Added an `isDemo` flag to every core entity so demo data created on signup can be tracked and later cleaned up.  
Key files: all main model classes (Asset, WorkOrder, Part, etc.) plus UserService and the Liquibase changelog.
> - `91d3d09`: Added full Hungarian, Dutch, and Simplified Chinese localizations: new UI translation files plus matching backend/mail property files, and registered the three locales (with flags) in the i18n module.  
Key files: frontend/src/i18n/i18n.ts, hu.ts/nl.ts/zh_cn.ts, messages_*.properties, mailMessages_*.properties
