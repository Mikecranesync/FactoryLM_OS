---
title: "Commit Notes - 2025-04-20"
date: 2025-04-20
tags: [project/cmms, type/commit-note]
status: active
---

# Commit Notes - 2025-04-20

## cmms — Push at 06:53 UTC

**Commits:** 11
**Trigger:** `634f142` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `634f142` | docs: readme | Ibrahima G. Coulibaly |
| `c0a496e` | docs: readme | Ibrahima G. Coulibaly |
| `a278f8f` | chore: sidebar text align start | Ibrahima G. Coulibaly |
| `a2fe74c` | Refactor `UserSettings` to encapsulate email notification logic | Ibrahima G. Coulibaly |
| `02c1d47` | chore: check isEmailUpdatesForWorkOrders before sendin PM mails | Ibrahima G. Coulibaly |
| `9682462` | chore: disable zendesk and remove unnecessary | Ibrahima G. Coulibaly |
| `9e3d130` | Merge pull request #30 from Grashjs/sso | Ibrahima G. Coulibaly |
| `5276dee` | fix: docker compose | Ibrahima G. Coulibaly |
| `fd93cbf` | chore: log license machine fingerprint | Ibrahima G. Coulibaly |
| `6f7b2b3` | fix: misc | Ibrahima G. Coulibaly |
| `29e81d6` | fix: misc | Ibrahima G. Coulibaly |

> **What changed:**
> - `634f142`: Updated README to replace “premium” with “advanced” when describing LICENSE_KEY features—no functional change, just clearer wording.  
Key file: README.MD
> - `c0a496e`: README.MD updated: clarified that SMTP credentials are only needed when email features are enabled, and added a new optional env var `ALLOWED_ORGANIZATION_ADMINS` to restrict who can create organizations.  
Key file: README.MD
> - `a278f8f`: Sidebar menu items now align text to the start (left) instead of center, improving readability and consistent layout.  
Key file: `frontend/src/layouts/ExtendedSidebarLayout/Sidebar/SidebarMenu/item.tsx`
> - `a2fe74c`: Refactored `UserSettings` to expose email-notification flags via getter methods instead of direct field access, consolidating the logic inside the model.  
Key files: `UserSettings`, plus `PurchaseOrderController`, `WorkOrderController`, `ScheduleService`, and `WorkOrderService` where the new getters are now used.
> - `02c1d47`: Added a filter so only users who have opted-in to work-order email updates receive preventive-maintenance reminder emails.  
Key file: `ScheduleService.java`
> - `9682462`: Disabled Zendesk support widget (frontend) and removed unused SSO service plus a stray logger annotation (backend).  
Key files: frontend/src/config.ts, api/src/main/java/com/grash/service/SSOService.java (deleted), api/src/main/java/com/grash/service/UserService.java
> - `9e3d130`: Merge pull request #30 from Grashjs/sso. Files: `.env.example`, `.gitignore`, `README.MD`, `api/pom.xml`, `api/src/main/java/com/grash/configuration/AuditConfig.java` (+32 more).
> - `5276dee`: Fixed a startup crash when LICENSE_KEY is unset by adding a default empty value (`:-`) in docker-compose.yml.  
Key file: docker-compose.yml
> - `fd93cbf`: Added console logging of the machine fingerprint used for license validation. This aids debugging by exposing the hardware identifier sent to the license server.  
Key file: `api/src/main/java/com/grash/service/LicenseService.java`
> - `6f7b2b3`: Added the missing `LICENSE_KEY` env var to the backend service and cleaned up the frontend build-args by removing the redundant `LICENSE_KEY` and making `OAUTH2_PROVIDER` optional.  
Key file: docker-compose.yml
> - `29e81d6`: Fixed a startup crash when the `license-key` property is missing by making the `@Value` injection optional (defaults to `null`).  
Key file: `LicenseService.java`
