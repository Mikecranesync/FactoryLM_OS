---
title: "Commit Notes - 2024-10-13"
date: 2024-10-13
tags: [project/frame_realtime_gemini_voicevision, type/commit-note]
status: active
---

# Commit Notes - 2024-10-13

## frame_realtime_gemini_voicevision — Push at 10:55 UTC

**Commits:** 3
**Trigger:** `b6d71c1` on `main`

| Hash | Message | Author |
|------|---------|--------|
| `b6d71c1` | renamed to frame_transcribe_googlespeech, removed some vosk assets and dependency | CitizenOneX |
| `55dd9e6` | Update README.md | CitizenOneX |
| `a906173` | switched audio input to Frame mic at 8kHz/16-bit PCM. Note that the Vosk Android models are expecting 16kHz sample rate, and no upsampling is currently occurring so it doesn't perform properly | CitizenOneX |

> **What changed:**
> - `b6d71c1`: Project pivoted from Vosk offline STT to Google Cloud Speech: renamed Android/iOS identifiers and app labels, stripped vosk_flutter dependency and its 3 MB English model, and cleaned up unused packages.  
Key files: android/app/build.gradle, Info.plist, pubspec.yaml, lib/main.dart.
> - `55dd9e6`: Switched the project from an offline Vosk engine to Google Cloud Speech API for live captioning, removing the 16 kHz sample-rate blocker and on-device model size limits.  
Key file: README.md
> - `a906173`: Switched the Flutter app to stream 8 kHz/16-bit PCM audio from Frame’s microphone instead of using the host mic. Added Lua-side handlers to start/stop the mic and ship the raw audio to the phone over BLE.  
Key files: assets/frame_app.lua, lib/main.dart, README.md
